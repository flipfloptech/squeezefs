# 1.2.0 release gate — one complete pass from zero (2026-09-03 → 04)

**RELEASE VERDICT: GREEN** — every leg of the release gate passed on ONE
commit, in one unattended chain, from zero — **twice**: on `ac3fb7eb`
(launch #4) and again on `3a8fafc1` (launch #6, the commit the tag's code
is), after the first run's `dist` artifact check exposed a `build.rs`
profile-stamping bug (below) whose fix is a product build input. Legs: the
full cargo gate (`task check`), fstests `-g auto` fail-fast, pjdfstests,
LTP filesystem syscalls, with the fuzz campaign
(`.benchmarks/2026-09-03-release-1.2-fuzz.md`) green on the same product
code. Zero unexpected failures in any suite on either run; the four
fstests failures are exactly the four standing by-design adjudications
matching their pinned diffs. This record is the evidence
`RELEASE_NOTES.md` §Verification names; the tag `stable-2026.09` is cut on
the docs commit that lands it.

## Provenance

| | |
|---|---|
| Tested commit | **`dev @ 3a8fafc1`** (launch #6; launch #4 = `ac3fb7eb`) — clean detached worktree `~/Source/squeezefs-release-1.2`, pinned for the whole chain so `dev` could keep moving |
| Product code | `3a8fafc1` = `ac3fb7eb` + the `build.rs` profile-name fix (`84d34424`, product build input — the reason for the second run) + a test-only race fix; `ac3fb7eb` was byte-identical to `2479d424` (the fuzzed tip) in product code. Since the 1.2.0 train bump `db52329a`: the fuzz targets + the `cluster_wire::hex_decode` fix, the kernel 7.2 track, rig scripts, two test-contract fixes, the build-profile fix and notes |
| Binary under the external suites | `target/release/squeezefs` 1.2.0 (`3a8fafc1d81f`) profile **`release`** (thin LTO), md5 `ec63375b86abe52e466c8d412fdc5f8e` (launch #4: `806a9b77708e1cd6210e27a7148a0f83`), rebuilt by each runner as the unprivileged user |
| Box | AMD RYZEN AI MAX+ PRO 395, 32 hw threads, 117 GiB RAM, NixOS, kernel `7.1.8-cachyos-lto`, rustc 1.97.1 |
| Driver | `/tmp/release-1.2/driver.sh` — `task check` → `sudo tests/run_fstests.sh` → `sudo tests/run_pjdfstests.sh` → `sudo tests/run_ltp_syscalls.sh`, fail-fast, detached (`setsid nohup`), one exit file per leg; refuses a dirty worktree and a non-setuid `fusermount3` |
| Wall | launch #6: 04:26:01 → 10:52:20 EDT (6 h 26 m): `task check` 52 m, fstests 5 h 20 m, pjdfstests 6 m, LTP 7 m (launch #4: 20:04:00 → 02:27:15, 6 h 23 m) |
| Artifacts | `/tmp/release-1.2/{driver3.log,taskcheck.log,fstests.log,pjdfstests.log,ltp.log,*.exit,tip.txt}` (launch #6); launch #4's set under `run-ac3fb7eb/`; the aborted launches' logs beside them (`taskcheck.enospc.log`, `taskcheck.fusermount-path.log`, `taskcheck.d1b-flake.log`, `taskcheck.sqz-exec-flake.log`, `fstests.aborted-fc7558b1.log`) |

## Legs

| Leg | Result | Detail |
|---|---|---|
| `task check` | **exit 0** | clippy `--all-features` + shipped config (`-D warnings`), `cargo fmt --check`, `cargo test --all-features -- --test-threads=1` (**4,601 tests**, 0 failed; 4,598 on launch #4), `RUSTDOCFLAGS=-D warnings cargo doc`, Criterion bench smoke, `check:fuse3` (the fork's clippy/fmt/test/doc/bench), `check:loom` (models build + fmt), `check:fuzz` (the fuzz workspace type-checks on stable), `check:docs` (303 files, 276 local links, 0 broken), `task audit` over both lockfiles (265 + 114 crates, `--deny unsound --deny yanked`) |
| fstests `-g auto` | **exit 0** (both runs) | 198 ran, 586 not-run (features FUSE does not expose: reflink, quota, swap, dax…), **4 failed — all EXPECTED-SHAPE**: `generic/003` + `generic/192` (noatime by design), `generic/213` (thin provisioning), `generic/798` — each matched its pinned diff exactly and the fail-fast runner continued; any other diff would have aborted. `generic/634` (standing finite-timestamp-range adjudication) **passed outright** both runs. Expected-PASS regression sentinels `generic/074` (5 s) and `generic/464` (105 s / 142 s) passed both runs. Longest: 795 (239 s), 650 (234 s), 749 (231 s) |
| pjdfstests | **exit 0** (both runs) | `All tests successful.` — 238 files, **8,798 tests**, 189 s / 175 s wall |
| LTP fs syscalls | **exit 0** (both runs) | **1,884 passed, 0 failed, 0 broken**, 44 skipped (TCONF — kernel-feature skips, continued by design) — identical both runs |
| Fuzz (TEST-4) | **PASS** | `.benchmarks/2026-09-03-release-1.2-fuzz.md`: 12/12 targets × 90 s × 2 jobs, 383.6 M execs, 0 crashes, 0 artifacts, on product code identical to this commit's |
| require-mount (TEST-2) | **exit 0** | `task check:require-mount` on the same worktree after launch #4 (`ac3fb7eb`; the leg's mount surface is unchanged by the two commits since): the twelve live-mount suites under `SQUEEZEFS_TEST_REQUIRE_MOUNT=1`, 78 tests, the live-mount surface executed; skip ledger = 1 record, class `root` (`transport_geometry_tests::test_raised_sysctl_mounts_with_4mib_ents` — a sysctl write, the class this leg leaves as a skip by design) |

## Restarts (counted-run law: each fix restarted the chain from zero)

None of the four aborted launches was a product regression. Recorded so the
next release does not rediscover them.

| # | Launch | Died at | Cause | Fix |
|---|---|---|---|---|
| 0 | 17:36 (driver2, `fc7558b1`) | fstests, 55 tests in | product code changed under it (`ba7421f1` hex_decode) — not creditable | stopped by PID; restarted on the tip |
| 1 | 18:45 (`2479d424`) | `task check`, compile | **disk 100 % full**: 42 finished subagent worktrees under `~/.grok/worktrees/` held **1.3 TB** of `target/` | deleted those `target/` dirs (→ 30 % used) |
| 2 | 18:54 | `task check`, first FUSE mount | `fusermount3: Operation not permitted` — the driver's `PATH` put the non-setuid nix-store `fusermount3` ahead of the setuid `/run/wrappers/bin` one | wrappers first; the driver refuses to start otherwise |
| 3 | 19:05 | `task check`, `publish_plane_batching_tests` | a **flaky test contract**: D-1b's owner-conveyor pass bounds (`≤ 4` strict, `≤ 12` natural for 24 publishes) pre-dated C-2's instant-drain apply pass; 3–14 passes measured over 51 runs (≈ 96 % green — passed the batch gates on luck) | `6bf9e94f`: the strict contract holds the owner's pass pre-drain, barriers on all 24 queued, releases, pins 1–2 passes (the co-queue law itself); natural row keeps passes as a measurement; 30/30 after. Board: C-2 note item 8 |
| 4 | 20:04 (`ac3fb7eb`) | — | **ALL GREEN** (6 h 23 m) | first green run |
| — | 03:0x, `task dist:rocky8` on `006cc6d0` | the artifact check's own `--version` line | **a product bug**: the fat-LTO `dist` binary said `profile release` — `build.rs` parsed the profile from `OUT_DIR` by the FIRST `build` path segment, and the container builds under `/build/target/…`; the two-profile law's whole point is that a row can trust that word | `84d34424`: derivation moved to `src/build_profile_core.rs` (`#[path]`-shared with build.rs), anchored on the trailing `build/<pkg>-<hash>/out`; tests pin both venues' `OUT_DIR` shapes; `check-artifacts.sh` refuses a binary whose version line does not name the built profile (verified: rocky8 `dist` → `profile dist`); `SQZ_DIST` → `BUILD_DIST` (the knob registry announced the selector as a typo). Product build input ⇒ chain restarted |
| 5 | 03:32 (`84d34424`) | `task check`, the fuse3 fork's `sqz_exec::tests::pluggable_park_is_unparked_only_while_parked` | a **test race**: `services >= 3` asserted the instant `rx.recv_timeout` returned, but `tx.send` runs INSIDE the lane's second poll and the post-poll hook call is still ahead on the lane thread (≈ 1 in 200) | `3a8fafc1`: the third service is awaited (bounded) like the first park already was; executor unchanged; 100/100 in both crates that compile the module |
| 6 | 04:26 (`3a8fafc1`) | — | **ALL GREEN** (6 h 26 m) | this record |

## What this gate does and does not say

* It says the shipped code (every product file identical to the fuzzed
  commit) passes the full cargo gate and all three external POSIX suites
  from zero, with no failure outside the four documented by-design shapes.
* The external suites ran on the **dev/gate profile** (`release`, thin
  LTO), as every gate does; the tagged artifacts are built by
  `task dist:*` (`dist`, fat LTO) from the same commit — the two-profile
  law (`Cargo.toml`, pinned by `tests/cli_version_tests.rs`).
* Loop devices / file-backed sandboxes on one box — a conformance venue,
  never a performance row. The 1.2 hero numbers are the field's
  (`README.md`; `.benchmarks/2026-09-03-4k-random-attribution.md` and the
  e2e audit notes carry the instrument/substrate rows).
* **zc-capability leg — RUN, GREEN** (`sudo tests/run_zc_capability_gate.sh`
  — the FUSE_URING_ZERO_COPY contract suites as root with
  `SQUEEZEFS_TEST_REQUIRE_CAPABILITY=1`; record
  `.benchmarks/2026-09-04-1.2.0-zc-capability-gate.md`). It needs root on
  an sqz-kernel box with a Rust toolchain, which no box had at tag time; a
  user-level toolchain + checkout were stood up on `squeeze-test`
  (`6.19.14-sqz`, Rocky 8, nvme-cli 1.16) the same day. **Run 1 on the tag
  (`6e603a56`)**: the zero-copy surface fully executed — 143/143 across
  the four zc suites + zcrx probes, **skip ledger EMPTY** — and the one
  red was the bundled live NVMe-reservation leg's INSTRUMENT: nvme-cli
  1.x's `nvme read` on a controller char device probes `BLKSSZGET`, gets
  ENOTTY and exits with no output; every product step of that leg was
  green and the semantics were confirmed by hand with `io-passthru`.
  Harness fix `b16c8a4e` (`char_io` rides `io-passthru`, both spellings
  of the conflict class). **Run 2 from zero on `dev b16c8a4e`: PASS,
  149/149, ledger EMPTY, rc 0**, unregistered write refused
  `RESERVATION_CONFLICT (0x6083)` verbatim. The four commits between the
  tag and `b16c8a4e` are the subtype (`fuse.squeezefs`), the SIGTERM
  wake-all fix, the umount-verb zombie fix and the harness fix — none on
  the zc data path — and run 2 doubled as their independent check: the zc
  suites' teardown fell from ~20 s to < 1 s per mount (147 → 8 s for the
  fusion suite) once the daemon exited on its own SIGTERM.
