# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

# Excluded from the root workspace (root Cargo.toml `workspace.exclude`);
# this empty table makes the crate its own workspace root so `cargo test`
# runs here standalone — the loom-models precedent (own workspace, own
# lock). Without it, cargo refuses with "believes it's in a workspace
# when it's not" (broken since the L4-1 root-workspace introduction;
# surfaced by the L4-6 gate, the first to run this suite since).
[workspace]

[package]
edition = "2021"
rust-version = "1.77"
name = "fuse3"
# Fully-diverged first-party fork (forked from upstream fuse3 0.7.3 — see
# THIRD_PARTY_NOTICES.md): carries the SqueezeFS release-train version
# (docs/operations.md §Versioning & releases), bumped as a release act.
version = "1.1.0"
authors = ["Sherlock Holo <sherlockya@gmail.com>"]
build = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "FUSE user-space library async version implementation."
readme = "README.md"
keywords = [
    "fuse",
    "filesystem",
    "system",
    "bindings",
]
categories = [
    "api-bindings",
    "filesystem",
]
license = "MIT"
repository = "https://github.com/Sherlock-Holo/fuse3"

[package.metadata.docs.rs]
features = [
    "file-lock",
    "unprivileged",
    "tokio-runtime",
]
rustdoc-args = [
    "--cfg",
    "docsrs",
]
targets = [
    "i686-unknown-freebsd",
    "i686-unknown-linux-gnu",
    "x86_64-unknown-freebsd",
    "x86_64-unknown-linux-gnu",
]

[lib]
name = "fuse3"
path = "src/lib.rs"

[dependencies.async-notify]
version = "0.3"

[dependencies.bincode]
version = "1.3.3"

[dependencies.bytes]
version = "1.5"

[dependencies.futures-channel]
version = "0.3.30"
features = ["sink"]

[dependencies.futures-util]
version = "0.3.30"
features = ["sink"]

[dependencies.libc]
version = "0.2.155"

[dependencies.nix]
version = "0.29.0"
features = [
    "fs",
    "mount",
    "ioctl",
]
default-features = false

[dependencies.serde]
version = "1.0.196"
features = ["derive"]

[dependencies.slab]
version = "0.4.9"

[dependencies.tokio]
version = "1.36"
features = [
    # "fs" and "process" retired by the rip-tokio-total sweep: mount-time
    # directory probes, fusermount subprocesses and /dev/fuse notify writes
    # ride std wrapped in `crate::sqz_blocking::run_blocking`.
    "rt",
    # The multi-thread runtime the crate's own async tests run on (house
    # rule: async tests use the multi_thread flavor to expose data races).
    # The consuming daemon enables it anyway; this keeps the standalone
    # `cargo test` of the fork honest instead of riding downstream feature
    # unification.
    "rt-multi-thread",
    "sync",
    "net",
    "macros",
    # The tpc_timer_handle driver runtime fires tokio::time wakers for
    # handler futures (downstream daemon timers); without this the
    # standalone `cargo test` of the fork only compiled via downstream
    # feature unification.
    "time",
]
optional = true

[dependencies.tracing]
version = "0.1.40"
# The daemon logs through `log`/env_logger and installs no tracing
# subscriber, so without this feature every transport warn!/error! —
# including the zc opcode-mirror guards, whose whole design is
# "loud-never-corrupt" — was structurally INVISIBLE in the daemon log
# (the 2026-08-06 out-paged-mirror divergence EIO'd every readdir with
# zero log lines anywhere). With `log`, tracing events emit `log`
# records when no subscriber is set; env_logger's level filter keeps
# debug!/trace! hot-path macros short-circuited as before.
features = ["log"]

[dependencies.trait-make]
version = "0.1"

[dependencies.which]
version = "6"
optional = true

[dependencies.io-uring]
version = "0.6"

[dependencies.once_cell]
version = "1.19"

[dependencies.core_affinity]
version = "0.8"


[features]
# tokio-runtime is the ONLY runtime (the rotted async-io-runtime feature was
# removed 2026-07-28 — it no longer compiled; the FUSE-over-io_uring
# transport is tokio-only) and the crate does not compile without it, so it
# is on by default. The consuming daemon still enables it explicitly.
default = ["tokio-runtime"]
file-lock = []
tokio-runtime = ["dep:tokio"]
unprivileged = [
    "nix/socket",
    "dep:which",
]

[target.'cfg(target_os = "linux")'.dependencies.nix]
version = "0.29.0"
features = ["user", "ioctl"]
default-features = false

# The microbench program (2026-08-04, root
# .benchmarks/2026-08-04-microbench-program.md): transport hot-path
# Criterion groups. `autobenches = false` above, so the target is
# declared explicitly — house style `harness = false`.
[dev-dependencies.criterion]
version = "0.5"

[[bench]]
name = "fuse3_hot_bench"
harness = false

# `lease_core.rs` (FUSE-over-io_uring payload-lease protocol core) is
# `#[path]`-included by the SqueezeFS loom-models crate and switches its
# atomics on `cfg(loom)`; the main build never sets it.
[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(loom)"] }
