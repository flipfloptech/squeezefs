# VL8 — the v1.1B pre-existing-bug stabilization catalog (evidence record)

Branch `fix/vl8-catalog` off `dev` @ `4338418`, 2026-07-21. Design-doc entry:
`docs/design-volume-lifecycle.md` → PR VL8 (Rev 10 charter, user ruling
2026-07-20). Every fix is a tests-first red→green commit series; every
externally-found failure's fix carries its cargo repro (the repro-port
mandate); multi-run counts restarted from zero after every fix.

Instrument note (standing rule): all fstests runs below use
`sudo tests/run_fstests.sh generic/NNN` (file-backed `/dev/shm` volumes,
tmpfs staging, release binary named per run in the logs); cargo repros are
the in-process sandbox harnesses named per item.

## Per-item verdicts

| # | Item | Verdict | Root cause / evidence | Fix / repro |
|---|------|---------|----------------------|-------------|
| 5 | NvmeBlockDev short-read silent garbage | **FIXED** | Both CQE sites mapped any success to `bytes.slice(0..size)`; past-EOF/straddling reads on file-backed substrates "succeeded" with recycled pool-buffer bytes (red run: `Ok(4096 bytes)` on a 0/2048-byte completion). | red `8f0d9b6` / green `0953d63`. Exact-length contract via one `finish_read` (short ⇒ loud `UnexpectedEof`); consumers enumerated first (routing + verify paths — all assume exactly `size` bytes). Repro: `tests/nvme_dev_tests.rs::test_read_{past,straddling}_eof_*`. |
| 6 | getattr 4a doc rider | **DONE** | — | `4a0752d` (trait-level + impl-site notes). |
| 4 | Clean-unmount heartbeat lingering | **FIXED** | The winning `destroy` ran the whole teardown on a session-task future that external unmount cancels mid-flight (reply-task `select!` / detached queue workers / daemon exit) — `client:`/`writer_claim` records stranded to the 45 s TTL. Red repro polls destroy once past the `dismount_once` claim and drops it: `writer_claim` provably lingered. | red `166e905` / green `63e4852`. Teardown moves to a spawned cancellation-immune task (no await between claim and spawn); `wait_dismount_teardown` + bounded daemon-exit wait (`dismount_wait + 60 s`). VL3 rig clean-unmount retry windows tightened 90 s → 20 s. Repro: `tests/dismount_teardown_tests.rs::test_cancelled_destroy_still_deregisters_heartbeat_records`. |
| 8 | add-meta kill-9 stamped-ahead re-run refusal | **FIXED** | Crash after writes 1..n (all stamps @count n+1) left `volume_set_generation(old URI)` + the config read refusing discovery, so the DOCUMENTED same-arguments re-run died with the rig's exact message. | red `53e03cb` (adds `AddMetaHooks` §5.5.2b crash seams — `MigrationTestHooks` precedent) / green `6333bd0`. Gated on hard resume evidence (device claims THIS set @count n+1): old-set generation recomputed from member-position-ordered superblock uuids (`generation_from_uuids`), config read retried on the extended URI. Refusals without evidence stand (the §5.5.1a gate untouched). Repro: `tests/meta_slot_migration_tests.rs::test_add_meta_rerun_converges_after_{survivor_stamps,new_member_claim}_crash`. |
| 7 | read-mid-patch bimodal EIO | **FIXED** (root-caused) | `get_block_for_index`'s bounded rebind loop conflated MOVEMENT (binding changed) with CONTENTION (a W1 patch storm bumping the SAME key's incarnation word): a reader whose device fetch overlapped every patch round exhausted 8 rebinds → `Io(Other)` → EIO. Machine-speed-dependent ⇒ the sticky bimodal windows. **Bidirectional confirmation in one session:** the pre-fix binary failed `read_mid_patch_coherence…` with the exact catalog signature (`Errno(5)`, `read ino 2 off 147456`) in this session's own full-suite run while the fixed binary passed ×3 in the same machine window; the new deterministic repro failed ×5 pre-fix, passes post-fix. | red `c389479` / green `c79e99c`. Read-path-only contention escalation: after 2 same-binding incarnation failures, ONE fetch serializes under the block's `BLOCK_FLUSH_LOCKS` stripe (the lock every §5.1 patch holds across its DMA). Serve rule unchanged (incarnation-valid ∧ current-binding — no unproven bytes); movement keeps the loud 8-rebind bound; seed/write sites keep `escalate_contended=false` (non-reentrant stripe). Patch laws intact (`patch_edge_rmw_reads` still 0 by construction; no read on the patch path). Repro: `tests/extent_patch_tests.rs::read_survives_perpetual_patch_storm_no_rebind_eio`. |
| 1 | fstests generic/074 intermittent corruption | **OPEN — recurred on the final binary** | First ×10 (pre-item-2 binary): 10/10 PASS. Final-binary ×10: **9/10 — run 9 FAILED with the exact catalog signature** (fstest.4 `-s 10M -b 512 -mS`, ONE 512-B unit stale by exactly one loop: `8d` where `8e` expected, offset 200704 = page-aligned, daemon logs clean, all counters silent). fstest.4 semantics pinned from source: writes are MMAP STORES (page-dirty → FUSE writeback WRITEs; the `-S` O_SYNC flag does not affect mmap stores), verify is pread — so the loss is sub-page staleness composed on the write-back/read path, NOT explained by item 5 (real in-range reads) and NOT the item-7 EIO class (that failed loud, never silently). Incidence ~1/20 single-runs on this box. Honest disposition: STILL OPEN; the QUICK-set expected-result table now names this signature as the known open bug (any other 074 diff = new regression). | Artifacts: `/tmp/vl8_fstests/final/074_run9.{out.bad,full}` + daemonlogs; run-3 VL4-era artifacts `/tmp/vl4_074_artifacts/run_3`. |
| 2 | 013/464 async write wedge | **PARTIALLY FIXED (cfr ABBA + watchdog blind spots); a second wedge MODE remains OPEN** | **Capture 1 (013-shape, pre-item-2 binary, 013 loop run 3)**: 8 ops permanently in flight (7 writes + 1 read), all threads idle-parked, transport/conveyor/device healthy; kernel census caught 2 `copy_file_range` + 1 `fallocate` + 4 `open` handlers stuck and watchdog-INVISIBLE; victims cascade through striped inode guards + kernel writeback throttling. Structural defect: cfr ordered its two inode guards by RAW INO over the hash-striped table (ABBA-capable). **Post-fix: generic/013 ×3 consecutive PASS, zero watchdog lines.** **Capture 2 (464-shape, FINAL binary, 464 loop run 3)**: writes-ONLY stuck-op census (10 inos, 2200+ s in flight), no cfr/fallocate/open involvement, TPC/session threads idle — a DIFFERENT wedge mode (correlates with the 464 staging-ring oversubscription storm + movement-bound rebind churn at onset), still OPEN. The strengthened watchdog converts any future capture into a named holder list. | red `73c1426` / green `9a9bb32`: (a) `inode_pair_lock_order` ascending-shard-index total order (repro `cfr_pair_guard_order_is_shard_monotone`); (b) CopyFileRange/Fallocate/Open register at handler entry (repros `{cfr,fallocate}_registers_with_watchdog_while_guard_blocked`). Evidence packs: `/tmp/vl8_fstests/wedge_*` (capture 1) + `/tmp/vl8_fstests/wedge464/*` (capture 2).
| 3 | generic/003 / 213 / 464 standing failures | **ADJUDICATED** | **003**: three legs. (i) atime legs — SqueezeFS never updates atime on reads (**noatime by design**; the JuiceFS reference-client default posture; no read-path atime write exists in the codebase) — acceptable-and-documented v1.1B gap (relatime/strictatime unsupported). (ii) parent-mtime/ctime-on-create leg — backend semantics verified CORRECT in-process (probe: create bumps parent mtime+ctime; kv layer); the fstests diff is the kernel-attr-TTL observation layer, not a durability bug. (iii) ctime-changed-across-remount leg — verified stable in-process across clean shutdown/reopen; the historical failure window is consistent with the pre-item-4 cancelled-teardown dropping the pending-times drain (fixed @ `63e4852`). Final-binary single run: EXACTLY the 10 documented atime/ctime ERROR lines (the expected-fail canary shape — no drift). **213**: thin provisioning by design — `fallocate(mode=0)` reserves nothing on a sparse/dynamic backend, so the ENOSPC golden line never appears (standing QUICK-set adjudication re-confirmed 2026-07-12; unchanged). **464**: EIO class = FIND-RW5-A staging-ring pressure (pre-existing, charter: rand-write closing report §10 residual 8); the WEDGE mode is item 2 above. | Final binary: 003 = documented shape exactly; 213 = documented shape exactly (missing ENOSPC golden line only); 464 = documented EIO signature ×2, then the OPEN wedge mode on run 3 (item 2 capture 2). |
| 9 | syncfs transient ENOENT | **FIXED (reproduced live, mechanism pinned)** | The bounded ×5 isolated churn+syncfs rounds stayed clean, but the FULL rig run reproduced the exact signature at leg 12 (`sync: error syncing … No such file or directory`, mount fully healthy immediately after — syncfs succeeded on retry). Mechanism: writeback cache + FLUSH elision ⇒ kernel flushes dirty pages AFTER close+unlink+FORGET destroyed the ino; the WRITE failure is recorded in the superblock errseq (`mapping_set_error` → `sb->s_wb_err`) and the NEXT `syncfs` reports it ONCE — precisely the transient-once/healthy-after observation. The pre-fix handler could ALSO silently resurrect the ino (missing-ino `fetch_metadata` fabricates a default inline layout — the zombie-record vector my in-process red run hit). | red `8ed3b41` / green `025c2b7`: guarded write-path probe (no open handle + no cached meta/attr → backend verify); verified-NotFound WRITEs ack as a counted discard (`writeback_orphan_discards`). Repro: `tests/fuse_watchdog_teardown_tests.rs::write_to_reclaimed_ino_is_a_counted_discard_not_enoent`; rig log `/tmp/vl8_rig_final.log` line 66. |

## Final-binary external gate (/tmp/vl8_fstests/final)

- generic/074 ×10: 9 PASS, run 9 FAIL (the OPEN item-1 signature — count
  aborted per the multi-run discipline; the failure is the pre-existing
  intermittent, now named in the QUICK-set expected-result table).
- generic/013 ×3: PASS ×3, zero watchdog lines (the capture-1 wedge did not
  recur post-fix).
- generic/464 ×3: runs 1–2 the documented FIND-RW5-A EIO signature; run 3
  the OPEN wedge mode (item 2 capture 2) — evidence archived, run killed.
- generic/003: documented expected-fail shape exactly (10 atime/ctime lines).
- generic/213: documented expected-fail shape exactly.

## Cargo gates

- Per fix: clippy `-D warnings` clean, `fmt --check` clean, targeted suites
  green (nvme_dev 11/11, dismount_teardown 5/5, meta_slot_migration 16/16,
  extent_patch 19/19 ×3, fuse_watchdog 11/11).
- Full `cargo test --all-features -- --test-threads=1`: green after items
  4/5/6/7/8 (one unrelated flake, `pending_free_at_cap_forced_cycle_…`,
  failed once under heavy concurrent build load and passed isolated ×3 and
  in the clean re-run — pre-existing timing-bounded poll, not touched by
  this branch); final-tree run recorded with the branch.
