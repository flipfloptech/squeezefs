# Symmetric metadata program — PR 7: pack per slot, the refcount probe, the shared-block index + clone protocol, gather mode, C16 (2026-09-15)

| | |
|---|---|
| **Design** | [`docs/design-symmetric-metadata.md`](../docs/design-symmetric-metadata.md) §5.4.3 (law 1 — pack per `(writer, slot, data volume)`, KD-SYM-8; law 2 — no seed walk, refcounts probed on demand; the shared-block index), §5.4.4 (the clone protocol — `MarkShared` / `ShareBlock` / `ReleaseShared`, the ORDERING law, the crash windows), §5.7.5 (gather mode), §5.8.5 (C16), §5.8.6, §11 (the family), §1.6 rows "Refcount probe" / "Clone", §8 gate 1's packing row; R-SYM-2/3; PR plan row **7** |
| **Branch / base** | `feat/sym-pack-and-refs` off `dev` @ `6f8d44e6` (PRs 1/2/3/4/11/16 in) — `2355ce89`..HEAD, every contract red-first; built in parallel with PR 5 (tokens), PR 6 (cross-owner tx) and PR 8 (the allocation lease) under the level-4 shared-file laws (§9) |
| **Contracts** | [`tests/sym_pack_tests.rs`](../tests/sym_pack_tests.rs) — 7; [`tests/sym_shared_refs_tests.rs`](../tests/sym_shared_refs_tests.rs) — 12; the loom model `shared_ref_core_composed_never_patches_or_frees_a_marked_block` in [`loom-models/src/lib.rs`](../loom-models/src/lib.rs) (weakening-verified, §1.3); the PR-7 arms of the `manager_call_frame` fuzz target + the proptest mirror in [`tests/decoder_property_tests.rs`](../tests/decoder_property_tests.rs); the unit pin `shared_flag_rides_the_value_and_a_plain_take_is_unchanged` in `kv/block_refs.rs`; both suites appended to [`tests/run_sym_forest_suites.sh`](../tests/run_sym_forest_suites.sh) (30 suites) |
| **Knobs** | none (the brief's law). Gather mode is an xattr (`user.squeezefs.gather`), not a knob |
| **Venue** | dev box for every number here (**scoping** — debug build unless stated, tmpfs-backed temp files, both arms in-process, three other implementers building on the same box); no squeeze-test row — the 2026-09-14 venue directive puts this rung on the LOCAL re-gate, the box bracket belongs to gate 1 / PR 13–14 |

## 1. What landed

Under bit 17 **and** `SQUEEZEFS_SYMMETRIC_META=1` (PR 4's plane) the data plane's two refcount DECISIONS — the W1 sole-owner patch and the terminal free — read the durable truth instead of the RAM count alone: one range probe in the referencing ino's slot tree (`block_ref_probe`) plus the SHARED mark of the clone protocol; a pack block's tenants are inos of ONE slot so its whole population sits in one slot tree (the `Packer` keys open packs by `(slot, data volume)`); a clone across two slot trees runs `MarkShared` → `ShareBlock` → publish-with-SHARED, and a terminal free of a SHARED block is decided at the index home; fsck C16 checks both sides of the index; a directory opts into gather-mode minting by xattr. **Unarmed (`=0`) and bit-17-absent mounts take the shipped path** (§2) — except for ONE PR-1 defect fixed on every forest volume (§1.1), which the first red pin found.

### 1.1 The PR-1 defect (`fix(meta)` `2c54f46d`) — a forest volume's references key their owner's LOCAL KEY ino

The forest codec routes a kind-6 record (`06 ‖ vol_tag ‖ block_idx ‖ owner_ino ‖ block_index`) by the top 24 bits of the OWNER at offset 17 — the local key form `(s + 1) << 40 | local` — while every product site keyed the owner's GLOBAL ino (the rung-19 law, `refs_owner`), whose top bits are zero for any realistic ino. Through the routed layer every reference of every slot's inode landed in the NATIVE slot tree (the manager's): complete as a census (the union over every tree), wrong for the plane — a publish from another appender would name the manager's slot (the cross-region refusal / the `Lease` class), and the pack law's per-slot probe read **0**. Pinned red on `6f8d44e6` by `a_layout_publish_through_the_routed_layer_lands_its_reference_in_the_owners_slot_tree`; fixed where `route_ino` lives:

- `RoutedMetaBackend::forest_ref_ops(v_idx, ops)` rewrites a symmetric-forest volume's reference owners to their local key form at EVERY routed entry point (`set_layout_and_size`, the group stage, `merge_layout_and_size` (+ chained), `commit_block_refs` (+ witnessed), `destroy_inodes_releasing` / `destroy_inode_chunked`, `migrate_block_map_train`, `kvmap_truncate_handoff`); a flat volume's ops pass through borrowed (`Cow`), byte-identical (`a_flat_volumes_ledger_keys_the_global_owner_verbatim`).
- The offline conversion (`config_ops::collect_flat_records`) re-keys kind-6 owners under the stamp's width + resolved native slot (`shared_refs::local_key_owner`), and the kind-6 set oracle (`builder::digest_backend_kind_set`) folds forest owners BACK to the global form (`rekey_owner` / `global_owner`) so the relayout digests equal (PR 11's contracts stay green on both legs).
- The two probe faces read either form: `block_ref_probe(vol, blk, Some(slot))` is ONE `refs_probe` over that slot tree (`SlotTrees::refs_probe`; `block_ref_probes`), `None` / a flat volume = the whole-volume `block_ref_count`. `a_backend_committed_reference_probes_in_the_slot_its_owner_names` pins the backend-level form.

### 1.2 Pack per `(slot, data volume)` (`pack.rs`, `routing.rs`)

`OpenPack` carries a `scope: u32`; the open map is keyed `PackKey { be_id, scope }`; `Packer::reserve(router, slot_len, scope)` / `refill` / `open_block` / `any_open(scope)` take the scope; `DataRouter::pack_scope_of(ino)` answers the ino's FOREST SLOT on an armed set (the mint policy's decision) and `0` otherwise, so `land_packed_image` / `repack_window` land a tenant in its slot's pack. Unarmed: ONE scope per data volume — PK2 byte-identical (`an_armed_set_packs_per_slot_and_an_unarmed_mount_keeps_pk2s_one_scope`: two slots → two open blocks → two `pack_blocks_sealed_dismount`; unarmed the same two inos share one block, one seal). `open_room_bytes` reads the roomiest scope; `open_scopes()` is the new gauge face (`pack_open_scopes`). The design's pin `a_pack_block_whose_slot_is_handed_over_keeps_its_whole_population_in_one_tree` runs the population before / after a slot handover (PR 4's `AcquireSlot` under the `1:4` partition) and across a crash-remount: the one-slot probe reads the whole population at every point, every other slot tree reads 0.

### 1.3 The shared-block index and the clone protocol (`kv/shared_refs.rs`, `routing.rs`, `block_allocator.rs`, `meta_ship/manager.rs`, `shared_ref_core.rs`)

- **The SHARED bit** is bit 1 of the block-ref VALUE's flags byte (`BLOCK_REF_FLAG_SHARED`, beside PK's `MAP_BLOB` bit 0; `BlockRefOp::taken_shared`, `BlockRefValue.shared`; a plain take's bytes unchanged — pinned).
- **The index** lives in **tree 0 of volume 0** as `shared_ref:` control records keyed by the GLOBAL owner (`shared_refs::index_home_volume()` is the ONE fn PR 8 re-points at the allocation holder; `TREE_SHARED_INDEX = 9` stays reserved and unwritten — §6 item 3). `share_block(refs)` is idempotent (`(inserted, already)`), one control entry in ring 0's USER class under `manager_gate` + `manager_verbs`; `shared_index_population` / `shared_index_scan` read it back; `the_index_lives_in_tree0_and_never_pollutes_a_slot_trees_probe` pins that an indexed block's per-slot probe still reads its slot tree's references alone.
- **`MarkShared`** (`mark_block_refs_shared`): under the OWNER's 4a guard, ONE tx rewriting the reference with the bit (`Marked | Already | Gone`); `Gone` aborts the clone ENOENT-class before anything is written (`mark_shared_is_idempotent_against_the_durable_witness_and_gone_aborts_the_clone`).
- **The clone path** (`DataRouter::clone_share_blocks`, called from `clone_file` after the pins and before the dest publish, over the entries the dest map SHARES with the source — never a staged source's spill copy): on an armed set, for every block whose two references would live in two slot trees — src and dest in different slots, or a source reference already SHARED (a clone of a clone) — step 1 marks the source reference (through the routed door `RoutedMetaBackend::mark_block_refs_shared`) (RAM mark set beside it — `BlockAllocator::mark_shared`), step 2 indexes both inos' references (`share_block`, chunks of 512), step 3 the dest publishes its references WITH the bit in the same tx as its layout — the ORDERING law: a reference is published only after its mark is durable at the owner. A failure undoes the pins and the spill site. A clone of a clone inherits the bit (`a_clone_of_a_clone_inherits_the_shared_bit`).
- **The W1 predicate** reads the mark under the same 4a guard `MarkShared` applies under: `begin_patch_sole_owner` = `refcount == Some(1) && !is_shared(offset)`, counted on `patch_ineligible_shared` (the ledger's existing clause) — **and confirms the RAM verdict durably on an armed set** (`DataRouter::sole_owner_durably(ino, allocator, offset)`, ONE call at the patch site in `fuse_client.rs` after `begin_patch_sole_owner`): one `block_ref_probe` of the patching ino's slot tree, `Ok(1)` = patch, anything else (a reference a PREDECESSOR lessee committed into a tree handed over since — which this mount's RAM map, seeded at ITS open, never saw — or an unreadable probe) = the CoW fallback. Unarmed / flat: `true` without a read. `the_w1_predicate_confirms_sole_ownership_in_the_inos_slot_tree_on_an_armed_set` pins both (a backend-committed ghost reference in f's slot refuses the patch while RAM still reads 1). This is the probe's PRODUCTION caller in PR 7; the free list's is PR 8's (§5 item 1).
- **The terminal free of a SHARED block is never decided locally**: `BackendRouter::free_block_verdict` finds the RAM mark and, with the free gate installed (`DataRouter::arm_shared_refs` at mount — `src/main.rs` after `set_meta_backend`), asks the home's `release_shared` (`SharedRelease::{NotShared, Held { remaining }, Freed}`): `Held` decrements the RAM count only while it stays > 1 (`release_shared_held`) and counts `pack_partial_frees` for a tenant-shaped key; `Freed | NotShared` clears the mark and runs the ordinary `begin_free`. The GC arm (`release_shared_at`) probes the flags of every indexed owner through `route_ino`, so a cloner that died after `ShareBlock` leaves entries C16 reports and the release GCs (`a_cloner_dying_after_share_block_is_reported_by_c16_and_gcd_at_the_release`); one that died after `MarkShared` leaves a harmless flag (`a_cloner_dying_after_mark_shared_leaves_a_harmless_flag_c16_reports`).
- **The marks are seeded from the index at the arm** (`the_shared_marks_are_seeded_from_the_index_at_the_arm`) — the only per-volume read the arm performs (a `shared_index_scan` of the home, ∝ shared blocks, not references).
- **The verbs** ride `ManagerCall` as the PR-7 block at the END of the enum — `MarkShared { vol_tag, block_idx, owner_ino, block_index }`, `ShareBlock { vol_tag, block_idx, refs }`, `ReleaseShared { vol_tag, block_idx, owner_ino }` → `Marked { already } | SharedGone | Shared { inserted, already } | SharedReleased { shared, remaining }` (bincode carries the variant index; the brief's 0x70–0x7F range names this block; `MANAGER_SCHEMA` 1, `CLUSTER_WIRE_SCHEMA` 5 unchanged). `ManagerClient::{mark_shared, share_block, release_shared}`; `the_clone_verbs_ride_the_manager_wire` runs them through `RpcListener` + `AsyncVerbRouter::with_manager`. The wire `ReleaseShared` takes NO GC verdict (§6 item 4).
- **The R6 pin** `a_clone_racing_a_truncate_never_frees_or_patches_a_shared_block`: a truncate's terminal free racing a clone's mark on the same block never frees it while the clone's reference stands and never patches it; both probes and C8 agree after.
- **Loom** `shared_ref_core` (`src/shared_ref_core.rs`, `#[path]`-shared into `loom-models/`): the mark word (`mark` = Release store, `is_shared` = Acquire load) composed as a THIRD word beside the W1 `patch_clone_core` / `refcount_core` fence pair — patcher `retire → fence(SeqCst) → peek == 1 ∧ ¬shared → publish`, owner `release → fence → is_shared`, cloner `mark → fence → try_acquire → fence → snapshot`. Invariants: the two-word law; never a patch past a visible mark; `¬(freed_locally ∧ pinned)`, `¬(shipped ∧ pinned)`; conservation. **Weakening result**: the patcher's `fence(SeqCst)` is load-bearing (→ `Release` fails); the cloner's two fences fail only when BOTH are weakened (stated in the test's doc).

### 1.4 C16 (`fsck.rs`)

`SuspectKind::C16SharedIndexDrift` / `FindingId::C16SharedIndexDrift { vol_tag, block_idx, owner_ino, block_index, flag_side }` — a SHARED flag without an index entry (`FlagWithoutEntry`) or an index entry whose ino carries no SHARED reference (`EntryWithoutFlag`); `shared_refs::shared_index_drift(routed, vol_tag)` is the census (every volume's flagged references resolved to their global owner vs the home's index — both sides), run as its own pass beside C8 in the unsharded run and the fleet finalize (§6 item 5), confirmed on a fresh census, report-only (the apply arm refuses), counter `shared_index_drift` → `fsck_shared_index_drift`. Every planted drift found, a healthy tree never (the two crash-window contracts + `a_clone_on_an_armed_forest_marks_the_source_shared_and_indexes_both_inos`).

### 1.5 Gather mode (`lib.rs`, `kv/backend.rs`, `meta_backend/mod.rs`, `kv/slot_lease.rs`)

`crate::GATHER_XATTR = "user.squeezefs.gather"` (the kernel's `user.` namespace — a `squeezefs.`-prefixed name never reaches FUSE), admitted by NAME through the reserved-xattr screen (`xattr_name_allowed`, the first clause — `user.squeezefs.gatherX` and the format config stay refused); `gather_xattr_opts_in` reads `1/true/yes/on` (trimmed, case-insensitive). `RoutedMetaBackend::gather_mint_slot(volume, parent)` is the ONE new arm, called first inside `pick_mint_slot_for`'s armed branch: a parent carrying the opt-in mints its child from the parent's slot cursor (`dir_gather_mints`) whatever the cap — excluded when the parent's slot is the native slot or another volume's (the ordinary policy decides). Never automatic: a plain directory past the cap spills to the rotor (`a_gather_mode_directorys_children_live_in_its_slot_past_the_cap_and_never_automatically` — also the closure `affinity_mints + rotor_mints + dir_gather_mints ≡ mints` and the opt-out by `removexattr`); unarmed the xattr is ignored and the gauge stays 0 (`an_unarmed_mount_ignores_the_gather_xattr_and_the_gauge_stays_zero`). The S10 `InoSupply` is not consulted: on an armed set the slot's cursor IS the mint's (PR 4 retired the bit-12 lanes for metadata under bit 17), so the holder's cursor is what the arm reads — §6 item 6.

### 1.6 The stats family (appended at the END of PR 4's symmetric block in `fuse_client.rs`)

`dir_gather_mints` (the lease snapshot), `block_ref_probes`, `share_block_calls`, `release_shared_calls`, `mark_shared_calls`, `shared_blocks` (an array of `{volume, shared_blocks}` over `durable_ref_volumes()` — the RAM mark population), `pack_open_scopes`; `fsck_shared_index_drift` beside `fsck_tenant_overlap_findings`; `pack_blocks_sealed_dismount` (PK2's, now the per-slot seal residue). All 0 on an unarmed mount (`an_unarmed_mount_clones_plain_and_every_gauge_stays_zero`).

## 2. The negative contract — `SQUEEZEFS_SYMMETRIC_META=0` and a bit-17-absent volume

Unarmed: `pack_scope_of` = 0 (PK2's one scope; `Packer::open_room_bytes` reads the ROOMIEST open pack's room where PK2 read the first unsealed one's — on an unarmed multi-data-volume mount that is a harmless behaviour change the byte-identical claim does not cover, review Issue 12a), `clone_file` publishes plain (no mark, no index, no verb — every gauge 0), `free_block_verdict` never consults the gate (no RAM mark exists), the W1 predicate's extra clause reads a never-set word, `pick_mint_slot_for`'s armed branch is not entered, `arm_shared_refs` returns without a read. Flat volumes: `forest_ref_ops` borrows (`Cow::Borrowed`) — the ledger keys the GLOBAL owner verbatim (pinned). The ONE change every forest volume sees regardless of the plane is §1.1's fix (a bit-17-absent volume is untouched).

## 3. Measurements (dev box — SCOPING; debug build; in-process; a temporary instrument over `tests/common/sym.rs`, not committed — `/tmp/grok-justin/pr7-logs/scoping.log`)

| Row | Result |
|---|---|
| **Probe ns** — 128 blocks published across 64 slot trees; the one-slot probe vs the whole-volume by-block count (every slot tree) | one-slot probe **p50 6.1 µs / p90 10.6 µs** (max 44.8 µs); whole-volume count p50 159.5 µs / p90 241.5 µs (max 257.9 µs) — **≈ 26× at p50**, the whole-volume count paying 64 tree cursors where the probe pays one. Debug build; RAM-resident nodes on both arms |
| **Pack ratio vs the Rev-0 scope** — 2,000 tenants, 8/16/32/64 KiB round-robin (58.6 MiB — the PK7 local row's population) reserved through the live `Packer` | **armed (per-slot): 64 scopes → 64 pack blocks, 64 sealed at dismount** (≈ 0.92 MiB per block — none filled); **unarmed (one scope): 15 blocks** (= the PK7 row's 15, `.benchmarks/2026-09-10-packing-rows-local.md` §1.1). Ratio **4.3×** at this population — the design's priced residue: ≤ `M` = 64 rotor partials per data volume per solo mount (≤ 108 with inherited slots), consumed by `defrag --pack`; at 2,000 files the packs are still 23 % full, and the ratio falls toward 1 as each slot's pack fills (a 4 MiB block per slot is the asymptote, not per file — 2,000 → 64, not 2,000 → 2,000) |
| **`ls -l` cost with / without gather** — 512 children, `readdir` + 512 `getattr` through the routed backend, 5 repeats | gather (1 slot tree) median **5.95 ms**; plain past the cap (children over 62 slot trees) median **4.68 ms**. In-process both arms are RAM node-cache hits and the gathered tree's leaves are 512 records deep where the spread children sit 8-per-tree, so the one-tree listing reads ≈ 1.27× the spread one here. What gather buys is the CROSS-OWNER cost — one slot's token / ship instead of 62 — which is PR 5's/PR 12's row (this venue has no foreign holder): stated, not measured |

The live packing row (`.benchmarks/rigs/2026-09-10-packing-rows-local.sh` `dismount`, 2,000 files, three postures on the tcp dev substrate) — §4.4: flat **15**, stamped-unarmed **15**, armed **262** → `defrag --pack` frees 255.

## 4. Verification

### 4.1 The contracts, red → green

| Contract | Red on | Green with |
|---|---|---|
| `a_layout_publish_through_the_routed_layer_lands_its_reference_in_the_owners_slot_tree` | `6f8d44e6` (the probe read 0 in the owner's slot) | `2c54f46d` |
| `a_flat_volumes_ledger_keys_the_global_owner_verbatim`, `a_backend_committed_reference_probes_in_the_slot_its_owner_names` | no `block_ref_probe` | `2c54f46d` |
| `an_armed_set_packs_per_slot_and_an_unarmed_mount_keeps_pk2s_one_scope`, `a_pack_block_whose_slot_is_handed_over_keeps_its_whole_population_in_one_tree` | one scope per data volume | `a17229da` |
| `a_clone_on_an_armed_forest_marks_the_source_shared_and_indexes_both_inos`, `a_clone_racing_a_truncate_never_frees_or_patches_a_shared_block`, `a_clone_of_a_clone_inherits_the_shared_bit`, the two crash windows, `mark_shared_is_idempotent_…`, `the_shared_marks_are_seeded_from_the_index_at_the_arm`, `the_one_slot_probe_agrees_with_the_derived_census_on_every_published_block`, `the_clone_verbs_ride_the_manager_wire`, `the_index_lives_in_tree0_and_never_pollutes_a_slot_trees_probe` | no protocol | `2c54f46d` + `a17229da` |
| `an_unarmed_mount_clones_plain_and_every_gauge_stays_zero` | (green by construction; pins §2) | `a17229da` |
| `the_w1_predicate_confirms_sole_ownership_in_the_inos_slot_tree_on_an_armed_set` | no `sole_owner_durably` (the RAM verdict alone) | `e961ecf3` |
| `a_gather_mode_directorys_children_live_in_its_slot_past_the_cap_and_never_automatically`, `an_unarmed_mount_ignores_the_gather_xattr_and_the_gauge_stays_zero` | no arm (`xattr_name_allowed` refused the name) | `f2b42a02` |
| loom `shared_ref_core_composed_never_patches_or_frees_a_marked_block` | — (weakening runs red as stated in §1.3) | `2355ce89` |

### 4.2 The matrix (`tests/run_sym_forest_suites.sh`, 30 suites, flat THEN stamped) — **both legs PASS** (`matrix rc=0`)

| suite | flat s | stamped s | ratio | suite | flat s | stamped s | ratio |
|---|---|---|---|---|---|---|---|
| kv_tree_tests | 50.5 | 76.9 | 1.52 | crash_contract_tests | 1.1 | 0.4 | 0.36 |
| kv_node_tests | 0.1 | 0.2 | — | crash_kill_tests | 7.6 | 4.5 | 0.59 |
| kv_backend_tests | 136.6 | 147.0 | 1.08 | writer_scoped_staging_tests | 3.3 | 2.9 | 0.88 |
| kv_journal_tests | 0.1 | 0.1 | — | readonly_mount_tests | 4.9 | 4.4 | 0.90 |
| kv_partitioned_append_tests | 0.1 | 0.2 | — | meta_slot_migration_tests | 5.4 | 5.3 | 0.98 |
| kv_leaf_merge_tests | 44.2 | 44.1 | 1.00 | pv_coordinator_tests | 4.8 | 5.4 | 1.13 |
| kv_node_cache_coherence_tests | 0.4 | 0.5 | — | kv_smo_crash_completeness_tests | 36.5 | 144.4 † | — |
| kvmap_tree_tests | 0.4 | 0.4 | — | sym_appender_tests | 16.5 | 18.6 † | — |
| kv_scale_tests | 34.7 | 32.8 | 0.95 | sym_manager_tests | 8.4 | 11.9 † | — |
| durable_block_refs_tests | 11.2 | 10.0 | 0.89 | sym_fence_tests | 0.5 | 4.1 † | — |
| fsck_tests | 15.2 | 14.8 | 0.97 | sym_slot_transfer_tests | 36.8 | 40.1 † | — |
| fsck_c9_tests | 3.4 | 3.1 | 0.91 | rename_lock_set_tests | 0.8 | 8.5 † | — |
| fsck_c10_tests | 4.9 | 4.2 | 0.86 | sym_convert_tests | 44.4 | 29.7 † | — |
| fsck_c12_tests | 4.1 | 3.8 | 0.93 | **sym_pack_tests** | 1.5 | 5.2 † | — |
| fsck_repair_tests | 25.3 | 12.6 | 0.50 | **sym_shared_refs_tests** | 2.4 | 5.8 † | — |

† The shared `CARGO_TARGET_DIR` (PR 4's retired worktree) was wiped by another party at 05:15 local, mid-way through the stamped leg; every stamped suite from `kv_smo_crash_completeness_tests` on RECOMPILED the crate inside its wall (`Compiling squeezefs` precedes each), so those walls are compile-dominated and their ratios are not reported. The green is what the matrix proves; the pre-wipe ratios sit in the 0.36–1.52 band, the noise band of a box running three other implementers' builds (the shipped-path suites read stamped ≤ flat as often as not). The remaining runs of this rung use this worktree's own `target/`.

**×10 stamped from zero** (`SQUEEZEFS_TEST_STAMP_SYMMETRIC=1`, `--test-threads=1`) on the FINAL tree: `sym_pack_tests` 10 × 7 passed, `sym_shared_refs_tests` 10 × 12 passed — no red run (`/tmp/grok-justin/pr7-logs/x10-final.log`; the earlier count before `e961ecf3` read 10 × 7 / 10 × 11, `x10-*.log`).

### 4.3 The C8-drift sweep — every layout-publishing suite, both legs, once (`SQZ_SYM_SUITES=… tests/run_sym_forest_suites.sh`, 46 suites)

`corpse_sweep_tests decoder_property_tests dismount_staged_residue_tests dlm_cowriter_tests dlm_multi_writer_tests durable_block_refs_tests fsck_c10_tests fsck_c12_tests fsck_c9_tests fsck_tests inline_raise_tests kvmap_bounded_save_tests kvmap_crossing_tests kvmap_mw_hazard_tests kvmap_read_tests kvmap_run_tests kvmap_sweep_tests kvmap_tree_tests kvmap_walker_tests mw_arm_s8_tests mw_authority_recycled_binding_tests mw_cowriter_free_leak_tests mw_cowriter_free_tests mw_cowriter_lane_tests mw_cowriter_pack_tests mw_data_alloc_lane_tests mw_publish_era_gate_tests mw_stamping_tests mw_widthn_refs_tests overlay_growth_merge_tests overlay_overwrite_tests pack_compaction_tests packed_mapping_wire_tests pack_tenant_ops_tests pv_owner_map_tests pv_owner_verb_tests pv_partial_arm_tests pv_partial_open_tests pv_shipped_free_ledger_tests small_file_packing_tests sqz_sync_convention_tests sym_appender_tests sym_convert_tests sym_forest_tests sym_manager_tests sym_slot_transfer_tests` (the `grep -l "SQUEEZEFS_TEST_STAMP_BLOCK_REFS\|block_ref" tests/*_tests.rs` set of this tree is 48 — the brief's "28" grown since; the 46 pre-PR-7 suites run here, the two PR-7 suites in the matrix both legs and ×10 stamped).

**Flat leg: 46 / 46 PASS** (`/tmp/grok-justin/pr7-logs/sweep.log`; `durable_block_refs_tests` 25/25 with C8 drift 0 on every contract — the re-scope the brief asked for needed no test edit: the routed layer's owner re-keying is what made the forest leg exact). **Stamped leg: 45 / 46 PASS + ONE pre-existing red, attributed to the BASE** — `kvmap_read_tests` fails two contracts stamped (`lookup_counters_split_exact_vs_range_and_count_leaf_reads` @1220 "the cold resolution demand-paged a tree-7 leaf"; `tree7_demand_leaves_enter_the_clock_on_probation` @1013 "the fixture must stream enough tree-7 leaves … (got 0)") and fails them IDENTICALLY on `6f8d44e6` stamped (re-run in a detached worktree of the base: `8 passed; 2 failed`, the same two lines). The two contracts read the tree-7 gauge `META_KV_BLOCK_MAP_LEAF_READS`, which a FOREST volume structurally never moves — under bit 17 the block-map kind lives in the mixed slot-tree leaves (PR 1's relayout), so no tree-7 leaf exists to demand-page; the demand paging and probation the contracts are about still happen on the slot-tree leaves (the generic node-cache counters). Not a PR-7 regression and not a product defect — a flat-shaped instrument on a suite no prior rung had run stamped (it is not in the 27/30-suite matrix); **reported to the orchestrator as PR 1's owed item** (the block-map leaf-read gauge's forest face, or the two contracts made layout-blind) — out of PR 7's surface (`kv/backend.rs`, the hot zone). The runner fail-stops the leg at the first red, so the remaining 31 stamped suites were run with the SAME command as its own leg (`SQZ_SYM_SUITES=<31> tests/run_sym_forest_suites.sh stamped`, `/tmp/grok-justin/pr7-logs/sweep-stamped-rest.log`): **31 / 31 PASS** (`sweep-rest rc=0`) — so the sweep reads 46/46 flat, 45/46 stamped + the one base-attributed red. C8 drift 0 on every layout-publishing contract on both legs. The stamped leg ran against the working tree WITH `e961ecf3`'s W1 clause (the flat leg before it — inert there: `true` without a read on every unarmed mount, and no sweep suite arms the plane).

### 4.4 The live packing row — `.benchmarks/rigs/2026-09-10-packing-rows-local.sh` `dismount`, arm B (the packing lever ON, the shipped default), 2,000 files (8/16/32/64 KiB round-robin, 58.7 MiB, no fsync) → `syncfs` → `squeezefs umount`; the remount on a second mount point with the C8 oracle armed; `fsck --json`

Release build of this branch at `3c3ee74b` (built before the W1 durable clause `e961ecf3` existed — a clause this row never reaches: the dismount pass promotes, it never patches in place), the tcp dev substrate (4 nvmet-tcp meta namespaces on null_blk + 4 data namespaces on zram, `sudo`, `/dev/shm` staging), three postures — flat (default format), stamped-unarmed (`SQUEEZEFS_TEST_STAMP_SYMMETRIC=1`, plane OFF), armed (+ `SQUEEZEFS_SYMMETRIC_META=1`; the daemon log: `symmetric plane ARMED — 65 slot(s) leased (native + 64 rotor), M = 64` on every meta volume, `shared-block index armed (symmetric PR 7): 0 SHARED mark(s) seeded, the terminal-free gate installed`). Logs `/tmp/grok-justin/pr7-logs/packing-{flat,stamped,armed}/`.

| posture | dismount summary | umount wall | **blocks after** | oracle drift | byte-mismatches | fsck findings |
|---|---|---|---|---|---|---|
| flat | `2000 staged-layout file(s) (61518875 B) … 0 inline, 2000 packed, 0 to blocks` | 1.01 s | **15** | 0 | 0 / 2000 | 0 |
| stamped, plane OFF | same | 0.35 s | **15** | 0 | 0 / 2000 | 0 |
| **armed** (pack per slot) | same (+ `dismount sealed 256 open pack block(s)`) | 0.24 s | **262** | 0 | 0 / 2000 | 0 |

The armed population under `defrag --report-only --json`: 262 pack blocks, 7–8 tenants each (2,000 / 262), live 61,440,000 B, mean occupancy 5.6 %, 255 below half — exactly the design's residue shape: 4 meta volumes × (native + 64 rotor) ≈ 260 slots reached by 2,000 creates under one directory (the parent-slot affinity binds first, the rotor takes the spill), one pack block per `(slot, data volume)` scope — 262 because the scope key carries the DATA volume the placement picked, so a slot whose pick moved opens one per volume — none full. **`defrag --pack` is the consumer, and it closes the loop on the real substrate**: 1.6 s, `pack_compaction_blocks_freed` 255, `pack_compaction_tenants_moved` 1,564 (`pack_partial_frees` 1,564 — every moved tenant's release nonterminal, as the pack law says), every byte read back exact (0 / 2,000 mismatches), oracle drift 0, `fsck_shared_index_drift` 0, `invariant_tripwires` 0, `fsck` 0 findings. The PK7 row's "2,000 → 15" stands on both unarmed postures byte-for-byte; the armed posture pays the priced residue (17.5× the blocks at 2,000 files — the ratio falls toward `Σ slots` as each slot's pack fills, since a slot's pack is per SLOT, not per file) and hands it to the compactor. Dev box, release profile — a SCOPING row; the box row is PR 13's.

### 4.5 The live-FUSE trio (the venue rule) — `SQUEEZEFS_TEST_REQUIRE_MOUNT=1`, both legs

`posix_mount_semantics_tests` 3/3 · `corpse_sweep_tests` 4/4 · `inline_raise_tests` 7/7 — flat AND stamped, every mount real (`/dev/fuse` + FUSE-over-io_uring), zero skips (`/tmp/grok-justin/pr7-logs/fuse-trio.log`). Fidelity tier: not run — `reservation.rs` / `data_custody.rs` / `nvmeof` untouched.

### 4.6 Static gates and conventions

| Gate | Round 1 | Round 2 |
|---|---|---|
| `cargo fmt --check` (root, `loom-models`, `fuzz`) | PASS | PASS |
| `cargo clippy --all-targets --all-features -- -D warnings` | PASS | PASS |
| `cargo clippy --all-targets -- -D warnings` (shipped config) | PASS | PASS |
| **`RUSTDOCFLAGS="-D warnings" cargo doc --no-deps`** | **FAIL — 11 errors, all PR-7 intra-doc links** (round-1 review Issue 16; this line was missing from the round-1 table) | **PASS** (`beeb159a`) — every future round runs it |
| `fuzz/` `cargo check --all-targets` + fmt | PASS | PASS |
| `task check:loom` (build `--tests`, `-D warnings`, fmt) | PASS | PASS |
| `tests/run_loom.sh` `shared_ref` | PASS | PASS |
| `tests/check_markdown_links.sh` (the four docs) | PASS | PASS |

The convention suites green — the convention suites green — `docs_parity_tests` (every new gauge in `docs/operations.md`), `env_knob_convention_tests` (no knob added), `skip_ledger_tests` (the new `tests/common/` module and both suites), `no_tokio_convention_tests`, `derivation_sweep_tests`.

## 4c. Review round 1 → round 2 (2026-09-15; commits `beeb159a`, `e7a2d7e9`, `fbaee6e0`, `b255825e`, the docs commit)

The review (`/tmp/grok-justin/grok-exec-review-dadee1dd-pr-7.md`) opened 17 issues, 4 bugs, plus the red rustdoc gate. Every one is addressed; the review file carries the per-issue `Status` / `Response` and the round-2 verification table. What changed, and the red → green pins (each pin verified RED by reverting ITS fix alone on the round-2 tree — `/tmp/grok-justin/pr7-logs/red_checks.log`, `red_checks2.log`, `red_check3.log`):

| Issue | Fix | Red pin (red on the round-1 tree) |
|---|---|---|
| Gate (16) | the eleven intra-doc links — absolute paths, code spans for private items | the gate itself |
| **Bug 1** | `block_ref_probe_flags` → `RefProbe { count, shared }`; `sole_owner_durably` = `count == 1 && !shared`; run BEFORE the incarnation retire (Issue 10); `symmetric_armed` latched at the arm (Issue 10); the served `MarkShared` sets the RAM mark through `shared_refs::install_routed` | `the_w1_predicate_confirms_…` — a sole reference under a backend-set SHARED bit (RAM mark absent) must refuse: `panicked at :639 "one reference under a durable SHARED bit is not sole ownership"` with the flag ignored |
| **Bug 2** | the protocol runs only when `spill_site.is_none()` (a spill is a fresh copy — nothing shared); the abort arm undoes pins once, names a failed undo, never releases the site | `a_staged_source_clone_under_a_full_ring_spills_and_succeeds_on_an_armed_set` (the `FuseRig`: riders pin the ring, `copy_file_range`) — `panicked at :692 "a staged clone never surfaces StorageFull"` with the protocol over the spill |
| **Bug 3** | `share_block` / `release_shared` pre-admit PARKING before `manager_verbs` (`HeldAdmission`; `release_shared` sizes outside the mutex and re-sizes when the population grew, bounded by `RELEASE_RESIZE_ATTEMPTS` = 8); the gate's journal-failure class KEEPS the reference (`shared_release_failures`, must-stay-0) — no `let _` on a free | `a_full_ring_0_parks_the_index_writers_and_never_fails_or_leaks` (the user budget HELD through `journal_ring().try_admit`, released 400 ms later): `ShareBlock under a full ring parks, never fails: JournalReserveExhausted { needed: 79 }` under `Try` |
| **Bug 4** | `block_refs::cancel_same_reference_pairs` at both translators — a re-description writes no op | `a_truncate_shrink_of_a_clone_shared_tenant_keeps_the_durable_shared_bit` — `panicked at :907 "the re-described reference keeps its SHARED bit"` without the cancellation |
| 5 | same-slot clone of an unshared block = the shipped clone; a SHARED source still runs the protocol; the permanent-SHARED survivor PRICED (below) | `a_same_slot_clone_of_an_unshared_block_runs_no_protocol_and_a_shared_source_does` |
| 6 | the module doc + the loom model's doc state the product's orderings and what the model models (a serializability pin of the design's protocol) | — (docs) |
| 7 | scope = `(meta volume, forest slot)` (`pack_scope_key`) | `two_meta_volumes_native_inos_never_share_a_pack_scope` — `panicked at :371` with the slot alone |
| 8 | `RoutedMetaBackend::mark_block_refs_shared` — the S9 ownership refusal, the S10 delegation gate, the cutover gate, the volume | (the clone path's call; the gates are PR 4/S9/S10's contracts) |
| 9 | `ReleaseShared { owner: Option<(ino, block_index)> }`; the release deletes ONE entry | the wire contract's `Some((ghost, 0))` |
| 10 | see Bug 1 | — |
| 11 | (a) the lock-order note in `stripe_locks.rs`; (b) the gate's failure keeps the reference, counted + named | the Bug-3 pin's `failures` assertion |
| 12 | (a) §2 names `open_room_bytes`; (b) WONTFIX with rationale (review file); (c) the C8 drift log folds owners to the global ino | — |
| 13 | `PackTenant::scope()` dropped; `base_key` / `slot_trees` / `shared_free_gate_armed` say "contract accessor" | — |
| 14 | the three routing `expect`s are `let … else` | — |
| 15 | the `allow(dead_code)` exception stated in the module doc | — |
| 17 | `share_block_screened` — every wire word confirmed (exists ∧ SHARED) through the routed hooks; one bad word rejects the frame (`manager_verb_rejected`) | the wire contract: an ino without a reference / with an unshared reference is rejected, nothing indexed |

**The surviving sole owner stays SHARED (Issue 5, priced).** After a cross-slot clone and the other side's release, the survivor's reference keeps its durable bit and the block its RAM mark (`Held { remaining: 1 }` clears nothing): every later W1-eligible small overwrite of that block takes the CoW fallback (`patch_ineligible_shared`) — ONE CoW rewrite per first touch of a formerly shared block, after which the rewritten block is the survivor's alone and W1 is back. The clone-template-then-diverge shape pays exactly that: N formerly-shared blocks → N CoW rewrites, then par. An un-share step (clear the bit under the survivor's 4a guard when the home reads the block sole again) needs a cross-process "share in flight" witness — a cloner between its `MarkShared` and `ShareBlock` would otherwise be un-shared under it — which the design does not yet name; owed to PR 12 with the wire protocol. A SAME-slot clone never marks, so the common same-directory clone shape pays nothing (pinned).

**Round-2 verification** (`/tmp/grok-justin/pr7-logs/round2/`, this worktree's own `target/`): `sym_pack_tests` ×10 stamped + ×3 flat from zero — 13 × 8 passed; `sym_shared_refs_tests` — 13 × 16 passed; the targeted suites both legs (`durable_block_refs_tests` 25/25, `pack_compaction_tests` 15, `pack_tenant_ops_tests` 8, `packed_mapping_wire_tests` 7, `small_file_packing_tests` 14, `mw_cowriter_pack_tests` 11, `fsck_tests` 22, `fsck_repair_tests` 17, `fsck_c12_tests` 8, the W1 pins `extent_patch_tests` 21 / `extent_overlay_tests` 14 / `overlay_length_floor_tests` 7 / `overlay_overwrite_tests` 32 / `write_visibility_tests` 28 / `placement_tests` 12 / `inplace_overwrite_tests` 3, `kernel_op_economy_tests` 3, `docs_parity_tests` 5, `env_knob_convention_tests` 22, `derivation_sweep_tests` 54, `skip_ledger_tests` 11, `decoder_property_tests` 44, `rename_lock_set_tests` 2, `sym_manager_tests` 29, `sym_slot_transfer_tests` 45 — every one green on both legs); the 30-suite matrix once both ways — **flat PASS, stamped PASS, `rc=0`, no ratio NOTE** (max stamped/flat 1.71 on `meta_slot_migration_tests` at a 4 s wall — the box's noise band; the two PR-7 suites 0.6/0.9 s and 17.9/19.8 s); the live-FUSE pair `posix_mount_semantics_tests` 3/3 + `inline_raise_tests` 7/7 under `SQUEEZEFS_TEST_REQUIRE_MOUNT=1` on both legs, zero skips. **One matrix red on the FIRST round-2 run, attributed**: the flat leg stopped at PR 2's wall-time contract `sym_appender_tests::a_full_declared_ring_kicks_the_next_cadence_tick_not_the_ceiling` ("the storm's 8 drains took 2.79 s", bound 2 × `CHECKPOINT_MAX_AGE_MS` = 2 s) at box load ≈ 9 (three other implementers' batteries beside this one); the storm runs `commit_block_refs` on a declared region — a path PR 7 does not touch — and the suite passed 34/34 ×3 re-run at load ≈ 2, then green inside the re-run matrix; the bound was not moved.

**The scope's population on the live row is unchanged** by Issue 7's fix for a fresh set (the mint policy excludes the native slot); a CONVERTED set's natives now take one scope per meta volume — the ≤ 64 + 1 per volume bound stands.

## 4b. Issues found on the way (attributed before any harness bound moved)

1. **The PR-1 owner-keying defect** (§1.1) — fixed here, red-first, on every forest volume.
2. **`kvmap_read_tests` red on the stamped leg** (§4.3) — pre-existing at `6f8d44e6`, a flat-shaped tree-7 instrument; reported, not fixed (out of surface).
3. **The shared `CARGO_TARGET_DIR` wipe** (§4.2) — an environment event on the four-way box, not a code finding; the matrix stayed green through it because every suite recompiled.
4. **The W1 predicate's durable clause was missing from the first build** (`a17229da` read the mark alone) — the probe had no production caller and its gauge would have read 0 on every mount; `e961ecf3` wires the design's law 2 at the patch site, pinned both ways. Found while writing this note's §3 (the probe row had no product consumer to name).

## 5. Where a contract needed PR 8's / PR 5's machinery — stopped and stated

- **The free list**: §5.4.3 law 2 makes the free list the allocation holder's bitmap (§5.5.1 — PR 8's). Until it lands the mount derives it from the ONE by-block scan exactly as today, so "a forest mount's open performs zero by-block census reads" is **PR 8's pin**, not this rung's; what PR 7 moves off the RAM count is the two DECISIONS: W1 confirms its RAM verdict with the one-slot probe on an armed set, a SHARED block's terminal free is decided at the index home. The RAM refcount map stays the free-list / cursor / pin engine on both layouts, and an UNSHARED block's terminal free is still the RAM count's verdict (`free_block` carries no ino, so the design's "the freeing ino's slot tree" probe has no slot to read there — PR 8's bitmap is what replaces that decision's input).
- **The index home**: the design homes the index in a control ino of the allocation HOLDER's slot; the holder role does not exist yet, so it lives in tree 0 of volume 0 behind `shared_refs::index_home_volume()` — PR 8 re-points the ONE fn (and moves the records under kind `0x09` in the holder's slot tree if it wants the design's key shape; bit 17's kind set is untouched here).
- **Foreign actors**: every actor (C, O, H) is this mount in PR 7 — the wire faces exist and are contract-proven through the listener, but the routed executor is the caller in every product path; PR 12's non-manager reaches the home over `ManagerClient`, and `MarkShared` at a FOREIGN slot holder is PR 5's/12's (the holder's 4a guard is process-local until then).
- **Gather's cross-owner row** (§3): PR 5's tokens.

## 6. Deviations from the design row — as built

1. The PR-1 defect (§1.1) — an unplanned `fix(meta)` on every forest volume, red-first.
2. The free list stays the by-block scan's derivation at mount, and an unshared block's terminal free the RAM count's verdict (§5 item 1); the probe's production caller in PR 7 is the W1 durable clause (count ∧ ¬SHARED off the probed records since round 2).
3. The shared index is tree-0 `shared_ref:` control records on volume 0 keyed by the GLOBAL owner, not kind `0x09` records in a slot tree (§5 item 2).
4. The wire `ReleaseShared` takes NO GC verdict — its GC arm needs the routed layer's `route_ino` to probe each indexed owner's flags, which the volume-level service does not have; the routed executor (`DataRouter::release_shared_at`) is the arm every product free runs, and the wire face answers `SharedReleased { shared, remaining }` from the index alone.
5. C16 rides its OWN pass beside C8 (the unsharded run + the fleet finalize), not the one-walk census PR 1 left owed — that census is still N passes.
6. The gather arm mints from the parent slot's cursor directly (the mint policy's own supply), not through `InoSupply` — under bit 17 the slot's cursor is the mint's (PR 4).
7. `open_room_bytes` reads the roomiest scope (the pack-pressure face was per data volume; per slot it has no single answer).
8. The gather xattr is `user.squeezefs.gather` (the design's `squeezefs.gather` cannot pass the kernel's xattr namespaces).
9. `SLOT_PAGE_BUDGET`'s "≤ 108 open packs" stays arithmetic — the contract pins two scopes → two blocks; the 64-scope population is §3's row.
10. (round 2) The pack scope is `(meta volume, forest slot)`, not the slot alone; `ReleaseShared` is keyed on `(ino, block_index)`; the protocol skips a same-slot clone of an unshared block; a surviving sole owner stays SHARED (priced in §4c); the S9 publish wire (`WireBlockRefOp`) carries no SHARED bit — a co-writer's shipped clone publish is PR 9's custody rewrite's to carry.

## 7. Owed

- PR 8: the bitmap free list (the zero-census-reads pin), the index re-point to the allocation holder (`index_home_volume`), the C8 oracle's bitmap term.
- PR 5/12: foreign `MarkShared` at a wire holder; the gather cross-owner row; the manager-side `FreeBlocks { shared: true }` when the freeing ino is not the home's.
- PR 9: the PK4 `pack_group` batch-pack wire (left in place per the brief).
- The one-walk census for C8 + C16 (PR 1's owed item).
- The box bracket (gate 1 / PR 13–14): the packing row's `pack_blocks_sealed_dismount` beside the 20,000 → 148 figure; a release-build probe row.
- The instrumented fuzz run of `manager_call_frame` with the PR-7 arms (nightly tier).
- (round 2) The un-share of a surviving sole owner (PR 12, with the wire protocol's "share in flight" witness); the S9 `WireBlockRefOp` SHARED bit (PR 9); a per-scope index for the packer's `any_open` scan if a promotion-path profile ever names it (review Issue 12b — declined, see the review file).

## 8. Shared-file touches (the level-4 laws)

- `tests/run_sym_forest_suites.sh` — a comment paragraph + `sym_pack_tests sym_shared_refs_tests` appended to `DEFAULT_SUITES`.
- `src/fuse_client.rs` — `METRICS.fsck_shared_index_drift` + its JSON key beside `fsck_tenant_overlap_findings`; the PR-7 keys appended after `meta_kv_leaf_lease_refusals` (the END of PR 4's symmetric block).
- `src/meta_ship/manager.rs` — the PR-7 variant block at the END of `ManagerCall` / `ManagerReply` + their `name()` / service / client arms; the `shared_refs` import.
- `src/meta_backend/mod.rs` — `forest_ref_ops` / `forest_refs_owner` (applied at the listed entry points), `gather_mint_slot` + ONE call at the top of `pick_mint_slot_for`'s armed branch; PR 4's policy otherwise untouched.
- `src/meta_backend/kv/backend.rs` — one `#[path] pub mod shared_refs;` line, `xattr_name_allowed`'s by-name clause, `stage_block_refs` writing `op.value()`, `slot_trees()`.
- `src/env_knobs.rs` — untouched (no knob). `CLUSTER_WIRE_SCHEMA` — untouched (5).
- `AGENTS.md` — the PR-7 paragraph before "### Metadata-throughput program"; the "free list = complement" sentence amended. `docs/operations.md` — the PR-7 section before the FUSE transport section, the C16 row after C13, the observability row after PR 4's, one C16 mention in the repair-counter row. `docs/design-symmetric-metadata.md` — row 7 ONLY.
