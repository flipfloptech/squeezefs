# Symmetric shared-disk metadata — the PR run (PR 1 → PR 16 → release 1.3)

**Owner directive (2026-09-12, verbatim):** "we are skipping the hero run and
focusing on this PR RUN, start PR1 and go on to PR16 without stopping, run full
release gat| 2026-09-17 08:00 | pr-12 implemented (`b0f74896`, 15 commits, +2,858/−176; 12 contracts ×10 stamped; 38-suite matrix both legs 0 NOTE/HUNG; live-FUSE trio + posix under `--all-features`; fidelity quick 78/0 incl. a NEW `sym-join-ladder` leg on real nvmet — `format --symmetric`, the ladder, a `--read-only` token reader on real FUSE seeing a foreign create exactly at its next resolve, kill-9 + successor, zero residue). The join ladder as ONE entry; `-o ro` per slot holder; the roles retired forward-only; the `plane_gate` sweep table; every seam reachable in ONE process delivered (PR 4/5/6/7/8/9's, the `kvmap_read_tests` forest gap, 7b's allow-list). **Two defects on the real PR target**: the knob-armed manager held rtype 1 on its metadata namespace (the WERO posture keyed on PR 2's test partition alone); **SHIPPED S9, every layout**: a clean leave left the mount's own data-namespace registrant (the custody sweep's `WeroHold` clone held across its 10 s cadence — the release ioctl lives in the drop) → `WeroHoldRef` (`Weak`) → RELEASE_NOTES 1.3.0. **STOP-AND-REPORT**: the N-daemon backend posture (a second RW daemon on one volume — `open_joined_appender`, own ring/page/checkpoint on a wire region, tree 0 as a projection, wire-side lease acts) is NOT built; a second RW mount still meets D0's `FreshForeign`. **DECISION: PR 12b** (serial after PR 12 lands; PR 13 after it). Reviewer `01a0af25` launched. |
| 2026-09-17 13:00 | pr-12 review round 1 (`01a0af25`): NOT LANDABLE — **OPEN 19, BUGS 2**: rustdoc RED (6 errors — the fourth PR of the last six red on this gate line at first review); rung 3 silently overrides an explicit `SQUEEZEFS_MEMBERSHIP_BIND=off` into `auto` while five docs say 'explicit off is refused' (a knob-law violation). Adjudication honestly reflected (LANDED IN PART, the scope exact, a second RW mount refused loud) except PR 12b named nowhere. The shipped S9 WERO-leave fix's diagnosis and `Weak` fix confirmed RIGHT — needs a FLAT-layout pin + a RELEASE_NOTES bullet. Every gate green incl. fidelity 78/0 with the `sym-join-ladder` leg 35/35 on real nvmet. Fix round `01a0af71` launched. |
| 2026-09-17 14:10 | Owner clarification on PR 12b: it is the **MANY-writer** posture — every non-manager RW daemon on a volume joins as a full writer; N is unbounded by design (per-writer pages + rings with no ledger bound, per-slot leases over W = 2^16 × 64 rotors, membership sharded by home volume; the design's own arithmetic is 12.5k writers per shard). 'Second daemon' was shorthand for the first non-manager. The brief pins N = 2 and 3 in-process, N = 3 on real nvmet, N = 4 × 2 processes on the fleet; PR 13 measures at fleet N. |
| 2026-09-17 16:40 | pr-12 fix round 1 done (`a3efbae3`, 30 commits; 18 fixed + 1 reasoned wontfix): rustdoc green (the gate line a permanent row); unset `MEMBERSHIP_BIND` ⇒ `auto`, explicit `off` ⇒ refused at rung 3 (red: armed as `owner` pre-fix); PR 12b named everywhere (design row 12b with scope/deps/estimate; PR 13 deps `12b, 7b, 16`); the D0 refusal names the plane; the shipped S9 WERO-leave fix got its FLAT pin (red against the pre-fix arm by checkout) + the 1.3.0 bullet; **rung 7 now WIRES the writer-side census binding** (the step shipper + every Live appender's endpoint — found and fixed an advertised-endpoint defect in `cluster_wire`); the negative holder cache; `sym-crash` 3/3 with this tree. Matrix 38/38 both legs 0 NOTE/HUNG; fidelity 78/0. Re-review `01a0aff7` launched. |
e at the end with this tagged as 1.3".

Design: [`docs/design-symmetric-metadata.md`](../docs/design-symmetric-metadata.md)
(Rev 7, approved 0-open after 7 review rounds; owner rulings R-SYM-1..8 folded).
Run state: `/tmp/grok-justin/grok-exec-plan-dadee1dd.json` (plan `dadee1dd`).
Base: `dev` @ `89f34ed7`.

## Landing law for this run

Adapted from the execute-plan skill to this repo's laws (no PR stack):

1. Each PR is implemented by ONE subagent in an isolated worktree off the
   current `dev` tip (or its dependency's landed tip): red-first tests first,
   then the feature, then docs + the evidence note; the implementer runs its
   own suites + `cargo fmt` + clippy in BOTH configs. It never runs
   `task check`, never pushes, never touches `dev`.
2. A mandatory reviewer subagent reviews every PR in the implementer's
   worktree; a fix loop runs until 0 open issues (wontfix needs a technical
   justification; a stalemate escalates to the owner).
3. The orchestrator fetches the worktree's commits, fast-forwards them onto
   `dev`, and pushes. A batch `task check` from the pinned gate worktree
   (`~/Source/squeezefs-gate`, `/tmp/five/gate/gate.sh`) runs every 1–2
   landed PRs with the laptop otherwise IDLE (no implementer runs beside a
   gate). A red gate is fixed on `dev` before the next PR branches.
4. The squeeze-test solo re-gate (A-B-B-A, pre-PR dev tip vs the PR's
   rocky8 build, same profile both arms) ran at PR 1 (MET) and runs at
   13/14 — **owner directive 2026-09-14: the box runs the MINIMUM
   acceptance brackets only and is never used to verify that something
   works; every functional proof is local first.** PR 2 and every other
   rung take the local re-gate.
5. PR 15 (the cloud row) needs the owner's expressed approval at launch —
   asked up front 2026-09-12, declined-to-answer; it is the ONE planned stop.
6. Release 1.3.0 at the end: the full release chain from zero (task check,
   fstests `-g auto`, pjdfstests, LTP, require-mount, zc-capability, fuzz),
   the release act (version bumps across the four crates, RELEASE_NOTES,
   tag `stable-2026.MM.N`), `task dist:all`, the rocky8 pair to the box.

## Linearized order (levels)

| Level | PRs |
|---|---|
| 0 | PR 1 (forest), PR 16 (retire SPDK) |
| 1 | PR 2 (appender region) |
| 2 | PR 3 (manager lease, WERO, REGCTL) |
| 3 | PR 4 (slot leases), PR 11 (convert / format --symmetric) |
| 4 | PR 5 (read tokens), PR 6 (cross-owner tx), PR 7 (pack + shared refs), PR 8 (allocation lease + sharded membership) |
| 5 | PR 7b (dir striping), PR 9 (custody), PR 10 (recovery) |
| 6 | PR 12 (every mount a writer) |
| 7 | PR 13 (acceptance on squeeze-test + SIM-1 + fidelity) |
| 8 | PR 15 (cloud row — approval) |
| 9 | PR 14 (default flip) |

## Log

| When (local) | Event |
|---|---|
| 2026-09-12 23:45 | Run opened. pr-1 implementer `01a098c7` (feat/sym-forest) and pr-16 implementer `01a098c8` (feat/retire-spdk-target) launched in isolated worktrees off `89f34ed7`. |
| 2026-09-13 01:00 | pr-16 implemented (`d758eb8a`, 4 commits, −10,427/+1,920); reviewer `01a0990b`: 11 issues (0 bugs); fix round → `ee2c1974` (11/11 fixed); re-review `01a09931` launched. |
| 2026-09-13 01:40 | pr-1 implemented (`ef05b8a7`, 4 commits, +3,882/−698; 22 new contracts, 23 regression suites green); reviewer `01a09932` launched. |
| 2026-09-13 01:55 | **pr-16 LANDED** on dev `b2673eab` (7 commits, 3 review rounds, 13 issues fixed, 0 bugs). Landing evidence (design §7 3b): nvmet-only fidelity `quick` PASS 18/0 in 1m07s; `full` PASS 90/0 in 2m14s (11 legs incl. guard-nvmet-x10, teardown zero residue) — logs `/tmp/sym-run/pr16-fidelity-{quick,full}.log`. Venue note: root's PATH lacks `jq` on NixOS — run the tier with `sudo env PATH=<nix jq>:$PATH`. Batch gate owed (with pr-1). |
| 2026-09-13 02:05 | pr-1 review round 1 (`01a09932`): NOT mergeable — 16 issues (7 bugs: forest keys panic the write-side audit on block-ref/block-map records; refs probe only the native slot; KEY_SPACE_MAX interior pointers route to a phantom slot; deferred root publish loses the floor; flat hot path gained an Arc clone/drop; RO reader mis-roots guests; the stamped-suite parametrization never run). Fix round `01a0995d` launched. |
| 2026-09-13 03:20 | pr-1 fix round 1 done (`b349d809`, 11 commits; all 16 marked fixed; `tests/run_sym_forest_suites.sh` 15 suites × flat/stamped; fuzz targets + proptest mirror; `FOREST_SLOT_MAX` codec hole found by the round-trip law). Re-review `01a099c6` launched. |
| 2026-09-13 04:45 | pr-1 review round 2 (`01a099c6`): 14/16 verified-fixed; **OPEN 6, BUGS 3** — stamped `kv_leaf_merge_tests` nondeterministic (4 red of 7 full-binary runs, two convergence/throttle expectations; attribution owed); non-writer opens (`-o ro`, co-writer) MINT at replay ⇒ device writes from a reader (PR-substrate co-writer mount refused when the authority has a fresh slot in its window); replay mint on `claim_user` ⇒ a `heap_full` volume that crashed with an unpublished mint is unmountable; plus Issue 13 (literal still there), `fsck_repair_tests` forest branch + gate-list widening, two nits. Fix round 2 `01a099eb` launched (stamped leaf-merge ×10 from zero required). |
| 2026-09-13 08:00 | pr-1 fix round 2 done (`5c843311`, 25 commits total, 0 wontfix). Issue 17 attributed: line 184 = PRODUCT (§4.7 P1 — `admit_heap_batch` promised a member's leaves one at a time and never returned the earlier promise when a later leaf was refused; layout-independent, red 12/12 on the shipped flat layout; fix `2ccb8f72`), line 1294 = HARNESS premise (the D4 arm's order). Stamped `kv_leaf_merge_tests` ×10 green from zero on the final binary (40–45 s each), flat ×3. Issue 18 = SKIP law (`OpenPosture` → `MintPolicy::{User,Recovery,Refuse}`; `meta_kv_forest_reader_window_skips`); Issue 19 = replay mint on `claim_internal`; Issue 20 surfaced a second product defect — `SlotTrees::range` failed a whole page at a refused key ⇒ truncated census ⇒ fsck repair FREED LIVE BLOCKS; now skip-not-fail walks + a raw per-slot-tree C1 walk (`C1RawKey`) repaired in place. Gate list widened to 20 suites, 20/20 both ways. Re-review round 3 `01a09a9a` launched. |
| 2026-09-13 09:20 | pr-1 review round 3 (`01a09a9a`): 13/17–21 all verified-fixed; OPEN 4, BUGS 1 (low) — the skip-not-fail law leaked into `refs_window` (a forest under-counts a block's population on a malformed ref key where flat refuses loud); 23 non-writer partial window unstated; 24 N re-mints need N reserve extents; 25 `C1RawKey` repair deletes unknown kinds. Fix round 3 `01a09abe` launched. |
| 2026-09-13 11:00 | pr-1 fix round 3 done (`c28a495b`, 39 commits total, 0 wontfix, 16 files +1,101/−114): refs scan refuses (flat law); Issue 23 became a PRODUCT rule — a non-writer's directory pager lists a child iff it holds the child's slot tree (`readdir_page_held`, gauge `meta_kv_forest_reader_unpublished_children`); §7.1 freezes the kind set (a new kind = a new incompat bit); the raw C1 repair deletes only a NAMEABLE malformed known-kind key, unknown kinds report-only; replay counts its re-mints and refuses ENOSPC-class by count. The classifier's proptest law found two more codec holes (known kind in the by-block position; decoder accepting a slot above the namespace the encoder refuses) — both closed+pinned. 20/20 both ways. Final verification pass `01a09b11` launched. |
| 2026-09-13 12:05 | pr-1 review round 4 (`01a09b11`): 22–25 verified-fixed incl. the beyond-the-ask pager, codec and ENOSPC-count changes; **one new bug (Issue 26)** — on the forest `kv_scale_tests::rightmost_separator_pointer_record_replays_clean` idles for exactly the parked 60 s cadence with every thread asleep (7/8 in-sequence stamped runs, 1/5 isolated, one run > 500 s; flat never in 9) — a wait only the cadence releases (lost ring-full→checkpoint wake or a forest-only floor holding the tail). NOT LANDABLE until attributed. Fix round 4 `01a09b4a` launched (attribute first, product fix, ×10 within 2× of flat). |
| 2026-09-13 13:20 | pr-1 fix round 4 done (`d47f54dc`, 44 commits). Issue 26 attributed to the LINE — verdict (c), a SHIPPED layout-blind lost wake: `shutdown()` = set flag + `Notify::notify_waiters` (wakes only registered waiters, stores no permit); a checkpoint task INSIDE its maintenance pass missed it and parked a fresh `notified()` with the flag unread until the cadence fired (gdb: the awaiter is `re.shutdown().await`; `journal_full_stalls` = 0; zero CPU). The forest exposes it because its post-replay fold is one big mixed-leaf split still running at shutdown. Fix: the signal is a permit (`notify_one`) + the task re-arms on the flag; the pending-times drain task had the identical shape and is fixed too. Pin RED on `c28a495b` on BOTH layouts (task parked 59 s), green ≈ 15 ms. Acceptance ×10: pair 20.1–21.3 s stamped vs 17.3–18.4 flat (1.13–1.23×; pre-fix 79–80 s = 4.6×), isolated ≤ 1.2×; the > 500 s run not reproduced (0/31). Gate now TIMED (per-suite wall per leg, stamped/flat ratio NOTE), 21 suites, 21/21 both ways, max ratio 1.38×. Verification round 5 `01a09b8a` launched. |
| 2026-09-13 14:10 | pr-1 review round 5 (`01a09b8a`): Issue 26 verified-fixed (the reviewer re-ran the pin RED on `c28a495b` in a throwaway worktree, both layouts); LANDABLE with one nit (Issue 27: the drain task had the permit but not the pre-park flag re-check; the pin ordered on a 300 ms sleep) → fixed `13c5f1f3` (belt added; the pin orders on the `shutdown_signalled` trace edge). OPEN 0 / BUGS 0. |
| 2026-09-13 14:40 | **pr-1 LANDED** on dev `73d3e74e` (45 commits rebased clean onto `3228fcb8`; `cargo check --all-targets --all-features` + knob-registry/docs-parity/forest/SPDK-retirement suites green on the rebased tip). Totals: 6 review rounds, 27 issues (10 bugs), 0 wontfix. **Three SHIPPED, layout-blind defects found by chasing forest-only symptoms and fixed red-first on both layouts**: (1) `admit_heap_locked` stranded a refused multi-leaf member's earlier promises for the life of the mount (§4.7 P1); (2) `SlotTrees::range`/the census failing a whole page at one refused key ⇒ fsck repair FREED LIVE BLOCKS (forest arm); (3) `shutdown()`'s `notify_waiters` to the checkpoint and pending-times drain tasks stored no permit — a task mid-pass slept to the cadence deadline (≤ 1 s per unmount shipped; 60 s under the parked test cadence). Owed: batch `task check` (with PR 2), squeeze-test A-B-B-A solo re-gate (design gate 1), the one-walk fsck census, the orphaned-extent hygiene sweep, the nightly fuzz campaign for `slot_tree_record`/`slot_state_record`, the one unexplained > 500 s pre-fix run (note §7 recipe). |
| 2026-09-13 14:45 | pr-2 implementer launched off dev `73d3e74e` (feat/sym-appender-region). |
| 2026-09-13 18:40 | **PR-1 solo re-gate (design §8 gate 1) MET on squeeze-test** — `.benchmarks/2026-09-13-sym-pr1-solo-regate.md`. Arms A = `3228fcb8` / B = `a9827378` (both `release`, rocky8; built on the laptop, checksummed on the box), A B B A at RT=60 flat-vs-flat: `w_fresh` 0.997, `rr4k` 1.001, `rw4k` 0.985 (band 0.3 %, inside the 3 % floor — the one number PR 2's bracket re-reads), fresh mount 0.981, populated remount + mdstorm mkdir DELTA in bracket 1 → both within noise on the reversed re-run bracket; `dlm_rpcs == 0` on all 27 mount legs, every must-stay-0 gauge 0, sector 0 bit 17 = 0 on every A/B volume, `.stats` census diff = exactly the 5 forest gauges (all 0 flat). Stamped SCOPING leg (`SQUEEZEFS_TEST_STAMP_SYMMETRIC=1` at `format --force`): forest real (63 slot trees on the storm), fio par (1.006/0.994/1.005), mdstorm write phases +6–12 % on the forest; priced: node-append bytes 3.5–4× on `rw4k` (20 trees' dirty leaves per checkpoint, 4.2 MB/60 s absolute), populated remount +0.2–0.27 s. Rig bug on the first launch (`pkill -x` vs `squeezefs-A` argv — the D0 guard refused the second mount, correct) → count restarted from zero. Box footprint in the note §11 (`/scratch/tmp/squeezefs` = squeezefs-B for the reset script; `/scratch/tmp/sym-pr1/`, `/scratch/tmp/rigs/`). Box left unmounted. |
| 2026-09-13 23:10 | pr-2 implemented (`09477d40`, 20 commits, +5,473/−102, 24 files; 25 contracts red-first). Two dark-path product bugs found by its own live-FUSE leg and ×10 and fixed red-first: identity binding under D0 (a same-node successor at another mount slot read its dead predecessor's Live page as foreign → own = node token, the D0 flock is the death proof); the cadence tick read ring 0 only (a committer parked at a full DECLARED ring, no node lock, ring 0 idle ⇒ no cycle ever due — a wedge; `AppenderSet::ring_pressure` + `rings_uncovered` join the tick). Deviations recorded (SLOT_PAGE_BUDGET 108 at 35 B entries; directory extent = one heap extent with a trailing header; structure stays the manager's ring until PR 3's grants). 22-suite matrix 22/22 both ways, ×10 stamped + ×10 flat. Reviewer `01a09ca7` launched. |
| 2026-09-13 23:55 | pr-2 review round 1 (`01a09ca7`): NOT LANDABLE — **OPEN 21, BUGS 3** (all on the dark seam path): (1) re-join page-slot visibility — the leave writes `Free` to the directory pair but the next join's `Live` page lands at `page_slot_for(gen)`, a ring-side slot of the NEW ring on 2 of 4 residues, reachable only through an A/B image naming that ring ⇒ a crash before the next-but-one checkpoint reads the region as Free = acked loss (reproduced k=2,3); (2) clean-unmount ring-extent leak (`release_unpublished` after the final checkpoint, nothing writes the bitmap after — 1001 → 993 free extents per cycle); (3) `commit_compensation` reserves in ring 0 for a region's failed window ⇒ `Lease` violation at the next mount. Flat path verbatim (traced), codec total, tick fix + node-token binding correct. Fix round `01a09ccc` launched. |
| 2026-09-14 01:10 | pr-2 fix round 1 done (`06f123bc`, 24 commits total; 21/21 fixed, 0 wontfix): the DIRECTORY-FIRST page-slot law (a page naming a ring table the directory pair does not yet fully name goes to every unconfirmed directory slot in one barriered step; ring-side rotation only once both name it — pinned k=0..3); leave = pages → barrier → release → bitmap → barrier (pinned ×3 cycles, free extents equal); compensation reserves in the failed window's ring; ceiling audit measures the leaf's dirty AGE; per-ring elision tails; foreign-Live refusal deferred to the join after the claim gate (names PR 10 only; `claim clear` unblocked). `sym_appender_tests` 29/29 ×10 stamped + ×3 flat; 22-suite matrix both ways, no NOTE. Re-review `01a09d0d` launched. |
| 2026-09-14 01:45 | pr-2 review round 2 (`01a09d0d`): 20/21 verified-fixed; **OPEN 4, BUGS 1** — the leaf-age audit compares against the cadence TRIGGER (1,000 ms) while the tick makes a cycle due AT the ceiling ⇒ the must-stay-0 `appender_flush_ceiling_overruns` fires on a healthy stamped mount at the default cadence (reproduced: 3 in 3.5 s, ages 1,013–1,060 ms); Issue 18 reproduced (the leave's consumed checkpoint seq dies with the process ⇒ DUR-4 WARN on every clean partitioned remount); a stage-B compensation vs growth race (dark); a flat-path clock read per clean→dirty. Fix round 2 `01a09d2a` launched. |
| 2026-09-14 03:05 | pr-2 fix round 2 done (`9c758f28`, 28 commits total; 24/24 fixed, 0 wontfix): the flush ceiling is the LANDING ceiling (`checkpoint_landing_ceiling_ms`, one accessor, 1,100 ms shipped; steady-stream contract red 2 → green ×5; side-find: tree 0's unstamped leaf dragged a region's minimum age to 0 — stamped leaves only); `checkpoint_seq` resumes at `max(ledger.seq, resume_generation())` (tripwire `meta_kv_bitmap_generation_raises`); `windows_inflight` counts stage-B windows against growth (seam `TEST_CONVEYOR_HOLD_PRE_ROLLBACK`; the red pin saw the race materialise); the dirty-age stamp is forest-only. 33/33 ×5 stamped + ×2 flat; matrix 22/22 both ways. Verification round 3 `01a09d60` launched. |
| 2026-09-14 03:45 | pr-2 review round 3 (`01a09d60`): 18/22/23/24 verified-fixed, LANDABLE, one docs nit (Issue 25: `meta_kv_bitmap_generation_raises` is process-wide, not a per-volume appender gauge) fixed by the orchestrator on the rebased branch. **pr-2 LANDED** on dev `f24b1160` (29 commits rebased clean onto `307cab40`; `cargo check` + docs-parity green on the tip). Totals: 3 review rounds, 25 issues (4 bugs: re-join page-slot visibility, clean-unmount ring leak, compensation in ring 0, the ceiling audit vs the trigger), 0 wontfix; plus the two dark-path bugs the implementer's own live-FUSE leg found (D0 identity binding, the ring-0-only cadence tick). Owed: the LOCAL re-gate (venue directive), PR 3's grants (own-ring SMOs), PR 4's leases, PR 10's foreign recovery + `appender clear`, drain-then-grow, the nightly `appender_page` fuzz campaign. Batch `task check` (PR 16 + 1 + 2) launched on `f24b1160`. |
| 2026-09-13 22:25 | **Batch `task check` GREEN on `f24b1160`** (PR 16 + PR 1 + PR 2; pinned worktree, laptop idle, from zero, 21:17–22:25): 382 suites / 4,993 tests all ok, all 18 stages, both audits (1 allowed = the adjudicated RUSTSEC-2025-0141 bincode). Log `/tmp/five/gate/taskcheck.f24b1160-green.log`. Every code landing of the run so far is gated. pr-3 implementer launching off `16aaf9a9` (docs-only over `f24b1160`). |
| 2026-09-14 03:35 | pr-3 implemented (`1682cbac`, 18 commits, +6,966/−338, 38 files; 22 contracts red-first; 24-suite matrix both ways; fidelity `quick` PASS 43/0 LOCAL — 128 AND 1,024 registrants all REPORTED, the manager's kill-9 successor at 1.3 s vs the 45 s bound, WERO rtype 3 on the device). **Two SHIPPED defects fixed (adjudicated as the shipped-bug-fix exception to the flat law)**: the REGCTL read (the 63-registrant truncation, the design-review board item) and a **P0 on every layout** — a root swap journals no pointer record, so a kill between the swap and the next ledger record replayed through the PREDECESSOR root while the swap's in-window `free(old)` was parked; the bring-up cover's tail then released the LIVE root's extent and the next claim would have handed the INODES root out as a fresh image (fix: replayed parks deferred until the trees open, frees of mounted roots DROPPED — `meta_kv_replay_root_frees_dropped`). PR 2's deviation 3 reversed (a region's SMOs in its own ring). `CLUSTER_WIRE_SCHEMA` 4 → 5. Reviewer `01a09eb1` launched. |
| 2026-09-14 05:15 | pr-3 review round 1 (`01a09eb1`): both shipped-bug fixes VERIFIED correct and complete (REGCTL; the root-swap replay hole covers tree 0 + every slot tree + every flat tree, a non-root free cannot slip through, the flat path byte-identical otherwise). **OPEN 15, BUGS 2** (dark, PR 3's own machinery): a clean remount builds `RegionGrant::default()` while tree 0's record still names live images ⇒ pre-remount images' retirements silently dropped (reproduced ×6 cycles, C13 blind); wire integers as authority (`ReturnExtents` expands a wire run into a 32 GiB `Vec`; explicit `want` bypasses the grant cap). Also: `kv_smo_crash_completeness_tests` red STAMPED 3/13 on the BASE too (outside the matrix) — attribution owed. Fidelity quick 43/0. Fix round `01a09ed6` launched. |
| 2026-09-14 07:25 | pr-3 fix round 1 done (`d57e4e92`, 23 commits; 15/15 fixed, 0 wontfix): the grant recovered from tree 0 at EVERY open (law `record ⊆ RAM sets` pinned; the C13 final pass now real); wire integers validated before execution (interval intersection bounded by the record; `manager_verb_rejected` split from the must-stay-0 refusals; fuzz arm 3 'decode never allocates proportional to a wire integer'); every verb idempotent per §5.3.5. **Two more PR-2-surface defects found on the way**: R2-A a rejoined region's fresh ring took its predecessor's extents with stale entries on disk (replay walked into them — an acked release read back at count 1) → carved ring extents zeroed; R2-B a fresh ring restarted positions at 0 → LWW resurrected an acked release → the Free page keeps the head, the carve continues from `max(page.head_hint, ring0.head)`. `kv_smo_crash_completeness_tests` stamped 4/13 attributed: all four harness-shaped (per-kind locators / the join's cycle = the first checkpoint); suite made layout-blind, joins the matrix (25). Fidelity quick 43/0. Re-review `01a09f4a` launched. |
| 2026-09-14 08:10 | pr-3 review round 2 (`01a09f4a`): 14/15 verified-fixed (R2-A/R2-B check out; cap-learn = SCT-1/SCT-7 on a consecutive repeat only; the four crash-completeness reds genuinely harness-shaped). **OPEN 5, BUGS 2**: (16, reproduced) a clean unmount of a declared partition LOSES ACKED RECORDS when the final cycle's flush SMOs a leased tree — the shutdown fixpoint is ring-0-only, so the SMO's records (now in the region's ring per PR 3) are left uncovered and the leave clears the roots (`missing_acked_bases=2/18`); (2 residual) pre-dedup allocation ∝ frame runs × record extents. Fix round 2 `01a09f68` launched. |
| 2026-09-14 09:05 | pr-3 fix round 2 done (`bda6d938`, 26 commits; 5/5 fixed): the final-cycle fixpoint covers ring 0 AND every region; the leave takes ONE coverage verdict and leaves an uncovered region `Live` (loud, recovered as own residue at the next open — never a silent loss; pinned rounds 1..=8 + the belt); `coalesce_runs` → merge-walk intersection with peak allocation MEASURED in `tests/manager_wire_bounds_tests.rs` (10K dup runs × 4,096-extent record ≤ 2×frame + 2×record; the product was 320 MiB); `drop_returned` refuses Pending; the reactive INTERNAL refill asks `SMO_IMAGES_MAX = 4`, never the derived grant; `cleared + already ≡ named`. 70/70 ×5 stamped; matrix 25/25 both ways; fidelity quick 43/0. Verification round 3 `01a09f97` launched. |
| 2026-09-14 10:05 | pr-3 review round 3 (`01a09f97`): all verified-fixed, LANDABLE, two nits → fixed `0361985e` (the final tick waits for EVERY ring's head — the leave's guarantee holds where it landed on the belt; the test-only intersection wrapper deleted; the coalesce's overflow split loops). **pr-3 LANDED** on dev `0361985e` (27 commits on `c4f94e01`, no rebase needed; `cargo check` green). Totals: 3 review rounds + a nit pass, 21 issues (4 dark-path bugs — grant divergence, wire integers, clean-unmount acked loss, pre-dedup allocation — + the two SHIPPED fixes + PR 2's R2-A/R2-B ring-carve defects), 0 wontfix. Level 3 opens: pr-4 (slot leases) ∥ pr-11 (enable-symmetric / format --symmetric). Batch gate owed after they land. |
| 2026-09-14 08:00 | Level 3 launched in parallel off dev `5eca0e12`: pr-4 implementer `01a09fc8-6878` (feat/sym-slot-leases — the biggest rung: slot leases, mint policy, `apply_locked` gate, handover, `SlotOwners`/`SlotHolderCache`, loom `slot_lease_core`, `SQUEEZEFS_SYMMETRIC_META`) and pr-11 implementer `01a09fc8-9a9a` (feat/sym-convert — `enable-symmetric` offline conversion + `format --symmetric` + the real bit-17 stamp). PR 4 lands first; PR 11 rebases onto it. Batch gate after both land. |
| 2026-09-14 10:20 | pr-11 implemented (`cba6270d`, 6 commits, +3,297/−45; 22 in-process contracts + a live-FUSE converted leg; 26-suite matrix both ways). Built as a RELAYOUT driven by durable state (fold read → forest into fresh extents → ONE ledger write naming both roots → ONE sector flip → old trees freed → marker removed) rather than a journaled key-range move — a journal entry is interpreted by the layout bit (adjudication with the reviewer). **Two more SHIPPED findings**: (a) every clean unmount leaked the images its final checkpoint cycle retired (the fixpoint converged on ring coverage alone; now also on dirty bitmap pages) — FIXED red-first (`b47f7c02`); (b) `rename` never locks `I{moved ino}` while staging its ctime `Delta` ⇒ co-queues with the writeback `Put` on the same key (EIO in debug, a lost ctime in release; reproduced on flat/fresh/converted) — OUT of PR 11's surface, **routed to PR 4** (owns `backend.rs`). Reviewer `01a0a045` launched. |
| 2026-09-14 11:35 | pr-11 review round 1 (`01a0a045`): the relayout deviation ADJUDICATED design-consistent (every commit point atomic + torn-immune; six crash windows traced incl. an un-seamed one; the clean-unmount leak fix exactly diagnosed, one predicate not a second loop; the `open` split = the five lines claimed). **OPEN 13, BUGS 1**: a second refusal class fires AFTER the markers are on every volume (peak claim `old + old×fill/0.75 + 66` extents ⇒ `NoSpace` mid-build on a half-full volume; the quiesce remedy says 'mount', which the marker refuses; no `--abort`) ⇒ writer-unmountable until `format --force`. Also: the offline steps run without the D0 flock; the §5b rename handover cites the trait-impl line (the FUSE path is `RoutedMetaBackend::rename` → `routed_rename_local`). Fix round `01a0a073` launched (capacity preflight before the first marker; every write-free check first; `--abort`). |
| 2026-09-14 12:10 | pr-4 implemented (`1fea372c`, 12 commits, +8,377/−228, 37 files — the program's largest rung): `slot_lease_core` + 4 weakening-verified loom models (106/106), the five manager verbs, the arm at join, the affinity/rotor/overflow mint policy, the `apply_locked` gate, flush-then-transfer + phase histogram + cooldown, LRU/forced-shrink/region release, `SlotOwners` + `SlotHolderCache`, the membership carriage, 21 contracts; 26-suite matrix both ways; fidelity quick 43/0. Six defects found + fixed en route (a leased slot's root republished into tree 0 as `Unleased{g:0}` every checkpoint; the departing ring's window holding the slot; `Releasing` entries rewritten by the join; image extents left claimed in the departing grant — C13 double-free class; concurrent releases double-attesting; PR 2's `live` counting released regions). Reviewer `01a0a0a4` launched with PR 11's rename finding ROUTED in as Issue 1. |
| 2026-09-14 13:00 | pr-4 review round 1 (`01a0a0a4`): NOT LANDABLE — **OPEN 19, BUGS 8**. The headline: the 'owed' globally monotone record-seq space is a SAFETY item, not economy — record seqs are per-ring positions and the leaf fold is seq-LWW, so after a handover from a busier ring to a quieter one the new lessee's same-key writes carry LOWER seqs ⇒ shadowed in RAM, skipped at replay = acked loss (orchestrator: IN SCOPE for PR 4). Plus the routed `rename` lock hole (verified: only the two parents locked, the moved child's `Delta`/`Put` unlocked), `manager_release_slot` rewriting tree 0 with stale words before refusing, the clean leave keeping image extents in the grant (C13 double-free class), lost-update RMWs on the lease sets, the door passing `Releasing` slots (`Corrupt` to the user on a legal schedule), `N_floor` degenerate (`ewma_ship_ns` never fed), the settle refusing legal lower-`g` `Live` residue. Fix round `01a0a0d3` launched. |
| 2026-09-14 14:25 | pr-11 fix round 1 done (`55a8822f`, 9 commits; 13/13 fixed, 0 wontfix): capacity preflight from the write-free inspection BEFORE the first marker (`needed = Σ nodes(slot records) + nodes(tree 0) + 1` via the writer's own chunking; tie `extents_needed ≡ extents_written` pinned 66 = 66); every refusal precedes the marker; `--abort` restores a flat+marker volume (pinned at three seams) and refuses past the flip; `VerbFlocks` holds the D0 flock for the verb; the residue seq ceiling; tree 0 as the emptied slot's cursor home; the digest oracle over kinds 6/7. **Found by the abort contracts**: the hybrid ledger's `journal_tail_seq: 0` restarted the ring's seq space below the flat trees' records ⇒ a flat write in the pre-stamp window lost every per-key LWW fold — now the ledger + page keep the quiesced tail `T` (PR 3's re-carve law). Disclosed: `kv_smo_crash_completeness_tests::pending_free_wedged_shape…` red stamped in 2/3 full matrices today (`dropped 2 vs 1`; 9/9 isolated; green on the base) — adjudication routed to the reviewer. 36 contracts ×10; matrix 26/26 both ways. Re-review `01a0a113` launched. |
| 2026-09-14 15:05 | pr-11 review round 2 (`01a0a113`): 13/13 verified-fixed, LANDABLE after the rebase; OPEN 5 / BUGS 0 (a torn-stamp `--abort` guard; the abort path's seq floor; three nits). The kept tail `T` verified design-consistent (forest records at seq 0 ≤ T are checkpoint-covered; flagged for PR 4: the two ledger writes + the page write mint ring-position seqs outside checkpoint.rs/appender.rs). **The matrix flake is HARNESS**: the crash-completeness pin reads a process-global counter across `reopen`; the `probe_claim_best_effort` arm's posture-blind `open_inner` counts once before the real open (`2 vs 1`); recovery itself deterministic. Nit pass + harness fix `01a0a139` launched; the product half (gate the tripwire on `OpenPosture::Writer`) routed to PR 4. |
| 2026-09-14 15:50 | pr-11 nit pass done (`78a0f899`, 12 commits; 0 open): torn-stamp `--abort` guard (reads the DUR-5 backup copy; refuses naming `--resume`); the abort path's residue seq floor via ONE ledger record; the crash-completeness flake's harness remedy (`reopen` waits for the Layer-A flock — stamped ×10 green). PR 11 PARKED at 0 open awaiting PR 4's landing for its rebase (rebase items for PR 4's `backend.rs`: `pub TRANSIENT_FLOCK_WAIT`, one refusal string, the tripwire's `OpenPosture::Writer` gate). |
| 2026-09-14 16:15 | pr-4 fix round 1 done (`ead44013`, 26 commits; 19/19 fixed, 0 wontfix). **The seq-space law**: a slot's seq floor travels with the lease — every ring stamps `position + seq_offset`, a release records the departing frontier as `seq_floor` (tree 0 v2), a grant `fetch_max`es the receiving ring's offset, durable on the page; flat rings keep offset 0 (byte-identical). The shipped `rename` lock hole fixed (discover → `lock_many` the full set incl. `I{moved}` + the overwrite victim → revalidate → retry; red on both layouts). `ReleaseSlot` consults the table before its write; the leave writes `Releasing` first and moves images out of its grant; lower-`g` `Live` = stale residue; the commit DOOR (tokens, park on handover, `SlotBusy` = EAGAIN, one-writer RMWs); `N_floor` fed; the door's Dekker pair on SeqCst fences + loom model 5 (107/107). **P0 found beside the round**: a region past its page budget pinned the ledger tail → clause-b fail-stop after 8 cycles — overflow roots now published into tree 0's `Leased` records. 27-suite matrix both ways; fidelity quick 43/0. Re-review `01a0a172` launched. |
| 2026-09-14 17:00 | pr-4 review round 2 (`01a0a172`): 19/19 verified-fixed (the seq-space law correct as built; the rename fix real; the door's Dekker pair a real loom model with the weakening recorded). **OPEN 5, BUGS 4 — consequences of the seq-offset change the pins did not reach**: rollbacks still address overlay records by raw POSITION (a rolled-out member stays applied in RAM on a ring that took a handed-over slot); complete `tails` in ONE tree-0 value (`ValueTooLarge` wedge past ≈ 5,400 leaves; `MAX_ENTRY_LEN` at ≈ 90 leaves at the leave ⇒ the next open refuses); the per-slot frontier is a `fetch_max` of positions across rings (B never clears after A→B); the first-touch acquire parks on ring space holding `manager_verbs` ⇒ deadlock with the checkpoint's grant verbs. Fix round 2 `01a0a196` launched. |
| 2026-09-14 20:40 | pr-4 fix round 2 done (`cea6a963`, 41 commits; 5/5 fixed): ONE overlay-address type (`journal_core::SeqSpan`) at both rollback arms — and a THIRD site the grep found: the §4.7 pending-free gate compared a stamp against the position tail (a retired extent parked `seq_offset` past its entry forever on a quiet ring; 3 SMO sites + both replay folds); tails on their own tree-0 record `slot_tails:{s}` (inline under the cap, spilled to barriered heap extents above; cap enforced loud; the leave chunked); the frontier reset to the receiving ring's head at `install_lease`; the acquire admits its entry BEFORE `manager_verbs`; page layout version 55. **Two more finds by the fixtures**: the round-2 `Releasing` gate refused the departing holder's OWN flush (every tree with pending splits failed its handover) → `apply_locked_structural`; PR 3's reactive refill clamped to a cap a fat split could never satisfy while idempotency answered the remainder verbatim forever (1,789 deferrals/90 s, D1.b escalations) → `GrantExhausted{needed}`. 40 contracts ×5 stamped; 27-suite matrix both ways; fidelity quick 43/0. Re-review `01a0a25c` launched. |
| 2026-09-14 21:30 | pr-4 review round 3 (`01a0a25c`): 20–24 + both fixture finds verified-fixed (the one overlay-address type everywhere; `peek_tail_offset` EXACT for a flushed quiescent leaf — the safe direction for PR 5's screen; the INTERNAL refill bounded by the split's own image count). **OPEN 3, BUGS 2**: the structural gate refuses the MANAGER's maintenance of an UNLEASED slot tree (`merge_underfull`/`run_merge_sweep` walk `all_trees()` unfiltered ⇒ the must-stay-0 refusal gauge counts every cadence, the heap-full recovery sweep never passes that tree); a valid-checksum FOREIGN-LAYOUT page is swallowed as absent by the four-slot reader (page 0 → `manager_lease vacant`, live roots lost) instead of refusing loud. Fix round 3 `01a0a27f` launched. |
| 2026-09-14 22:50 | pr-4 fix round 3 done (`fd28c530`, 49 commits; 3/3 fixed): `LeaseGate::verdict_structural` (leased admitted; UNLEASED admitted on the manager only; a slot another appender leases refused, `merge_sweep_foreign_skips`); `maintainable_trees` the ONE filtered population for sweep/recovery/D4/census/audit; `PageRead::ForeignLayout{version}` refuses the open loud (torn pages still fall to the predecessor); the release's cold-tree I/O floor priced. **Two more finds by the pin**: `dead_bset_census` resolved a leaf's tree by header id alone (every slot tree's id is 0 ⇒ no guest tree ever had a D4 merge candidate since PR 1); `JoinAppender` reused a Free page id a RELEASED in-process region still held (a wire joiner routed as that region). Final verification `01a0a2c0` launched (incl. the 27-suite matrix this round skipped). |
| 2026-09-14 23:35 | pr-4 review round 4 (`01a0a2c0`): 25–27 + both finds verified-fixed; 27-suite matrix no NOTE; fidelity 43/0. **OPEN 1, BUGS 1** (wire appenders only): the `foreign` bit is installed AFTER a wire grant's durable write and the sweep filter is a per-pass snapshot ⇒ a grant of an unleased tree racing the manager's sweep merging it lands an SMO in ring 0 for a slot leased elsewhere (or trips the belt on a legal schedule). Fix round 4 `01a0a2e2` launched (mark foreign at `table.acquire`; the sweep re-checks under the SMO mutex and skips-not-refuses). |
| 2026-09-15 00:20 | pr-4 fix round 4 done (`07704f86`, 53 commits): Issue 28 closed by THE GRANT WAITS — a non-manager grant takes the per-volume SMO mutex before `manager_verbs` (the checkpoint task's own order); ring 0's window cycled clear of the handed-out slots under it; `foreign` marked at `table.acquire`, rolled back with the table on every failure path. Found on the fixed path: the manager's merge of a RELEASED tree stamped its parent flips in ring 0 below the departing ring's seqs (LWW shadowed them) → the release takes the seq-space law's grant step for ring 0 too. Pin ×10 stamped. Verification round 5 `01a0a303` launched. |
| 2026-09-15 01:05 | pr-4 review round 5 (`01a0a303`): Issue 28 verified-fixed (lock order checked at every holder; the clearing loop converges on in-flight windows; every rollback path covered; `fetch_max` monotone; 27-suite matrix no NOTE). **OPEN 2, BUGS 1**: the wire `ReleaseSlot`'s slot words are UNVALIDATED (`words.into()` straight through) — a `seq_floor` near `u64::MAX` saturates every stamp on ring 0 ⇒ the next replay drops every window record (acked loss, seq space exhausted); a wire `cursor = u64::MAX` becomes the never-lowered ino floor; a bogus `root` overwrites tree 0's — PR 3's bounded-execution law had no counterpart here. Fix round 5 `01a0a319` launched (validate every wire slot word against durable/derived state before any effect; fuzz arm). |
| 2026-09-15 02:00 | pr-4 fix round 5 done (`50ce83d3`, 55 commits; 0 open): every wire slot word screened BEFORE any effect against the appender's own `Live` page + the grant (`seq_floor` strictly above the grant's floor and ≤ a manager-derivable bound; `cursor`, `slot_tree_extents`, `root` verified — the root's header read; identity screen on every slot verb); every verb's `Rejected` carries `STATUS_REJECTED` (only `ReturnExtents` did); `GrantDeferred` EAGAIN for a slow legal schedule; `COVER_CYCLES_MAX` one law. Red pin: the `u64::MAX` release LANDED on `07704f86`. Verification round 6 `01a0a348` launched. |
| 2026-09-15 02:45 | pr-4 review round 6 (`01a0a348`): 29/30 verified-fixed (the bound derivable and tight given a refreshed `head_hint`; the header read outside `manager_verbs`; `Live` the right admit class). **OPEN 3, BUGS 1 — gate stability**: the Issue-25 pin's witness is a GLOBAL merge-counter delta a legal earlier merge satisfies (the release's covering flush or the backlog sweep) ⇒ ~8 % flaky stamped (11/1 isolated), and the 27-suite matrix went RED on it. Product correct; the PIN witnesses the tree from now on. Plus: the wire joiner's `head_hint` never refreshed in PR 4 (a > 2-lap release would be refused — a PR-12 obligation, safe degrade meanwhile); the root witness ignores the node's slot. Fix round 6 `01a0a35e` launched (×20 stamped isolated + the matrix). |
| 2026-09-15 03:40 | pr-4 review round 6 (`01a0a348`): 29/30 verified-fixed; the Issue-25 pin's global-counter witness ~8 % flaky stamped (a legal earlier merge satisfies it) → fixed as a PIN (tree witness under a parked cadence, ×20 stamped; the class swept across the suite) + the wire joiner's `head_hint` obligation stated for PR 12 with a safe degrade + the root witness checks the node's slot. **pr-4 LANDED** on dev `89645637` (58 commits, no rebase needed; `cargo check` green). Totals: 7 review rounds, 33 issues (15 bugs — the seq-space SAFETY law, the shipped `rename` lock hole, a page-budget P0, the grant-vs-sweep race, the unvalidated wire slot words, …), 0 wontfix; loom 107/107. **pr-11 rebased onto pr-4** (three additive conflicts composed by hand; ONE semantic seam: PR 4's `Unleased` v3 record — `slot_tree_extents` from the writer's node count, `seq_floor` = the kept quiesced tail, `258eb8ba`) → seven suites green on the combined tree, the conversion suite green on BOTH legs → **pr-11 LANDED** on dev `258eb8ba` (13 commits). Level 3 complete. Batch `task check` (PR 3 + 4 + 11) launching on `258eb8ba`+. |
| 2026-09-15 03:00 | Batch gate on `4a752431` (PR 3 + 4 + 11): every stage GREEN through the fork/fuzz tail — 389 suites / 5,143 tests — and RED at the LAST stage, `task audit`: **RUSTSEC-2026-0285** (`rustls` 0.23.40 — TLS 1.3 handshake messages accepted across encryption-level boundaries, medium; fixed ≥ 0.23.45) landed as an advisory since the previous gate (advisories arrive without commits — the nightly tier's reason to exist). Bumped `rustls` → 0.23.45 (+ `rustls-webpki` 0.103.15) in the root lockfile (`c8e885d3`; the fork carries no rustls); the four wire suites green; root audit back to the two adjudicated warnings. Gate re-launched on the bumped tip. |
| 2026-09-15 03:05 | **Level 4 launched FOUR-WAY** off dev `6f8d44e6`: pr-5 tokens `01a0a3dc-4a85` (feat/sym-tokens), pr-6 cross-owner tx `01a0a3dc-6e27` (feat/sym-cross-owner-tx), pr-7 pack+refs `01a0a3dc-93c2` (feat/sym-pack-and-refs), pr-8 alloc lease `01a0a3dc-bb6c` (feat/sym-alloc-lease). Common coordination brief `/tmp/grok-justin/grok-exec-prompt-dadee1dd-level4-common.md` (ownership table, wire discriminant ranges 0x60/0x70/0x80, append-only shared files, landing order = completion order). The rustls bump full-gates with the first level-4 landing. |
| 2026-09-15 05:25 | pr-8 implemented first of the four (`d596ffca`, 4 commits, +6,490/−9, 33 files; 20 contracts — 3 red on first run, one a REAL plane defect: the remount's bring-up checkpoint advanced the ledger tail past the window before any allocation lease was re-held, so the recovery arm found nothing while the pages showed the granted range FREE — the deltas now kept until the recovery arm folds them). New: `data_alloc_bitmap.rs`, `block_grant.rs`, `kv/alloc_lease.rs`, `park_core.rs` + `park_gate.rs`; matrix 29/29 both ways (2.2–2.4× ratio NOTEs attributed to a FULL DISK under four implementers — I deleted 200 GB of landed worktrees' `target/`: 356 → 552 GB free); ×10 stamped; fidelity quick 43/0; loom park_gate + grant_table. Deviations: `alloc_lane_grant.rs` not renamed (60 refs in files the parallel PRs edit); grant top-up asked by the writer; every appender homed on volume 0 until PR 12. Reviewer `01a0a45a` launched. |
| 2026-09-15 06:35 | pr-7 implemented (`e43fd614`, 8 commits, +4,232/−100; 19 contracts incl. `a_clone_racing_a_truncate_never_frees_or_patches_a_shared_block`; loom `shared_ref_core`; matrix 30/30 both ways; ×10 stamped; the 46-suite C8-drift sweep flat 46/46, stamped 45/46 — the one red `kvmap_read_tests` is stamped-red on the BASE too, a flat-shaped instrument nobody had run stamped, PR 1's owed class). Found the W1 one-slot probe had NO production caller → `DataRouter::sole_owner_durably` at the patch site (a predecessor lessee's reference refuses the patch into CoW). Scoping: probe 6.1 µs vs whole-volume 159.5 µs (26×); 2,000 files → 15 blocks unarmed vs 262 live packs on the tcp substrate, `defrag --pack` freeing 255. Reviewer `01a0a49b` launched. |
| 2026-09-15 07:45 | pr-8 review round 1 (`01a0a45a`): NOT LANDABLE — **OPEN 21, BUGS 6** in the durable-truth core, two reproduced in-process: the data bitmap pages are written AFTER the ledger record that advances the tail (a kill between loses every journaled SET below the new tail ⇒ DOUBLE ALLOCATION — the headline crash row open; the pin crashed before any checkpoint); `finish_free`'s CLEAR journaled at the NEXT checkpoint, after a re-grant's SET ⇒ seq-LWW folds a granted block CLEAR; `write_data_alloc_pages` runs on EVERY volume's checkpoint with a filter comparing a constant to itself ⇒ volume 1 writes a `KVDA` page onto ITS device at volume 0's heap offset (every contract mounted one volume); the `poison` split is class-blind (the S9 custody client PARKS — DMA unpoisoned, no expiry driver); the pre-`T_self` arm parks then returns loop-terminal ⇒ a permanent silent park; `AllocLeaseAcquire.blocks` / bitmap refs persisted unvalidated. Plus `install_block_grant_arm` has ZERO callers — 'the armed writer allocates from its grant' undelivered and unstated. Gates green (matrix 29/29 both legs, NO ratio NOTE — the 2.2–2.4× rows read 0.99×/1.16× on a free disk). Fix round `01a0a49c` launched. |
| 2026-09-15 08:00 | pr-6 implemented (`54eecbfb`, 5 commits, +5,590/−1,120; 15 contracts; matrix both legs, 0 NOTEs; ×10 stamped; live-FUSE trio both legs). Its own scoping instrument (200 foreign creates + 200 cross-holder directory renames) caught a REAL two-mutual-initiator deadlock — the holder's `insert_dentry` parked behind a 4a stripe the initiator held across the ship; the first build's 'no foreign guard travels' deviation was unsound → fixed to §5.6 line 1: guards TRAVEL (`XvGuards` 0x62 parks them at the holder under the op's scope, tables in ascending appender-id order, `XvRelease` 0x63 on the guard set's drop, scope expiry with the initiator's lease — `xv_cross_owner_guard_expiries` must-stay-0). Scoping: create-in-foreign-dir 863 µs/op (72 % the file-backed intent barrier), cross-holder dir rename 2.66 ms/op. D18 arms deliberately kept (reachable on the flat PV path). Reviewer `01a0a4b1` launched. |
| 2026-09-15 08:55 | pr-5 implemented — the last of the four (`18847944`, 9 commits, +5,799/−142; 16 contracts; ×10 stamped; matrix both legs no NOTE; live-FUSE trio both legs). `TokenCall` on its own verb block; record-carrying grants; the recall hook ONE fn at the top of the conveyor pass under the batch's 4a guards; `-o ro` = member-reader + token client (every reader TTL derives to 0, `reader_staleness_bound_ms` = 0, zero device writes pinned); recall-driven free-grace with the ring as the timeout path; frame v2 + the 3-rule screen at both loaders through ONE accessor; the idle poll 128 KiB → 4 KiB. Deviations: the epoch fan-in retired as the QUALIFICATION, kept as the `=0` mechanism (deletion = PR 14); the reader dials `SQUEEZEFS_MW_AUTHORITY` until PR 12's per-slot binding. A harness with a hardcoded v1 `NodeLayout` found + fixed. Reviewer `01a0a4b4` launched. **All four level-4 PRs now in review/fix.** |
| 2026-09-15 09:50 | pr-7 review round 1 (`01a0a49b`): NOT LANDABLE — **OPEN 17, BUGS 4** + a RED gate (rustdoc `-D warnings`, 11 intra-doc errors, the note's gate table omitted the line): `sole_owner_durably` counts references but IGNORES the SHARED flag it fetched (the durable half of law 2 incomplete, masked by a RAM mark the wire arm cannot set); a staged-source clone under a full staging ring always aborts `ENOENT` and double-releases; `share_block`/`release_shared` `Try`-admit under `manager_verbs` (a busy ring 0 fails clones and `let _ = free_block()` leaks references); a legal truncate-shrink re-Puts the ref with flags 0, stripping SHARED (C16 permanent). **`kvmap_read_tests` stamped red re-attributed as a FOREST PRODUCT GAP** (the `TREE_BLOCK_MAP` arm's A7 probation admission — no slot-tree leaf takes it): PR 1's owed item, must close before the release gate. Fix round `01a0a4d0` launched. |
| 2026-09-15 10:20 | pr-6 review round 1 (`01a0a4b1`): NOT LANDABLE — **OPEN 20, BUGS 5**: rustdoc red (three private intra-doc links); the ancestor walk under the set-wide lock SELF-DEADLOCKS (each hop's `lookup_exact` takes a shared `D{}` stripe guard in the table where the initiator holds exclusive `D{}` — a collision parks it behind itself holding `dir_rename` ⇒ every directory rename in the set wedges); unscreened wire identities on `DirRenameLock/Unlock` (any peer unlocks anyone; accepted on any volume); the roll-forward cadence is a TIMER and re-applies the scanned plan without re-reading under guards (a live op spanning two ticks ⇒ a resurrected name — C10's leak class — or a re-minted compensated child; a RAM ghost trips `intents_stuck` forever); the cycle pin is hollow (both initiators as appender 0, `already: true`; the renames share `I{b}`). Verified correct: the traveling-guard ordering (own table ranked at `own_id` inside the ascending walk), parked guards visible to local ops, seam-driven crash windows; D19/D20 arms are LIVE flat code (title overclaims). Fix round `01a0a4d2` launched. |
| 2026-09-15 10:45 | pr-5 review round 1 (`01a0a4b4`): NOT LANDABLE — **OPEN 20, BUGS 7**: rustdoc red (9); **the central law violated on a load-selected schedule** — a first-touch grant races the pass (`serve_grant` records the grant AFTER reading; `recall_and_wait` marks inflight only for existing holders and short-circuits at `outstanding == 0`) so a reader installs pre-commit records no recall reaches; the recall gate is process-global (a `=0` S5 reader on the same set loses its free-grace protection — a data-plane regression); voluntary release runs no drain/purge; the deadline is a `T_owner` TIMER (a full 45 s stall per dead reader, other grants never swept) not the lease's expiry; the ack law inert under two A/B levers; the R5 bound nominal. Wire block collision-free; matrix both legs no NOTE. Fix round `01a0a4e3` launched. **All four level-4 PRs in fix round 1.** |
| 2026-09-15 13:00 | pr-8 fix round 1 done (`07d2c60a`, 8 commits; 21/21 fixed, 0 wontfix): the data bitmap pages take the meta bitmap's exact order (before barrier #1 and the ledger record; seam `TEST_CHECKPOINT_HALT_AFTER_LEDGER`; the round-1 order re-read RED); every bit mutation + delta under one per-holding lock with a head-first drainer, CLEAR at `finish_free` via a single-flight journaler, deltas carry the holder term; `AllocHolding::home` binds the pages to the holder's home backend; `self_fence` shipped-terminal for every existing caller, `self_fence_as(SymmetricAppender)` parks (renewal tick its one caller); `Parked` retryable with expiry at `T_park_max` (pinned over the wire); the wire screened. **Deliverable 7 delivered**: `arm_symmetric_allocation` on the mount path (acquire → hold/re-hold/same-node takeover → publish → grant arm; 50 % top-up off the write path; the bitmap IS the free list armed; the 2-writer pin mints 200+200 disjoint). 30 contracts ×10 stamped; matrix 29/29 both legs; fidelity quick 43/0. Re-review `01a0a529` launched. |
| 2026-09-15 13:50 | pr-6 fix round 1 done (`0078001f`, 22 commits; 20/20 fixed, 0 wontfix): both ancestor arms read GUARD-FREE (the lease IS the consistency) + an O(1) directory-parent memo (the collision pin red at 5 s pre-fix); `screen_dir_rename_words` (volume 0 only, Live non-own id, holder-only unlock; fuzzed); the intent register carries `InFlight`/`Abandoned` — the cadence adopts ABANDONED (lease-expired) only, `recover_one` re-reads under its guards (the resurrected-name pin red pre-fix); the cycle pin has two identities + disjoint guard sets and FAILS with the lease a no-op (verified, reverted); RAII lease; child ino screened; lazy scope mint (unarmed hot path pays nothing). Title reworded: 'D18 reversed (the D19/D20 arms stay for the flat recipe until PR 12/14)'. ×10 stamped; matrix both legs (one NOTE = load; 1.00 alone). Re-review `01a0a545` launched. |
| 2026-09-15 14:20 | pr-7 fix round 1 done (`e751788d`, 13 commits; 16 fixed + 1 reasoned wontfix): rustdoc green; `RefProbe { count, shared }` — sole ownership = `count == 1 && !shared`, run before the incarnation retire; the protocol only when no spill site (a staged clone never surfaces `StorageFull` — `FuseRig` pin on a full 4 MiB ring); `HeldAdmission` parking before `manager_verbs` on both shared verbs, a journal-failure keeps the reference (`shared_release_failures` must-stay-0); `cancel_same_reference_pairs` at both translators (a re-description writes no op — SHARED survives); same-slot clones of unshared blocks skip the protocol; the permanent-SHARED survivor PRICED (one CoW per first touch; un-share owed to PR 12). Every red pin verified red by reverting its fix alone. Matrix 30/30 both legs no NOTE (a first run red at PR 2's wall-time contract under load ≈ 9 — 34/34 ×3 at load ≈ 2; bound not moved). Re-review `01a0a54e` launched. |
| 2026-09-15 15:30 | pr-8 review round 2 (`01a0a529`): 21/21 verified-fixed (the one ordering law for both bitmaps; no device I/O under the holding lock; a lost CLEAR is the safe direction; the S9 client's fence terminal; the park pinned through the production tick; the arm real on the mount path). **OPEN 5, BUGS 2 on the delivered arm**: the checkpoint's belt drain admits in the USER class inside the cycle ⇒ livelock under ring pressure → D1.b; the FIRST hold writes an all-clear bitmap never seeded from the derived allocator ⇒ arming a POPULATED volume re-grants LIVE blocks (reproduced: `population = 0 (live blocks: 3)`, first armed mint = block 0). Also: `sym_slot_transfer_tests` shows three load-shaped flake signatures (PR 4's suite; errnos captured) — routed to PR 4's owner at the next rung. Fix round 2 `01a0a574` launched. |
| 2026-09-15 15:55 | pr-6 review round 2 (`01a0a545`): 20/20 verified-fixed — **LANDABLE, OPEN 7 / BUGS 0** (the guard-free exact reads confirmed against the RAM-authoritative tree with the parent memo confirmed per hop; adoption by register STATE, no timer; the cycle pin's two guard sets disjoint, the 200 ms an observed wait). Seven non-bug items (the process-local intent scan = a PR 12 obligation; a sweep/doc contradiction; the ticket join's kernel reliance to be stated; two pin gaps; the compensation window; a duplicated derivation). Matrix 29/29 both legs, 0 NOTEs. Nit pass `01a0a578` launched — PR 6 lands FIRST of the four. |
| 2026-09-15 16:20 | pr-7 review round 2 (`01a0a54e`): 17/17 verified-fixed — **LANDABLE, OPEN 4 / BUGS 0** (the durable probe reads SHARED from the record; the spilled clone names only its private copy; no path parks under the mutex; pair cancellation on the full four-field identity). Adjudicated: 'un-share owed to PR 12' accepted — on an armed set a file cloned across slots pays ONE whole-block CoW on the first small overwrite of each formerly shared block after the other clone is deleted (flat patches in place), bounded at one rewrite per block, then par; same-directory clones pay nothing. Two PR 12 requirements recorded (the served `MarkShared` under the W1 site's 4a guard; through the routed door). Matrix 30/30 both legs no NOTE. Nit pass `01a0a582` launched — PR 7 lands second. |
| 2026-09-15 16:45 | pr-7 nit pass done (`9c2835bf`: O(n log n) pair cancellation with an early-out; `Weak` hooks; the served-mark window DETECTED via `served_mark_shared_under_patch` on `invariant_tripwires` until PR 12 closes it; one probe per release). **pr-7 LANDED** on dev `cdead68d` (14 commits rebased clean; `cargo check` green) — first of the four. Totals: 2 review rounds + nit pass, 21 issues (4 bugs), 1 wontfix (12b, accepted). Owed to PR 9: the two backend-side recompute translators' pair cancellation; to PR 12: the served `MarkShared` through the routed door + under the W1 site's guard, un-share on population 1. |
| 2026-09-15 18:10 | pr-6 nit pass done (`f2decdb7`: the intent scan scoped to slots this mount's step-home is Local for — the cross-process face PR 12's; the sweep keeps an unknown client's scope for the grace belt; ONE permit per (volume uuid, identity) replaces the ticket join (no `s_vfs_rename_mutex` dependency); the retirement rides the last local inverse's entry; the cycle pin's pair disjoint by construction + `Covered`/tripwire pins; one dentry-capacity derivation). **pr-6 LANDED** on dev `78dedf4c` (33 commits rebased onto PR 7: 8 conflicts in `ManagerCall`/`ManagerReply`/the fuzz mirror composed PR 7-block-then-PR 6-block with two split struct variants repaired by hand; docs rows composed; six seam suites green incl. the S8 census at 17 verbs). Totals: 2 review rounds + nit pass, 27 issues (5 bugs), 0 wontfix. Owed → PR 12: the cross-process intent adoption. Batch gate (rustls + PR 7 + PR 6) next. |
| 2026-09-15 18:15 | Gate cadence deviation, stated: the batch gate for rustls + PR 7 + PR 6 is DEFERRED to the level-4 sync point (after PR 5 and PR 8 land, before level 5 launches) — PR 5's and PR 8's fix rounds are building on the laptop and the gate's timing contracts flake under load (the standing board item); one gate then covers five landings instead of the usual one-to-two. Every landed tree has passed its own suites, both clippy configs, rustdoc and the 29–30-suite matrix locally. |
| 2026-09-15 19:30 | pr-8 fix round 2 done (`ccad6da1`, 12 commits; 22–25 fixed, 26 routed): the checkpoint never drains on the cycle (a non-empty queue re-kicks the single-flight journaler off it; a refused window counted `data_alloc_bitmap_deltas_deferred`); the FIRST hold seeds the bitmap from `BlockAllocator::derived_allocation_snapshot()` (below-cursor ∧ not-on-free-list ⇒ SET incl. limbo/quarantine/grace/trim-window; pages + barrier before the record; every hold LOSS-checked → the arm refuses loud on drift; the flat free-list-first pass gated behind the grant arm); `leave_refused()` latch on every volume's shutdown. Pins red against round 2 (`JournalReserveExhausted { needed: 61 }` failed the cycle; the populated-volume repro). 33 contracts ×5 stamped; matrix 29/29 both legs no NOTE. Disclosed: a stale-binary ×5 (7/7 red on the drained pin) exposed the misdirection pin's raw-byte fixture race → page MAGIC witness. Re-review `01a0a5c3` launched. Disk 87 % — targets of landed worktrees to clear next. |
| 2026-09-15 20:10 | pr-8 review round 3 (`01a0a5c3`): 22–26 verified-fixed — **LANDABLE, OPEN 3 / BUGS 0** (queued-but-unjournaled deltas safe in both directions; the seed leak-safe; the process latch = the intended 'the mount is parked'). Matrix 29/29 both legs in ONE run, no NOTE — PR 4's load signatures did not recur on the quieter box. **Issue 27 is a rebase hazard**: the first-hold seed depends on the allocator's mount-time derived seed, which PR 7 (now on dev) SKIPS on a forest ⇒ the snapshot would be empty and the LOSS check blind on a PR-7 forest. Rebase onto `19dd525a` + the forest seed (per-slot refs probe ∪ shared index) + 28/29 handed to the implementer as one pass `01a0a5e1`. |
| 2026-09-15 20:40 | pr-5 fix round 1 done (`2bbf267f`, 36 commits; 20/20 fixed, one sub-item 16(c) wontfix-with-rationale stated as owed): `token_grant_core` — register-before-read, whole-union in-flight marking, park-to-settle (2 loom models, weakening = steps swapped → red); lease expiry INSIDE the wait loop + the dead-member sweep; per-reader-class free-grace gate (an S5 `=0` reader keeps its epoch qualification); one drained release path; byte-bounded R5 cache; per-ino purge; session pool + paged records; `NotHolder`; rollback recall; the `pass_token_recall` phase; token-wire fuzz + proptest. **Own late find** (`15267a3c`): a released tree read EMPTY after remount because a release keeps `g` — screen rule 4 made inert on unleased; pinned; the ledger restarted from zero. ×10 stamped; matrix both legs no NOTE (attempt 1's flat leg lost 3 PR-4 timing contracts at 2.4× wall under a sibling's `--all-features` build — load, log kept). Re-review `01a0a5e4` launched. |
| 2026-09-15 21:45 | pr-5 review round 2 (`01a0a5e4`): 19/20 verified-fixed (the `token_grant_core` gate correct on all four interleavings, the park off every lock, the loom models the product's shape; the late find right). **OPEN 6, BUGS 2**: Issue 5 still open one step later — the owner's `membership_sweep` EVICTS a past-deadline member, after which `lease_verdict` reads `Unknown` and is waited like LIVE to the 45 s deadline (the contract drove an oracle, never the real sweep); `fetch_once` re-checks `revoke_gens` BEFORE `evict_to_budget().await` and installs after — a recall inside the drain installs pre-commit records live. Both confined to `token_plane.rs`. Rebase seams onto PR 8 named (the `finish_free` arm beside `grace.defer`, `membership.rs` reads over the sharded members, `lease_live` under the `poison` split). Fix round 2 `01a0a615` launched. |
| 2026-09-15 23:30 | pr-8 rebased onto dev `19dd525a` (PR 7 + 6) at `4a1eaa68` (17 commits; conflicts all additive theirs-then-mine — the `ManagerReply` boundary cut PR 6's `DirRenameUnlocked { already: bool },`, restored by hand; `loom-models/src/lib.rs` recomposed after `task check:loom` caught an unclosed delimiter; 110 models) + 27–29 closed: the seed made STRUCTURAL (`highest == 0 ∧ durable refs > 0 ⇒ refuse loud`; seed + LOSS check over derived ∪ durable; 2-volume forest pin); `grant_supply_blocks` feeds the valve (the pin exposed a REAL last-grant race — inline vs proactive ask, one `Full` — closed by `block_grant_topup_join`); 1-bit snapshot + torn-snapshot refusal. **Seam finding**: PR 7 KEPT the mount-time by-block scan on the forest (its summary's 'zero census reads' claim is not delivered — owed to PR 12/14: the bitmap replaces the RAM refcount map as the terminal-free engine); the index HOME re-pointed to the allocation holder (one fn; the unheld projection PR 12's). Matrix 31/31 both legs. Verification round 4 `01a0a641` launched. |
| 2026-09-16 00:15 | pr-5 fix round 2 done (`16d1a38d`, 48 commits; 6/6 fixed): the install decided under the cache entry against the generation the records were read under (pin served 0o644 after a commit to 0o600 pre-fix); `membership.rs` gained an additive departure sink — every armed holder registers, `sweep_departed` runs at `evict`/`leave`, a token client the owner no longer lists reads `Expired` (the pin drives the REAL `expire_due` sweep on a manual clock: 44.8 s → < 2 s, 63 swept); refcount in-flight gate (loom two-user model); the reader-class verdict cached against (census gen, token-client gen). 29 contracts ×5 stamped; matrix both legs 1.00/1.00. Parked at 0 open awaiting PR 8's landing for its rebase; one verification then covers fixes + seams. |
| 2026-09-16 00:50 | pr-8 review round 4 (`01a0a641`): 27–29 verified-fixed; the rebase composition CONFIRMED (20/23 variants each once; `name()` no duplicate; loom 110/110; no duplicated stats key) — **LANDABLE, OPEN 5 / BUGS 0**. Seam (a) adjudicated: PR 7's note and summary both say the zero-census-reads pin is PR 8's — neither wrong, no PR-7 pin asserts it; PR 8 re-owed it to PR 12/14 (its arm adds a SECOND full scan today); two PR-7 doc sentences stale (nit). Seam (b): every consumer routes through `index_home_volume_for`; the unheld→held transition a no-op on every PR 8 shape; the real home change (PR 12) would orphan `shared_ref:` records — owed with the remedy. One real cost found: `population()` popcounts the WHOLE bitmap per call on the allocation funnel whenever the grace ring is non-empty (32 MiB/allocation at 1 PiB) → a maintained counter. Matrix 31/31 both legs no NOTE. Nit pass `01a0a65e` launched; PR 8 lands third, then PR 5 rebases. |
| 2026-09-16 04:20 | pr-5 rebased onto dev `c6683b19` (PR 7 + 6 + 8) at `8b9a1ea5` (55 commits; 9 additive conflict sites composed theirs-then-mine; `TokenCall` its own enum so no `manager.rs` conflict). Seams: (a) the `finish_free` recall gate precedes PR 8's holder-homed free ladder (2-volume pin); (b) sharded members — one plane per process, `Unknown ⇒ Expired` per shard; (c) a token READER poisons at `T_self`, never parks; PR 8's `admits_token_service` stand-in wired (an expired park refuses every token verb); (e) the recall hook precedes PR 6's `XvGuards` (pinned: cross-owner create + token reader on the parent); (f) a stats-key clash renamed (`free_grace_recall_timeout_deferrals`). **The rebased ledger found a real PR-5 defect** (`sym_cross_owner_tests` 10/27 red both layouts): the bring-up cover flushed the D0 claim's leaf pre-arm with a `(0, 0)` stamp and rule 3 screened it — `prime_frame_stamps` before the cover, native pre-arm stamp `g + 1`, `frame_g_floor` monotone per slot; red-first. Matrix 33/33 both legs 1.00; loom 113. Verification round 3 `01a0a70f` launched. |
| 2026-09-16 05:15 | pr-5 review round 3 (`01a0a70f`): ALL 24 prior issues verified-fixed (the departure sink closes Issue 5 through the REAL sweep < 2 s; the install a sound two-lock chain; the composed `finish_free` recall → PR 8's ring → the holder's clear; `7c7ce96a`'s pre-arm `g + 1` sound, flat v1 byte-identical). **OPEN 4, BUGS 2**: the SAME frame-stamp class on the other legal transition — an armed session then a `=0` WRITER session on the once-armed bit-17 volume stamps `(0, 0)` on leaves at `g ≥ 1` and rules 2/3 screen its acked commits at the next load (silent partial loss, uncovered by any contract); an unattributed load-selected `no reply to call 1 within 10s` on PR 6's `XvGuards` wire (1/11 flat) — attribution owed before the ledger row stands. Matrix 33/33 both legs no NOTE; loom 113. Fix round 3 `01a0a73b` launched. |
| 2026-09-16 08:35 | pr-5 fix round 3 done (`da60408f`, 67 commits; 4/4 fixed). **Issue 25 → the REFUSAL**: option (a) proven unsound (`(0, g)` = rule 2's zombie shape; `(0, g+1)` pre-empts a generation tree 0 never granted; moving tree 0's `g` IS the plane) — a `=0` WRITABLE open of a plane-stamped volume refuses loud at the marker gate's position, nothing written; readers/probes/co-writers open; a PR 1–3-shaped volume stays exactly shipped; §7.2 'once armed, stays armed until the PR-14 flip'; pin RED by the LOSS pre-fix. **Issue 26 → verdict (b), a PR 6 PRODUCT defect attributed to the line and fixed**: the S8 owner lane is stop-and-wait and `XvGuards` parks at the holder on the previous scope's stripe while that scope's `XvRelease` rode the same lane BEHIND it (33.9 s = 13.5 + 2 × 10 s bounds; tape: `exclusive stripe 5283 … contended — last acquirer <same key>; parking`) — parking verbs (`MetaCall::parks_at_holder()`) now ride the pipelined `MuxSession`; deterministic via `TEST_XV_RELEASE_HOLD_MS`, RED pre-fix, 51 ms post. Non-member token clients refused first. Attempt 1 killed at the matrix (PR 7's unarmed pack half on a once-armed volume — Issue 25's law) → fixed, count from zero: `sym_cross_owner_tests` 13/13 quiet; matrix 33/33 both legs. Verification round 4 `01a0a7ea` launched. |
| 2026-09-16 09:20 | pr-5 review round 4 (`01a0a7ea`): 25–28 verified-fixed — **LANDABLE, OPEN 2 / BUGS 0** (the refusal the only sound answer; the gate at the marker gate's place, nothing written; the three landed contracts kept or strengthened; the PR 6 attribution RIGHT and the fix mechanically sound — the owner-side lane spawns each frame's serve as a task, so a parked `XvGuards` yields and the release on the same mux is served concurrently; the wedge was the client's stop-and-wait lane; the publish plane's mux lever untouched; 0 stalls in 9 runs). Two small items (the coalesce-factor gauge undercount + a sub-ms `Unknown` window with a one-line close; nits incl. what `=0` IS after the PR-14 flip). Matrix 33/33 both legs. Nit pass `01a0a812` launched — PR 5 lands last of level 4. |
| 2026-09-16 10:15 | pr-5 nit pass done (`128fbf22`: `BATCHED_VERBS` counted; `None ⇒ Expired` under an installed owner — the sub-ms window pinned RED at the full 45 s deadline; the post-flip `=0` law stated in §7.2 + the knob row + PR 14's row (a writable open of a stamped volume stays a REFUSAL; `-o ro` is the reader posture — PR 14 to CONFIRM); the refusal pin brackets the device digest; the Issue-26 pin waits on `parked_scopes() == 0`). **pr-5 LANDED** on dev `d72a9e79` (71 commits, clean rebase; `cargo check` + fmt green). Totals: 4 review rounds + nit pass, 30 issues (9 bugs), 0 wontfix. **LEVEL 4 COMPLETE** — 5, 6, 7, 8 all landed; 122 issues across the four (28 bugs), plus the PR 6 lane wedge and the pre-arm stamp found by PR 5's ledger. Batch `task check` (rustls + 7 + 6 + 8 + 5) launching on the tip, laptop idle; level 5 (7b ∥ 9 ∥ 10) after. |
| 2026-09-16 00:47 | **Batch `task check` GREEN on `07375bd7`** (the rustls bump + PR 7 + 6 + 8 + 5; pinned worktree, laptop idle, from zero, 23:21–00:47): 394 suites / 5,308 tests all ok, all 18 stages, both audits (1 allowed = the adjudicated bincode). Log `/tmp/five/gate/taskcheck.07375bd7-green.log`. **Every code landing of the run is gated.** Level 5 (7b ∥ 9 ∥ 10) launching off the tip. |
| 2026-09-16 01:00 | **Level 5 launched THREE-WAY** off dev `c4cb3730`: pr-7b directory striping `01a0a87f-d277` (feat/sym-dir-stripes), pr-9 custody by the slot holder `01a0a87f-f939` (feat/sym-custody), pr-10 dead-appender recovery `01a0a880-3d43` (feat/sym-recovery). Common brief `/tmp/grok-justin/grok-exec-prompt-dadee1dd-level5-common.md` (ownership table, 0x90/0xA0 discriminant ranges, the wire-word law now standing, append-only shared files, landing = completion order). Owed items routed: PR 7 → 9 (translators' pair cancellation); PR 2/3/4/5/6/8 death paths → 10; PR 4's `sym_slot_transfer_tests` load signatures → 10 (attribute); `kvmap_read_tests` forest gap + PR 7/8's door/guard/un-share/migration/top-up/homes → 12. |
| 2026-09-16 02:50 | pr-9 implemented first of level 5 (`6401e9ae`, 5 commits, +2,633/−68; 9 contracts ×10 stamped; S9 suites both legs; 34-suite matrix both legs no NOTE; fidelity quick 43/0). Custody server = the file's slot holder via PR 6's `step_home` + `SlotHolderCache` (one JOINed client per holder, single-flight; own file 0 RPCs pinned; foreign file 1 grant); the grant CARRIES the token on PR 5's wire (`TokenCall::CustodyGrant`); a slot with live custody DEFERS its handover (`slot_custody_live`); W1 declines a foreign-custody file; PK4 `pack_group` adjudicated STAYS (the unarmed co-writer ships it — 13 live contracts; armed mounts issue none). **PR 7's owed pair cancellation found a LIVE kvmap defect**: a run start re-adopted as a point freed a block the record still held (pinned red-first). Scoping: carried grant 289 vs 413 µs/op. Reviewer `01a0a8e1` launched. |
| 2026-09-16 04:05 | pr-7b implemented (`8c3a4ece`, 5 commits, +5,260/−363; 17 contracts ×10 stamped; 34-suite matrix both legs; live-FUSE trio + posix both legs). The flip as a `StripeDir` intent on PR 6's protocol; stripes supplied by the K heaviest creators via `InoSupply`; lazy migration; K-way readdir merge; rmdir under an exclusive token; C17. Wave rows N=7/K=14 and N=31/K=62: `ships ≡ foreign creates` exactly, max ≤ 1.46× mean, zero handovers. **Two reds attributed OUTSIDE the surface**: PR 5's `a_displacing_publishs_recall_…` (test-side race — routed to PR 12); **a BASE-TREE defect in PR 10's file** — the forest WRITER's own-residue open adopts a newer page slot-root via `KvTree::adopt_root`, which REFUSES on every writer cache (kill after a checkpoint wrote the page with a guest root its flush pass moved, before the next publication — the steady-state window under load; `corpse_sweep_tests` stamped 1/4) — routed to PR 10 with the fix direction (note §5c). Reviewer `01a0a923` launched. |
| 2026-09-16 05:20 | pr-9 review round 1 (`01a0a8e1`): NOT LANDABLE — **OPEN 19, BUGS 8**: rustdoc RED (a `pub` for a contract probe made an intra-doc link private — the gate line absent from the implementer's table); the arm's LEAVE unwired (`disarm_slot_custody` has no caller — a clean unmount leaks carried tokens + grants at every holder); the wire word `object` reaches the S9 arbiter unvalidated; a process-wide async mutex across the 10 s holder dial; N holders' epochs folded into ONE `fetch_max` generation (adopting a grant from a busier holder voids in-flight DMA under another's grant); `install_carried` drops `xattrs_complete`; `Busy` → EINVAL not `Deferred`; a token-install error discards custody. Adjudicated: `TOKEN_SCHEMA` 1 right; `pack_group` STAYS right; **the kvmap pin is a REAL shipped data-loss-capable defect** (a live block entered the free stream on the bit-16 claims-train path). The live-FUSE trio never run by the implementer (reviewer ran it: green). Fix round `01a0a92b` launched. |
| 2026-09-16 06:40 | pr-7b review round 1 (`01a0a923`): NOT LANDABLE — **OPEN 22, BUGS 8**: the automatic trigger flips a STRIPE (no is-a-stripe guard → nested stripes `D` never consults → names vanish at ≥ 2 creators); the `rmdir` dying mark is its own intent (a crash before `D`'s removal leaves `nlink 0` stripes under a LIVE directory — the corpse sweep destroys them); `..` of a striped directory resolves to ITSELF on the reconnect path; unrevalidated key-parents racing migration (spurious ENOENT; a rename strands a name); `serve_is_empty_covered` reads 8 entries and calls 66 markers empty; the R26 closer misses `link`/rename-into; `parse_marker` accepts 1–3 digits vs the 2 the encoder writes (the fuzz round-trip law FALSE); **the stamped matrix RED at its own suite** (appender 1's grant exhausts at the floor, the shutdown refuses the refill, the ring stays uncovered at the leave, the offline probe does not replay it → 13–54 C9 FPs — two base-tree faces routed). Also: PR 6's `xv_local_step_unguarded` tripwire fires on EVERY cross-owner create — pre-existing, hidden because no other contract initialized a logger → PR 12. Both out-of-surface attributions verified. Fix round `01a0a96b` launched. |
| 2026-09-16 07:15 | pr-10 implemented — last of level 5 (`335c17ec`, 14 commits, +6,066/−140; 15 crash-matrix contracts ×10 stamped; 34-suite matrix both legs; live-FUSE trio; fidelity quick 43/0 + FULL 115/0 incl. the `pr-matrix` preempt leg; `mw_fleet.sh --symmetric` `sym-crash` GREEN 10/10 from zero on the LOCAL tcp devsub). The death ledger's driver (`install_death_sink` → `dead_member:`; `RecordDeath` activated + screened); `kv/backend/recovery.rs` runs §5.9 per dead region in order with `appender_recovery_phase_ns`; manager death = D0 successor → grace → non-reclaimers only; C14/C15 at the mount gate; `appender clear`. **Seven earlier-rung defects found + fixed red-first**: PR 2's own `Recovering` page dropped its acked window; PR 5's offline fsck folded zombie frames writers screened; the tail scan read 0 bytes; C9 judged unreplayed foreign windows; C14 double-reported; the fresh-claim preflight; **F7 (fleet leg): a crashed incarnation's block-grant window leaked forever — PR 8's LEAK half unwired**. Reviewer `01a0a97d` launched with 7b's `adopt_root` finding routed in as Issue 1. **All three level-5 PRs in review/fix.** |
| 2026-09-16 09:40 | pr-9 fix round 1 done (`2cbe11cf`, 10 commits; 19/19 fixed, 0 wontfix): rustdoc green; the leave wired (tokens then grants, drained — 0 held pinned); `screen_custody_words` (fuzz service-edge arm); per-holder single-flight dial; `CustodyScope::SlotHolder` (per-holder generations — a busier holder's grant retires nothing under another); **the handover RECALLS custody through the S9 pull channel** (`RecallNotice` on the four carriers + a standing poll, `CUSTODY_SCHEMA` 7 → 8; `CUSTODY_DEFERRED` mid-handover; one renewal beat; nothing voided — chosen over bound-then-revoke because a revoked live grant loses acked data); the fence scoped to the holder; typed `Deferred`/EAGAIN; `patch_ineligible_foreign_custody` priced. ×10 stamped; matrix 34/34 both legs; live-FUSE trio 6/6; fidelity 43/0. Re-review `01a0a9ae` launched. |
| 2026-09-16 10:25 | pr-10 review round 1 (`01a0a97d`): NOT LANDABLE — **OPEN 21, BUGS 9**. The routed `adopt_root` defect CONFIRMED and reproduced on the branch (`corpse_sweep_tests` stamped 3/4 — F1 widened only the ring replay; the page-root pass still `Live`-only + `adopt_root`), and DUPLICATED at `recovery.rs:813` for a dead lessee's root; step 4 releases the RAM lease table BEFORE step 7's durable tree-0 write (any `Err` between ⇒ the re-run skips the slots ⇒ acked loss; the manager's first-touch acquire in that window regresses tree 0 — §5.8.4's recover-before-grant clause violated); `pr_fenced` set regardless of whether the preempt landed; the death write one `Try`, never retried; `appender clear` gated on `page.is_manager` (vol 0's claim unchecked); wire `pr_key` drives a preempt unvalidated; a step-2 TOCTOU; no incarnation on `dead_member:` (a rejoined member recovered while live). **The structural finding**: the in-process fixtures (one process = one RAM tree, one actor) cannot reach the driver's real defects — the fix round builds the two-backend/fleet shape FIRST + the acked-writes oracle. Gates all green (the green is honest). Fix round `01a0a9b5` launched. |
| 2026-09-16 11:50 | pr-7b fix round 1 done (`ad7a57a4`, 9 commits; 22/22 fixed, 0 wontfix): a stripe never carries a map; `rmdir` ONE intent under ONE travelling guard set (5 kill windows roll forward whole); markers name stripe 0; mutations key the stripe and re-home FIRST, `D`'s tree the permanent secondary read home; emptiness = no user dentry; `refuse_dying_parent` at every insert path; canonical marker codec; Issue 21 (a) the INTERNAL-class refill admitted during the shutdown fixpoint (one fn in `kv/backend.rs`, red on the reverted arm), (b) fsck records no inode-plane verdict on a non-writer over a `Live` page, (c) the oracle asserts the leave covered. **Found beyond the review**: an offline fsck PROBE's `shutdown` wrote a checkpoint (`open_probe` is `Writable`, no task → `checkpoint_now`) and LOST 50 inodes of a crashed forest writer — one predicate in `shutdown` (shipped-or-forest adjudication owed to the reviewer). Matrix 34/34 both ways twice, 0 NOTEs; ×10 stamped. Re-review `01a0a9f1` launched. |
| 2026-09-16 12:50 | pr-9 review round 2 (`01a0a9ae`): 18/19 verified-fixed (the leave in the right outside-in slot; every custody word screened; per-holder single-flight; the scoped fence). **OPEN 9, BUGS 3 — the recall design has three holes**: the recall never reaches the FUSE layer's cached `LockLease` (all four `active_leases` sites skip `is_held()`; `authorize_dma` passes ⇒ a writer keeps writing under a RELEASED grant; the deferral unbounded again; a pin asserted the custody-less write as a feature); `RecallNotice` is one-shot (a lost carrier orphans the recall); the holder's clean leave never recalls the grants it issued (a dual-custody window ≈ 40 s on a routine unmount). Plus a FLAKY new contract (`fenced` latched before the fence's work — the matrix's first run tripped on it). Fix round 2 `01a0a9f3` launched. |
| 2026-09-16 17:35 | pr-10 fix round 1 done (`282c2974`, 19 commits; 21/21 fixed, 0 wontfix). **The two-backend fixture built first** (`tests/common/sym.rs` `RegionImage` over `region_device_ranges`: A checkpoints → storms until its flush pass MOVES the slot's root → checkpoints → dies; B the recoverer opens with the slot at the OLDER root — asserted). The nine bugs red-first on it (Issue 1's pin reproduces the exact `root adoption refused` text on `335c17ec`); the ordering law (RAM lease table LAST, the slot in-recovery refuses first-touch acquires, every `Err` leaves `Recovering`); `pr_fenced` iff the preempt landed; the death write retried; `appender clear` under the D0 ladder on every volume; `pr_key` validated; `dead_member:` carries the incarnation. **Four more defects found through the fixture**: `SlotTrees::new` seeding `published` with the OPENED root (tree 0 stale forever — a clean leave dropped 25 dentries); the re-run past tree 0 refusing the window as violations; a death between page `Recovered` and `recovered:` leaving PR 8's gate closed; `publish_forest_roots` regressing a release's `g` to 0 — plus the SAME shipped `open_probe` checkpoint PR 7b fixed (rebase seam). `corpse_sweep_tests` stamped 10/10 (was 3/4); matrix 34/34 both ways; fidelity full 115/0; **`sym-crash` ×10 from zero with the acked-writes oracle — 426 fsynced files across ten kills all present with content**. Re-review `01a0aa87` launched. |
| 2026-09-16 21:20 | pr-9 fix round 2 done (`d55932d5`, 19 commits; Issue 9 + 20–27 fixed, 0 wontfix): the recall reaches the FUSE `LockLease` (ONE accessor `cached_lease_token` at the four sites; the per-ino `CustodyUse` drain; the gated release; a post-mark write takes the CONFLICT class into the ladder — the epoch deliberately NOT advanced); recall = STATE on every carrier; `recall_custody_at_leave` before `release_leases_at_leave` (`T_owner + renew`, `slot_handover_recall_bound_ms` published); the fence pin observes the work. **Two more windows of the class surfaced by the counted runs and closed before the count restarted**: a grant INSIDE the flush-then-transfer (RAII `HandoverCustodyMark` armed before the census, held to the terminal outcome; every grant path re-checks after `owner.grant` — a Dekker pair) and the stale-resolve window (`NotHolder{0}` for an unleased slot on a non-manager; the writer re-resolves after every grant, `dlm_custody_stale_holder_grants`). ×20 + ×20 isolated 40/40; matrix 34/34 both legs ×2; live-FUSE trio under `--all-features` 6/6; fidelity 43/0. Re-review `01a0aac0` launched. |
| 2026-09-16 22:30 | pr-10 review round 2 (`01a0aa87`): 21/21 verified-fixed; the two-backend fixture JUDGED GENUINE (every shared process-global enumerated — reset, replaced or out of the loop; what it does not model stated) — **LANDABLE after the rebase, OPEN 6 / BUGS 3**: the `open_probe` fix keyed on `non_writer` (7b's landed `probe` field is right — the rebase drops PR 10's); `RecoveryRollback` restores the lease table but not the gate's `foreign` bit (a failed recovery lets the sweep SMO a dead lessee's tree — loud refusal); the recovered root install does not raise the seq handle (PR 11's residue-seq collision class on an empty-window death). `corpse_sweep_tests` stamped 4/4 ×4; matrix 34/34 both legs; fidelity full 115/0. Rebase onto `6c80d70f` (PR 7b) + 22–27 handed to the implementer as one pass `01a0aac3`. |
| 2026-09-16 23:50 | pr-9 review round 3 (`01a0aac0`): Issue 9 + 20–27 verified-fixed (the drain — every mutating handler + the four detached DMA continuations + the pipeline upload hold a `CustodyUse`; the settle drops a parked lease only at an OBSERVED `inflight ≤ 0` — adjudicated CORRECT without an epoch advance; the Dekker pair traced correct; the counted runs on the final tree). **OPEN 6, BUGS 1**: the leave's bound-expiry posture is documented as 'the page stays Live' but `custody_blocked` regions are excluded only from `release_leases_at_leave` — the page is written `Free` while tree 0 names its slots `Leased` (KD-SYM-3 lost). Plus: no loom model for the revoke/enter pair; a guard's lifetime as a binding position; a PR-2 contract wedge hit once (→ PR 12). Fix round 3 `01a0ab07` launched (rebase after PR 10 lands). |
| 2026-09-17 01:00 | pr-9 fix round 3 done (`29990d0e`, 23 commits; 28–32 fixed, 33 recorded + routed; 0 wontfix): `custody_blocked` folded into `uncovered_ids` before the verdict (RED on round 3: `left: Free right: Live`); `src/custody_revoke_core.rs` `#[path]`-shared with 4 loom models (weakening: both fences → red; the grant's steps swapped → red against the real `GrantTableCore`); the mark's lifetime a named act. **Found by the first ×5**: after a slot moved TO this mount a LOCAL lease was cached under a word the last recall had cleared — refused forever; fixed, count restarted. loom 117/117. Parked at 0 open awaiting PR 10's landing for its rebase. |
| 2026-09-17 03:15 | pr-10 rebased onto dev `6c80d70f` (PR 7b) at `0249dfb2` (21 commits; `fsck.rs` 13 hunks composed 7b-then-10 with the call order `c16 → c17 → c14_c15`; Issue 22 resolved by KEEPING 7b's `probe` field — PR 10's forest-loss pin green against it; 7b's `DIR_STRIPE` markers through the kind-blind recovery PINNED) + 23–27 closed: `RecoveryRollback` restores table + `foreign` + door waiters as one RAII; `KvTree::install_recovered_root` the ONE install for open path + driver (raises the handle); DELETE's child via `lookup_kind` (a C10 'DATA-LOSS RISK' false positive pinned); `death_record_retire_age_ms` = 2 × T_owner tied; the fleet oracle = per-file `dd conv=fsync`. `sym-crash` ×3 green with 1,146 / 149 / 460 fsync-acked files present; matrix 35/35 both legs. **Flagged honestly, unattributed**: 7b's `stripe_dirs_at_mkdir…` HUNG once on the flat leg (18 min inside its closing offline `fsck_clean`, 1/9) — handed to the reviewer as an attribution item (a plausible PR-10 mechanism: the new C14/C15 mount-path gate in the probe open parking on a live lease). Verification round 3 `01a0ab5c` launched. |
| 2026-09-17 05:00 | pr-10 review round 3 (`01a0ab5c`): 22–27 verified-fixed; the rebase composition confirmed — **LANDABLE, OPEN 3 / BUGS 2** (one narrow: a failed recovery with no re-run pins ring 0's tail via the installed root's floor; one nit). **The hang ATTRIBUTED — it is PR 7b's, landed on dev**: `flip_dir_inner` takes `lock_many_leased` (`I{dir}` + 66 `D{}`) then 64 SEPARATE `lock_inode_exclusive(stripe)` on the same striped 4a table (+ the mkdir's held `I{parent}` under `-o stripe_dirs`) — the ONE-canonical-`lock_many` law violated; ≈ 0.8 % self-collision per flip at the shipped width (≈ 16 % over 22 flips = the 1-in-9); DETERMINISTIC at the registered crucible `SQUEEZEFS_DLM_STRIPES=1` (both flip shapes hang; PR 10's whole crash suite passes at width 1 except the 7b seam pin, which parks BEFORE any recovery step); a production automatic or explicit flip would park the directory FOREVER. Fix branch `fix/sym-dir-stripe-flip-lock-many` off dev (`01a0abad`: mint the K stripes first, ONE canonical `lock_many` for `I{dir}` + markers + stripes, `stripe_at_mkdir` after the create's guards drop; red pin at width 1). PR 10's nit pass `01a0abac` in parallel. |
| 2026-09-17 05:50 | pr-10 nit pass done (`3864b74b`): `RecoveryRollback` restores the slot's RAM TREE too (captured `Untouched/Installed/Adopted`; `Drop` discards every cached node of the slot then restores the prior root + floor — RED before: a floor `{4: 155791}` pinned ring 0 forever); `open_guest_trees` via the ONE install; **the matrix runner gained a derived per-suite watchdog** (`max(600 s, 4 × flat wall)`, kills the process tree, `=== HUNG …` NOTE row; proven to fire; inert on green — 35/35 both legs in 20.8 min). 30 crash-matrix contracts. **Self-found, adjudicated BUILD**: a recoverer dying between its step-6 flush (compactions journal interior records into ring 0 for a slot outside its set) and step 7 leaves tree 0 `Leased{dead}` ⇒ every later open refuses with a `Lease` violation nobody can clear — a permanent refusal on the death path (Issue 31 → fix round `01a0abd7`: the detector exempts slots whose tree-0 lessee's page is `Recovering`; C15 re-runs the recovery). |
| 2026-09-17 07:20 | **The 7b flip lock-law fix LANDED on dev `fe62cb0e`** (`fix/sym-dir-stripe-flip-lock-many`, 3 commits): supplied stripes asked while holding NOTHING → remainder inos allocated guard-free → ONE `lock_many_leased` over `I{dir}` + every remainder `I{stripe}` + the K+2 marker `D{}` → the mints + the intent under that one set; `stripe_at_mkdir` runs after the create's guards drop (still awaited). Red pin at the width-1 crucible: both shapes HUNG deterministically on `4216dc4f` (`exclusive stripe 0 … contended; parking`), 10/10 green after (221–281 ms ≈ flat); `sym_dir_stripe_tests` ×10 stamped + ×5 flat from zero; matrix 34/34 both legs. AGENTS.md/design §5.6.5/operations.md corrected (they claimed the marker set is taken FIRST). Credit: PR 10's reviewer. PR 9 rebases onto this tip now; PR 10 when its Issue-31 pass returns. |
| 2026-09-17 09:30 | pr-10 Issue 31 built (`3079ec2d`; the first pass died on an API stream error after ~100 min with +1,027 uncommitted — resumed and finished): the shape CORRECTED by the pin — the window is INSIDE a cycle (between the flush pass's SMO records and the covering ledger record; seam `TEST_CHECKPOINT_HALT_BEFORE_LEDGER`); the detector exempts the MANAGER's interior records for slots whose tree-0 lessee's PAGE is `Recovering` (a `Live` lessee's slot / a content record / a third appender's record stay violations — pinned); the forest replay STASHES those records (`AppenderSet::recovering_structure`) instead of folding them into the foreign tree; the C15 re-run applies the stash onto the `Recovering` page's root. **Found by the pin**: the frame screen's rule 4 read the recoverer's `(0, g)` frames as foreign at every reload (`SlotLeasePlane::recovering_lessees` makes rule 4 inert for such a slot). 31 crash-matrix contracts. Stated for PR 12: the `Recovering` page should carry the recoverer's identity so a dead node's rejoin over a foreign recoverer's page refuses. Parked awaiting PR 9's landing → rebase → review round 4 (the stash + rule-4 change need a reviewer). |
| 2026-09-17 10:45 | pr-9 rebased onto dev `50b822c6` (PR 7b + the flip fix) at `ca193aee` (26 commits; 2 additive conflicts composed theirs-then-mine; every later commit auto-merged). **Seam (c) find**: the custody screen ADMITTED directory/stripe inos (any existing record) — a custody object is now a REGULAR FILE (`custody_object_mode_admissible`, the mode read in the same leaf read), red with the rule bypassed ('directory object 0x3 was GRANTED custody'), fuzz + proptest mirrors. Seams (a)/(b)/(d) verified by reading (PR 9's consults in `dlm_slot.rs` under the op's inode + lease lock; the leave order preserved; the INTERNAL refill cannot meet a `custody_blocked` region). Matrix 35/35 both legs no NOTE; loom 117; live-FUSE trio 6/6; fidelity 43/0. Verification round 4 `01a0ac4c` launched. |
| 2026-09-17 12:15 | pr-9 review round 4 (`01a0ac4c`): 28–33 + the seam find verified-fixed (the reviewer independently reproduced the pair-1 loom weakening in a scratch crate; the rebase 23 code-identical replays by `range-diff`; 'regular file' the right admissible set — every product caller a regular-file data op) — **LANDABLE, OPEN 3 / BUGS 0** (a few-instruction refused-but-unparked window; the leave's worst-case wall in the docs; PR 8's `sym_block_grant_tests` supply-terms contract tripped the matrix's run 1 — 10/10 isolated, attributed outside PR 9 → PR 12). Nit pass `01a0ac97` launched — PR 9 lands second of level 5. |
| 2026-09-17 15:20 | pr-10 rebased onto dev `b7be8177` (PR 9 + the flip fix) at `9b8bb83d` (28 commits; 4 additive conflict files composed theirs-then-mine; the watchdog kept — 36 suites). Seams: (a) a custody-blocked region takes PR 9's uncovered posture, neither recovery path recalls the surviving grant (S9 retires it at the writer — pinned; the S9 owner's missing grace window at the production arm stated for PR 12); (b) the driver's door is the lease TABLE, both grant paths consult it first (pinned: a custody grant of a slot mid-recovery is the dead holder's, never the recoverer's); (c) kind-blind; **(d) found by the pin**: step 7b never republished the S4 lock plane — every `acquire_lock` on a recovered slot's file refused as a foreign home until the next grant/release (fixed, +9 lines); (e) the 7b seam pin green. Matrix 36/36 no NOTE/HUNG; fidelity quick 43 + full 115 (`pr-matrix` 11/11); `sym-crash` ×3 green (oracle 729/306/948 files present). Verification round 4 `01a0ad10` launched (Issue 31's stash + rule-4 exemption + the rebase). |
| 2026-09-17 16:35 | pr-10 review round 4 (`01a0ad10`): 28 verified on dev (the two width-1 contracts 0.74 s), 29–31 verified (Issue 31 adjudicated (a)–(d): the exemption keyed on ring 0 AND the lessee's PAGE state; the stash hold-until-consumed; rule 4 inert on a `Recovering` slot the right law with its residual class to STATE), every rebase seam verified (the S4 republish refreshes all three projections) — **LANDABLE, OPEN 4 / BUGS 1**: `unpublished_root_floors` has no lease filter — a slot LEASED to a foreign appender whose page root is ahead of tree 0 pins ring 0's tail for the mount's life (masked in-process; deterministic on PR 12's venue at any manager restart); the death path's custody quarantine for `appender clear` / the same-node takeover; two nits. Matrix 36/36 no NOTE; fidelity quick 43 + full 115. Nit pass `01a0ad4f` launched — PR 10 lands last of level 5. |
| 2026-09-17 18:00 | pr-10 round-7 pass done (`392c1319`, 33 commits; 32–35 fixed): a slot leased to a live FOREIGN appender opens at its page root PUBLISHED (no ring-0 floor); the lease filter on `unpublished_root_floors`; **found on the way — the in-process masking was itself a hole**: the bring-up's `publish_forest_roots` skipped `Leased` records yet NOTED them published, lifting floors with nothing durable (Issue 31's hold included) — now `written ≠ pending`, `cover_bring_up_residue` covers down to the recovery hold; the death record carries its recorder class, `appender clear` / the same-node takeover write EARLY, step 7b quarantines the recovered slots' custody until `ts + T_self`; rule 4's suspension window + residual class stated; the two hang knobs registered. Verification round 5 `01a0ad9d` launched (the floor-law change). |
| 2026-09-17 18:50 | pr-10 review round 5 (`01a0ad9d`): 32–35 verified-fixed — the floor law adjudicated on all three questions (no floor-worthy ring-0 content for a live foreign-leased slot by the standing laws; the cover reaches `min(hold, head)` and the hold at open IS the ledger tail; an unledgered `Recovering` page refused LOUD naming `appender clear`, whose record makes the next C15 gate consume the hold) — **LANDABLE, OPEN 1 / BUGS 0**: the custody quarantine's `ts_ms` is the recorder's wall clock vs the manager's with the bare `T_self` (the law's re-grant bound is `T_owner`), and the map is RAM across a restart. Matrix 36/36 no NOTE. Final pass `01a0adc0` launched. |
| 2026-09-17 19:40 | pr-10 final pass done (`f14f8b28`: the quarantine bound `T_owner + 2·skew_max` past the record's `ts_ms` — tie-tested; durable per slot as `custody_quarantine:{slot}` beside each released slot's `Unleased`, re-derived by the C15 gate — red without it). **pr-10 LANDED** on dev `ecf2b665` (37 commits, clean rebase; `cargo check` + fmt green) — last of level 5. Totals: 5 review rounds + 2 passes, 36 issues (13 bugs), 0 wontfix; 7 earlier-rung defects + 4 fixture finds + the S4 republish + the bring-up publish hole fixed en route; the 7b flip lock-law defect found by its reviewer and landed separately. **LEVEL 5 COMPLETE** — 7b, 9, 10 + the flip fix; 13 of 18 PRs on dev. Owed → PR 12: the `Recovering` page carrying the recoverer's identity; the S9 owner's grace window at the production arm; the cross-shard rejoin exposure; PR 2's drain-then-grow. Batch `task check` (7b + flip fix + 9 + 10) launching on the tip, laptop idle; PR 12 after. |
| 2026-09-17 02:59 | **Batch `task check` GREEN on `46ab01e7`** (PR 7b + the flip fix + 9 + 10; pinned worktree, laptop idle, from zero, 01:39–02:59): 397 suites / 5,417 tests all ok, all 18 stages, both audits (1 allowed = the adjudicated bincode). Log `/tmp/five/gate/taskcheck.46ab01e7-green.log`. **Every code landing of the run is gated; 13 of 18 PRs on dev.** PR 12 (mount posture — the seam-collapse rung, serial) launching off the tip. |
| 2026-09-17 03:15 | **pr-12 launched** (serial) off dev `964749be` — feat/sym-mount-posture, implementer `01a0ae2a`: the join ladder (every RW mount a writer; `-o ro` bound per slot holder; the roles retired forward-only, refusing), the ~40-site `plane_gate` sweep, and the complete 'until PR 12' seam ledger routed from PR 4/5/6/7/7b/8/9/10 incl. two base-tree product defects (`kvmap_read_tests`' forest A7 gap; PR 6's `xv_local_step_unguarded` tripwire on every cross-owner create). |
| 2026-09-17 13:00 | pr-9 nit pass done (`4bd96b31`: publish-then-recheck after the lease insert — Issue 35's window zero, pinned RED via `TEST_PARK_BEFORE_LEASE_INSERT`; the leave's worst-case wall stated). **pr-9 LANDED** on dev `3625c5cb` (29 commits, clean rebase; `cargo check` + fmt green) — second of level 5. Totals: 4 review rounds + nit pass, 37 issues (12 bugs), 0 wontfix. **Shipped fix**: a live kvmap block entered the free stream on the bit-16 claims-train path (PR 7's owed pair cancellation found it) → RELEASE_NOTES 1.3.0. Owed → PR 12: the epoch carrier's process-wide `advance` under N holders; the writer's read divert / holder-side owner / registrant key; PR 8's `sym_block_grant_tests` supply-terms flake (Issue 37). PR 10 rebases onto this tip. |
| 2026-09-16 13:55 | pr-7b review round 2 (`01a0a9f1`): 22/22 verified-fixed (the rmdir roll-forward runs before the corpse sweep; the background mover clears `migrating`; the no-verdict law right with its coverage loss stated). **OPEN 5, BUGS 2**: the beyond-review find is a **SHIPPED defect** — `run_offline`'s probe `shutdown()` wrote a checkpoint on every offline fsck of a FLAT volume too (content-equivalent flat, an inode loss on a crashed forest writer) — but the fix's `non_writer` predicate also matches a §4.11-DEGRADED write mount (its teardown ladder skipped); `mkdir` under `-o stripe_dirs` pays a reverse dentry scan per call. Matrix 34/34 both legs 0 NOTEs; corpse sweep stamped 4/4 ×4. Fix round 2 `01a0aa29` launched. |
| 2026-09-16 14:50 | pr-7b fix round 2 done (`b1453ce8`: the no-write teardown arm keys on a new `probe` field set by `open_probe` alone — a §4.11-degraded writer keeps its whole ladder (pin red on the round-2 predicate); the mkdir flip knows its parent — no reverse scan per mkdir (red: 20 scans for 20 mkdirs); the `#[allow]` deleted; five constants tied). **pr-7b LANDED** on dev `bddea3e3` (10 commits, clean rebase; `cargo check` + fmt green) — first of level 5. Totals: 2 review rounds + nit pass, 27 issues (10 bugs), 0 wontfix. **Shipped fix**: the offline fsck's probe `shutdown()` wrote a checkpoint on every `squeezefs fsck` on every layout (content-equivalent flat; an inode loss on a crashed forest writer) → RELEASE_NOTES 1.3.0. Owed → PR 12: the remote-holder `IsEmpty { scope: 0 }` composition; PR 6's `xv_local_step_unguarded` tripwire (allow-listed by name, counted). PR 9 and PR 10 rebase onto this. |
| 2026-09-16 01:40 | pr-8 nit pass done (`8bd9658f`: O(1) maintained `population()` with a once-per-checkpoint drift check on `invariant_tripwires`; one-pass 1-bit union; the 0x80 fuzz/proptest arms; PR 7's two doc sentences reworded; the `shared_ref:` migration at a home change owed to PR 12 with the remedy). **pr-8 LANDED** on dev `c6683b19` (21 commits, clean rebase; `cargo check` + fmt green). Totals: 4 review rounds + nit pass, 34 issues (8 bugs), 0 wontfix. Owed → PR 12/14: the bitmap replaces the RAM refcount map as the terminal-free engine (the zero-census-reads pin), the `shared_ref:` migration, the wire top-up carriage, homes ≠ 0. pr-5 rebase onto `c6683b19` handed to its implementer (`01a0a682`) with the three named seams + the PR 6 guard-order seam. Landed PR 8's `target/` (117 GB) cleared. |
| 2026-09-14 02:05 | **Owner directive (venue)**: the majority of things are tested and proven LOCALLY before ever touching squeeze-test; the box runs the minimal A-B-B-A acceptance rows and is never used to verify that something works. Recorded in AGENTS.md §Benchmark VENUE and the design's gate-1 row; PR 2's re-gate becomes local. This session's box use so far: the one PR-1 gate-1 row set (≈ 26 min: one bracket + one reversed re-run of the two DELTA rows + one stamped scoping leg). |

