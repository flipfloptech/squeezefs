//! Fixtures shared by the symmetric-metadata contract suites (lifted from
//! `sym_slot_transfer_tests.rs` when a second suite needed them — PR 7).
//! Not a test target: cargo compiles `tests/common/` only through the
//! suites that `mod common;` it.

pub mod sym;
