# MW §6.3 S4 re-gate — stamped-solo vs unstamped-solo (rung-5 acceptance)

**Date:** 2026-08-15. **Binary:** `bfd3583e` (release, default features). **Venue:** strixhalo
tcp devsub (`SQZ_DEVSUB_TRANSPORT=tcp`), fresh substrate + format per leg, A-B-B-A
(stamped → unstamped → unstamped → stamped), 60 s sustained rows, `/tmp/s4_cell.sh` /
`/tmp/s4_rand.sh` (the B4 cell with a `format` arg lever; snapshots `/tmp/s4_*_p{0,1,2}.json`).
**Question:** does `format --multi-writer` (rung 5, KD-MW-1 Phase A — all nine bits stamped)
change solo-mount performance or behavior? The gate: within noise, `dlm_rpcs == 0`.

## Rows

| Leg | Format | Seq 4M nj8 qd4 sustained | j/GiB | Rand 4k nj8 qd32 sustained | j/GiB |
|---|---|---|---|---|---|
| A | stamped | 840 MiB/s (io 49.5 GiB) | 36.9 | 635 MiB/s (37.2 GiB) | 1050 |
| B | unstamped | 689 MiB/s (40.6) | 37.4 | 551 MiB/s (32.3) | 1109 |
| B2 | unstamped | 625 MiB/s (37.0) | 36.9 | 594 MiB/s (34.8) | 1044 |
| A2 | stamped | 720 MiB/s (42.5) | 36.9 | 595 MiB/s (34.9) | 1060 |

## Verdict: PASS

* **Stamped never regresses in either order** on either row class. The apparent stamped
  advantage (both rows, both orders) is NOT claimed — tonight's leg-to-leg venue spread is
  ~15–30 % (the box ran all-day parallel builds; thermal state uneven) and the same-box
  relative rule applies. The load-bearing identity is **CPU/byte: 36.9–37.4 j/GiB across all
  four seq legs** (rand within ±3 % both directions) — the stamp costs nothing per byte.
* **Solo invariants on EVERY leg** (from the `_p2` snapshots): `dlm_rpcs = 0`;
  `writer_guard_mode = flock+pr` on all four meta volumes; `invariant_tripwires = 0`.
* **Bit-9 live evidence (beyond the gate)**: the stamped legs ran the durable block-refs
  ledger FOR REAL (production stamp, not the `SQUEEZEFS_TEST_STAMP_BLOCK_REFS` seam) under
  60 s fio write traffic — `meta_kv_block_refs_drift = 0` throughout. The B4 overlay arm
  engaged identically on stamped legs (smoke row: overwrite share 0.841, `rewrite_amp`
  1.0000, dev/user 1.000).

## Residuals still owed on the §6.3 table (before the ARM rungs complete, not before rung 6's rig)

* The mdstorm row and the scoreboard smoke at the stamped-solo posture (this note covers the
  B4 seq-write row + rand-4k).
* The stamped-solo external QUICK set (`SQUEEZEFS_FSTESTS_QUICK`) — release-gate cadence,
  alongside the standing full-battery obligations.
