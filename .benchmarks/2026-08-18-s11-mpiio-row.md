# 2026-08-18 — S11 rung 18: the MPI-IO acceptance rows — the §9.5 closing matrix, six product fixes, and the S11 gate verdict

Branch `test/s11-mpiio-row` (off dev `28cab7a9`); charter
`docs/design-full-multi-writer.md` PR-plan row 18 + §9.5 (OQ-4's pinned-ior
adjudication). Deps: rungs 15/16/17 + the zeros-interleave fix
(`.benchmarks/2026-08-17-s11-zeros-interleave-fix.md`). Venue: the mw fleet
rig — tcp devsub (nvmet-tcp on localhost), 1 authority + **8 co-located
co-writers** (the §9.5 N=8 shape), `SQZ_MWFLEET_RANGE_CUSTODY=1`,
`SQZ_MWFLEET_MW_PORT=54193`, `SQZ_MWFLEET_OSS_GB=32`, fresh fleet per
measured leg. Instruments: **ior 4.0.0, pinned release + sha256
`510b7d4ad0f287375848121aa5a1f9842db077c1d81ad0dde738e96255298158`** (built
on demand into `target/mw-ior`; GCC≥15 build nuance `-std=gnu17` recorded in
the leg), mpirun (Open MPI) 5.0.10, the stats-inode deltas, the C8 oracle.
Evidence tier: measured-simulated (one box, co-located members).

## HEADLINE

* **The §9.5 row matrix ran, engagement exact, on real ior** — and worked
  exactly as the crucible doctrine predicts: the rows convicted **six real
  product bugs** (every one fixed red-first, ledger below) plus **two
  standing composition residuals** that keep the S11 gate's fsck/C8 half
  **standing-red by adjudication** (the G-RW2 pattern — the legs exit
  nonzero naming this note; a different failure shape is a regression).
* **The MPI-IO acceptance row**: **BLOCKED-STANDING-RED at width 32** on
  the same-ino publish composition (below) — the ENGAGEMENT machinery is
  proven (grants account, zero cap refusals/conflicts/demotions on the
  aligned decomposition, the own-lease fencing fix took the retry storm
  from 1,841 stale retries/20 MiB progress to 0 retries), and the probe
  passes aggregate **0.7–2.0 GiB/s at 32 ranks**, but the A-B-B-A phases
  do not COMPLETE: the width-32 divergence/compaction churn (retried, no
  longer latched) still starves the page-cache writeback pipeline. The
  ≥ 0.8× verdict is therefore NOT ISSUED — the row is rung 19's first
  acceptance once the width-N composition closes (its leg, gates and
  instrument are landed and proven at every sub-checkpoint).
* **The block-cyclic row (Issue-19 adjudicated live)**: grants ≈ blocks
  (790/512 incl. re-acquires), **ZERO cap refusals**, table bounded
  (sampled max 36 spans / 1,872 B vs the 345 MB R5 share), aggregate
  **1.52×** its same-width disjoint control. fsck/C8 half: the width-N
  refs-composition standing-red (below).
* **The adversarial tiny-ranges bounds row**: 4,096 byte-granular unaligned
  writes in 0.78 s → **2 grants + 15 extensions** (admit-time coalescing at
  its designed limit), live table peak 2 spans / 96 B, zero refusals, no
  wedge, foreign-client latency **1.02×** its baseline (6.53 → 6.68 ms
  median). The at-budget refusal law stays the standing rung-15 in-process
  pins (no live sub-budget shape can honestly reach the derived byte
  budget).
* **The demotion-barrier row + the rung-17 price table**: the fold-settle
  blocker (residual a) is FIXED — the row runs end-to-end with engagement
  GREEN and the price table published (below). Its fsck/C8 half stands red
  on the fourth dangling-take face (adjudicated, rung 19's).
* **`SQUEEZEFS_RANGE_CUSTODY` default adjudication: STAYS OFF** (below).

## The fixes ledger (all red-first; commits on the branch)

| # | Conviction (live signature) | Fix | Pin |
|---|---|---|---|
| 1 | **Fold-settle exhaustion** (rung-18 residual a — the s11-subblock blocker): the authority assembler's fold seed fetch runs UNDER the fold's held `BLOCK_FLUSH_LOCKS` stripe, so the read path's settle arm (which ACQUIRES (3)) was structurally unavailable — 24-rebind exhaustion became an fsync-path EIO on `FlushExtents` while (3.5)-only publishers legally churned the binding | the CALLER-STRIPE settle arm: `get_block_for_index_stripe_held` — exhaustion hands off to one resolve-then-fetch under `INODE_META_LOCKS` (3.5) alone, the caller's held (3) completing the write path's own extended order; counted `seed_settle_escalations` (stats inode) | `rebind_starvation_tests` face 4 (RED with the exact live signature) |
| 2 | **Ranged POSIX-5 ladder** (residual b / rung-17 findings 5a+5b): `acquire_write_lease_for_span` failed EIO after ONE lost 5 s wait; the §9.3 demotion barrier's renewal-bounded resolution (TTL/3 — 15 s at the shipped 45 s TTL) exceeds one wait BY CONSTRUCTION | the acquire arm rides `acquire_lease_with_retry` (watchdog budget, default 30 s) — 5b's reconciliation: per-attempt wait bounded, budget covers the barrier bound, a wedged holder still fails loud EIO | `mw_ranged_lease_ladder_tests` contracts 1+2 (RED with 5a's live refusal text) |
| 3 | **The v1 desired-hull grab** (rung-15 residual #3 — "the ior row's business"): aligned-ask ∪ whole-hull + always-double BRIDGED the gaps between a strided mount's stripes, fabricating cross-mount conflicts (and, geometry-armed, demotions) on the very rows whose falsifier is "nothing should share a block" | the v2 stretch: union only an OVERLAPPING-or-ABUTTING own span; forward-double only on a sequentially-advancing per-ino write frontier (`range_stream_frontier`) | `strided_asks_never_bridge_the_gap...` (RED) + `sequential_stream_still_converges...` (the preservation guard) |
| 4 | **Idle-reap vs the publish client** (residual d, convicted live): the wire's 60 s idle reaper closed a co-writer's session between rows; the next LANE RAISE refused ("the coordinator closed the session") → allocator refused its claimed offset → app EINVAL, `alloc_lane_raise_refusals` (must-stay-0) moving | resend-safe publishes reconnect-and-resend ONCE (the S8 batch precedent; classification structural — refusals come back Ok+status): the witnessed class, FreeBlocks, the MONOTONE raise/harvest, the pure reads; un-witnessed mutators keep one attempt | `an_idle_reaped_publish_session_reconnects_and_the_raise_lands` (RED with the live signature) |
| 5 | **Over-cap extent ships** (convicted live): the page cache coalesces buffered records into max_write (1 MiB) FUSE writes; the extent-ship of that slice built a 1,048,633 B frame the wire's CONTROL cap refused → app EINVAL | `ship_extent` chunks over-cap payloads into byte-adjacent witnessed sub-extents (own request_id + retention record each — retention granularity == wire granularity); the cap stays as the loud guard | `an_extent_larger_than_the_wire_cap_ships_chunked_and_assembles_exact` (RED, byte-exact assembly across the boundary) |
| 6 | **The geometry source's size read — Issue-19 at the SOURCE** (convicted live on the MPI-IO row): size read from the LAYOUT head only — absent on a truncate-created sparse file, undecodable on a chained/indirect head — so a 10 GiB shared file answered 0, the span cap floored at 16, and the 32-rank row's 17th stripe refused "at capacity": 35-attempt ladder exhaustions, 760 s watchdog writes, a wedge-crawl | geometry size = **max(inode record size, layout head size)** | `the_geometry_source_reads_a_truncate_created_files_size` (the truncate contract; the chained-head face = the live leg, per the repro-port exception) |
| 6b | **The §6.2-item-9 divergence refusal latched TERMINAL** (convicted live at width 32): concurrent same-ino publishers keep the owner's chain compacting; every in-flight delta naming the prior base refused "divergent layout-delta chain … folds onto 0x0" — a transient supersession classified terminal → EINVAL latched (POSIX-16) → ior aborts. The class travels in THREE coats (local `InvalidOperation`, coalesced-pass `Io`, and the shipped `Refused{errno}` WireError round trip) | the divergence marker joins the RETRIED writeback class (the save's refusal arm already resets provenance — the retry refetches and converges); every other corrupt-encoding refusal stays terminal | `the_divergence_refusal_is_the_retried_supersession_class` (all three coats) |

Plus three authority-side composition fixes for the sub-block plane's
dangling-take mint (faces 1–3 of four):

| face | Mechanism | Fix | Pin |
|---|---|---|---|
| 1 | a range holder's scoped Put treated DEMOTED bytes as its truth — its stale key reverted the authority's assembly ([C8] 1-durable-vs-0 + [C2] leak) | demoted regions excluded from the Put truth-span, both directions (`dlm::demoted_regions` in `custody_scoped_layout`) | `a_scoped_put_never_reverts_a_demoted_blocks_assembly` (RED with the revert) |
| 2 | the authority's OWN local publishes of a range-granted ino ran outside `SERVE_INO_LOCKS` — a fold merge landing inside a served Put's read→commit window was erased | `local_publish_guard`: local layout publishes of a range-granted ino take the serve stripe; the serve wrapper exempts the EXTENT verbs (their executors' folds take the guard at the funnel — wrapper-held would self-deadlock `FlushExtents`) | `a_local_publish_of_a_range_granted_ino_parks_on_the_serve_window` (+ custody-free never-parks control) |
| 3 | SERVED layout commits bypass the authority fs's RAM view — its fold's DISPLACED-release set never saw the peer's last direct key (the dangling take) | `install_served_layout_invalidation` (the co-writer `install_release_hook`'s mirror): committed layout-class serves invalidate the fs's metadata/attr caches for the named inos; refused serves invalidate nothing | `a_served_layout_publish_invalidates_the_authoritys_ram_view` |

## The rows

### 1. The MPI-IO acceptance row (`s11-mpiio`)

Shape: 8 co-writer mounts × 4 procs = 32 ranks, ONE shared file, `-b 4m
-t 4m -s S` (4 MiB-aligned block-cyclic segments), probe-sized sustained
window (≥ 60 s, ≥ 3 steady iterations, flatness ≤ 30 % first-vs-last steady
iteration), A-B-B-A vs `-F` file-per-proc on the SAME fleet; correctness =
`-r -R -C` reorder-tasks cross-mount read-back under a fixed `-G` signature
+ cold-authority fsck + C8.

RESULT: **BLOCKED-STANDING-RED at width 32** (see the headline). The
row's ten from-zero attempts each convicted-and-fixed one more product
bug (the fixes ledger — a textbook crucible sequence: PRRTE slots →
geometry cap-16 wedge → divergence latched terminal (three coats) →
extent-ship wire cap → own-lease fencing livelock), converging to the
final posture: writes flow (probe 0.7–2.0 GiB/s aggregate), every ledger
stays clean (`writeback_errors_latched 0`, `lease_retry_exhaustions 0`,
cap_refusals 0, conflicts 0, demotions 0), and the phase stalls in the
retried divergence/compaction churn — the width-N composition residual's
throughput face. The leg exits nonzero; the row's verdict issues at rung
19 with the composition closed.

### 2. The block-cyclic row (`s11-blockcyclic`) — Issue-19 adjudicated live

8 mounts × 1 proc, round-robin over ONE 2 GiB file (512 × 4 MiB blocks; a
holder's spans never adjacent — nothing coalesces). From-zero, final-binary
lineage:

| gate | value | verdict |
|---|---|---|
| grants ≈ blocks | 790 vs 512 (incl. re-acquires across close/reopen) | ✅ |
| `range_custody_cap_refusals` | **0** | ✅ the Issue-19 column |
| demotions on an aligned row | 0 | ✅ |
| live table peak (sampled 0.5 s) | 36 spans / 1,872 B vs budget 345,518,443 B | ✅ bounded (peak is report-only: grants release at close) |
| aggregate vs same-width disjoint control | 2,273 vs 1,492 MiB/s = **1.52×** | ✅ ≥ 0.8× band |
| fsck/C8 | drift 6 (the width-N refs composition — below) | standing-red, adjudicated |

### 3. The adversarial tiny-ranges bounds row (`s11-tiny`)

One co-writer floods 4,096 × 137 B writes at seeded-random unaligned
offsets across one 64 MiB (16-block) file; the second co-writer's own-file
fsync ops sample foreign latency before/during.

| gate | value | verdict |
|---|---|---|
| storm completes (no wedge) | 0.78 s | ✅ |
| coalescing holds O(blocks) | 2 grants + 15 extensions; live peak 2 spans / 96 B | ✅ |
| cap refusals | 0 | ✅ |
| table vs R5 share | 96 B vs 345 MB | ✅ |
| foreign-client latency | 6.53 → 6.68 ms median (1.02×) | ✅ within noise |
| at-budget refusal law | the rung-15 pins (a)/(b) + wire face — refusals name the arithmetic, converge by release | standing in-process pins (no live sub-budget shape can reach the derived budget honestly) |
| fsck + C8 | findings 0, drift 0 | ✅ |

### 4. The demotion-barrier row + the rung-17 price table (`s11-subblock`)

The §9.3 shape verbatim: incumbent m50 streams 4 KiB records over [0,2M)
(buffered, per-pass fsync — pre-demotion it holds whole-block custody and a
co-writer's small overwrite rides CoW-rewrite, W1 being authority-only BY
DECISION; its post-demotion slices ship as max_write-coalesced CHUNKED
extents), challenger m51 streams O_SYNC records over [2M,4M) mid-stream.
Final-binary from-zero run (`s11sb-1787021497`):

| column | value |
|---|---|
| the barrier | `demotions 1 ≡ acks 1`, `fence_resolves 0`, `fenced_publishes 0` (single publisher at every instant) |
| the clause ledgers (rung 16's firing venue) | `patch_ineligible_range_shared` +1837, `overlay_ineligible_range_shared` +698 |
| extents shipped | m51 512 (per-record O_SYNC) + m50 2,798 (coalesced chunks) = 3,310; authority `extent_served` accounts (shipped ≡ served); `retained_bytes` → 0 at quiesce |
| B through-demotion | 4.96 s for 512 O_SYNC records (≈ 9.7 ms/record — barrier wait amortized, ships + owner round trips inclusive) |
| incumbent per-pass wall (2 MiB/pass) | first-50 mean 7.6 ms → all-passes mean 11.6 ms (776 passes; the demotion lands ≈ 1 s in — the tail IS the post-demotion coalesced-extent shape) |
| authority merge CPU | 980 ticks / 9.80 s over the row = **≈ 2,961 µs/extent upper bound** (serve+merge+fold+publish inclusive, /proc utime+stime) |
| correctness | engagement GREEN; cold byte verify GREEN; fsck/C8 standing-red on face 4 (below) |

The price verdict (KD-MW-8 as designed): sub-block sharing is the priced
EXCEPTION path — ~3 ms authority CPU per extent vs the aligned rows' direct
DMA; D1 (rarely-shared) holds.

### 5. The range kill matrix (`s11-killrange`)

From-zero on the final binary (`--rounds=3`, fresh 48 GiB fleet):

| cell | rounds | verdict |
|---|---|---|
| **H** — kill -9 a range HOLDER mid-write | ×3 (plus ×3 on each of two earlier from-zero runs = ×9 total lineage) | GREEN strict-clean every round: survivor's concurrent stream completes, the authority sweeps the victim's era (`dlm_revokes_expired` moves), `range_custody_active` → 0, `dlm_custody_grace_conflicts` 0, victim re-admits, **fsck findings 0 + C8 drift 0 after every cell** |
| **A** — kill -9 the AUTHORITY mid-ASSEMBLY | ×2 | GREEN: the fleet recovers (authority remount, every co-writer re-admitted — twice per round: post-kill AND post-cold-verify, the S7 fence-on-bounce law made mechanical in the leg), the re-driven two-holder pass lands byte-exact cold, retained extents re-ship (MW-11's live face); fsck reads the ADJUDICATED fourth-face shape EXACTLY (one C2+C8 dangling-take pair per demoted-churn round, accumulated report-only — any other class/text/drift dies) |

Zero silent survivors: every cell's kill is followed by the census; the
S7/era laws held in every round.

## The two standing-red composition residuals (rung 19's, adjudicated)

1. **The sub-block dangling-take, face 4**: after faces 1–3 closed, the
   priced row still mints ONE `[C8] 1 durable vs 0 layout references [ino
   2 idx 0]` + `[C2]` leak pair per row, with `read_settle_lost_serialized`
   tripwires and a schedule-dependent settle-arm EIO on `FlushExtents` —
   the authority fold's displaced-set/seed-incarnation window vs the SERVED
   free/publish path (which rides neither (3) nor (3.5)). Leak-safe,
   oracle-detected, bytes unaffected (cold verify green).
2. **The width-N same-ino refs composition**: at 8–32 concurrent direct
   publishers of one ino, caller-computed block-ref deltas are not atomic
   with the chain-composed map (the conveyor/chain plane; rung 17's suites
   ran width 2) — ONE swapped pair per race (a durable-without-map take +
   a map-without-durable entry; the bc row measured drift 6 per 512-block
   from-zero pass). `SQUEEZEFS_PUBLISH_COMMIT_GROUP_MAX=1` (the registered
   pre-Lever-B lever, now create-recordable as
   `SQZ_MWFLEET_PUBLISH_GROUP_MAX`) does NOT close it — the composition is
   in the chained direct path too, not just the aggregated window.

Both keep the affected legs' fsck/C8 half loud-red naming this note (the
G-RW2 pattern); any different failure shape is a regression.

## The `SQUEEZEFS_RANGE_CUSTODY` default adjudication (charter item 6)

**STAYS default-OFF.** The rung-15/17 adjudication is UPHELD a third time,
now with the rows' own numbers: the aligned rows' engagement/throughput
halves are green (block-cyclic 1.52× its disjoint control; tiny-ranges
collateral 1.02×), but the never-lossy law keys on the fsck/C8 half, and
TWO characterized composition residuals stand red at fleet width. The flip
requires: face 4 closed, the width-N refs composition closed, and the
`s11-range` + row gates green ×3 from zero on the flipped default. The
fleet keeps arming it explicitly (`SQZ_MWFLEET_RANGE_CUSTODY=1`).

## Gates

* **No-regression legs (final binary, from-zero)**: `s11-range` GREEN
  (composition gate included — engagement exact, Issue-19 column 0, kill
  arm, cold verify, fsck 0/drift 0; the v2 stretch changed its RTT shape
  to 1 acquire + 63 extensions per 256 MiB half — the kernel-split
  frontier residual, sub-1 % at localhost) and `s9-fanout` GREEN at its
  historical width 2 (engagement exact, amp columns present — user
  4,608 MiB / device 6,507 MiB = 1.412×, tripwires flat, fsck 0/drift 0).
  s9-fanout at width 8 on a custody-armed fleet WEDGES in the shipped-free
  double-release churn — NEW territory (the width-N family), recorded as
  a rung-19 residual, not a regression of the proven leg.
* Touched + adjacent suites, serial: rebind_starvation 4,
  mw_ranged_lease_ladder 7, mw_authority_assembler 21, mw_cowriter_lane
  24, dlm_range_custody 33, mw_truncate_lease_strand 1,
  fsync_writeback_tail_loss, write_pipeline 22, mw_publish_era_gate 5,
  mw_cowriter_free 13 — green (per-fix runs; the final serial sweep is
  in the closing gate run below).
* `cargo clippy --all-targets --all-features -- -D warnings` +
  `cargo clippy --all-targets -- -D warnings` (shipped config): clean.
* `cargo fmt --check`: clean. shellcheck: clean (run_mw_matrix.sh,
  mw_fleet.sh). markdown link check: PASS.
* Zero-residue teardown after the final leg ("teardown complete — zero
  residue").
* Full `task check`: DEFERRED per the rung charter.

## Residuals for rung 19 (ordered)

1. The width-N same-ino publish REFS composition (standing-red #2): make
   the ref delta atomic with the chain-composed map — compute
   displaced/inserted INSIDE the chained merge (the backend knows both),
   never caller-side; the bc row ×3-from-zero is the acceptance.
2. The sub-block dangling-take face 4 (standing-red #1): the authority
   fold's displaced-set/seed window vs the served free/publish path — the
   settle-arm EIO on `FlushExtents` is the same window's latency face.
3. The free-grace ack cadence under rewrite churn (rung-17 finding 5c,
   reproduced at scale): a storm's deferrals outrun releases
   (`free_grace_offsets` climbs, lane-share ENOSPC follows — the manual
   two-holder storm exhausted a 32 GiB volume's lane in ~460 passes);
   wants its own row + a pressure-coupled release valve.
4. The kernel-split write stream defeats the exact-match sequential
   frontier (v2 stretch): live dd streams pay 1 extend RTT per block
   (63/64 extensions observed on s11-range — sub-1 % at localhost; the
   R2-classifier window remains the fabric-venue refinement).
5. `range_custody_grant_census` (VAL-7a opt-in) — still unexported;
   rung-15 residuals 4–6 unchanged.
6. The ior fleet venue notes: PRRTE slot default = physical cores
   (`--map-by :OVERSUBSCRIBE` is load-bearing in `run_ior`); ior 4.0.0
   needs `-std=gnu17` under GCC ≥ 15 (recorded in `ensure_ior`).
