# 2026-09-11 — 1.2.4 release gate (`stable-2026.09.4`)

**Tested tree: `fe4c9390`** (`dev`'s tip after the leaf-merge finalization
`53e4feeb`, the suite-tree harness `6117b0c7`, and its exec-bit fix). The
tag points at the release-act commit over it (version bump 1.2.3 → 1.2.4
across the five first-party crates + lockfile stamps, the four documented
version lines, the release notes, this record) — `git diff fe4c9390 <tag> --
src crates/*/src fuzz/fuzz_targets` is empty; the lockfile diff is the
workspace crates' own version lines plus cargo's Windows-only `windows-sys`
edge re-resolution (0.52 → 0.59 — the flip the 1.2.3 bump made the other
way; never compiled on a Linux target).

Venue: the dev laptop (strixhalo, 32 CPUs, `7.2.3-cachyos-lto`, an
sqz-series kernel — the zc leg runs here), the detached release worktree
`/home/justin/Source/squeezefs-release-1.2.4`, one unattended driver
(`/tmp/release-1.2.4/driver.sh`; artefacts under `/tmp/release-1.2.4/`).

---

## 1. One chain, from zero on `fe4c9390`

| leg | wall | result |
|---|---|---|
| `task check` | 18:49 → 19:55 | green — **380 suites / 4,961 tests**, 18 stages, both audits |
| fstests `-g auto` (`SQUEEZEFS_FSTESTS_EXCLUDE=generic/650`) | 19:55 → 21:26 | green — **787 ran, 783 clean, 4 expected-shape** (003/192 noatime, 213 thin provisioning, 798 write-through — the pinned shapes), **0 unexpected** |
| pjdfstests | 21:26 → 21:29 | green — **8,798, PASS** |
| LTP syscalls | 21:29 → 21:35 | green — **1,884 TPASS, 0 fail** — UNATTENDED through the durable suite tree (`tests/suite_tree.sh`): the 1.2.3 chain's manual LTP resume is gone |
| require-mount | 21:35 → 21:52 | green — skip ledger clean |
| zc-capability (root, sqz kernel) | 21:52 → 21:55 **venue**; 21:58 → 21:59 green | **187 tests**, skip ledger EMPTY (§2) |
| fuzz campaign (12 targets × 90 s × 2 jobs) | 21:59 → 22:13 | green — **399,325,992 execs, 0 crashes** |

## 2. The zc leg's venue condition (not a red)

The first zc pass exited 1 at 21:55 with every zc surface test green (94/95
in `bench_tests`, every other suite clean): `bench_tests::test_resolve_shape_
auto_defaults` declined under `SQUEEZEFS_TEST_REQUIRE_CAPABILITY=1` — "scratch
fs has < 68 GiB free; exact auto-size pin skipped". `/home` was at **98 %**
(38 GiB free): the main checkout's `target/` had grown to 601 GB across a
week of feature branches, the merged subagent clones held another ~190 GB.
Cleared (regenerable artifacts only; the gate and release worktrees kept
their warm builds) → 850 GiB free. The chain RESUMED from the zc leg on the
same tip (`/tmp/release-1.2.4/driver-resume.sh`): the product under test is
unchanged, so the five earlier greens stand — a venue repair is not a fix
under the counted-run law. Board: the release driver should REFUSE to start
under a free-space floor the bench pins need (the bench baseline's thermal
refusal is the precedent), and the main checkout's `target/` wants a
periodic clean.

## 3. What this gate covers since 1.2.3 (`stable-2026.09.3` = `fff56fcb`)

All landed on `dev` and batch-gated before the chain:

- **KV leaf merge** (`fac25656`) + its finalization (`53e4feeb`): the tree
  shrinks — §4.6a; cross-parent convergence proven (0 stranded, bound
  `3·h₀+1`), exact candidate gauge, the sweep bounded to one checkpoint tick
  with a resume cursor and priced by bench. Record
  `.benchmarks/2026-09-11-kv-leaf-merge.md`.
- **Board items 2–4** (`862b8077`): a full metadata heap = ENOSPC never
  FAILED; the release rides the destroy (closing the ≳ 3,000-reference
  unlink ledger orphan 1.2.3 ships); PK8's packed direct-drive arm (closing
  the whole-block dead-lifetime wrong-bytes exposure 1.2.3 ships). Record
  `.benchmarks/2026-09-11-post-123-board-items-2-4.md`.
- **The suite-tree harness** (`6117b0c7`, exec bit `fe4c9390`): durable
  home, marker liveness, legacy adoption, autotools from the store.

Batch gates on the way: `862b8077` (379 / 4,946), `fac25656` (380 / 4,956),
`6117b0c7` RED on the new script's exec bit (test-side: `script_exec_bit_
tests`), `fe4c9390` (380 / 4,961).

## 4. Venue adjudications (unchanged from 1.2.3)

`generic/650` excluded on this laptop (its CPU-hotplug storm hangs the Strix
Halo firmware); fstests 003/192/213/798 are the pinned by-design shapes; the
zc leg ran on this box's sqz-series kernel (squeeze-test carries no
toolchain since the 2026-09-09 cleanup — the box zc leg stays recorded
not-run).

## 5. Open beside the release (named, not fixed)

- The release driver's free-space floor (§2) and the `target/` hygiene.
- Leaf merge's one unreachable shape (two adjacent interiors whose
  separator folds exceed ¾C together) — bounded `stranded ≤ heavy_boundaries
  ≤ interiors − 1`, stated in the release notes.
- The 1.2.3 board: the s11 venue's capacity statement, write board #11
  (partial-block fsync escalation), the lost wake's loser; bench baseline
  compare + save; the scoreboard.
