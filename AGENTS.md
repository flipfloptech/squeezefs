# AGENTS.md — Squeezefs One Source of Truth

Squeezefs is a high-performance distributed POSIX FUSE filesystem (Rust + io_uring + the in-house `sqz_exec` executor — the shipped binary links NO tokio, user ruling 2026-08-13; `tests/no_tokio_convention_tests.rs` is the rail) with a decoupled block-based logical volume metadata store backend (MetaLV) and an NVMe / NVMe-oF block data backend. Linux-only.

**This combined document is the single authoritative reference.** It merges architectural rules, non-negotiables, development workflow, build/test gates, profiling, and repo conventions. Read it before touching core logic.

---

## Non-Negotiables

### Performance is the only terminal requirement (user ruling 2026-08-01)

**Nothing in this section is dogma — "the goal is the absolute fastest we can be."** Every mechanism below (io_uring transports included) holds its slot on **measured evidence**, and any of it may be displaced by counted A/B evidence that an alternative is faster at the relevant shape on target hardware. What stays banned is displacement by *convenience*: switching to a classical mechanism to dodge a bug or make a test pass is still an anti-pattern — displacement requires measurement, not frustration. (FUSE-over-io_uring currently holds its slot on evidence: 44k → 316k IOPS, `.benchmarks/2026-07-15-iops-parity-decomposition.md`.)

Corollary (user doctrine, 2026-08-02): **"if we get maximum possible throughput and IOPS then all the rest will land into place."** Maximum-speed pursuit is the organizing program, and the 2026-07/08 record shows why it works: saturation explores schedules nothing else reaches, so the perf campaigns kept surfacing latent correctness bugs (load-dependent hangs, watchdog panics, wedges, ledger inversions) that quiet testing never would — each then pinned by a red-first cargo repro forever. Chasing the ceiling IS the correctness crucible; "load-dependent hangs are first-class product bugs" is the same law seen from the other side.

### Portable by default (user directive 2026-07, NT-store campaign)

**Scope (ruling D13, user 2026-08-06): portability governs CPU/topology, never kernel version.** The sqz custom kernel series (`docker/kernel-sqz/`) is a first-class product surface — "anything is OK in the realm of a custom KERNEL or custom KERNEL requirements for it to work"; the program tracks the latest mainline and uses modern kernel features as needed (kernel-dependent fast paths negotiate and degrade loudly on stock kernels, but the requirement itself is sanctioned).

No CPU-model-specific code paths and no per-CPU tuning matrices. Microarchitecture-sensitive optimizations are admissible only when they are (a) measured wins across the target hardware set, (b) derived from **runtime behavior** (never CPU-ID tables), and (c) harmless where the microarch trait is absent. Same law for topology: anything NUMA-aware must scale on its own across arbitrary domain counts (not all hosts have 2 domains — see `crates/squeezefs-ipc/src/numa_core.rs` for the pattern).

**Resource caps derive from system resources** (user directive 2026-08-02, verbatim: "remove any hard coded limitations in favor of dynamically computed values based on available system resources"; derivation sweep 2026-08-04, `.benchmarks/2026-08-04-derivation-sweep.md`): sizing defaults are percentages/functions of the R5 memory budget, core count, block/ring geometry, or measured bandwidth — never free-floating byte/thread constants. Fixed clamps require a documented physical/format/kernel/measured reason **on the line**; floors are legitimate only as physical minima or the never-regress-below-shipped posture (the `Q_DEPTH_FLOOR` law), never as tuning. Env overrides stay explicit-wins-verbatim with precedence absolute > percentage > derived (the ipc-cap pattern), and every derivation carries a drift-is-red tie test (`tests/derivation_sweep_tests.rs`). Every knob also obeys the ONE parsing convention — registry entry required, loud refusal on a bad value (ENG-10; see Repo-specific conventions).

### Always use io_uring when we can

*(Presumption, not dogma — see the 2026-08-01 ruling above: this slot is held by measurement and is displaceable by measurement.)*

**Policy for agents and humans:** prefer and require **io_uring** for every I/O path where the Linux kernel can do it. Do **not** “temporarily” fall back to classical `read`/`write`/`pread`/`pwrite`/`/dev/fuse` polling as a way to unblock a bug. Fix the uring path, or fail loud.

| Path                              | Expectation |
|-----------------------------------|-------------|
| **FUSE request hot path**         | **FUSE-over-io_uring only** after arm (`REGISTER` / `COMMIT_AND_FETCH`). No userspace opt-out. Mount fails if setup fails. |
| **FUSE_INIT + classical sideband** | Classical `/dev/fuse` for `FUSE_INIT` (kernel requires `fch->initialized` before REGISTER), then the **kernel-mandated classical sideband** only: the kernel keeps FORGET/BATCH_FORGET + INTERRUPT + `fuse_resend` resends + `fiq->ops` switchover stragglers on the classical queue even when armed (fs/fuse/dev_uring.c). A dedicated sideband session services them — **via io_uring `Readv`** — or they strand forever (`waiting ≥ 1`, umount EBUSY). All other requests stay over-uring. |
| **NVMe / block data**             | `NvmeBlockDev` io_uring workers (fixed files when available). |
| **Ad-hoc local files**            | `crate::uring_fs` (not std file APIs) where practical. |
| **Not uring**                     | TLS peers, network TCP/TLS stacks. Staging **mmap** segments stay mmap by design. |

If over-uring or block uring misbehaves: **debug and fix uring** — never reintroduce a classical escape hatch “just to make tests pass.”

**FUSE uring knobs (env / mount)**

| Variable | Effect |
|----------|--------|
| `SQUEEZEFS_FUSE_IO_URING_SQPOLL_IDLE_MS` | Enable SQPOLL with idle timeout (ms) |
| `SQUEEZEFS_FUSE_IO_URING_SQPOLL_CPU` | Pin SQPOLL kernel thread |
| `SQUEEZEFS_FUSE_IO_URING_ENTRIES` | SQ depth for classical FUSE rings (INIT/notify) |
| `SQUEEZEFS_FUSE_OVER_IO_URING_Q_DEPTH` | Entries per FUSE-over-io_uring queue — explicit value wins verbatim (clamp 1..32). **Default = L1 policy** (2026-07-15 IOPS-parity): desired **32**, degraded to fit the payload-buffer cap `mem_budget/8` (no fixed ceiling — derivation sweep 2026-08-04; `SQUEEZEFS_TRANSPORT_MEM_MAX` MiB / `SQUEEZEFS_TRANSPORT_MEM_PCT` override, absolute > pct > derived), floor 4 (= pre-L1 posture) |
| `SQUEEZEFS_FUSE_OVER_IO_URING_QUEUES` | Number of queues (**testing only**; default = kernel **possible CPUs**, clamp 1..512 — fewer than possible CPUs never becomes ready) |
| `-o max_background=N` / `-o congestion_threshold=N` | Mount-level INIT-reply overrides. Defaults: `clamp(queues × depth, 64, u16::MAX)` — the delivered ring capacity; the fixed 256 ceiling retired 2026-08-04 (derivation sweep; `-o max_background=256` = the A0 lever) — and ¾ of it (pre-L1 the INIT reply hardcoded 12/9; runtime-writable per connection via fusectl) |

Mount always enables FUSE-over-io_uring after INIT (required). Expects: "FUSE-over-io_uring ready..." and "transport enabled for this session". Transport geometry + INIT limits are gauged on the stats inode (`transport_{queues,q_depth,payload_buffer_bytes,max_background}`), and the payload arenas ride the R5 memory budget as the non-sheddable `transport_payload_buffers` component. Evidence: `.benchmarks/2026-07-15-iops-parity-decomposition.md` (44k → 316k measured) + `.benchmarks/2026-07-15-l1-transport-concurrency.md` (defaults acceptance).

**io_uring coverage**

| Path | Mechanism |
|------|-----------|
| Primary block device R/W | `NvmeBlockDev` worker + fixed-file registration when supported |
| Ad-hoc file R/W / fdatasync | `crate::uring_fs` process worker |
| Mmap page hint | `IoUringPrefetcher` (`MADV_WILLNEED`) |
| FUSE transport (default) | `fuse3` `BlockFuseConnection` (classical rings during INIT); **FUSE-over-io_uring** (`IORING_OP_URING_CMD` + REGISTER/COMMIT_AND_FETCH) after arm; post-arm **classical sideband session** (io_uring `Readv` on `/dev/fuse`) for kernel-mandated FORGET/INTERRUPT/resend traffic + switchover stragglers |
| FUSE_WRITE payload delivery | Zero-copy **payload lease** (`Bytes::from_owner` over the registered uring payload buffer) with deferred COMMIT_AND_FETCH re-arm; the lease-severance boundary bounds every lease to one handler invocation — `docs/design-zero-copy-write-path.md` §5.4 |
| Staging / read-segment hot path | **mmap** (by design — zero syscall) |
| TLS peers | Not uring (network) |

Hardening: pool not marked ready until all queues submit REGISTER; per-qid commit channels; shared inbound queue; eventfd wake; full-size payload buffers.

### No dead code

**Do not leave unused code in the tree.** Agents and humans must remove it, not silence it.

- **Delete** unused functions, methods, fields, imports, constants, modules, and feature-gated stubs that nothing calls.
- **Do not** paper over dead code with `#[allow(dead_code)]`, `#[allow(unused)]`, or broad `allow` attributes “for later.” If it is not used now, delete it; restore from git when needed.
- **Clippy/gate:** `cargo clippy --all-targets --all-features -- -D warnings` must stay clean — that includes unused items. Fix by **removing** dead code, not by allowing warnings.
- **Exceptions only** when the item is part of a public API surface that must stay stable (`pub` for crates/downstream) or is required for `#[cfg]` / trait impl completeness and truly cannot be omitted — document why in a one-line comment on that item. Prefer not exporting unused symbols.

### Zero-copy and latch-free data paths

**Policy for agents and humans:** maintain and enforce a zero-copy, latch-free hot-path design. Do not introduce traditional blocking locks (like standard `Mutex` or `RwLock`) or unnecessary data copy operations on the primary read/write data path.

- **Zero-Copy Hot Path:** Hot staged files are mapped directly using memory mapping (`mmap`). Buffer segments must be returned or updated in-place via pointer/slice references. Avoid allocating new buffers or cloning vectors during standard read/write execution.
- **Latch-Free/Lock-Free Caching & Indexes:** Hot metadata tables, directory entry indices, and block routing tables must use lock-free or latch-free data structures (e.g., `scc::HashMap`, sharded atomic clock rings, atomic reference counts). Traditional read/write synchronization locks are only permitted for FUSE operations and metadata/lease transactions (see Lock Order constraints).
- **Zero-Copy GPU Direct (GDS):** When `gds` feature is active, bypass host RAM completely. Copy block data directly between NVMe/NVMe-oF and GPU memory via RDMA.
- **Zero-copy write path (normative design):** the large-write hot path is **1 userspace copy + 1 DMA** — transport payload leases (no per-request copy/alloc), one merge copy into the exclusive-owner `ActiveBlockBuf`, complete-block write-through past staging, guard-backed DMA for residual staged flushes. Design + acceptance evidence: `docs/design-zero-copy-write-path.md` and `.benchmarks/2026-07-08-zero-copy-write-path-closing.md`. Do not reintroduce copies or staging detours on this path.
- **Near-zero-copy census + cost levers (2026-07-31, `.benchmarks/2026-07-31-near-zero-copy.md`):** the campaign's copy ledger DECLARED the two remaining hot-path copies **load-bearing** — the ring sever IS the L4 §5.2 isolation boundary, and the kernel-path merge is what the §5.4 lease-severance law + the ACK-before-DMA pipeline detach require — and cut their COST instead: DMA-destined copies (the lease→`ActiveBlockBuf` merge and the placed-sever arena→assembly write) ride **non-temporal stores** (`src/nt_copy.rs` — the trailing `sfence` is load-bearing for the existing publication edges; knobs `SQUEEZEFS_NT_COPY`/`SQUEEZEFS_NT_COPY_MIN`, engagement `nt_copy_bytes`), and **session arenas are PMD-aligned and populate+collapsed by the deferred single-lane `sqz-ipc-thp` prep worker** (`crates/squeezefs-ipc/src/thp.rs`, `#[path]`-shared per the `wake_core` precedent; `SQUEEZEFS_IPC_ARENA_THP=0` disables — `MADV_COLLAPSE` bypasses the field fleet's `shmem_enabled=never`; the PMD-alignment law: an unaligned vma maps huge folios 4 KiB-wise, `map_shared_pmd_aligned` is what makes the collapse pay). **`SessionOk` never waits for prep** (fleet-parity fix 2026-08-05: the retired admission-inline populate+collapse cost O(fleet × arena) serialized memory work at every fleet launch — 256 sessions × ~120 MiB ≈ 20 GiB before first ops, the D12 item-2 term; `ipc_arena_prep_{queued,done}` is the ledger, mbind placement still precedes any population so the NUMA compose law holds). Never NT-store a destination a CPU reads next (read serves, pooled sever, CoW) — that trades an LLC hit for a DRAM miss on the consumer. Field kernel posture (verified): nvme-tcp TX is zero-CPU-copy (spliced GUP pages, digests off — never enable digests on perf fleets without re-running the ledger); RX pays one irreducible kernel copy per read byte.

---

## High-Performance Distributed Filesystem Architecture

### Target Scale & Layout
* **Scale:** 15,000+ Concurrent Nodes — the **design target**, meaning (user ruling D1, 2026-08-02) 15 k nodes all reading AND writing but rarely the same files (at-scale AI-training mixed workloads). **Shipped (spec §6.12 correction, restated at the MW program close 2026-08-18; per-volume claim admission accepted 2026-08-24): ONE metadata authority per volume set (the D0 write mount) plus N read-only mounts plus N co-writer mounts — or, on a set the offline `squeezefs volume set-owners` verb assigned, K PER-VOLUME owners (D20: the slot-0 volume's owner is the SET authority — custody, lanes, the one grace ring; every other owner is a PARTIAL authority that appends to its own volumes and ships the rest).** Readers (`-o ro` / `--read-only` — DLM S5: no lease, no claim, no PR key, orthogonal to the guard) serve the newest checkpoint they have polled, with a monotone staleness bound published live as `reader_staleness_bound_ms`; the DATA half of that bound is **eliminated when the S6 membership plane is armed** (§6.8 item 3's freed-offset grace period, `src/free_grace.rs`) and stays bounded-not-eliminated with the plane `off`, the shipped default. Co-writers (DLM S9, `SQUEEZEFS_MULTI_WRITER=1` + the five-rung admission ladder — PR-capable data namespaces required, refused loud otherwise) DMA their own data under authority-granted custody and SHIP every metadata mutation; byte-range custody of one shared file (S11) exists behind the default-off `SQUEEZEFS_RANGE_CUSTODY`. **Evidence tiers per `docs/rc-manifest.md` (mandatory on every scale claim):** what is *measured-real* is the single-node proving fleet (up to 32 co-located mounts + a qemu/KVM guest member, real nvmet-tcp PR target — `.benchmarks/2026-08-16-mw-s{6,7,8,9}-arm.md`, closing record `.benchmarks/2026-08-18-mw-program-closing.md`); fleet fan-out rows are *measured-simulated* (one box, one memory bus); **every 15 k number is arithmetic-on-measured-constants** — and the published arithmetic itself says the single-authority publish plane bounds aggregate co-writer ingest at ≈ 2.6 GiB/s independent of writer count (the S9-a note), which is the wall the S10 fleet-of-authorities recipe exists to move — **built and accepted 2026-08-24** (per-volume claim admission, `docs/design-per-volume-claim-admission.md`: `squeezefs volume set-owners` + `SQUEEZEFS_MW_ROLE=set-authority|partial-authority` stand a K-owner fleet where each owner appends to its own volumes and ships the rest; the S10 tar-x gate is MET at 0.98× of S0 on a 2-owner fleet, closing record `.benchmarks/2026-08-22-pv-claim-admission.md`). 15 k has never been measured; claims that imply otherwise are wrong.
* **Architecture Type:** Decoupled metadata (MetaLV) + **block data** (local NVMe / NVMe-oF), exposed via POSIX FUSE.

### Technology Stack
* **Client Daemon (FUSE Engine):** Built in **Rust** on `io_uring` (FUSE-over-io_uring after INIT) with the in-house executor (`crates/squeezefs-ipc/src/sqz_{exec,time,channel,blocking,taskset}.rs` — rip-tokio-total, 2026-08-13: the scheduler-bug remedy was REMOVAL; tokio survives as a `#[tokio::test]` dev-dependency only, enforced by `tests/no_tokio_convention_tests.rs`).
* **Metadata & Distributed Lock Manager (DLM):** Local or distributed logical volume backend (**MetaLV**). Primary meta store for attrs, layout maps, leases, and volume format.
* **Data Backend (primary):** **NVMe / NVMe-oF block devices** via `NvmeBlockDev` (io_uring workers). Progressive layouts (inline / staged / striped) live on this path.

### Progressive Data Layout & I/O Routing

Logical file growth uses three layouts (thresholds are implementation-defined; current code uses ~4 KiB inline, up to ~4 MiB staged when staging dirs exist, else striped):

1. **Inline (tiny):** Payload in Metadata Volume (`inline_data:…` key/attribute) with type `inline`.
2. **Staged (small):** Local NVMe staging (`file_id` + optional `mapping:…`); writeback/flush promotes to durable blocks.
3. **Striped (large):** 4 MiB (configurable) blocks on the active block backend with `block_map:…` and refcounts.

Writes that grow past thresholds promote layouts **durably** (block I/O before meta type flip — see P0 layout atomicity).

### Distributed Lock Manager (DLM) & Consistency

**Shipped scope (corrected 2026-08-02, spec §6.1/§6.12 — the prior text described an unimplemented design):** `src/dlm.rs` is a **process-local** lock manager — a `scc::HashMap` with no network, no persistence, no TTL on a held lease, no renewal, no revocation, no shared mode, and no cross-process visibility. It serializes writers *within* one mount; **cross-mount exclusion is the D0 single-writer mount guard's job, not this module's.** Fencing tokens are monotonic per file **per process** and restart at 0 on every mount (spec §6.11 — the "stale tokens discard staged work" remount law cannot fire at mount time today; the staging generation stamp is what actually protects that path). The DLM program that makes this genuinely distributed is spec §6.9 stages S0–S11; treat every claim below as *design intent under construction*, and never write code that assumes a capability the stage table has not landed.

**The full multi-writer program is COMPLETE** (2026-08-18): `docs/design-full-multi-writer.md` (consensus 2026-08-15 — arm S6–S9, build S10–S11, single-node client fleets) ran all 19 rungs plus its fix campaigns 2026-08-15 → 2026-08-18; the closing record is `.benchmarks/2026-08-18-mw-program-closing.md` and the design doc carries the per-rung landed SHAs and the **rung-20 residual board** (the next program's input). Landed and load-bearing: the KD-MW-2 `(node_token, mount_slot)` client identity, per-mount NVMe-oF identity (`SQUEEZEFS_HOSTNQN`/`SQUEEZEFS_HOSTID`, pair-or-neither) + durable `fabric_endpoint:` records, the KD-MW-14 `SQUEEZEFS_FLEET_SHARE` root divisor, one-act nine-bit stamping with **`format` multi-writer-capable BY DEFAULT** (rung 10b — `--single-writer` is the opt-out), the sqz-kernel host-scoped fabric subsystems (patch 0030, `docs/design-mw-multipath-kernel.md`), S6–S9 armed and crucible-proven, KD-MW-16 fleet-parallel maintenance, S10 delegations/intents/placement (the tar-x gate adjudicated as the honest product statement — 6.73×, structurally unmeetable while one node owns every volume), and S11 byte-range custody with the MPI-IO verdict **MET** (`SQUEEZEFS_RANGE_CUSTODY` stays default-off pending the indirect-map width-N composition). Every distributed plane still ships **dark by default** — a plain mount arms nothing and re-proves the solo re-gate (`dlm_rpcs == 0`); consult that design + the residual board before touching any DLM/membership/custody surface. The paragraph above (the process-local `src/dlm.rs` core) remains accurate for an UNARMED mount, which is every mount that does not opt in.

Write custody maps to leases on Metadata Volumes:
* **Acquisition:** Lease locking + fencing token `INCR` (no external distributed database required).
* **Granularity:** File-level write leases; never directory-wide for data.
* **Leases & Heartbeats:** the **process-local** per-inode lease has no expiry by design (the `ttl` argument is the *waiter's* deadline) — a solo mount needs none. TTL + background renewal + revocation SHIP on the distributed planes: S6 membership leases (owner TTL vs the member's stricter `T_self`) and S9 custody leases (pull-based revocation, self-fence, dead-epoch quarantine) — armed only by opt-in.
* **Fencing Tokens:** Monotonic per-file tokens; writers present tokens; stale tokens → `FencingTokenExpired` / reject. **Monotone per process only** until S1/S2 (global `grant_seq` + durable `WriterClaim.term`).
* **POSIX advisory locks (fcntl byte-range, OFD, flock) are KERNEL-LOCAL** (2026-07-22, VL10 release gate — fstests generic/131/478/504): the INIT reply never advertises `FUSE_POSIX_LOCKS`/`FUSE_FLOCK_LOCKS` (pinned in the fuse3 fork's negotiation tests), so the kernel's canonical `posix_lock_file` arbitrates per mount — full POSIX semantics (unlock-on-close, splits, OFD owners, `/proc/locks`) for free. Intra-mount arbitration is the whole requirement under the D0 single-writer mount guard; the former daemon lock table and the DLM per-inode delegation surface it rode were deleted (never construct a daemon lock path "for the cluster" — cross-mount write exclusion is D0's job).

### Tiered Caching & Paths

* **Tier 1 (optional GDS):** GPU Direct path when `gds` feature is enabled.
* **Tier 2 (RAM LRU):** Sharded Clock/LRU read & write caches.
* **Tier 3 (Local NVMe staging / read cache):** Staging segments + optional read block cache; dehydrate on eviction.

### FUSE Client & Asynchronous I/O

* The in-house `sqz_exec` lane executor (per-core handler lanes, `sqz-meta` lanes, blocking pool); core pinning where configured. No tokio in product code.
* **Block path io_uring:** `NvmeBlockDev` worker (bounded request queue, backpressure, fixed-file register when available).
* **Path file I/O io_uring:** `crate::uring_fs` for ad-hoc local files (e.g. GDS cache materialize). Staging **mmap** segments stay mmap for zero-syscall get/put.
* **FUSE transport:** the first-party **fuse3** fork (`crates/fuse3`) — classical `/dev/fuse` only for `FUSE_INIT` (kernel requires initialized connection before REGISTER); **FUSE-over-io_uring is required** for the request hot path after arm (`flags2` `FUSE_OVER_IO_URING`, `REGISTER` / `COMMIT_AND_FETCH`). No userspace opt-out; mount fails if setup fails. Auto-enables `fuse.enable_uring=Y` when possible. One queue per possible CPU; session arms only after all queues REGISTERed.
* Always use io_uring when we can (non-negotiable) — see Non-Negotiables section.
* No dead code (non-negotiable) — see Non-Negotiables section.
* Not uring: TLS peer paths (network). Staging mmap segments stay mmap by design.

### Reference fast-FUSE clients (user directive, 2026-07-14)

The **DAOS client** (`github.com/daos-stack/daos`, `src/client/` — dfuse / libdfs / libioil / libpil4dfs) and **JuiceFS** (`github.com/juicedata/juicefs`) are the designated reference fast-FUSE clients. **Consult them for perf/recovery patterns before designing new client-side machinery** — both independently converged on the levers the metadata-throughput program validated (negative-dentry TTLs, clean-handle FLUSH elision, submit batching, per-class kernel TTLs), and their recovery/ops machinery (external supervisor + FUSE-connection abort, fsck/dump/backup, cache-disk health FSM, seamless-upgrade fd handover) is the standing gap board. The full survey — findings classified HAVE / IN-PROGRAM / GAP / N/A with ranked P1/P2/P3 adoption boards and code anchors — lives at **`docs/reference-clients-survey.md`**; extend it (same classification) when surveying them again.

### Metadata Cluster Topology

* Keys for volume control use `fs_prefix` / `fs_key!` helpers.
* **Layout keys** (`metadata:…`, `inline_data:…`, `block_map:…`, `mapping:…`, `active_block:…`) are **unprefixed historical** forms — use `crate::keys::*` helpers; do not migrate under `FS_PREFIX` without an on-disk format change.

### Metadata format: v3 CoW KV (the only format)

Format v3 is the **only** on-disk metadata format. Legacy v2 support was removed entirely (user directive: always forward — no backwards compatibility): a v2 superblock refuses to mount loud ("no longer supported; reformat required"), `squeezefs format --force` reformats it to v3, and the offline `squeezefs migrate` converter was deleted with the v2 reader. Normative spec `docs/design-cow-kv-metadata.md`, measured gates `.benchmarks/2026-07-09-kv-v3-gates.md`.

* **v3:** a bcachefs-style **copy-on-write typed-KV btree** (`src/meta_backend/kv/`). One node layer, three logical trees per volume (`TREE_INODES`/`TREE_DENTRIES`/`TREE_XATTRS`) plus, behind incompat bit 9, a fourth (`TREE_BLOCK_REFS` — durable data-block references, see below), memcmp-ordered keys. **256 KiB log-structured CoW nodes** (format knob `--meta-node-kib`, 64 KiB–1 MiB) with internal sorted-bset appends; a **lock-free reservation journal** (ring `clamp(volume/64, 8 MiB, 32 MiB)`, `--meta-journal-mb` override, **read at every mount**) carrying one checksummed entry per transaction; **checkpoints** flip per-tree roots in an A/B root ledger on the background flush cadence, **never on the commit path**; **monotonic ino allocation** (no reuse, no seeding scan); an **A/B extent-bitmap allocator** (1 bit / 256 KiB extent) with a journaled pending-free protocol. Superblock: magic `METALV01`, version 3, `features_incompat` bit 0 = `KV_V3` (`src/meta_backend/kv/superblock.rs`). Caps: ≥ 100 M inodes, 1 M+ entries/dir, unlimited xattrs (value ≤ `min(65536, node_size/4)` B). Reads stay RAM-authoritative and latch-free (scc node index + arc-swap snapshots, demand-paged, clock eviction). Every on-disk unit is checksummed and copy-on-write: whole-transaction atomicity (one tx = one checksummed journal entry) + torn-write immunity (torn writes detected-and-ignored) — strictly stronger than the retired v2 D0/D1/D2 sector contract (§4.10).
* **Env knobs:** `SQUEEZEFS_META_NODE_CACHE_MB` / `SQUEEZEFS_META_NODE_CACHE_PCT` (node-cache budget; default derived `max(budget/16, 512 MiB)` per volume — derivation sweep 2026-08-04, absolute > pct > derived), `SQUEEZEFS_META_CHECKPOINT_MAX_DIRTY_NODES` (dirty-node checkpoint cap = mount-replay working-set bound; default derived `max(4096, budget/32 ÷ node_size)`); `SQUEEZEFS_META_FLUSH_INTERVAL_MS` is the journal/checkpoint cadence (`0` = strict per-commit).
* **Stats fields (stats inode):** `meta_format_version` (constant `"3"` per volume — kept because operators key on it); `meta_volume_atomicity` (contract class — constant `cow-checksummed`) alongside `meta_volume_atomicity_physical` (the informational hardware probe); and the `meta_kv_*` family — `node_cache_{hits,misses,evictions}`, `node_appends`, `node_{append,rewrite}_bytes`, `node_{compactions,splits}`, `node_freeze_shadow_dropped` (freeze-time shadow-fold engagement, 2026-07-30 meta-plane-writes: overlay records excluded from frozen bsets because a newer same-key Put/Delete in the same freeze shadows them — high under same-key commit storms by design), `journal_{bytes,entries,full_stalls}`, `journal_{entries,bytes}_per_volume` (per-volume journal attribution arrays — the meta-plane balance instrument; a healthy multi-volume set moves EVERY volume under load, the 2026-07-30 field conviction's shape was one volume at ~21–25k device-writes/s and its sibling at 0.00), `checkpoints`, `commit_smo_retries`, `node_dropped_tail_bsets`, `dentry_collision_overflows`, `delta_orphans`, `replay_{entries,dropped_torn,ms}`, `block_refs_{staged,released,recovered,drift,unresolved}` (durable block-reference accounting — see **Durable block refcounts** below; `drift` is a must-stay-0 tripwire), `free_extents`, `pending_free`, `pending_free_{parked,released,overflow}` (SMO frees parked on / released by durable-tail coverage — design-smo-replay-currency Option A; `parked − released` tracks the `pending_free` gauge, the wedge detector; `overflow` = retirements forced past the at-cap FIFO into the unbounded overflow — the 2026-07-27 §4.7 pinned-floor cycle-break's engagement gauge, ≈ 0 in steady state. The at-cap FIFO is a pressure valve for threshold SMOs only: the checkpoint flush pass's compactions and mount-side re-parking are never refused, successor leftover floors are exact per-record `entry_floor` stamps, the clause-b progress audit rides EVERY barriered cycle, and the mount gate pre-claim-drains an exhausted ring — evidence `.benchmarks/2026-07-27-kv-pending-free-cycle.md`). The retired v2-only counters (`meta_commit_sectors`, `meta_sector_lock_*`, `meta_inode_alloc_*`, `meta_tx_concurrency*`, `meta_quarantined_inodes`) no longer exist.
* **Filesystem generation identity:** per-volume the v3 superblock `uuid` (random per `format` invocation), joined in volume order (`meta_backend::volume_set_generation`) — local staging is bound to it. (The v2-era `FormatConfig.fs_uuid` config stamp was deleted with v2; the superblock uuid is the sole generation identity.)

### Durable block refcounts and free list (Implemented 2026-08, incompat bit 9 — stamped by the DEFAULT format since the rung-10b flip)

Pre-RC engineering spec §6.2 **item 1** ("the largest item"), ruling **D9**. `BlockAllocator`'s refcount map, free list and allocation cursor had **no on-disk representation** — all three were re-derived at every mount by walking the live inode tree and reading each ino's `layout` xattr ("pure derived state"). §6.2's verdict: *"Without durable shared ownership accounting, no multi-writer data path is expressible"* — two writers each derive a private answer from the subset of the tree they walked, so a block node A holds at refcount 2 reads 1 on node B, whose W1 sole-owner patch then rewrites it in place with `patch_ineligible_shared` never incrementing (silent on a passthrough volume). Design + rationale `docs/design-durable-block-refcounts.md`; measured cost `.benchmarks/2026-08-04-durable-block-refcounts.md`; contracts `tests/durable_block_refs_tests.rs`.

* **The representation is one record per REFERENCE** (a backpointer), in a fourth logical tree `TREE_BLOCK_REFS = 6` (`src/meta_backend/kv/block_refs.rs`): key `(vol_tag u64 ‖ block_idx u64 ‖ owner_ino u64 ‖ block_index u32)` BE, value `(version u8 ‖ flags u8 ‖ reserved u16)`. `refcount(block)` = the population of the 16-byte `(vol_tag, block_idx)` prefix. A count record would need a lost-update-prone RMW (two inodes cloning one block share no lock); a delta record cannot express the FIRST reference (Δ-without-base is a counted no-op). `vol_tag` decodes the durable `vol-{16 hex}` id verbatim (KD-5) — never a path, an ordinal, or a set position.
* **The delta rides the layout transaction** — `set_layout_and_size` / `merge_layout_and_size` / the Lever-B aggregated pass stage the ops into the SAME `KvTx` (one tx = one checksummed journal entry, §4.10): the ledger can never disagree with the layout that justifies it, and the publish stays ONE commit (**pinned**: `accounting_rides_the_publish_transaction_and_adds_no_commit` asserts journal-entry equality vs an un-stamped volume). Never re-split it.
* **O(batch), never O(file size)** — §5.3's one merge discipline means `merge_block_mappings_if_epoch` / `publish_pass` already know every inserted and displaced key; measured **40–52 ns CPU + 46 B journal per reference**.
* **The free list needs no durable structure**: it is the complement of the referenced set below the cursor — the arithmetic `recover_block` / `fsck_reconcile_accounting` already run. `begin_free → reclaim → finish_free` has **no intermediate durable state** (the terminal free's durable effect IS the `Delete` that rode the publish), so a crash recovers to referenced or free — never a leak, never a double free; what it loses is the queued discard (hygiene, which is why `trim --full` walks the free list).
* **The write-path wiring is complete and the oracle is the proof**: fsck class **C8** (`C8DurableRefDrift`) detection is **UNGATED** and `meta_kv_block_refs_drift` is a live must-stay-0 tripwire; the oracle runs clean with the ledger engaged across all 28 layout-publishing suites (`SQUEEZEFS_TEST_STAMP_BLOCK_REFS=1` is the seam that engages bit 9 in isolation on a `--single-writer` base; the default format stamps it since the rung-10b flip). The last gap was a CLASS — a site that mutates the RAM map and leaves the layout dirty defers its accounting to a save that is handed a map already containing the change — closed structurally by the per-ino deferred-op accumulator (`pending_block_refs`), so any future deferring site is correct by construction.
* **The derived walk survives as the ORACLE, not the truth** — `verify_durable_block_refs` diffs the durable census against the layout walk's, both through the SAME extraction path. Drift is fsck class **C8** (`C8DurableRefDrift`, report-only — restating the ledger from the walk would erase the evidence of why the invariant broke; the layouts stay authoritative) and `meta_kv_block_refs_drift` must stay 0. Mount runs it only under `SQUEEZEFS_BLOCK_REFS_VERIFY=1` (running it would pay the very walk the records delete).
* **Compatibility**: **bit 9 is stamped by the DEFAULT `format` since the rung-10b Phase-B flip** (2026-08-16, one act with the other eight multi-writer bits — `.benchmarks/2026-08-16-mw-default-flip.md`; ruling D9's build-dark posture governed until the §6.3 evidence table + the S9 acceptance went green, and the D9-era history is why the number matters: bit 8 is `KV_PARTITIONED_APPEND` — the two were authored in parallel against the same free bit; partitioned append landed first, so durable block references took bit 9, `src/meta_backend/kv/superblock.rs`). A `--single-writer` or pre-flip volume mounts DERIVED exactly as before, sector 0 byte-identical (pinned); `set_block_refcounts_bit` / `squeezefs volume enable-multi-writer` is the offline upgrade path, after which the first writable mount mints the root; a read-only mount never mints and never accounts. The stamped-solo cost is measured ≈ 0 and the ledger ran drift-free live under the S4 re-gate (`.benchmarks/2026-08-15-mw-s4-regate.md`).
* **What S9 still owes**: per-writer key scoping (§6.2 item 8 — `active_block:` writer ids; this key layout leaves room), the other eight §6.2 format assumptions, cross-writer custody arbitration (the ledger states what the set believes; it does not serialize writers), the §6.8 freed-offset grace period, and node-cache revalidation/partitioning for a second reader of this tree.

### Metadata-throughput program (Implemented 2026-07)

The 4.4× FUSE-layer metadata multiple was closed by the M1–M12 program — design + landed-SHA record `docs/design-metadata-throughput.md`, gate adjudication `.benchmarks/2026-07-15-metadata-throughput-closing.md`, beta-blocker fix `.benchmarks/2026-07-15-find-m11a-fix.md`. What is now load-bearing machinery (do not regress):

* **Single-writer mount guard (D0)** — every write mount claims each meta volume: a **dedicated daemon-lifetime `flock`** (same-host, kernel-arbitrated, instant crash reclaim), **NVMe Persistent Reservations** Write-Exclusive where `RESCAP` advertises support (cross-host *enforcement* — a fenced holder fail-stops at its first post-fence journal barrier), and a **`writer_claim`** heartbeat record (identity + detection everywhere). **No bypass knob**; automatic cross-host takeover is disabled on non-PR substrates — recovery is dead-pid proof → PR preempt → TTL → the attested **`squeezefs claim clear <sqmeta-uri>`** verb, in that order of automation. The guarantee-class table ships in docs/operations.md verbatim; the barrier-failure escalation governs journal-durability barriers only (the data-path writeback ladder stays retry-forever — see below).
* **Commit conveyor (group commit v2, D5)** — all user commits on a volume enqueue `{staged records, Arc<[DlmGuard]>, oneshot}`; a detached, panic-guarded per-volume **pass task** drains a batch and runs one admission → union-leaf-lock pass → **N ordinary checksummed entries** in one contiguous reservation → one write → one barrier. **One tx = one checksummed journal entry is unchanged** (zero on-disk format change; whole-tx atomicity + torn-write immunity transfer verbatim). Queue entries co-own their tx's DLM guards until terminal outcome — a dropped committer future cannot strand isolation. **Two stages since D-2 (e2e perf audit DLM board #2, 2026-09-03, `.benchmarks/2026-09-03-d2-two-stage-conveyor.md`)**: the pass is the APPLY stage only — it drops its leaf locks and **submits** the batch's entries (one `uring_fs` push, no wait), and a per-volume **durability lane** (the same loom-modeled `ConveyorCore` handoff protocol) awaits the writes in handoff order and takes each group (the head window + every successor already landed) through ONE completed-prefix wait, the §4.4 pt 4 hole checkpoints, ONE strict barrier and the fan-out in journal order — so the device's completion latency never sits inside the serialized server (the D-1b authority ran its pass at ρ 0.97 with 650 of 752 µs being the ring write's round trip). N windows sit between the stages, bounded by the ring's admissible capacity (every window holds a registered reservation; admission parks before any node lock) — never a constant. Acks stay in journal order and only after the entry landed / the barrier completed; guards release at the STAGE-B terminal outcome; the fail-stop lattice fires from stage B; the lane takes leaf locks in exactly one arm — the §4.4 pt 4 rollback of a FAILED write (the 4b argument is `backend.rs`'s module doc + `stripe_locks.rs`). Engagement `meta_conveyor_windows_inflight[_hwm]` (1 = serialized, ≥ 2 = overlapped) / `meta_conveyor_durability_passes`; phases `meta_txpass_phase_ns.{window_lane_wait,window_total}` (`pass_total` is now the apply stage's service time — ρ = Σ ÷ wall reads the serialized server), `lock_phase_ns.leaf_lock_hold` (the "never across device I/O" tripwire). Test seam `uring_fs::arm_device_latency` (a SLOW device — writes/barriers complete no earlier than the armed latency; the stalls are PARKED devices). **Both stages run on the volume's own journal lane since C-2 (DLM board #3, 2026-09-03, `.benchmarks/2026-09-03-c2-uring-fs-completion-hop.md`)**: one OS thread per writable volume (`sqz-jrnl{N}`, derived — never a constant; readers / co-writers / peer-owned volumes commit nothing and get none) that OWNS the volume's journal io_uring and parks IN it (`sqz_exec::LanePark` + `uring_fs::OwnedReactor`: a task wake and a device CQE are one `io_uring_enter` wait; the lane reaps its own CQEs, so a completion never crosses a thread to reach the durability task and never waits behind the `sqz-meta` lanes' serve work — the same-binary fleet bracket measured that shared-lane `wake_hop` at 59–62 % of the journal write's round trip: `uring_fs_write_phase_ns.{queue_hop,device,wake_hop,total}`, always-on, exact-sum). `SQUEEZEFS_JOURNAL_LANE=0` is the A/B control (the D-2 shape on the shared pool); engagement `journal_ring_lane_writes` ≈ passes (`journal_ring_pool_writes` carries the rest), `journal_lanes_{spawned,ringless}`, CPU class `sqz-jrnl`. The residue is the io-wq punt a buffered block-device write takes (`blkdev_write_iter` refuses NOWAIT buffered writes — no inline completion at `submit()` on any venue), two kernel wakes per window. **Group commit since D-1c (e2e perf audit §5.3 row 1, 2026-09-04, `.benchmarks/2026-09-04-d1c-conveyor-group-per-frame.md`)**: `ConveyorCore::enqueue_many` pushes a SET of staged txs under ONE queue-lock acquisition (drain takes the same lock, so no drain ever sees a partial group — loom `conveyor_group_never_drains_partially`), `KvMetaBackend::commit_tx_group` runs the pipeline over the set (every member built and admitted first — an inadmissible member fails ALONE and never enqueues; one enqueue, one election, fan-outs awaited in order), and the group's 4a guards are ONE canonical `lock_many` (distinct inos may share a DLM stripe — per-member guards taken while siblings hold theirs would self-deadlock). Queue-side only: one tx = one checksummed journal entry unchanged, the drain caps still govern (the byte cap may split an over-cap group, progress-first), replay identical to single commits (pinned). Gauges `meta_conveyor_group_{commits,txs}` (txs ÷ commits = the live group size; 0 on a solo mount by construction).
* **Journal-entry economy (D4)** — rename is one whole-tx entry (incl. parent Δtimes); the kernel's SETATTR ctime/mtime echo is absorbed, not committed; unlink destroys ride batched `destroy_inodes`. Measured **≈ 1.0 entries/op rename/unlink** (gate G4 ≤ 1.05) — regressions show in `meta_kv_journal_entries` per-op ratios.
* **Fold overlay + snapshot memo (D7)** — record folds serve from fold-forward overlay heads and immutable-snapshot memos (fold algebra untouched, property-tested equivalence; replay rides the same fold). `InodeDelta::decode` fell off the profile (26.3 % → < 0.5 % of daemon CPU); memo/overlay bytes are charged to the node-cache budget.
* **Watchdog, not per-op timeouts (D1.b)** — handlers no longer wrap in `tokio::time::timeout` (that expiry *dropped* futures mid-`commit_tx` — the ring-budget/reservation-wedge vector). A deadline watchdog logs overdue ops loudly (`fuse_op_watchdog_overdue`); ring-admission parking past the threshold escalates via the `disabled_volumes` fail-stop lattice; barrier waits keep bounded errors. Do not reintroduce per-op timeout wrappers.
* **Round-trip + transport economy (D2/D3)** — clean-handle FLUSH/RELEASE fast paths, `FOPEN_NOFLUSH`, kernel-side negative-entry caching, refresh-instead-of-invalidate parent attrs (fuse_ops/create 5.18 → ~4.0); over-uring COMMIT_AND_FETCH submits batch per drain (`io_uring_enter`/create 31 → ~9). SQPOLL on the queue rings exists as a knob and measured **not recommended** (M10; README posture note).
* **Supersession-aware never-lossy writeback (FIND-M11-A fix)** — flush units re-validate staged ownership per attempt and present the ino's *current* DLM generation; fencing-stale units resolve as contractual no-ops, verified-NotFound (reclaimed-ino) units discard their orphans, and everything genuinely transient still retries forever. "Stale fencing tokens discard staged work" is the **remount** contract, not a license to drop live acked custody.
* **Env/mount knobs added:** `SQUEEZEFS_META_COMMIT_BATCH_TXS` (default derived `max(64, cpus × 2)`) / `SQUEEZEFS_META_COMMIT_BATCH_BYTES` (default derived `max(256 KiB, ring/16)`, clamped to the ring's admissible capacity) — conveyor batch caps (floors = the shipped M7 posture, derivation sweep 2026-08-04); `SQUEEZEFS_OP_PROFILE=1` — per-op phase histograms + the under-`i_rwsem` estimator (zero cost off); per-class kernel TTLs `-o attr_timeout/entry_timeout/dir_entry_timeout/negative_timeout` / `SQUEEZEFS_FUSE_{ATTR,ENTRY,DIR_ENTRY,NEGATIVE}_TTL_MS` (default 1 s each); `mount --daemon --supervise` external watchdog (+ `SQUEEZEFS_SUPERVISE_{INTERVAL,UNRESPONSIVE}_SECS`) with FUSE-connection abort.
* **Stats families added (stats inode):** `writer_guard_{mode,fenced,pr_reacquires}`; `meta_commit_group_{size,bytes}`, `meta_conveyor_{leader_passes,pass_panics}`; `meta_kv_fold_{head_serves,memo_hits,memo_misses,memo_bytes}`; `meta_kv_times_echo_{absorbed,pending,drained,drain_commits}`; `fuse_op_phase_ns` / `fuse_create_under_lock_ns` / `fuse_op_watchdog_overdue`; `fuse_{flush,release}_clean_fastpath`, `fuse_lookup_negative_replies`, `fuse_attr_cache_refreshes`; `transport_commit_batch*`; `meta_reclaim_gather_*` + `meta_reclaim_inflight_waits` (finding 50, 2026-09-03: a reclaim batch that finds an ino claimed by a concurrent batch never double-drives AND never returns before that owner's outcome — `reclaim_orphaned_batch(inos).await` ⇒ every ino reached its terminal outcome; the gauge counts the parks, growth = the release-enqueued pool racing a FORGET/explicit reclaim); `writeback_{superseded_noops,stale_token_retries,orphan_discards}`.

### Random-small-write program (Implemented 2026-07)

The ~2,500× whole-block RMW amplification on small random writes (the vs-JuiceFS scoreboard's only genuine product loss: 354–397 IOPS) was closed by the RW1–RW5 program — design + landed-SHA record `docs/design-random-small-writes.md`, closing adjudication `.benchmarks/2026-07-17-rand-write-program-closing.md`. Load-bearing machinery (do not regress):

* **Sole-owner extent patch (W1)** — isolated LBA-aligned small overwrites of exclusively-owned, passthrough, whole-block-mapped striped blocks are **one in-place sub-block DMA** (zero reads / meta / staging; 61–67 k IOPS, W all scoreboard regimes). The predicate lives in ONE place (`is_whole_block_mapping()` + the 6-clause decision ledger); the clone/patch race is closed by the §5.1 `fence(SeqCst)` protocol on both sides (composed two-word loom model — the fence is load-bearing, verified by weakening). v1 is aligned-only **by contract**: only app-written sectors are ever rewritten (crash blast radius), and `patch_edge_rmw_reads` must stay 0.
* **Coverage-union write-through trigger (RW3b / FIND-L1-A fix)** — block completeness is the ACCUMULATED written-coverage union (`record_write`'s completion transition), never one write's end offset; kernel-split out-of-order O_DIRECT WRITE segments are the normal case (`FOPEN_PARALLEL_DIRECT_WRITES` stays on). Inline write-path seed fetches are deleted; `write_path_seed_read_bytes` is a must-stay-0 tripwire; a fully-covered buffer can never pay a flush-seed read (`seed_deferred ⇒ union partial`, structural).
* **Extent overlay + staged extent records + batched fold (W2)** — patch-ineligible shapes park extents (byte-budgeted, R5-gauged), spill as versioned+checksummed `active_block_ext:` records (**no seed read at spill, ever**), fold seed-once with measured amortization (`fold_fill` median ≥ 16; compressed rand-4k ~2,500× → 15–26×). Recovery: generation-bound + fencing-stamped like staged blocks; future-version records/dirs refuse loud; clean unmount drains records to fold (a clean staging dir carries none); below-RW4 downgrade is declared-unsupported with loud orphan forward-detection.
* **Incompressible store-raw escape (FIND-RW4-A fix)** — compression is best-effort per block (frame bit 31 = raw, below the AEAD layer); transformed volumes reserve chunk headroom at format and mounts refuse geometries that cannot hold `max_stored_image_len(block_size)`; every transformed upload site refuses oversize images loud (never overflow, never truncate).
* **The device overlay's length floor (finding 47, 2026-09-02 — `overlay_length_eligible`, `src/fuse_client.rs`; `tests/overlay_length_floor_tests.rs`; design-overlay-overwrite §5.1)** — the W1 cap is ALSO the B4 device overlay's minimum segment length: overlay-eligible by length ⇔ `len > patch_max_bytes()` (the predicate-5 oversize verdict — the two fast paths tile the sub-block population exactly; `SQUEEZEFS_PATCH_MAX_BYTES=0` empties both the W1 class and the sub-cap class). Before it, a sub-cap write the patch declined at STATE time (hole / clone-shared / decorated / co-writer-refused / stream-adjacent) rode the overlay: a whole CoW dest minted and held per touched block, a whole-block old-image read + complement seed at every settle (`.benchmarks/2026-08-17-mw-shipped-free-c8-fix.md`: six refused 4 KiB patches ⇒ `overlay_gap_seed_old_bytes` = 6 × 4 MiB − 24,576), and small-bs sequential segments stored per op (`wareq-sz` collapsing to bs) instead of accumulating into one whole-block write-through. `overlay_ineligible_sub_cap` is the floor's ledger (BOTH shapes).
* **The device overlay rides the write-pipeline depth governor (W-3, e2e perf audit write board #3, 2026-09-02 — `.benchmarks/2026-09-02-w3-overlay-depth-governor.md`)**: the ACK-early store used to issue OPEN-LOOP (no admission against the BDP target, in-flight bytes on no R5 component, CQEs never feeding the lane — the 0.68–0.80× device-bound-venue loss the overlay notes' own bracket rows recorded). Now `try_device_overlay_store` takes a write-pipeline permit (`WritePipeline::admit_segment` — segment bytes counted against the target at the volume block-size scale) after the registry join and before the claim, and the continuation releases it at the CQE BEFORE the coverage publish (a parked admitter holding the block guard can never wait on a permit whose release needs that guard), feeding `record_completion_sized` on the destination lane so the BDP estimate and the probe-up governor learn from overlay completions too. ONE gauge (`write_pipeline_inflight_bytes`), ONE target, ONE backpressure gate for both write vehicles; ACK-early semantics unchanged — only ADMISSION waits. Lever `SQUEEZEFS_OVERLAY_DEPTH_GOVERNOR` (default on; `0` = the open-loop control); engagement `overlay_governed_stores` (≡ overlay stores on a governed mount); census word `ov_admit`. Field `w_rewrite` A-B-B-A: par within noise, p99.9 −17 %/−33 %; the tcp-devsub adjudication is owed.
* **The rewrite epoch's supply-coupled close on co-writer lanes (2026-09-07, `.benchmarks/2026-09-07-rewrite-epoch-supply-close.md`; design-rewrite-program §5.3 + the KD-1.7 amendment)**: a rewrite epoch parks each displaced A key until a close trigger fires, and on a CO-WRITER the freed key returns only through the recycle loop (ship → grace ring → lane list → harvest RPC) — so KD-1.7's close-on-`StorageFull`-then-retry-once is structurally late there, and on a shared-file rewrite no routine trigger fires during an iteration (the s11 fleet's 9–31 s supply gaps, finding 15's next binding term). `SQUEEZEFS_REWRITE_SUPPLY_CLOSE` (default on; `0` = the shipped KD-1.6/1.7 triggers, the A/B lever) makes the laned co-writer's ahead-refill tick close the mount's open epochs when `lane_reachable_blocks < watermark` (the ahead-harvest's own `rate × horizon` threshold, capped at share/4), largest epoch first until the yield covers the deficit `watermark − reachable` — which is also the per-tick bound (never more closes than blocks short); the close is `close_rewrite_epoch` verbatim, so every §5.6 crash window holds and the write hot path gains nothing; installs only on a harvesting lane (`DataRouter::arm_rewrite_supply_close`), so single-writer / authority mounts are byte-identical. Gauges `rewrite_shadow_supply_closes` / `rewrite_shadow_supply_close_blocks` + the ledger `rewrite_shadow_supply_close_declined_{covered,no_parked}` / `rewrite_shadow_supply_close_bounded`; contracts `tests/rewrite_shadow_supply_close_tests.rs` (incl. the s11 per-volume closed-loop model: parked residence 2.3 → 0.85 s, starvation −54…−59 % where the parking binds). The fleet row is owed.
* **Instrument-alignment lesson (standing)**: elbencho page-aligns its O_DIRECT buffers; `squeezefs bench` (tokio-copied) does not — an unaligned 1 MiB buffer spans max_pages+1 and the kernel **splits it into 2 concurrent out-of-order FUSE WRITEs**. FIND-L1-A hid from one instrument and reproduced under the other for three sessions. **Every measurement must state its instrument**, and write-path changes must stay correct under split/reordered WRITEs (that is now pinned by `tests/write_through_coverage_tests.rs`).
* **Env knobs added:** `SQUEEZEFS_PATCH_MAX_BYTES` (default derived block_size/8 — 512 KiB on the shipped 4 MiB block; `0` = the acceptance A/B lever, not an operational escape), `SQUEEZEFS_FOLD_MAX_EXTENTS` / `SQUEEZEFS_FOLD_MAX_BYTES` (fold triggers, default 64 / block_size/4 — the byte trigger derives, the extent count stays the measured amortization constant).
* **Stats families added (stats inode):** `patch_{writes,write_bytes,dma_errors}`, `patch_edge_rmw_reads` (**0 by definition in v1**), `patch_ineligible_{unmapped,decorated,unaligned,overlay,shared,transform,adjacent,oversize}` (the decision ledger — growth on a shape that should patch = predicate rot); `active_block_ooo_runs`, `write_path_seed_read_bytes` (must stay 0), `write_through_blocks`, `overwrite_seed_{materialized,skipped}`; `parked_extent_bytes` / `parked_full_buffer_bytes` (R5 components), `extent_{parks,escalations,implicit_escalations,spills,spill_bytes,record_absorbs}`, `fold_{passes,seed_reads,extents_folded,fill}`, `staged_rider_{extent_writes,folds}`, `extent_records_{recovered,stale_discarded,torn_discarded,future_refused}`; `compress_stored_raw`.

### LD_PRELOAD interception data plane (Implemented 2026-07)

The L4 program (`docs/design-preload-interception.md`; closing record `.benchmarks/2026-07-19-l4-interception-closing.md`) shipped the DAOS-libioil-class client bypass: `-o interception` arms a per-mount session host (abstract AF_UNIX + sealed-memfd shm sessions; the §5.2 daemon fd screen is THE security boundary), and `LD_PRELOAD=libsqueezefs_il.so` (built ONLY via `--profile preload-release --features interposers` — the Issue-4 `panic="abort"` compile guard makes wrong-profile builds unrepresentable) routes data ops on bound fds over a lock-free ring. Load-bearing machinery (do not regress): pinned single-consumer service threads (default ceiling = the drain-LANE derivation `squeezefs_ipc::sizing::il_drain_lanes_default` = `clamp(3×cpus/8, 2, 64)` — the counted 2026-08-06 field width sweep, `.benchmarks/2026-08-06-dd-width-slope.md`: one lane = svc submitter + direct-drive reaper = 2 OS threads, sized so the lane-pair population takes ¾ of the core budget, and the SAME function widths the direct-drive shards so the pair cannot drift; it DOMINATES the shim's per-mount session default `il_sessions_default` = `clamp(cpus/4, 2, SESSION_REGISTRY_SLOTS/2)` = `clamp(cpus/4, 2, 16)` at every machine size, so every default session still owns a drain thread — the flat `clamp(cpus/4,2,8)` was the DEFAULTS-MISMATCH constant the 2026-07-28 ingest-economy campaign ran to delete; warm serves execute ON them), the §5.5.1 sync fast path (per-inode `try_read()` + the drop-guard-before-enqueue demote rule + the sync tier serve mirroring the handler's staging/hot/read-cache legs), sever-at-dequeue write custody (§5.5.2), KD-7 build-commit equality (dev override forgives `-dirty` degeneracy, never inequality), KD-11 forced write-through (~3× tax on unintercepted buffered small writes, priced in the closing report), the fork-child `close(2)`-not-`shutdown(2)` poison law, idle reap, and the W1 `notify_inval_inode` handoff riding the classical sideband post-arm. Client levers: `SQUEEZEFS_IL_SESSIONS` (fd-sharded sessions — override lever only since the 2026-07-28 ingest-economy campaign: the DEFAULT is the shared derivation `squeezefs_ipc::sizing::il_sessions_default` = `clamp(cpus/4, 2, 16)`, which the daemon's drain-lane ceiling dominates at every machine size since the 2026-08-06 width re-grade, with paired tie tests making drift a red test), adaptive spin (`SQUEEZEFS_IL_SPINS` pins), `SQUEEZEFS_IL_OP_TIMEOUT_MS`. The 2026-07-28 **ingest-economy campaign** (`.benchmarks/2026-07-28-ingest-economy.md`, design Rev 13) closed the field's large-sequential-ingest wall (7.5 of 16.6 GB/s on a 32-CPU 2×200GbE client — half the drain threads idle from the defaults mismatch): service threads now **spawn on session admission** up to the derived ceiling (`ensure_service_threads` — a session-less host, i.e. every default mount, owns ZERO service threads; `ipc_service_threads` gauges the SPAWNED count), and ring-write severs recycle through the **severed-buffer pool** (`SeveredPool`, `ipc_severed_pool_{hits,misses,bytes}`; retention rides R5 as the non-sheddable `ipc_severed_buffers` component — deletes the per-op slab-alloc engine: 1.76 M minor faults/s across saturated svc threads at 12.7 GB/s, ~70 % %system → ~1.5 %; the §5.5.2 severance law's ONE arena read + single memcpy unchanged). fuse3 TPC lanes are now named `fuse3-tpcN` (they inherited `sqz-ipc-svc0`'s comm before — pidstat/perf attribution on interception mounts was polluted). The same campaign root-caused and fixed a BLOCKING latent dev wedge: the §5.4 transport-lease watchdog's `debug_assert` panicked write-handler tasks whose invocations legitimately exceeded 1 s under writeback backpressure — losing the FUSE reply (fsync D-state forever, umount joins) and the ent re-arm; it is now the loud-never-fatal `transport_lease_overlong` tripwire (stats inode), deterministically pinned by `tests/transport_lease_overlong_tests.rs` via the `SQUEEZEFS_TEST_WRITE_STALL_MS` seam (load selects such schedules; it never causes them — load-dependent hangs are first-class product bugs). The 2026-07-26 reap-economy program (`.benchmarks/2026-07-26-ipc-reap-economy.md`) made the libaio reap **event-driven in the sparse regime** (qd > 24 per ctx batches on a 50 µs bounded sleep — the deep-regime economy), made the service-thread drain pass registry-mutex-free (epoch-cached owned-session snapshots), and added the daemon lever `SQUEEZEFS_IPC_SPIN_US` (service-thread empty-pass spin window before parking; **default 0** — every nonzero setting buys libaio-fleet IOPS by taxing sync-lane CPU, so it is an explicit fleet lever, never an ambient default) plus the `ipc_service_parks` counter (park-economy gauge). The 2026-07-26 handoff-economy fix (`.benchmarks/2026-07-26-ipc-handoff-economy.md`, design Rev 10) closed the reap note's §7 residual: **ring handoffs (read misses AND writes) ride the fuse3 per-core handler lanes** (`handoff_spawn` → `tpc_spawn` — the venue kernel-lane handlers run on), never a `Handle::spawn` from the foreign service thread onto the multi-thread runtime's global inject queue (the measured ~130 µs/op cold-firehose queueing term; venue pins in `ipc_service.rs`, weakening-verified). Cold ddt il rows moved 0.87×/1.05× → **1.07×/1.13× kernel** (t16qd16/t32qd32, fabric-latency rig, medians of 3, engagement exact) with the 16-process fio fleet at 1.04× and the sync lane +13.6 %; do not reintroduce a runtime-handle handoff. G-L4-2 adjudicated (devsub, elbencho sync drivers, medians of 3): **warm 1,017,548 IOPS** (kernel-FUSE warm floor 644,726; target 1.0 M met at median) and **device-true 622,112** (floor 600 k; beats the kernel-FUSE 604,313 reference) — engagement exact on every run. The scoreboard's `SQUEEZEFS_SB_MODES=il` pass emits separately-labeled, never-W/L-gating rows whose engagement check exits nonzero on any silent-passthrough cell; its instrument must be a DYNAMIC elbencho (the pinned static one cannot load the shim). OQ-1 (libaio interposers) resolved: v1.1 economics item, no floor depends on it. stat/del storms and buffered-il semantics are N/S by design. The 2026-07-28 **ipc op-economy campaign** (`.benchmarks/2026-07-28-ipc-op-economy.md`) made the warm §5.5.1 serve prelude **allocation-free** (12 → ≈0 allocs/op: single metadata get + `Arc<str>`/CompactString `CachedMetadata` fields, stack-formatted keys, serve-into-arena via `crate::PayloadSink`, zero-mint staging probes, allocation-free service parks — pinned by `tests/ipc_op_economy_tests.rs`, whose `SQZ_ALLOC_TRACE=1` mode is the standing alloc-site profiler) and replaced the per-ticket completion wakes with the **per-session completion doorbell** (`squeezefs-ipc` `cqe_core::CqeDoorbell`, header line 2, IPC_ABI 2): the daemon pays a completion `FUTEX_WAKE` only toward a registered parked reaper (Dekker `fence(SeqCst)` pair is load-bearing; `ipc_cqe_parked_reaper_never_stranded` loom model, weakening-verified ×3), and the libaio sparse-regime reap parks on ≤ IL_SESSIONS session words (register→snapshot→re-scan) instead of a `futex_waitv` array per pending ticket. Phase B (counted A-B-B-A on the 235 µs fabric-latency venue, medians of 3, engagement exact) adjudicated: warm il rows **+22–29 % IOPS / −18–23 % daemon CPU/op** (lever 1); the doorbell's shared-session wake herd at qd ≤ old-threshold was found by the bracket (−21 % t16qd16) and closed by the counted retune **`REAP_EVENT_PARK_MAX` 24 → 2** (qd ≤ 2 keeps the event-driven wake — qd1 RTT clat preserved at 250–251 µs; deeper batches — t16qd16 +4–6 % over pre-doorbell, fleet −10 % daemon CPU; `SQUEEZEFS_IL_REAP_PARK_MAX` is the measurement lever).

The 2026-08-07 **shim-iops campaign** (`.benchmarks/2026-08-07-shim-iops.md`) wrote the rand-4k direct-drive per-op cost ledger and landed its instrument + levers: **`ipc_direct_phase_ns`** (stats inode, always-on — admit / inflight / finish / total; the direct-drive path has no handler, so `read_serve_phase_ns` structurally never sees the shim's hot path, and `fio clat − total` is the client+ingress subtraction the ledger keys on), **reaper/drain fusion** (`ipc_direct_inline_reaps` engagement gauge; the owning service thread's flush pass drains its lane's CQ in userspace behind a try-locked per-shard `cq_gate`, the reaper stays the blocking backstop on an EXT_ARG-bounded 100 ms wait — pre-5.11 kernels degrade loudly with fusion auto-disarmed; `SQUEEZEFS_IPC_DD_INLINE_REAP=0` is the A/B lever — measured WASH on the local venue at 10.7 % engagement with 20 %-CPU reapers, target venue = the field's 80–85 %-CPU saturated reapers, the "local hides it" class), and two registered measurement levers at the shipped defaults: `SQUEEZEFS_IPC_DD_EAGER_FLUSH` (mid-sweep dd enter at K unflushed SQEs; +2.3 % counted at 32×8, below the bar) and `SQUEEZEFS_IL_REAP_QUANTUM_US` (the client's deep-regime batch-reap sleep, default the shipped 50 µs — the 1×8 row priced the blind sleep at 4.6×, the fleet shape reads it as a wash). Local sustained rows: 32×32 = **1.03–1.04 M IOPS flat** (the 1 M class at depth), 32×8 = 741.8 k flat = 0.61× same-shape raw; the 594 k-field-row residual is named and split (client reap quantization / daemon inflight burst tails / device-at-load) with the field A/B spec filed in the note. **Round 2 — the reap-fanin campaign (2026-08-08, `.benchmarks/2026-08-08-shim-reap-fanin.md`)** built the shaped follow-ons: the v5 **slot ingress stamp** (`IpcSlot::stamp_ingress`, IPC_ABI 5, `mono_core.rs` #[path]-shared clock) turns the clat−total subtraction into the MEASURED **`ipc_ingress_ns`** term (row-validity: its count delta must account for the row's ring ops) — and the local ledger verdict is that the "client+ingress" term is ~ALL RING INGRESS (publish→dequeue queue wait: mean ≈ 145 µs at 32×8 / ≈ 520 µs at 32×32, observation ≈ 0), so field attribution must read `ipc_ingress_ns` before blaming the client reap; the **CqeDoorbell batch-wake threshold** (`park_begin_batch(k)` + the 16-byte doorbell's `wake_at` mark; `park_begin` ≡ batch(1); loom `ipc_cqe_batch_parked_reaper_never_stranded`, fence weakening re-verified) replaces the client's deep-regime blind 50 µs sleep with an event park cut short at the k-th completion (k = `sizing::reap_batch_wake_threshold` = `clamp(pending/4, 2, pending)`, derived — never a knob; the quantum knob is now the park's AGE BOUND), and the reap pass ceremony went allocation-free (`pending_tokens_into`, pass-reused buffers). Counted A-B-B-A vs the shipped pair (tcp devsub): 32×8 clat p50 **−16 %** / IOPS +2 %, 32×32 wash locally (ingress-dominated); the flat-event-park herd row (`SQUEEZEFS_IL_REAP_PARK_MAX=4096`) loses 3–10 % IOPS with +16–36 % tails at 0.58–0.80 wakes/op vs the threshold's 0.14–0.47 — the batch mark beats BOTH prior postures at fan-in. **Round 3 — the drain-funnel campaign (2026-08-08, `.benchmarks/2026-08-08-shim-drain-funnel-r3.md`)**: the first field `ipc_ingress_ns` read attributed ~85 % of the 32×32 field clat (1,561 µs mean at 656 k) to RING INGRESS — ~1,000 of 1,024 in-flight ops pooled pre-dequeue with the device at a fifth of its concurrency — and the local profile convicted the dd rings' kernel **`uring_lock`** (31 % of svc-thread cycles: the flush-ALL sweep putting every svc thread on every shard's enter + TWA_SIGNAL completion task-work landing mid-drain). Landed: **lane-scoped flush** (a svc thread enters only its OWN lane's ring; `SQUEEZEFS_IPC_DD_LANE_FLUSH=0` = the pre-r3 flush-all A/B lever — foreign-lane stragglers ride that lane's reaper's bounded 100 ms cadence), **COOP_TASKRUN dd rings** (task-work on ring entry, never by signal; pre-5.19 degrades loudly), span-form phase records (−2 clock reads/op), and the always-on funnel instruments **`ipc_drain_pass_ns`/`ipc_drain_flush_ns`/`ipc_drain_empty_passes`** (pass count/ceremony cost; ops÷count = live ops/pass). Counted A-B-B-A vs dev tip: **32×8 +40 % (931 k vs 667 k), 32×32 +28 % (1.186 M vs 927 k medians; sustained 1.223 M flat 90 s)**, ingress mean −67..−75 % (583→190 / 161→41 µs), per-op svc wall 15.6→10 µs; the `LANE_FLUSH=0` isolation leg gives most of the win back (977 k @ 32×32) — the lane scope is the dominant lever. Session-width re-derivation was FALSIFIED (sessions 12/16 no-ops — the funnel was contention, not capacity).

### Volume lifecycle & online maintenance program (Implemented 2026-07)

The v1.1 VL1–VL10 program (`docs/design-volume-lifecycle.md`, Rev 12; closing record `.benchmarks/2026-07-22-volume-lifecycle-closing.md`) shipped the six-requirement charter: dynamic add/remove of metadata AND data volumes with honest capacity preflight and automatic migration, online-first defrag and fsck (with per-class repair), percentage-throttled jobs distributed across mounted clients, and offline distribution over the same wire. Load-bearing machinery (do not regress):

* **Job fabric + coordinator (VL2/VL2b)** — every maintenance op is a job type on ONE fabric: durable `JobRecord`/`ShardRecord` state as `job:` xattrs on ino 1 (KD-2 — whole-tx atomic, torn-immune, offline probe-readable; the FUSE **reserved-xattr screen** makes `job:`/format-config invisible/EPERM through FUSE), coordinator = the D0 writer-claim holder, per-worker **duty-cycle throttle** (KD-3, live-retunable), pause/resume/cancel, crash-resume by idempotent plan regeneration (KD-6), R5 `job_copy_buffers` budget component (Red ⇒ jobs pause loudly, `job resume` is the operator's — interaction pin (e)). **Remote workers** ride the §5.1.6 job-shard execution wire (TCP/TLS via tokio-rustls — network, deliberately not-uring; NOT a networked DLM, KD-15): storage-trust HMAC enrollment against the `job:enroll` meta-KV secret, shard leases (TTL + heartbeat) with fencing-checked result *proposals*, the **pre-allocated-unpublished-destination law in both halves** (remote writes touch only coordinator-pre-allocated unpublished offsets; reassignment always mints FRESH destinations — expired-lease tuples enter a do-not-publish quarantine), coordinator-held **WERO (rtype 3)** on data namespaces + preempt-on-expiry where `RESCAP` supports it, plaintext ⇒ mandatory-100 % verify-reads before publish (sampled under TLS).
* **Durable volume identity + online data add (VL3)** — never-reused `vol-{hex}` ids in `FormatConfig` (KD-5; legacy basename ids grandfathered byte-identically); `volume add-data` is online (admin lane) or offline (guarded open); `KV_VOLUME_LIFECYCLE` = superblock incompat bit 3 (KD-14 — old binaries refuse loud). The `/dev/shm` runtime-config file is DELETED; health overrides ride the admin lane.
* **Drain/remove + movers (VL4)** — §5.2 capacity preflight (refusals print the numbers; checkpoint re-verification self-pauses `paused-capacity`), CoW evacuation via the `move_one` engine (copy-then-republish; source frees only after durable publish + tier purge; clone-shared blocks move ONCE via the pre-publish refcount ledger — whose pinned SOURCE also rides it, the VL9 pin-(b) fix), Draining volumes still serve reads, `undrain` cancels cleanly, retirement is permanent. Auto-rebalance is submitted on `add-data` by default (KD-12, `--no-rebalance` opts out).
* **Balance-aware write placement (VL4b, KD-16)** — `health_effective = health − clamp(k×(fill−set_mean),0,300)` published as an ArcSwap'd `PlacementTable` refreshed by the health worker: lock-free per-write pick, balance never outvotes health (penalty capped), failover semantics unchanged, new-allocations-only. On a laned CO-WRITER (`BlockAllocator::lane_placement_governed`, `SQUEEZEFS_COWRITER_LANE_PLACEMENT`, 2026-09-07 — `.benchmarks/2026-09-07-cowriter-lane-aware-placement.md`) the weight is the lane-reachable fraction of the lane share instead (a lane exhausts and refills PER VOLUME; the device fill says nothing about it), the pick skips a drained volume / reaches a refilled one off the allocator's O(1) counters, and `BackendRouter::allocate_placed_block` (the ONE pick+allocate act every fresh-block site calls) fails over to the sibling volumes before the bounded park — single-writer/authority placement byte-identical, `backend_placement_lane_{failovers,exhausted_picks}` the ledger.
* **Meta slot migration + guest keyspaces (VL5a/VL5b, KD-7; width surface superseded 2026-08-02 by `docs/design-dynamic-meta-routing.md` — user ruling: the frozen user-chosen width must be dynamic)** — routing modulo frozen PER SET as the **DERIVED** `routing_width W = 2^16` (never a knob; the u16 slot-id namespace; W ≤ 1 identity pinned for the in-RAM test constructor; global inos eternally stable), **stride-run membership stamps under incompat bit 6 (`KV_DYNAMIC_ROUTING`, presence-REQUIRED — frozen-width-era volumes refuse loud, reformat required)** with encoding-budget caps `STAMP_MAX_RUNS`/`STAMP_MAX_CURSORS` replacing the retired 64-hosted-slot cap, **mint spread** (`MINT_SPREAD = 64` slots/volume rotor — what makes the width real granularity: any volume's load is divisible into ≥ 64 movable slices from birth; gauges `meta_routing_width`/`meta_slot_mint_spread`/`meta_slot_stamp_{runs,cursors}_max`), durable slot map + root-ledger membership stamps (order-independent bootstrap, URI-disagreement refusals), guest slots under incompat bit 2 (`KV_GUEST_SLOTS`), conveyor pass-task delta tee with overflow fallback, per-slot cutover gate checked **before 4a acquisition** (parked ops hold no locks; never escalates to fail-stop), the §5.5.2b target-first ordered flip with enumerated crash windows, and **KD-8**: meta set changes drain staging then **rebind** the generation stamp (crash-safe two-phase staged-payload rebind — durable staged payloads survive membership changes). `add-meta`/`remove-meta` are offline verbs (stated loudly); `migrate-meta-slot` is online. Stamped-ahead crash re-runs converge on hard resume evidence (VL8 item 8).
* **fsck detect/repair + the zero-FP ladder (VL6a/VL6b, KD-9/KD-10)** — seven classes (C1–C6 + the C7 data scrub: AEAD/frame/readability per stored form, KD-17), **verify-before-report** (suspect → settle → lease re-check; C2/C3 use the allocation-epoch side map whose two-epoch survival only ESCALATES to the in-flight-registry liveness check — age alone is never a verdict) and **verify-before-repair** (repair consumes zero-FP-verified findings only, dry-run default, quarantine-first, honest no-fabrication where redundancy does not exist), offline `--shards k/N` + `merge-reports`, distributed scrub shards over the wire. `fsck_findings` must stay 0 on healthy volumes. Since 2026-08 the class set is **C1–C10**: C8 = durable-vs-derived block-reference drift (see *Durable block refcounts*), and **C9 = unreferenced inodes** (`src/fsck.rs`, `tests/fsck_c9_tests.rs`) — the class `docs/design-cow-kv-metadata.md` §4.10a left owed by keeping cross-volume `create` un-wrapped, and the ONLY detector + cleanup path for **pre-S3.5 field damage** (a crash mid cross-volume `link`/`unlink` leaked the inode *and* every block it owned, permanently). Walk direction is load-bearing: ONE `TREE_DENTRIES` pass marks every dentry's TARGET ino into a slot-decomposed `InoBitmap` (1 bit per inode, ≈ 12.5 MiB at the ≥ 100 M cap) differenced against the live-inode bitmap the census already produces — never the per-inode reverse-dentry scan (POSIX-4's `meta_parent_scans`). Zero-FP rests on the **writer era's ino floor** (DLM S2's era, the §4.8 watermark captured per keyspace at open — `KvMetaBackend::minted_in_prior_era`): a live create legitimately holds its inode record before its dentry, so only prior-mount records are candidates (residue from the CURRENT mount is reported by the NEXT one — no coverage lost, since residue is by definition from a prior mount), composed with settle + a FRESH dentry pass re-checked under the ino's exclusive 4a lease; an INCOMPLETE dentry pass records no verdict. Scoped to `nlink ≥ 1` on purpose — the `nlink == 0` shape is POSIX unlinked-but-open (and POSIX-15's rename-overwrite orphan) and needs the live open-count registries to judge. Repair = quarantine the record + every xattr (the `layout` IS the block map) with the freed keys enumerated → `delete_file` (durable refs released, blocks freed via the terminal-free law/reclaim queue) → destroy record+xattrs in ONE tx; C9 actions run FIRST in the apply loop so block-class findings about a doomed inode's blocks verify as healed and refuse. No incompat bit, no env knob. **C10 = inode-plane reference consistency** (`tests/fsck_c10_tests.rs`) is C9's safety half — the two shapes the C9 author enumerated and left undetected: `nlink` ABOVE the name count (a leak: the inode and its blocks are unreclaimable forever) and `nlink` BELOW it / `nlink == 0` with a live name / a name whose inode has no record (**the loss direction**: once ordinary unlinks drive the count to 0, `destroy_inodes`' live-nlink skip stops protecting an inode a live path still resolves — S3.5's `24ef223c` made such a dentry removable and nothing FOUND one). It rides C9's ONE dentry pass: `InoBitmap::mark`'s return value separates the first name from the rest, so only inos named twice cost a small map, and the census's non-directory `nlink != 1` map is the mirror (the hardlink population, never the tree) — both empty on a healthy tree without hardlinks. The DANGEROUS arms are the REVERSE difference of C9's two sets (the census skips `nlink == 0`, so both shapes land in `referenced \ live`) split by one fresh record read, so the class degrades only in its LEAK direction when the count map hits its derived budget. Zero-FP is NOT the era floor (a prior-era inode can be hardlinked a microsecond ago): it is the record witness `(nlink, ctime)` read under the ino's exclusive 4a lease BEFORE and AFTER the fresh dentry pass (every same-volume name-count change mutates the record in the SAME tx — link/unlink move nlink, rename stamps Δctime — so the pass's read-over-time skew can never produce a finding), the open cross-volume intent exemption (`xv_scan_intents` — the inode plane's `inflight_contains`), distinct `(GLOBAL parent, name)` counting (a VL5b migration's mid-copy duplicate dentry dedupes), directory exclusion from the count arms (`.`/`..` are synthesized), and the completeness gates. Repair is asymmetric by law: raising is safe, LOWERING runs only when a third independent pass agrees, a dangling name can only be removed (key+value quarantined), and directories/healed/reversed/in-flight cases are refused loud. Repair order is C9 → C10 → the block plane, and an applied raise sets `census_stale` so the block classes re-walk — the census's `nlink == 0` skip means a zero-count inode's blocks read as C2-leaked, and freeing them would destroy what the raise restores. `nlink == 0` WITHOUT a name stays POSIX-15's, unclaimed. No incompat bit, no env knob.
* **Four-axis defrag (VL7, KD-11)** — D1 free-space contiguity / D2 file locality / D3 staged-extent pressure / D4 meta node occupancy, each a gauge (`frag_d1..d4`) + an independently invocable mover that REUSES existing machinery (`move_one` with contiguity-aware picks, the W2 fold, the KV SMO compactor); `--report-only` measures without moving; `--rebalance` is the operator arm of the VL4 pass.
* **Interaction policy pins (VL9, `tests/interaction_tests.rs`)** — mover-scope SERIALIZE-loud at `claim_next` (one mover-class job per volume; `job_serialized_waits`); drain-during-fsck both proceed with FP = 0; defrag naming a draining volume = queue-then-refuse-loud; migrate + drain both converge; R5 Red pauses jobs loudly (never self-resuming).
* **Instruments**: `tests/run_volume_lifecycle.sh` (the rig, 17 legs), `tests/run_lifecycle_soak.sh` (the canonical 9-step soak, rotating 10-op menu, abort-on-failure), `tests/run_vl9_matrices.sh` (the counted ×10 G-VL matrices). Stats-inode deltas are the engagement instrument (a drain row is INVALID unless `evacuate_bytes_moved` accounts for the victim's used bytes).
* **Env/CLI knobs:** `--throttle <1-100>` on fsck/defrag/remove-data + `job throttle` live retune; `squeezefs job worker <sqmeta-uri>` (remote enrollment); (`format --meta-slots` is RETIRED — a hard error naming `volume add-meta --take-slots`/`migrate-meta-slot`; widths are derived); `SQUEEZEFS_FSCK_SETTLE_MS` (suspect settle window).
* **Stats families added (stats inode):** `job_{submitted,completed,cancelled,failed,tasks_done,checkpoint_writes,copy_buffer_bytes,paused_mem_pressure,paused_capacity,serialized_waits}`; `job_remote_{workers,enrollments,enroll_refused,shards,submissions,refused_stale,lease_expiries,reassignments,bytes_moved,verify_read_bytes,quarantined_destinations,pr_preempts,fence_mode}` (`job_remote_shards` = the remote-engagement instrument; `enroll_refused`/`refused_stale` are security/fencing tripwires); `evacuate_{blocks_moved,bytes_moved,shared_blocks_moved,inflight_bytes,stale_token_noops,deferred_staged_blocks,replans,needed_bytes,avail_bytes,transient_bytes}`; `volume_preflight_refusals`; `placement_table_refreshes` + `backend_placement_picks` / `backend_fill_{ratio,spread}`; `fsck_{inodes_scanned,nodes_walked,blocks_checked,refcounts_checked,suspects,suspects_cleared,epoch_exempted,inflight_exempted,mover_ledger_exempted,findings,scan_secs}` (`fsck_findings` must-stay-0 on healthy volumes) + the C9 pair `fsck_dentry_refs_indexed` (the cheap-direction pass) / `fsck_current_era_exempted` (the live-create shield's engagement — the inode plane's `epoch_exempted`); the C10 family `fsck_nlink_mismatch_high` (the LEAK direction) / `fsck_nlink_mismatch_low` + `fsck_nlink_zero_named` + `fsck_dangling_dentries` (**the LOSS direction — nonzero is the stop-and-read signal**) / `fsck_nlink_names_counted` (the multi-named population the counting rides, 0 on a hardlink-free tree) / `fsck_nlink_transient_cleared` (C10's guard engagement — the inode plane's `inflight_exempted`); `scrub_{blocks_scanned,bytes_scanned,aead_verified,frame_verified,readability_only,failures}`; `fsck_repairs_{planned,applied,refused}` + `fsck_quarantined_{records,blocks,bytes}` + `fsck_repair_classC{1..10}` (C8 is structurally 0 — reported, never auto-repaired); `defrag_{blocks_moved,bytes_moved,folds_kicked,meta_compactions_kicked,passes}` + `frag_d1_{contiguity,reclaimable_tail}` / `frag_d2_locality` / `frag_d3_pressure_bytes` / `frag_d4_dead_bset_ratio`; `fuse_reserved_xattr_refusals` (tamper tripwire).

### Error Handling & Crash Recovery

* FUSE op deadline **watchdog** (per-op `tokio::time::timeout` wrappers were retired in M4 — overdue ops are logged loudly, ring-admission parking escalates to `disabled_volumes`); staging recovery on remount (the `NvmeStaging::new` mount-time recovery scan, `src/cache/nvme.rs`) with fence + layout checks.
* Stale fencing tokens discard staged work (the **remount** contract); missing inode meta discards orphan active blocks. Live writeback units apply the same law supersession-aware (FIND-M11-A fix): fencing-stale ⇒ verified no-op, reclaimed-ino NotFound ⇒ verified orphan-discard, transient ⇒ retry forever (never-lossy).
* **A write-path allocation refusal is TERMINAL for that write** (2026-09-06, `.benchmarks/2026-09-06-cowriter-enospc-wedge.md`): `BlockAllocator::allocate_block_grace_bounded` — the finding-29 park every write-path mint runs under its `BLOCK_FLUSH_LOCKS` guard — waits for grace-held space at most `free_grace::pressure_park_wall_ms()` = 2 × the routine fence bound floored at 1 s, measured in WALL time (a DURATION — never `bound()`, the reallocation label whose `u64::MAX` sentinel made the wall infinite on every co-writer and reader), and on a co-writer only while the last harvest reply witnessed a held ring; past it `StorageFull` propagates (`ENOSPC`, gauge `write_enospc_refusals` for synchronous refusals), the stripe releases, and the never-lossy ladder owns only ACKed custody (the writeback classifier's ENOSPC-is-transient arm governs staged bytes the drain frees space for — that is not misfiled). A write that neither replies nor errors under `alloc_lane_enospc_refusals` growth is the wedge shape and a first-class bug.
* Write verification is **opt-in** (`--write-verification`, optional sample rate).
* **Metadata crash contract**: whole-transaction atomicity + torn-write immunity **by construction** (v3 CoW KV — see Metadata format section + `docs/design-cow-kv-metadata.md` §4.10; the historical D0/D1/D2 ladder it replaced is `docs/design-wal-crash-consistency.md` §3).
* Mount probes each meta volume's sector atomicity (sysfs) — purely informational, surfaced as `meta_volume_atomicity_physical` while the contract field `meta_volume_atomicity` reads `cow-checksummed`. (The `--strict-meta-atomicity` flag only ever gated v2 volumes and was deleted with them.)

### Lock order & connection scope (must not)

Always acquire in this order; **never invert** (P1-9):

1. `active_inode_locks` (per-inode `RwLock`) — FUSE op serialization
2. `lease_locks` (per-inode) — only while acquiring/refreshing DLM lease
3. `BLOCK_FLUSH_LOCKS` (per block) — active-block mutation
3.5. `INODE_META_LOCKS` (routing, per-inode `Mutex`, 4096-way) — the striped block-map merge domain (`DataRouter::merge_block_mappings`, §5.3 one merge discipline; documented in `src/stripe_locks.rs:15` and depended on by 20+ sites). **Never held across a terminal `free_block`** (RES-1): the at-cap reclaim enqueue parks up to `SQUEEZEFS_RECLAIM_CAP_PARK_MS` per key — collect the displaced keys under the lock and free them after the guard drops (the pattern `write_striped`, `truncate_layout` and `release_superseded_staged` use).
4. MetaLV metadata-transaction locks, in sub-order:
   - **4a.** DLM `I{ino}` / `D{parent:name}` (per-object; `DlmLockManager`). **Striped, DERIVED width (D-3, 2026-09-05):** `stripe_locks::dlm_stripe_width` = `SQUEEZEFS_DLM_STRIPES` explicit, else `next_pow2(max(4096 shipped, 16 × possible_cpus × q_depth))` — 16,384 on a 32-CPU box; the same law over the 1024 floor sizes the lease waiter / grant-floor pair (`src/dlm.rs`) and the S9 serve stripe (`meta_ship/publish.rs`). The guard is held across the whole commit park (D5), so a stripe collision there costs the colliding op a full commit; the census `<table>_stripe_collisions` (false sharing — the width's) vs `<table>_key_waits` (same key — the workload's) + `lock_phase_ns.dlm_guard_wait` says which. Guards drop at the terminal outcome BEFORE the ack is sent (the one permitted scoping — a committer never parks on its own previous tx). Evidence `.benchmarks/2026-09-05-d3-dlm-stripe-derivation.md`; field rows owed.
   - **4b.** (design-cow-kv-metadata §4.9 4b): **per-node write locks — the commit path takes leaf locks only, in ascending NodeId order, deduped, lock-then-revalidate-then-retry against SMOs; interior-node locks belong exclusively to the serialized per-volume checkpoint/SMO task (parent-then-child), which is what keeps the two lock populations acyclic. Node locks are never held across device I/O (commit apply is RAM-only; the journal entry write happens after unlock; writeback freezes under the lock and appends outside it; SMOs reserve in-window and write after release) and never held while waiting on ring space (ring admission happens before any node lock — §4.4 pt 5; the checkpoint task's own admissions never park, they drain-and-retry).** Since the M7 commit conveyor (design-metadata-throughput §5.5), the leaf-lock **taker** population is exactly {the per-volume conveyor **pass task**, the checkpoint/SMO task}: user committers take no node locks — they enqueue and park on oneshots, each queue entry co-owning its tx's 4a DLM guards until terminal outcome, and the pass takes the batch's **union** leaf set under the same 4b discipline while it *holds-but-never-acquires* DLM guards (no new wait-for edges; see `src/stripe_locks.rs`).
   - **4c.** The journal reservation — a wait-free atomic, not a lock; ordered inside 4b by protocol, it imposes no ordering edges.

**Must not:**
* Hold inode **write** guard across long backend I/O when block locks suffice (striped data path = meta-prep only under write lock — P1-8).
* Hold a **pooled MetaLV connection/handle** across durable NVMe / staging I/O (P1-10: open → short meta → drop → I/O → re-acquire for commit).
* Acquire (1) while holding (3).
* Burn fencing tokens on failed lock acquisition.

### OS / fabric notes

* Dirty ratios and fabric tuning remain operator concerns for large clusters.
* Primary data path uses the NVMe/NVMe-oF block storage backend.

---

## TDD Development Workflow

A disciplined, test-first development workflow for building industrial-grade, high-performance, highly scalable, asynchronous Rust systems. Tests define the contract. Code fulfills it.

### Core Principle: Tests First, Code Second

Every feature, bugfix, or refactor follows the same cycle:

1. **Define** — What should the correct behavior be?
2. **Test** — Write tests that assert that behavior (they will fail).
3. **Implement** — Write the minimum code to make the tests pass.
4. **Refine** — Refactor for clarity and performance while tests stay green.
5. **Commit** — Each logical step gets its own git commit.
6. **Merge** — Bring the code base back into the primary branch.

### Phase 0: Branch from `dev`

All work happens on feature/fix branches off `dev`. Never commit directly to `dev` or `main`.

```bash
git checkout dev
git pull origin dev
git checkout -b feat/short-description   # or fix/short-description
```

- **Branch naming**: `feat/`, `fix/`, `refactor/`, `perf/`, `docs/` prefixes matching conventional commit types.
- **One branch per logical unit of work**: a feature, a bugfix, or a hardening pass.

#### SqueezeFS I/O constraint (always io_uring when we can)

This repo is Linux + **io_uring**-first. The 2026-08-01 ruling applies here too: io_uring is the measured-fastest presumption, not dogma — a counted A/B showing a faster arrangement displaces it; a failing test never does. When planning or implementing:

- **Default to io_uring** for FUSE request traffic (FUSE-over-io_uring after arm), NVMe/block I/O (`NvmeBlockDev`), and local file I/O (`crate::uring_fs`) whenever the kernel can support it.
- **Do not** introduce or re-enable classical `/dev/fuse` or POSIX file I/O fallbacks to “make it work.” That is an anti-pattern here. Fix the uring path or fail loud.
- The intentional classical FUSE uses are the one-shot **`FUSE_INIT`** exchange (kernel requires it before REGISTER) and the post-arm **classical sideband session** for the traffic the kernel refuses to put on the ring (FORGET/INTERRUPT/resends + `fiq->ops` switchover stragglers) — serviced via io_uring `Readv`, never as a hot-path fallback. All other requests/replies are over-uring only after arm.
- If a failing test tempts you to disable over-uring or switch to blocking `read`/`write`, stop — write a failing test that encodes correct uring behavior, then fix uring.

#### No dead code

- Remove unused functions, fields, imports, and modules as part of the same change that made them unused.
- **Never** add `#[allow(dead_code)]` / `#[allow(unused_*)]` to park unused code. Delete it; git has history.
- Clippy with `-D warnings` failing on unused items means **delete**, not allow.

### Phase 1: Understand & Plan

Before touching any code:

- State the goal in one sentence.
- Identify the crate(s) and module(s) that will be affected.
- List the behaviors that need to be correct when you're done.
- Identify edge cases, error conditions, and concurrency concerns upfront.
- Determine `Send`/`Sync` requirements for all shared state.
- Identify async boundaries — which types must implement `Future`, which tasks cross `.await` points.
- **I/O path:** does this touch FUSE, NVMe, or local files? If yes, plan the **io_uring** design — not a classical shortcut.
- **New client-side machinery?** Check the reference fast-FUSE clients first (DAOS client, JuiceFS — `docs/reference-clients-survey.md`) for a proven pattern before inventing one.

Produce a brief execution plan:

```
Goal: [one sentence]
Affected: [crates/modules]

Behaviors:
1. [Expected behavior] → verify: [how]
2. [Expected behavior] → verify: [how]
3. [Edge case] → verify: [how]

Concurrency:
- Shared state: [Arc<Mutex<T>>, Arc<RwLock<T>>, lock-free, actor]
- Executor: [sqz_exec lane (which class), sqz_blocking pool, no async at all — NEVER tokio in product code]
- Cancellation: [CancellationToken, drop-based, deadline]
```

### Phase 2: Write Tests

Write the test file(s) BEFORE any implementation:

- **Happy path**: The expected, normal-operation case.
- **Error paths**: What happens when inputs are invalid, connections drop, `CancellationToken` fires, channels close?
- **Boundary conditions**: Empty inputs, maximum values, zero-length streams, single-node mesh.
- **Concurrency**: Race conditions under parallel access. Multiple tokio tasks hitting the same state. Use `#[tokio::test(flavor = "multi_thread", worker_threads = N)]` to stress shared state.
- **Timeouts & Cancellation**: `tokio::time::timeout`, `CancellationToken` propagation, slow peers, hung connections.
- **Backpressure**: Channel saturation, bounded queue overflow, consumer lag.
- **Shutdown**: Graceful drain under in-flight work, partial completion, resource cleanup on abort.

#### Test Quality Checklist

- [ ] Each test has a clear, descriptive name (`test_broadcast_storm_uuid_dedup`)
- [ ] Tests are independent — no shared mutable state between test cases
- [ ] Parameterized tests via `rstest` (`#[case]`, `#[values]`) for combinatorial scenarios
- [ ] Async tests use `#[tokio::test(flavor = "multi_thread")]` to expose data races
- [ ] Assertions include meaningful failure messages (`.expect("msg")`, `assert!(cond, "msg")`, or `pretty_assertions`)
- [ ] No `tokio::time::sleep` for synchronization — use channels, `tokio::sync::Barrier`, `watch` channels, or `CancellationToken` for coordination
- [ ] Tests that spawn tasks verify cleanup (no leaked tasks, no dangling `JoinHandle`s)
- [ ] Property-based tests via `proptest` or `quickcheck` for complex invariants

#### Commit: Tests

```
test(crate): define behavior for [feature]

- Happy path: [describe]
- Error cases: [describe]  
- Edge cases: [describe]
- All tests currently FAIL (no implementation yet)
```

### Phase 3: Implement

Write the minimum code to make all tests pass:

- Don't add features beyond what the tests require.
- Don't add abstractions for hypothetical future needs.
- Don't optimize prematurely — correctness first.
- Run `cargo test --all-features` after every meaningful change.
- Run `cargo test --all-features -- --test-threads=1` if order-dependent failures are suspected.

#### Implementation Checklist

- [ ] All tests pass (`cargo test --all-features`)
- [ ] `cargo clippy --all-targets --all-features -- -D warnings` clean
- [ ] `cargo fmt --check` clean
- [ ] All async functions and shared types are `Send + Sync` where required
- [ ] Error types use `thiserror` (library) or `anyhow` (application) with `.context()` for operation context
- [ ] Resources cleaned up via RAII — `Drop` impls, `scopeguard`, or `_drop_guard` patterns
- [ ] No `unsafe` without a documented safety invariant and a safety comment
- [ ] No `unwrap()`/`expect()` in library code — propagate errors
- [ ] All `JoinHandle`s are awaited or explicitly detached with documented rationale
- [ ] No TODO/FIXME left without a tracking issue
- [ ] **I/O uses io_uring where applicable** — no new classical FUSE/file fallbacks; FUSE request path stays over-uring after arm
- [ ] **No dead code** — unused items deleted; no new `#[allow(dead_code)]` / unused allows to hide them

#### Commit: Implementation

```
feat(crate): implement [feature]

- All tests passing
- Clippy clean, fmt clean
- [Brief note on approach taken]
```

### Phase 4: Refine, Harden, & Benchmark

With green tests as your safety net:

- Refactor for clarity if the implementation is messy.
- **Mandatory Benchmarking**: EVERY public function must have a corresponding Criterion benchmark. Performance is a first-class feature.
- Profile allocations for data-plane code via Criterion's allocation measurements.
- Run `cargo bench` to record historical performance; commit results to `.benchmarks/` or use `cargo-criterion` with `--save-baseline`.
- Run `cargo loom` on lock-free or highly concurrent data structures to exhaustively check memory-ordering correctness.
- Add any additional edge case tests discovered during implementation.

#### Commit: Refinements

```
refactor(crate): [what changed and why]
```
or
```
perf(crate): optimize [hot path] - [result]
```

### Phase 5: Integration Verification

Before considering work complete:

1. `cargo build --all-targets --all-features` — full project compiles.
2. `cargo test --all-features` — all tests pass project-wide.
3. `cargo clippy --all-targets --all-features -- -D warnings` — zero warnings.
4. `cargo fmt --check` — formatting is clean.
5. `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps` — documentation builds without warnings (rustdoc's intra-doc-link lints are warnings by default and cargo never fails on them; the flag is what makes this line a gate).
6. `task audit` (= `cargo audit --deny unsound --deny yanked` over BOTH lockfiles — the root workspace and the excluded `crates/fuse3` fork). Vulnerabilities, unsound and yanked crates fail; the `unmaintained` residue is adjudicated in `docs/rc-manifest.md` §5. ENG-2: this line had no enforcement point until 2026-08-03 — it is now part of `task check`.
7. Review the diff: every changed line traces to the original goal.

If a `task check` or `cargo xtask check` target exists, run it — it is the authoritative quality gate.

**Required verification gate (must pass before commit — tiered by change class; user directive 2026-07-18: stop running full test suites on doc-only changes):**

| Change class | Required gate |
|---|---|
| **Docs/markdown only** (no `.rs` / `.toml` / tests / scripts touched) | markdown link/anchor check only — **no cargo gate** |
| **Manifest metadata only** (`Cargo.toml` non-dependency keys) | `cargo check` + `cargo clippy --all-targets --all-features -- -D warnings` + `cargo fmt --check` |
| **Code, dependencies, tests, or harness scripts touched** | the **full gate** below (plus loom when a lock-free core changes) |

The **full gate** — `task check` runs exactly this (ENG-2/7/8/17 closed the
holes: audit, the shipped feature configuration, and the excluded fork):

```bash
task check                        # the authoritative gate; the lines it runs:
cargo clippy --all-targets --all-features -- -D warnings
cargo clippy --all-targets -- -D warnings   # the SHIPPED (default-features) config — ENG-8
cargo fmt --check
cargo test --all-features -- --test-threads=1
RUSTDOCFLAGS="-D warnings" cargo doc --no-deps   # rustdoc lints are warnings by default — the flag is the gate
cargo bench --benches -- --test   # criterion smoke: one iteration per bench, no measurement
task check:fuse3                  # the excluded fork's own suite/fmt/clippy/doc/bench — ENG-17
task check:loom                   # the isolated loom crate COMPILES against the cores it #[path]-includes (build+fmt, -D warnings; the model RUN is tests/run_loom.sh)
task check:fuzz                   # the excluded fuzz workspace TYPE-CHECKS on stable (check+fmt, -D warnings) — the twelve TEST-4 targets rotted silently for a month without this line; the instrumented RUN is the nightly tier
task check:docs                   # markdown link/anchor check (tests/check_markdown_links.sh)
task audit                        # cargo audit, BOTH lockfiles, --deny unsound --deny yanked — ENG-2
```

`--all-features` compiles `dhat-on`, which **replaces jemalloc** (and drops
the load-bearing `dirty_decay_ms:1000`): correct for a correctness gate, never
valid for a number. Such a binary says so in its own `--version` line and logs
it at mount and on `squeezefs bench` (ENG-8). Measurement builds are
`cargo build --release` — default features.

CI runs the same gate (`.github/workflows/gate.yml`, ENG-7): per-PR/push on
non-docs changes, docs-only changes get `docs.yml`, and `nightly.yml` re-runs
`task audit` (advisories land without commits) + the gate on `dev`. The
external POSIX suites, the bench baseline and every measurement rig stay OFF
CI by design — a shared cloud runner cannot produce a valid row.

### Phase 6: Merge to `dev` & Cleanup

After all checks pass, merge the feature branch back into `dev` and delete it:

```bash
git checkout dev
git merge --ff-only feat/short-description
git branch -d feat/short-description
```

- **Always `--ff-only`**: If `dev` has diverged, rebase the feature branch first (`git rebase dev`). No merge commits for linear history.
- **Delete the branch**: Once merged, the branch serves no purpose. Delete it immediately.
- **Push**: `git push origin dev` to sync remote.

### Git Discipline

- **`dev` is the integration branch**: All feature/fix branches start from and merge back into `dev`. `main` is reserved for releases.
- **Atomic commits**: One logical change per commit. Tests and implementation can be separate commits.
- **Conventional format**: `type(scope): description` — types: `feat`, `fix`, `test`, `refactor`, `perf`, `docs`, `chore`.
- **No direct commits to `dev` or `main`**: Always use a branch.
- **Commit messages explain WHY**, not just what. The diff shows what changed; the message explains the reasoning.
- **Delete branches after merge**: Stale branches are clutter. Merge → delete → move on.

### Anti-Patterns to Avoid

| Anti-Pattern | Correct Approach |
|---|---|
| Writing code first, tests after | Write tests first — they define the contract |
| Testing only the happy path | Cover errors, boundaries, concurrency, cancellation, backpressure, shutdown |
| Giant commits with tests + code + refactor | Separate commits for tests, implementation, refinement |
| "Improving" unrelated code while fixing a bug | Touch only what the goal requires |
| Skipping concurrency tests because "it's simple" | Always use `multi_thread` flavor. Simple async code has races too |
| Using `tokio::time::sleep` for test synchronization | Use channels, `Barrier`, `watch`, or `CancellationToken` |
| Speculative abstractions | Build what's needed now. Refactor when a real pattern emerges |
| `unwrap()` in library code | Propagate errors with `?` and `thiserror` |
| `unsafe` without safety comments | Document invariants; prefer safe alternatives |
| Blocking the async runtime | Use `tokio::task::spawn_blocking` for CPU-bound or blocking I/O |
| Unbounded channels in production code | Use `mpsc::channel(bound)` with explicit backpressure |
| Leaking tasks (fire-and-forget `spawn` without tracking) | Use `JoinSet` or structured concurrency patterns |
| Ignoring `Send + Sync` bounds | Design types to be `Send + Sync` from the start; document why if not |
| Falling back to classical `/dev/fuse` or POSIX file I/O to dodge an uring bug | **Always use io_uring when we can.** Fix the uring path or fail the mount/op — never reintroduce classical escape hatches |
| Leaving unused code with `#[allow(dead_code)]` “for later” | **Delete dead code.** Restore from git when needed; keep clippy `-D warnings` clean by removal |

---

## Build

System deps (Ubuntu/Debian): `build-essential pkg-config libfuse3-dev fuse3 clang libclang-dev`. The build links FUSE 3 and uses `io-uring` + `/dev/fuse` — it will not compile on non-Linux.

```bash
cargo build --release
```

**go-task is the packaged build path** (user directive 2026-07-25; `Taskfile.yml` + `docker/` — README §Building has the full workflow): `task build` = host dev build, `task build:{rocky8,rocky9,ubuntu2404,ubuntu2604}` / `task build:all` = containerized distro builds, each landing `dist/<target>/{squeezefs, libsqueezefs_il.so}` under default names (the folder is the same-commit daemon+shim pairing unit — KD-7). In-container assertions: `--version` never `unknown`, per-distro glibc ceilings. `task check` runs the authoritative full cargo gate. Plain `cargo build` remains valid for dev work.

### Optional Cargo features (off by default)
- `gds` — GPU Direct Storage RDMA path (pulls `libloading`).
- `dhat-on` — heap profiling (`dhat`); a static `dhat::Alloc` replaces the global allocator in `src/main.rs`.
- `coz-on` — causal profiling; the `coz_progress!` macro (defined in `src/lib.rs`) becomes active.

`[profile.release]` keeps `debug = true` so release binaries carry symbols for profiling. `tikv-jemallocator` is the global allocator on Linux.

**The two-profile LTO law (user directive 2026-09-02):** `release` is **thin LTO** with parallel codegen — the dev / field-A/B / gate profile (fat LTO + one codegen unit cost every rebuild minutes for a few percent). **Tagged releases only** ship from **`dist`** (`inherits = "release"`, `lto = "fat"`, `codegen-units = 1`; shim twin `preload-dist`) via `task dist:<distro>` — never the dev or gate profile. The binary names its profile (`--version` … `profile <name>`; `.stats build_profile`), and a measurement row must state it: both legs of an A/B carry the same profile, and release-gate / scoreboard rows run `dist`. Pinned in `tests/cli_version_tests.rs` (only `dist` may carry fat LTO).

### First-party `fuse3` fork (`crates/fuse3`)

`crates/fuse3` is a maintained **fork** of the upstream crates.io `fuse3` crate (forked from v0.7.3, Sherlock Holo, MIT — upstream attribution retained in `THIRD_PARTY_NOTICES.md` and `crates/fuse3/LICENSE`). It has substantially diverged from upstream: the FUSE-over-io_uring transport, payload leases, and flag sweeps live here — and it now carries the **SqueezeFS release-train version** (1.2.x), not upstream's 0.7.x. Treat it as first-party code — edit it directly when FUSE behavior changes; **never** replace it with the crates.io version or "restore" `Cargo.toml.orig`. `Cargo.toml` declares the prepublish-style requirement `fuse3 = { version = "1.2", … }` plus `[patch.crates-io] fuse3 = { path = "crates/fuse3" }`: only the in-tree fork can satisfy it (crates.io fuse3 never can), and the patch indirection is **load-bearing for workspace topology** — the fork is its own excluded workspace root (standalone suite, own lock), and cargo rejects a direct path dependency to a nested foreign workspace root ("multiple workspace roots found in the same workspace"); patch entries bypass that check.

### Build features for profiling

| Feature | Purpose |
|---------|---------|
| *(default)* | Production binary; `tikv-jemallocator` on Linux |
| `dhat-on` | Heap profiling via `dhat` (replaces global allocator in `main`) |
| `coz-on` | Causal profiling; `coz_progress!` active |
| `gds` | GPU Direct Storage path |

```bash
cargo build --release
cargo build --release --features dhat-on
cargo build --release --features coz-on
```

Release profile keeps `debug = true` for symbolicated profiles.

---

## Testing

Most integration tests execute against local file-backed MetaLV sandboxes.

### Required verification gate (tiered by change class)

Match the gate to what the commit touches (user directive 2026-07-18: stop running full test suites on doc-only changes). Same table as the TDD Phase 5 gate — keep the two in sync:

| Change class | Required gate |
|---|---|
| **Docs/markdown only** (no `.rs` / `.toml` / tests / scripts touched) | markdown link/anchor check only — **no cargo gate** |
| **Manifest metadata only** (`Cargo.toml` non-dependency keys) | `cargo check` + `cargo clippy --all-targets --all-features -- -D warnings` + `cargo fmt --check` |
| **Code, dependencies, tests, or harness scripts touched** | the **full gate** below (plus loom when a lock-free core changes) |

The **full gate** — `task check` runs exactly this (ENG-2/7/8/17 closed the
holes: audit, the shipped feature configuration, and the excluded fork):

```bash
task check                        # the authoritative gate; the lines it runs:
cargo clippy --all-targets --all-features -- -D warnings
cargo clippy --all-targets -- -D warnings   # the SHIPPED (default-features) config — ENG-8
cargo fmt --check
cargo test --all-features -- --test-threads=1
RUSTDOCFLAGS="-D warnings" cargo doc --no-deps   # rustdoc lints are warnings by default — the flag is the gate
cargo bench --benches -- --test   # criterion smoke: one iteration per bench, no measurement
task check:fuse3                  # the excluded fork's own suite/fmt/clippy/doc/bench — ENG-17
task check:loom                   # the isolated loom crate COMPILES against the cores it #[path]-includes (build+fmt, -D warnings; the model RUN is tests/run_loom.sh)
task check:fuzz                   # the excluded fuzz workspace TYPE-CHECKS on stable (check+fmt, -D warnings) — the twelve TEST-4 targets rotted silently for a month without this line; the instrumented RUN is the nightly tier
task check:docs                   # markdown link/anchor check (tests/check_markdown_links.sh)
task audit                        # cargo audit, BOTH lockfiles, --deny unsound --deny yanked — ENG-2
```

`--all-features` compiles `dhat-on`, which **replaces jemalloc** (and drops
the load-bearing `dirty_decay_ms:1000`): correct for a correctness gate, never
valid for a number. Such a binary says so in its own `--version` line and logs
it at mount and on `squeezefs bench` (ENG-8). Measurement builds are
`cargo build --release` — default features.

CI runs the same gate (`.github/workflows/gate.yml`, ENG-7): per-PR/push on
non-docs changes, docs-only changes get `docs.yml`, and `nightly.yml` re-runs
`task audit` (advisories land without commits) + the gate on `dev`. The
external POSIX suites, the bench baseline and every measurement rig stay OFF
CI by design — a shared cloud runner cannot produce a valid row.

### External POSIX / IO suites (require root + a mounted FS)

These are **not** part of `cargo test`. Run as **root** on Linux/WSL after material write-path, layout, or FUSE lock changes:

```bash
# pjdfstest POSIX conformance suite (full run; single-test arg for fix loops)
sudo tests/run_pjdfstests.sh

# LTP filesystem syscall tests
sudo tests/run_ltp_syscalls.sh

# fstests filesystem checks
sudo tests/run_fstests.sh

# Mount-level IO benchmark
sudo tests/run_elbencho_mount.sh
```

Prerequisites: Volume formatted/mounted per `QUICKSTART.md`. Failures here can pass pure unit tests and still indicate mount regressions.

Dev boxes without spare raw NVMe: `sudo tests/dev_substrate.sh create` builds the preferred pseudo-everything substrate — memory-backed null_blk (mds) + zram (oss) exposed as real `/dev/nvmeXnY` namespaces via nvmet-loop (QUICKSTART → *Dev Box: Virtual NVMe Substrate*); `SQZ_DEVSUB_TRANSPORT=tcp` builds the same shape over **nvmet-tcp on localhost** (the fabric-sensitive benchmark venue — see *Benchmark substrates* under Benchmarks & Profiling). Do not measure barrier-bound work on file-backed-on-btrfs volumes: the substrate bracket is 165× on the journal barrier (`.benchmarks/2026-07-14-metadata-throughput-baseline.md`).

### Test tiering (do not run acceptance suites at per-commit cadence)

fstests/LTP are **wall-clock-bound** (fixed-duration fsx/fsstress soaks, mount-cycle overhead) — a full `-g auto` is ~5 h regardless of CPU speed. Running them per fix wastes hours re-failing tests already known to fail. Use three tiers:

| Tier | What | When | Cost |
|------|------|------|------|
| **Per-commit** | the change-class gate above (docs-only: markdown link/anchor check, no cargo; manifest-metadata: check+clippy+fmt; code-class: the full cargo gate — clippy/fmt/`test --test-threads=1`/doc/bench-smoke; loom when a lock-free core changes) | every commit, sized to its class | seconds–~5 min by class |
| **Per-PR (data-path)** | the full cargo gate + the relevant house rigs (`tests/run_preload_gate.sh` for ipc/preload surfaces, `tests/run_volume_lifecycle.sh` for volume-lifecycle surfaces) — **external POSIX suites (fstests/LTP/pjdfstests) do NOT run per-PR** (user ruling 2026-07-20: they added hours per code commit re-confirming known failures; they are the release gate below) | PRs touching the write/read/layout/FUSE paths | minutes |
| **Per-PR (preload/interception)** | `tests/run_preload_gate.sh` (leg 1, unprivileged: sanctioned cdylib build + Issue-4 guard proof + passthrough battery) / `sudo tests/run_preload_gate.sh` (leg 2: mount parity + §3 rule-4 engagement + dup/close_range/lseek rows + kill-9 and fork-kill-parent soaks) | PRs touching `crates/squeezefs-preload/`, `src/ipc_host.rs`, `src/ipc_service.rs`, or the fuse3 notify surface | leg 1 ~2 min; leg 2 ~5 min |
| **Per-PR (nvmeof/guard fidelity)** | `sudo tests/run_nvmeof_fidelity.sh quick` — both-stack (SPDK + kernel nvmet) product-verb round-trips + 1 guard kill-9 cycle per stack, zero-residue snapshot assert; real kernels, zero mocks (design-nvmeof-target-management §6.8) | PRs touching `src/nvmeof/`, `src/meta_backend/reservation.rs`, or the writer-guard gate | ≤10 min (2m20s measured, zram/localhost dev box) |
| **Release gate (MANDATORY before any release tag)** | **pjdfstests (`sudo tests/run_pjdfstests.sh`) + full LTP (`sudo tests/run_ltp_syscalls.sh`) + full fstests (`sudo tests/run_fstests.sh`, `-g auto`) must ALL pass** (user ruling 2026-07-20: every release passes all three; known-failure exceptions require a documented adjudication in `.benchmarks/`; **every failure triggers the repro-port mandate below** — the fix lands with a cargo test reproducing it), plus `sudo tests/run_elbencho_mount.sh` and `long_validation.py` scale mounts. Standing adjudications (2026-07-23, the ONLY ones): fstests **generic/003 + generic/192** (one class — noatime by design + kernel-TTL attr observation; 192 is the atime-delta face of the same ruling), **generic/213** (thin provisioning by design), and **generic/634** (timestamps are i64 nanoseconds — a deliberate ±292-year range, the ext4-u34/xfs-bigtime finite-range class; the daemon saturates deterministically and pins it in cargo, but incore clamping needs `sb->s_time_max`, which the FUSE protocol cannot advertise — kernel-interface-only) — pinned expected shapes in `tests/run_fstests.sh`; **generic/464 and generic/074 are expected-PASS** (fixed 2026-07-21, `.benchmarks/2026-07-21-wedge-and-074-fixes.md` — any diff on them is a regression). First full three-suite pass: `.benchmarks/2026-07-22-volume-lifecycle-closing.md` | before EVERY release tag; also nightly unattended where a box is available | hours |
| **Release gate (cargo leg): require-mount** | `tests/run_require_mount_gate.sh` (= `task check:require-mount`) — the twelve live-mount suites re-run with **`SQUEEZEFS_TEST_REQUIRE_MOUNT=1`**, which turns every mount-class self-skip into a hard FAILURE, plus the machine-readable **skip ledger** (`target/skip-ledger.jsonl`, one JSON object per line, `##SQUEEZEFS-SKIP##` on an uncaptured stderr handle). Spec §11 **TEST-2**: before this leg existed, `cargo test --all-features` could be all-green on a box without `/dev/fuse` / `fuse.enable_uring` / `fusermount3` with the product's core mechanism entirely unexecuted, and libtest swallowed the notices without `--nocapture`. **Every environment skip in the tree routes through `crates/squeezefs-testkit`** — `tests/skip_ledger_tests.rs` fails the build on a private `[SKIP]`-and-return, which is what stops a 15th file from inventing its own | before EVERY release tag, alongside the three external suites | ~15–30 min |
| **Release gate (cargo leg): zc-capability** | `sudo tests/run_zc_capability_gate.sh` — the capability-class suites (the FUSE_URING_ZERO_COPY contract sets `fuse_zc_write_fusion_tests`/`zc_bridge_cqe_wedge_tests`, the `bench_tests` zc rows, the zcrx lib ring probes) run as **root on an sqz-kernel box** with **`SQUEEZEFS_TEST_REQUIRE_CAPABILITY=1`**, turning every capability-class self-skip into a hard FAILURE (user ruling 2026-08-14: "whats the point of tests that don't actually run?" — the kernel arms zc only for `capable(CAP_SYS_ADMIN)`, so unprivileged `cargo test` self-skips the whole zc surface even where the sqz kernel is present). The pin `the_capability_gated_suites_all_ride_the_zc_capability_gate` (tests/skip_ledger_tests.rs) auto-discovers every suite declaring `SkipClass::Capability` and fails the cargo gate until this script names it — a new zc suite cannot quietly stay out. Builds run unprivileged (`run_as_user`); root runs the binaries | before EVERY release tag on the sqz dev box (any box with the sqz kernel + root), alongside require-mount | ~4 min |
| **Nightly (fuzzing)** | `cargo +nightly fuzz run <target> -- -max_total_time=<secs>` over the twelve `fuzz/fuzz_targets/` targets (spec §11 **TEST-4**): the on-disk decoders (`kv_record`, `kv_bset`, `kv_node`, `kv_journal`, `kv_superblock`, `layout_wire`, and since 1.2 `kvmap_record` — the block-map tree's key/value/head codecs, incompat bit 16), the cluster wire (`job_wire_frame` — the job-shard vocabulary; since 1.2 `cluster_wire_frame` — the `RpcFrame` vocabulary under every class cap + the per-frame MAC path + the S8 verb bodies — and `publish_wire` — the S9 schema-13 multi-call request/reply frames, `MigrateBlockMap`/`MapMigrated` included), and the IPC shared memfd (`ipc_bootstrap`, `ipc_ring_header`) — the three untrusted-input surfaces. Requires a nightly toolchain + `cargo-fuzz`; `fuzz/` is its own workspace (sanitizer RUSTFLAGS must not poison the main graph) and carries its own `[patch.crates-io] fuse3` entry. **Per-commit cadence gets the proptest mirror instead** (`tests/decoder_property_tests.rs` — the same never-panic-on-arbitrary-bytes and round-trip laws on stable, inside the normal gate). A crash found here follows the **repro-port mandate**: the fix lands with a cargo test reproducing it (the inaugural one is `kv_bset_record_count_cannot_drive_allocation` — a lying `record_count` was an allocation authority, 128 GiB / SIGABRT under any address-space cap). Inaugural smoke: 9 targets × 60 s, ~74.9 M execs, zero crashes. **1.2 release campaign** (`.benchmarks/2026-09-03-release-1.2-fuzz.md`): 12 targets × 90 s × 2 jobs, 383.6 M execs, zero crashes on the final tree — after two finds: `cluster_wire::hex_decode` panicked on a non-ASCII `job:enroll` secret (str slice by byte index; a daemon abort on a corrupt/tampered xattr — repro-ported + fixed), and the workspace itself had ROTTED (the S3 `cluster_wire` port made `job_wire_frame`'s reader synchronous the day the target landed; nothing compiled `fuzz/` for a month — `task check:fuzz` now does, on stable, inside `task check`). On NixOS the nightly is the fenix flake (`nix build github:nix-community/fenix#minimal.toolchain nixpkgs#cargo-fuzz`), run inside `nix-shell` with the fenix `bin/` ahead on PATH and `LD_LIBRARY_PATH` naming the gcc `-lib` output for the libFuzzer runtime's `libstdc++` — `rustup` is not the path here | nightly; MANDATORY before a release tag and after any on-disk/wire format change | ~10 min build + N × campaign |
| **Nightly + pre-merge (perf PRs): bench baseline** | `tests/run_bench_baseline.sh` — the full Criterion set (root + fuse3 workspaces) measured and compared vs the committed reference (`.benchmarks/criterion-baselines/reference.json`); exits nonzero past the per-group regression threshold (default 10 % ns/op — table in the script). SAME-BOX relative tripwire: pins to quiet cores, refuses hot CPU (≥ 80 °C) / foreign cargo work / cross-box compares. Per-commit stays bench SMOKE only (`cargo bench --benches -- --test`, unchanged). Refresh the reference (`save`) as part of intentional perf landings | nightly; MANDATORY pre-merge for PRs claiming or risking hot-path perf | ~20–40 min quiet-box |
| **Nightly (nvmeof fidelity)** | `sudo tests/run_nvmeof_fidelity.sh full` — guard kill-9 ×10 per stack (restart-from-zero) + SPDK PTPL power-cycle leg, PR/PTPL matrix, loud-fail matrix (G3), crash-window injection (§6.4 law 6), adopt legs (§6.10), target-restart persistence (G2, both stacks), soft-RoCE plumbing leg (rdma_rxe — plumbing only, no guard/perf claims), A/B smoke rows (recorded), teardown-to-zero-residue proof | nightly + nvmeof program/release gates | ≤70 min budget (5m46s measured, zram/localhost dev box) |
| **Per-release (competitive)** | `tests/run_scoreboard.sh` — the standing **multi-reference scoreboard**, the top-3 proof surface (PERFORMANCE IS PRIMARY, 2026-07-18; absorbed `run_vs_juicefs.sh` forward-only): SqueezeFS vs **JuiceFS**, **SeaweedFS native** (weed server + weed mount), **geesefs** + **mountpoint-s3** over one shared local RustFS store — pinned releases w/ checksums, matched substrate/budgets, 3 regimes × 6 elbencho workloads, **RW6 durability-leveled write rows** + **RW6-del durable delete rows** (relaxed published labeled; fsync/syncfs-inclusive *durable* rows govern write **and delete** verdicts — the old seq-write ACK allowlist retired to ∅, and the inaugural run's three adjudicated entries were deleted the day their follow-ups landed: **standing allowlist ∅, bare run**), declarative + empirically-verified **N/S capability matrix** (neutral cells, recorded refusals), per-row-family **top-3 rank adjudication**; exits nonzero on any unattributed LOSS vs any reference or INVALID sqz cell; `SQUEEZEFS_SB_ALLOW_LOSS` tracks attributed losses (legacy `SQUEEZEFS_VS_*` spellings honored). `SQUEEZEFS_SB_SMOKE=1` micro-grid is the per-commit-tier plumbing check | every release + after perf-relevant landings; baseline `.benchmarks/2026-07-18-multi-reference-scoreboard.md` incl. the 2026-07-18 allowlist-∅ addendum (JuiceFS-only lineage: `.benchmarks/2026-07-15-vs-juicefs-scoreboard.md`) | ~2–4 h (smoke ~10 min) |

**NVMe-oF fidelity tier scripts** (design-nvmeof-target-management §6.8, PR 5/N5): `tests/nvmeof_target_substrate.sh` (create/teardown/status/mkzram/snapshot — product-verb-driven dual-stack fabric, `:fideli-` ownership marker, test port slice 54000–54099, hugepage record/restore, built-in zero-residue snapshot diff), `tests/run_nvmeof_fidelity.sh` (the quick/full orchestrator above), `tests/guard_smoke.sh` (writer-guard matrix on product-shared namespaces, `--stack spdk|nvmet --loops N [--ptpl]`). They supersede the ad-hoc root gates that lived under `.agents/spdk-scoping/` (rig scripts removed from the tree 2026-07-18 — git history at `c615e3a`; the scoping record itself lives on as `.benchmarks/2026-07-17-spdk-target-scoping.md`). The multi-run discipline below applies verbatim to the ×10 guard matrices.

**The repro-port MANDATE (user ruling 2026-07-20 — non-negotiable):** for **every test we fail in pjdfstests, LTP, or fstests**, a **real cargo test that reproduces the found issue** MUST be written (the `tests/*_tests.rs` layer, red against the bug, green with the fix) and land WITH the fix. This is what makes the release-cadence external gate safe: a bug found once can never be reintroduced, because its repro runs on every commit forever. No fix for an externally-found failure merges without its cargo repro; where a scenario is genuinely not reproducible in-process (kernel-interface-only behavior), the exception is documented in the fix commit and the scenario joins `SQUEEZEFS_FSTESTS_QUICK` instead.

**`SQUEEZEFS_FSTESTS_QUICK`** (defined in `tests/run_fstests.sh`) is the **standing regression set**: every fstests case that has ever caught a real SqueezeFS bug, plus core fsx/fsstress soak, hole/punch/seek coverage, and mount basics. **Grow it whenever a new test surfaces a bug.**

**Fix-loop discipline (FAIL FAST — user rule 2026-07-22; retires the former "inventory once" posture for full runs):**
1. **Full runs fail fast** — `tests/run_fstests.sh` (full `-g auto` AND the QUICK set) aborts IMMEDIATELY at the first UNEXPECTED failure (nonzero exit, artifacts preserved, the failing test named loudly); `tests/run_ltp_syscalls.sh` aborts on the first FAILED/BROKEN (TCONF skips continue). The adjudicated by-design set (fstests 003/192 noatime, 213 thin provisioning) continues ONLY when a failure diff matches its pinned expected shape EXACTLY — any other diff on those tests aborts too (`expected_shape_diff` in the runner is the single source of the shapes).
2. **Fix immediately, red-first** — the aborted run's failure gets its cargo repro-port (red) and fix per the repro-port mandate, verified with the single-test mode (`sudo tests/run_fstests.sh generic/NNN` — classic one-shot, unchanged).
3. **Resume from the failure point** (user directive 2026-07-23 — fix-loop efficiency): after the fix, re-run the failing test, then `sudo tests/run_fstests.sh --resume-from <test>` continues the sweep from that position to the END of the persisted from-zero expansion order (the runner saves it on every from-zero run, so resumes are deterministic). Resume passes are DEBUGGING efficiency only; prior-position greens from the aborted pass are provisional.
4. **Final acceptance is one complete from-zero pass** — after the LAST fix, the FULL run must complete end-to-end fail-fast-clean from zero on the final binary (the counted-restart discipline below applies verbatim: resume passes and pre-fix progress are never acceptance evidence, and the closing report cites only the from-zero pass). The release gate's "all three suites pass" means exactly that.

**Multi-run discipline (counted runs: soaks, ×N repro suites, acceptance medians):**
1. **A deterministic or attributable failure on any early run aborts the count** — stop, fix, then **restart the count from zero**. Runs completed before the fix verified the old binary; they are not creditable toward the fixed one's acceptance.
2. **Completing the remaining rolls is legitimate only as *declared* rate/signature gathering** — measuring how often a flake fires or capturing its tape for attribution — and must be labeled as such in the evidence note, never counted as acceptance.
3. **Acceptance counts always restart post-fix** (e.g. "green ×10" means ten consecutive greens of the final binary, not eight-before plus two-after).

---

## Benchmarks & Profiling

### Cloud runs require EXPRESSED approval (user mandate 2026-08-28)

**No AWS/cloud cluster may be launched without the user's expressed
approval for THAT run.** The cheap-first pipeline (2026-08-28) is the
standing order of operations: every binary passes a FREE local fleet
probe first; the cloud venue exists ONLY for the final sustained
verdict, and each launch is individually approved by the user before
`tests/cloud_bench_cluster.sh launch` is invoked. A gate/fix train never
implies cloud approval.

### Benchmark VENUE: A/B and A-B-B-A rows run on squeeze-test, never the dev box (user directive 2026-09-07)

**Every A/B, A-B-B-A bracket, per-lever leg and acceptance row runs on
`squeeze-test`** (the 32-core Xeon field box). The dev laptop has **heat
soak**: its clocks fall as a row sequence runs, so the second arm of a
pair is measured on a slower machine than the first and the delta is
skewed — in most cases beyond the effect being measured. Dev-box rows are
**scoping evidence only** (mechanism engagement, tripwires, a first read
of a shape) and every note carrying one must say so; the acceptance
pair is re-run on the box before any verdict is written. This is the
same fact the criterion baselines already record ("the thermally-capped
dev box gives relative truth only") applied to every measurement class.
The multi-writer fleet rig runs on the box too (`tests/mw_fleet.sh` +
`tests/run_mw_matrix.sh` over the dev substrate; Open MPI is the vendor
build at `/usr/mpi/gcc/openmpi-*/bin`, put it on `PATH`; a user-level
toolchain + bundle-fed checkout live under `/scratch/tmp/sqz-agent/`).
Cloud runs still need expressed approval (below).

### Benchmark substrates: the two-substrate rule (2026-07-27)

The 2026-07-27 amplification campaign proved the nvmet-**loop** rig HIDES bandwidth-economy and network-stack effects (the 1.85×-amplification field capture reproduced on nvmet-tcp and was invisible on loop — `.benchmarks/2026-07-27-shim-write-amplification.md`). Both substrates come from `tests/dev_substrate.sh` (same backings — memory-backed null_blk mds + zram oss; same create/teardown/status verbs; the two coexist on one box via disjoint names/state dirs/ports):

| Substrate | Build | Use for |
|---|---|---|
| **loop** (default) | `sudo tests/dev_substrate.sh create` | Controlled-latency A/B: per-op decomposition, barrier-bound metadata work, latency-shaped micro-comparisons — the lowest-noise venue |
| **tcp** (`SQZ_DEVSUB_TRANSPORT=tcp`) | `sudo SQZ_DEVSUB_TRANSPORT=tcp tests/dev_substrate.sh create` | **MANDATORY for fabric-sensitive rows**: writes, bandwidth-bound shapes, multi-connection workloads. nvmet-tcp on `127.0.0.1` runs the real NVMe/TCP queue + softirq machinery, so device-bandwidth economy (write amplification, request-size collapse, per-connection contention) is measurable. TCP service-port slice **54100–54199** (default 54129) — never the fidelity tier's 54000–54099 |

Every measurement states its substrate alongside its instrument (the standing instrument-alignment lesson). A loop-only result on a fabric-sensitive row is scoping evidence, never acceptance.

**Write-amplification instrument (standing row requirement for write/bandwidth rows):** report **device bytes ÷ user bytes** on the DATA namespace (per-row `/proc/diskstats` deltas — meta rides its own namespace, so the data delta is exact), **`wareq-sz` vs the volume block size** (request-size collapse is the fragmentation face of the same failure), and the **`block_free_*` reclaim counters** (`block_free_{discards,discard_bytes,file_punches,punch_bytes,reclaim_skipped}` — a freed-block path that writes instead of deallocating shows here; the 2026-07-27 Write-Zeroes-on-free conviction — plus the async-reclaim family `block_free_reclaim_{queued,queue_bytes,batches,sync_drains,fence_halts,commands,cap_parks,cap_overflow}` (write-wall campaign, 2026-07-31: blocks ÷ `commands` = the live coalesce factor; `cap_parks` = at-cap enqueues parked by park-don't-spill — device commands NEVER issue from the enqueue context; `cap_overflow` = park-bound expiries soft-overflowed into the queue, ≈ 0 steady state): terminal frees queue their discard/punch to the background reclaimer (`src/block_reclaim.rs`, `.benchmarks/2026-07-27-async-block-reclaim.md`) so the displaced-block discard stream never rides the write path; `sync_drains` is the ENOSPC pressure valve and must stay 0 except under real space pressure, `fence_halts` counts entries dropped-without-finish_free because the D0 writer guard fenced/fail-stopped — the reclaimer observes the same `failed` latch the journal-barrier escalation sets, so a fenced zombie never issues destructive discards (0 on healthy mounts; investigate alongside `writer_guard_fenced`) — and the discard/punch ledger still counts per displaced block). `tests/write_amp_rig.sh` is the packaged instrument (kernel/shim/shim-frag sides, per-row stats-inode deltas, charter-rule-4 engagement checks); a write row without its amplification columns is INVALID for scoreboard/acceptance purposes.

**A-B-B-A alternating-order brackets (standing comparison rule, 2026-07-27):** any A-vs-B comparison where a shared store AGES across the runs — overwrite rows against an already-written file set, before/after-binary brackets over one formatted volume, anything where run order changes free-list shape, staging temperature, or device thin-state — must run **A-B-B-A** (both orders) and cite BOTH brackets; a single-order delta on an aging store is an ordering artifact until the reversed bracket reproduces it (the first-write-vs-overwrite field conviction was proven order-independent exactly this way).

**Sustained-state rows (standing measurement rule, 2026-07-29 — user directive: speeds must SUSTAIN):** every headline perf claim must include a **sustained steady-state row** — ≥ 60 s (`--infloop --timelimit` or equivalent), throughput flat across the window (no decay beyond noise between the first and last third) — alongside any short bracket rows. A burst number that decays is a FAILED row, not a result: report the sustained figure as the claim and the burst separately if informative (the field's 1.2 M → 250 k seq-read collapse — a warm-tier burst masking the cold ceiling — is the motivating case). Cache-warm bursts, first-pass-only rates, and any row shorter than the machinery's convergence horizon (governor probing, reclaim drain, tier churn) are label-only.

### Criterion benches (the microbench program, 2026-08-04)

The hot-path machinery carries Criterion coverage (all `harness = false`; coverage table + inaugural numbers: `.benchmarks/2026-08-04-microbench-program.md`). Every bench documents its FIELD-derived input shape in-file with the evidence-note citation — toy inputs are program violations:

- `cargo bench --bench high_concurrency_bench` — in-memory lock / pool / cache contention + dynamic-meta-routing (`route_ino_width`).
- `cargo bench --bench squeezefs_bench` — full FS stack + `CryptoCompressState` compression/encryption micro-benches.
- `cargo bench --bench meta_lv_bench` — KV metadata: bset build/search/merge/fold, tree lookup/insert/scan, the D7 fold regimes, and the `kv_journal` group (entry encode+xxh3 checksum at the D4 create shape + the lever-B publish-batch-64 shape; the §4.4 pt 5 wait-free admit/reserve core).
- `cargo bench --bench ipc_hop_bench` — IPC protocol floors: ring push/pop, slot cycle, payload moves, and the `cqe_doorbell` group (complete elide/wake decision, park ceremony — both Dekker fences priced).
- `cargo bench --bench copy_path_bench` — copy primitives: NT-vs-std ladder across the `SQUEEZEFS_NT_COPY_MIN` floor (near-zero-copy campaign), the `SeveredPool` hit-vs-alloc-miss sever cycle (ingest economy), the zero-heap `StackKey` serve-prelude keys (op economy — the ns/op face of the `SQZ_ALLOC_TRACE` allocs/op==0 law).
- `cargo bench --bench zcrx_bench` — `SpanLedger` grant/release + contended clone-drop, and the chunk-stream PDU parser at page-grain chunk shapes (32×4 KiB-PDU stream / single 128 KiB MDTS-face PDU).
- `cargo bench --bench read_path_bench` — `ReadLaneHold` insert/serve/retire + credit-0 probe + miss probe (EXA 4 MiB-block / 1 MiB-sub-read shape), and the §5.3 classifier's three arms (match, classified-membership reorder, foreign claim).
- `cargo bench --bench write_path_bench` — coverage-union `record_write` (in-order + kernel-split OOO), extent overlay park/fold (`fold_fill` = 16 shape), supersession snapshot + CoW + the `BLOCK_FLUSH_LOCKS` stripe route, and layout publish encode (full-save-1024 vs delta-64 vs delta-apply).
- fuse3 (own workspace): `cd crates/fuse3 && cargo bench` — `fuse3_hot_bench`: ent header codec (in-header decode, attr/entry-out reply encode — the exact session bincode shape), commit-batch drain accounting, kmbuf attach/recycle law on the `sim_anon` venue.

**Bench smoke is part of the required gate:** `cargo bench --benches -- --test` runs one iteration of every bench without measurement (seconds, not minutes). It exists because `meta_lv_bench` sat silently broken from PR 8 until a perf investigation tripped over it — a panicking bench must fail the gate, not wait for the next baseline run.

**Baseline teeth (nightly / perf-PR tier):** `tests/run_bench_baseline.sh` runs the full set (both workspaces), extracts every bench's median, and compares against the committed reference `.benchmarks/criterion-baselines/reference.json` — exiting nonzero past the per-group threshold (default 10 % ns/op; looser only where the venue is structurally noisier: contention 25 %, async fixture-heavy KV groups 20 %, crypto throughput 15 % — the table lives in the script). Baselines are **SAME-BOX RELATIVE tripwires** (the thermally-capped dev box gives relative truth only): the script pins to the quiet cores, refuses to measure when Tctl/Tdie ≥ 80 °C or foreign cargo work is running, and refuses cross-box compares (stamped hostname). Refresh with `tests/run_bench_baseline.sh save` after intentional perf landings and commit the new reference. Per-commit cadence stays SMOKE-ONLY — never the measured compare.

### Profiling command set (release)

#### CPU (perf)

```bash
# One-shot sample while a mount + workload runs
perf record -g --call-graph dwarf -o perf.data -- \
  target/release/squeezefs mount …   # or attach: -p $(pidof squeezefs)

perf report -i perf.data
```

#### Heap (dhat)

```bash
cargo build --release --features dhat-on
# Run workload; on exit dhat prints / writes heap profile per its config
DHAT_OUT=dhat-heap.json target/release/squeezefs mount …
```

#### Causal (coz)

```bash
cargo build --release --features coz-on
coz run --- target/release/squeezefs mount …
```

Use coz/dhat **after** a known-good cargo test gate, against a representative mount.

---

## Module Map (non-obvious wiring)

- `src/main.rs` is the entire CLI: `format`, `status`, `clients`, `claim`, `mount`, `umount`, `bench`, `clone`, `tune`, `config`, `volume` (PR VL3: `add-data`/`list` — durable `vol-` identity, KD-5; VL4/VL5b added drain/remove + meta add/remove/migrate), `fsck`/`scrub` (PR VL6a/VL6b), `defrag` (PR VL7 — §5.7 four-axis model, `--report-only`/`--data`/`--meta`/`--fold`/`--rebalance`, live via the admin lane or offline via the D0-guarded coordinator), `job`, `df`, `storage`, `nvmeof`. `src/lib.rs` is the library surface (incl. `DataVolumeRecord` + `FormatConfig::resolved_data_volumes` legacy grandfathering). (The former `config` fake admin verbs were deleted in the volume-lifecycle program's PR VL1 — `docs/design-volume-lifecycle.md` — and refuse loudly naming their successors.)
- `src/fuse_client.rs` — FUSE daemon + `format_volume_ext` / `SqueezefsFilesystem`.
- `src/defrag.rs` — the PR VL7 online-defrag measurement engine (KD-11 four-axis model: D1 free-space contiguity, D2 file locality, D3 staged-extent pressure, D4 meta node occupancy; `frag_*` gauges + `--report-only`; the §5.8 offline report probe and D0-guarded offline mover harness). Movers live on the job fabric (`src/jobs.rs` `JobType::Defrag{Data,Meta,Fold}`) and REUSE existing machinery: the VL4 `move_one` engine with contiguity-aware destination picks, the W2 fold, the KV SMO compactor.
- `src/fsck.rs` — the PR VL6a/VL6b online fsck: detection classes C1–C7 (incl. the C7 data scrub), the verify-before-report zero-FP ladder (suspect/settle/lease-re-check; allocation-epoch + in-flight-registry + mover-ledger exemptions for C2/C3), offline `--shards k/N` + `merge-reports`, and the §5.6a per-class repair planner/executor (dry-run default, quarantine-first). `src/job_wire.rs` — the §5.1.6 remote job-shard execution wire (enrollment, leases, fencing-checked result proposals, WERO preemption ladder).
- `src/routing.rs` — `DataRouter` (progressive layout: inline / staged / striped) plus the `OnceCell`-held `CryptoCompressState`.
- `src/dlm.rs` — distributed lock manager (`DlmClient`, `acquire_lock`, fencing tokens, heartbeat renewal).
- `src/env_knobs.rs` — the env-knob REGISTRY (name, kind, admissible range, default, purpose for every `SQUEEZEFS_*`/`SQZ_*` variable) plus the startup refusal gate `main` calls before anything is parsed or mounted, and the `bool_knob`/`int_knob`/`opt_int_knob`/`enum_knob` readers every site uses. The value law itself is `crates/squeezefs-ipc/src/env_knob_core.rs`, `#[path]`-shared into the root crate, the fuse3 fork and the preload shim (ENG-10).
- `src/block_allocator.rs`, `src/nvme_dev.rs`, `src/storage.rs` — block allocation + NVMe/LVM pool plumbing.
- `src/block_reclaim.rs` — the background block-reclaim queue (2026-07-27 overwrite-throughput fix): terminal frees enqueue their device reclaim (`BLKDISCARD`/`PUNCH_HOLE`) + `finish_free` here instead of issuing them on the write path; exactly-once by pop ownership, offsets stay non-reallocatable until reclaimed, ENOSPC pressure valve wired into `BlockAllocator::allocate_block`, unmount teardown drains, and the queue observes the D0 writer-guard fence (the per-volume `failed` latch, once per batch): a fenced holder ceases ALL device reclaims permanently — entries drop without `finish_free` (successor recovery owns the accounting) into `block_free_reclaim_fence_halts`. Since the 2026-07-31 write-wall campaign + iteration loop (`.benchmarks/2026-07-31-write-wall.md` — the field's rewrite wall: queue pinned at cap with most reclaims running inline on the write path; the width experiments proved the TARGET deallocate service is the under-load drain ceiling at ANY client width): the worker drains across **demand-derived parallel blocking-pool lanes** (group by device → sort → consecutive-slice chunking so adjacency survives into each lane's coalescer; one lane per `batch_blocks` of a device's queued demand, capped per device; pop-ownership/`processing`-reservation/finish_free-after-reclaim/fence-per-lane invariants unchanged), under the **reclaim manners law** — foreground DEVICE-byte movement + queue below cap ⇒ the drain DEFERS entirely (a stats poller can never hold it deferred — the probe is device-plane bytes, not ops); at-cap ⇒ drain regardless (parked enqueues are foreground writers); idle ⇒ full-width catch-up — and **park-don't-spill**: an at-cap enqueue parks (async, bounded by `SQUEEZEFS_RECLAIM_CAP_PARK_MS`, default 1000) and soft-overflows on expiry; device commands never issue from the enqueue context (the retired inline arm charged a field-measured 12–22 ms synchronous fabric round-trip to the write path). **Since W-4 (e2e perf audit ladder row 14, 2026-09-05, `.benchmarks/2026-09-05-w4-reclaim-derivation.md` — mechanism landed, field rows owed) the park is EVENT-DRIVEN and both liveness constants DERIVE**: the cap's population is queued + in-flight (the count `queue_bytes` always gauged), `finish_free` runs per coalesced range and wakes parked producers (room flows at the drain rate, never in pass-sized bursts), and the quantum is only the stalled-drain safety bound — `bound = clamp(4 × batch_blocks ÷ measured drain rate, 50 ms, 1000 ms)` (the shipped 1 s, which the 2026-09-01 rewrite rows read straight off the p99.9, is now the CEILING; fleet under load ≈ 140 ms), `cap = clamp(measured displacement rate × that room latency, 4096 = the shipped floor, budget/1024 ÷ entry RAM)`. Knobs `SQUEEZEFS_RECLAIM_BATCH_{BLOCKS,MS}` / `SQUEEZEFS_RECLAIM_LANES_PER_DEV` (default 32, idle-engaged) / `SQUEEZEFS_RECLAIM_QUEUE_MAX_BLOCKS` + `SQUEEZEFS_RECLAIM_CAP_PARK_MS` (explicit overrides of the derived defaults — `4096` / `1000` are the A/B levers); counters `block_free_reclaim_{queued,queue_bytes,batches,sync_drains,fence_halts,commands,cap_parks,cap_overflow}` + the derivation faces `block_free_reclaim_{drain_rate,arrival_rate,queue_cap,park_bound_ms,park_tick_wakes}`. The sibling write-path lever: `SQUEEZEFS_INPLACE_OVERWRITE=1` opts a substrate into IN-PLACE eligible full-block overwrites (`write_through_inplace_overwrites`; zero displacement/dealloc — the W1 whole-block face; **default OFF, substrate-measured**: on zram-lz4 targets a slot-replace write costs ≈ 2× a fresh-slot write, so CoW + deferred discard + manners wins there; real-SSD DSM fleets opt in).
- `src/cache/`, `src/tiering/` — tiered cache (GDS / RAM LRU / NVMe staging) and tier selection.
- `src/crypto_compress.rs` — compression (lz4/zstd) + symmetric AEAD encryption, applied across all three write paths via `process_write` / `process_read`. **The key wrap is HKDF-SHA-256 → the volume's own AEAD, both from `ring`** (`src/keyfile.rs`, scheme `hkdf-sha256/aead-kw/v2`; design `docs/design-key-handling.md`): KW-1 / ruling D3 REPLACED the RSA primitive rather than adjudicate `rsa 0.9`'s RUSTSEC-2023-0071 (Marvin) — do not reintroduce `rsa`. Key material resolves from `--encrypt-key`, `SQUEEZEFS_ENCRYPT_KEY_FILE`, or `/etc/squeezefs/keys/<key_id>.key`.
- `src/jobs.rs`, `src/nvmeof/`, `src/config_ops.rs` — maintenance-job worker skeleton (throttle law + queue; the volume-lifecycle fabric extends it), NVMe-oF control, and the offline **guarded** admin verbs over durable on-volume state (cache-path policy, `volume add-data`/state — PR VL3). (The former `src/p2p.rs`/`src/tiering/dht.rs` peer subsystem was unreachable and deleted, ENG-13; its shared cluster TLS core lives on as `src/tiering/cluster_tls.rs` for the job wire.) (Staging crash recovery lives in `src/cache/nvme.rs` — `NvmeStaging::new`; the former `src/recovery.rs` stub was deleted, ENG-12.) The historical `/dev/shm` runtime-config file is DELETED (VL3): live `enable`/`disable` health overrides ride the admin lane (`BackendRouter::set_health_override`); offline enable/disable is durable `DataVolumeRecord.state`.

## Repo-specific conventions

- **Cache-path policy (mount can never override):** staging/cache directories (`--disk-cache-paths`) are declared at **format** and recorded in the format config — the single source of truth. `mount` reads them from the config and **rejects** the flag with a loud error; format without the flag ⇒ a permanently **cache-less** filesystem (RAM tiers + direct block I/O; beyond-inline writes route striped — no staged layout, no conjured default staging dir). Changing paths is the admin op `squeezefs config set-cache-paths <sqmeta-uri> <paths...>` (format-grade live-client refusal; wipes the new dirs so generation stamping starts clean) with `get-cache-paths` for reads. Contracts pinned in `tests/cache_path_policy_tests.rs`.
- **Env-knob convention (ONE law, ENG-10 — `src/env_knobs.rs` is the registry, `crates/squeezefs-ipc/src/env_knob_core.rs` the shared parser `#[path]`-included by the root crate, the fuse3 fork and the shim):** absent = unset/empty/whitespace-only; booleans are `1/true/yes/on` vs `0/false/no/off` (case-insensitive — `=0` DISABLES, including knobs whose default is ON; the retired presence-based form made `SQUEEZEFS_FREE_FORENSICS=0` *enable* forensics); integers carry an admissible range and out-of-range is a **refusal, not a clamp**; explicit > percentage > derived precedence unchanged. A malformed/out-of-range/retired knob **refuses the process at startup**, naming every offender, before anything is opened or mounted (`env_knobs::refusal_report()` in `main`) — so no call site may `panic!` on a knob value (a panic in a mount-time constructor is `abort` under the release profile) and none may silently default one. An unregistered `SQUEEZEFS_*`/`SQZ_*` name is announced as a probable typo, never refused. **Adding a knob means adding a registry entry** — `tests/env_knob_convention_tests.rs` fails the gate on an unregistered knob literal, which is what keeps the knob surface documented. The LD_PRELOAD shim is the one documented asymmetry: it announces a bad value and keeps the default rather than killing its host application. Operator surface: `docs/operations.md` §Environment knobs.
- **Staging/cache file modes (VAL-7b, 2026-08-05):** every staging and read-cache **directory** is `0700` and every **segment ring file** `0600` — on a passthrough volume those bytes are literal plaintext user data, and the segment headers carry staged object keys regardless. `config_ops::create_private_dir_all` is the ONE policy point (`STAGING_DIR_MODE` / `STAGING_FILE_MODE`); it deliberately does **not** use `O_NOFOLLOW`, because the mount layout's `<isolated>/cache_segment → ../cache_segment` symlink (the shared read cache) is load-bearing and `O_DIRECTORY|O_NOFOLLOW` on a symlink is `ENOTDIR`. `O_NOFOLLOW` belongs to the format-grade stamp (`stamp_precleared_staging_dir`), which applies mode AND ownership through one `O_DIRECTORY|O_NOFOLLOW` fd (`fchmod` + `fchown`) — the path-based `chown` it replaced followed a symlink planted between the create and the chown, as root, on an operator-supplied path.
- **`--log-file` is `0600` + `O_NOFOLLOW`** (VAL-7h, `squeezefs::open_log_file` — all three open sites): the log carries device paths, staging dirs and key names; a symlinked target refuses `ELOOP` and, per ENG-3, fails the mount rather than redirecting. **Root subprocess `PATH` is sanitized once at startup** (VAL-7i, `harden_root_subprocess_path` — drops empty/relative/non-dir/group-or-world-writable entries, falls back to the system path, never empty): argv-only was already the discipline, this closes the *resolution* half for the ~63 root subprocess sites.
- **The ADMIN lane runs the data plane's ladder** (VAL-7c): ABI + `build_commit` equality → bootstrap-nonce freshness → `SO_PEERCRED`, in that order, mirroring `handle_hello_msg` so the two cannot drift (`AdminHello` carries the three fields; `IPC_ABI` 3). The administering identity is explicit — `--admin-uid` / `-o admin_uid=N` (`admin_uid_from_options`) — instead of inherited from `SUDO_UID`; an unparseable value keeps the fallback and never widens to root.
- Metadata keys are namespaced via the `fs_key!("suffix")` macro and the global `FS_PREFIX` (`src/lib.rs`). Code that touches metadata keys must go through the macro, not hardcode prefixes.
- `WRITE_VERIFICATION` is a process-global `AtomicBool` toggled by `--write-verification` on mount; read-after-write checksum verification uses it. Library code should call `write_verification_enabled()` rather than reading CLI args.
- The architecture uses the **NVMe / NVMe-oF block** backend as the sole primary data path.

## Stats surface

**Access + census posture (VAL-7a, 2026-08-05):** the `.stats`/`.config` inodes are **mode 0400 owned by the mount uid** (`fuse_client::VIRTUAL_INODE_MODE`; `-o default_permissions` is what enforces it) — the payload maps every backing-device path, staging directory, cached block key and per-inode write custody, so it was never safe world-readable on an `allow_other` mount. The **key census** (`read_lru_keys`, `write_lru_keys`, `nvme_staged_write_file_ids`, `nvme_read_cache_block_keys`, `active_writes`) is **opt-in** behind `SQUEEZEFS_STATS_KEY_CENSUS=1` (read live, self-reported as `stats_key_census`); the census-free count gauges always export and are what tooling keys on — `read_lru_key_count`, `write_lru_key_count`, `nvme_staged_write_file_count`, `nvme_read_cache_block_count`, `active_write_block_count` (the last two are `squeezefs umount`'s unflushed-staged-write check). Multi-user resource posture is **single-tenant by declaration** (VAL-7d): no per-uid quota/rate-limit/accounting in the FUSE path, every R5 component process-global — stated in `docs/operations.md` §Multi-user mounts.

Mounted volumes expose process metrics under the virtual **stats** inode (JSON), including layout mix, bg admission, uring queue-full, and lease acquire outcomes. The 2026-08 frontier/rewrite wave adds: the **zcrx lane** family (`zcrx_fill_bytes`, `zcrx_gather_bytes`, `zcrx_lane_poisoned` must-stay-0, area/violation gauges — engagement law `gather ≡ fill`, closure byte-exact; `docs/design-zcrx-read-lane.md`), the **fuse3 transport-geometry/zc** gauges (`transport_max_write`/`transport_max_pages` — the negotiated-geometry law that replaced the hardcoded 256-page plan; `fuse3_kmbuf_negotiated` 0/1, `fuse3_zc_replies`, and the `commit_flush` fifth phase of `read/write_transport_phase_ns`), and the **rewrite program P0** families (`rewrite_{blocks,user_bytes,device_write_bytes}` — the vehicle-blind `rewrite_amp` SLO attribution, gates ≤1.05 + zero mid-row discards; `rewrite_shadow_{swaps,bytes,fallbacks,fence_drops,open_epochs}` with `fence_drops` must-stay-0 and `open_epochs`→0 at quiesce; the discard-elision debt ledger whose law is `queued + elided ≡ terminal frees`; supersession engagement counters — `docs/design-rewrite-program.md`). Prefer these for live regression signals over ad-hoc logging. Per-volume metadata format + durability fields (`meta_format_version`, `meta_volume_atomicity[_physical]`) and the `meta_kv_*` family are listed under **Metadata format: v3 CoW KV (the only format)**; the metadata-throughput program's families (`writer_guard_*`, `meta_commit_group_*` / `meta_conveyor_*`, `meta_kv_fold_*`, `meta_kv_times_echo_*`, `fuse_op_phase_ns` / `fuse_create_under_lock_ns` / `fuse_op_watchdog_overdue`, `transport_commit_batch*`, `writeback_{superseded_noops,stale_token_retries,orphan_discards}`) are listed under **Metadata-throughput program (Implemented 2026-07)**; the L4 interception program (2026-07-19, `docs/design-preload-interception.md` §8, closing `.benchmarks/2026-07-19-l4-interception-closing.md`) adds the session-host families `ipc_sessions_{active,total,poisoned,reaped}`, `ipc_binds[_dev_override]`, `ipc_bind_refused_{version,nonce,flags,mode,budget,peercred,disabled}` (`disabled` = control-plane-only mounts refusing data HELLOs — the expected default-mount posture, counted apart so the security classes stay clean; Rev 8), `ipc_admission_refusals`, `ipc_arena_bytes`, `ipc_descriptor_rejects` (with `ipc_descriptor_rejects`/`ipc_sessions_poisoned` as must-stay-0 tripwires), the data-plane families `ipc_ops_{read,write}` / `ipc_bytes_{in,out}` (**the charter-rule-4 engagement instrument** — an il benchmark row is INVALID unless their deltas account for the row's ops), `ipc_fast_path_serves` vs `ipc_async_handoffs` + the `ipc_fast_path_{lock,miss}_demotions` split (fast-path health), `ipc_service_threads`, the W1 invalidation pair `ipc_inval_{notifies,suppressed}`, and the il hold-probe engagement pair `ipc_hold_probe_{serves,misses}` (2026-08-03 campaign, `.benchmarks/2026-08-03-il-hold-probe.md`: the read-lane hold is the §5.5.1 sync fast path's fourth leg — staging → hot → hold → NVMe read-cache, the handler ladder's order — probed lock-free on the service thread before any demotion/direct-drive; ledger-visible serves dispatch their R1b ceremony to the fuse3 handler lanes exactly-once; a bracket row is INVALID unless the serves delta accounts for its hold serves; 0 by construction under `SQUEEZEFS_READ_LANE=0` and on `direct_device_true` mounts); the shim-parity campaign (2026-07-28, `.benchmarks/2026-07-28-shim-parity.md`, design Rev 14) adds the placed-sever family `placed_{severs,adoptions,merge_elides,sever_fallbacks,assemblies,assembly_bytes}` — the 1-copy ring write path (whole-block ring chunks sever directly into a shared per-(ino, block) assembly that the first merge ADOPTS as the `ActiveBlockBuf` backing; `merge_elides` counts copies avoided, `sever_fallbacks` counts ineligible shapes riding the pooled path, `placed_assembly_bytes` is the non-sheddable `placed_assemblies` R5 component; the write_matrix **il-vs-kernel parity verdict** is the governing instrument: kernel and IPC at minimum at par, IPC ahead in the majority — any beyond-band il loss exits nonzero); the near-zero-copy campaign (2026-07-31, `.benchmarks/2026-07-31-near-zero-copy.md`) adds `nt_copy_bytes` (NT-store engagement at the two DMA-destined copy sites — a census/lever row is INVALID unless its delta accounts for the row's merge/sever bytes; 0 on `SQUEEZEFS_NT_COPY=0` mounts and sub-256 KiB shapes) and the live arena-THP engagement instrument is the daemon's `smaps_rollup` `ShmemPmdMapped` sampled DURING a row (sessions reap at client exit — `tests/copy_census_rig.sh` is the packaged census instrument); the NUMA-affinity campaign (2026-07-31, `perf/numa-affinity`, `.benchmarks/2026-07-31-numa-affinity.md` — measured field verdict: baseline crossing ≈ 50 % proven on every high-volume pass, stage-1 placement takes il rows to 1.000 local, **wr-il sustained +23.7 % / rd-il +12.7 %**, kern rows par, A0 ≈ B pins attribution) adds the **UPI-crossing estimate instrument** `numa_{local,remote}_bytes` (daemon copy passes: the §5.5.2 ring-write sever + the §5.5.1 arena completion serves) and `fuse3_numa_{local,remote}_bytes` (transport passes: FUSE_WRITE lease delivery ≈ the K1 kernel copy, reply-body serves) — classification is DISTANCE-based (`numa_core::is_local_choice`: local iff the memory node was a minimal-distance choice from the executing node — honest on any topology; unknown nodes never counted) plus `numa_nodes` (the map's node count; 1 = the whole machinery is structurally a no-op). The N-topology-general nearest-resource map lives in `crates/squeezefs-ipc/src/numa_core.rs` (`#[path]`-shared into the root crate and fuse3 per the `thp.rs` precedent, runtime-built from sysfs node distances/cpulists, injectable in tests — contracts in `tests/numa_affinity_tests.rs` cover single-node/2×1:1/NPS-4-node/CXL shapes); placement actions (session-arena `mbind`+populate composed BEFORE the THP collapse, CPU-weighted owner→node partition + locality-first session admission, service-thread + fuse3 queue-worker node pins ∩ process mask, node-targeted handler-lane handoffs `tpc_spawn_on_node`, per-queue payload-arena node binding) are all gated by **`SQUEEZEFS_NUMA=0`** (the A/B lever — the instrument stays alive on both sides) and are structurally inert on single-node machines; the ipc op-economy campaign (2026-07-28, `.benchmarks/2026-07-28-ipc-op-economy.md`) adds the completion-doorbell pair `ipc_cqe_wake_{writes,elided}` (the `transport_wake_*` discipline for the completion direction: wakes are paid only toward parked reapers — `writes/(writes+elided) ≈ 1` under a saturated reaping client means the parked gate stopped eliding; the same campaign made the warm §5.5.1 serve prelude allocation-free, pinned by `tests/ipc_op_economy_tests.rs`); the **wake-economy campaign** (design `docs/design-il-wake-economy.md`; PR 1 landed instruments-first 2026-08-14) adds `ipc_cqe_wake_collapsed` (the PR 2 wake-collapse latch's elisions — mark-passed completions toward a parked reaper whose FUTEX_WAKE the per-era latch elided; when PR 2 lands the wake-economy gauge's denominator becomes `writes/(writes+elided+collapsed)`; **the latch is MARK-VALUED since 2026-09-06** — `wake_paid_mark` records the mark the era's pay satisfied and a completion collapses iff it reads that same mark, with NO parker-side clear: the v3 0/1 flag + clear lost a wake when an already-REAPED op's late doorbell completion (the daemon publishes DONE, then bumps) won the era's CAS toward a parker not yet asleep, and the parker's real completion collapsed — a strand to the reap park's age bound, a latency tail never a hang; loom `ipc_cqe_latched_reaped_prior_completion_never_strands` with futex-bucket fidelity is the pin, `.benchmarks/2026-09-06-cqe-doorbell-lost-wake.md` the record; IPC_ABI unchanged — no layout change, KD-7 guards the word's meaning) and `ipc_cqe_pass_wake_flushes` (the decision-gated PR 4 pass-scoped wake flush; `flushes ≤ sessions × passes` by construction) — both **structurally 0 until their mechanisms land**, and `.benchmarks/rigs/2026-08-14-wake-economy-rig.sh` is the campaign's per-row instrument (PSI + wakes/s + gauge + engagement columns, pre-fill rule built in); the L3 transport-economy program (2026-07-18) adds `transport_wake_{writes,elided}` — queue-eventfd wake coalescing (lever B): `writes/(writes+elided) ≈ 1` under saturated load means the coalescer stopped eliding (evidence `.benchmarks/2026-07-18-l3-transport-economy.md`); the killpriv-v2 campaign (2026-07-28, `.benchmarks/2026-07-28-fuse-killpriv-v2.md`) adds `fuse_killpriv_negotiated` (0/1 per mount — 1 ⇒ `FUSE_HANDLE_KILLPRIV_V2` accepted at INIT: the kernel's per-write(2) `GETXATTR("security.capability")` killpriv probe is gone and the daemon owns the clearing law — suid always, sgid only if group-exec, caps xattr drop; 0 = pre-5.11 kernel or the testing-only `SQUEEZEFS_FUSE_NO_KILLPRIV=1` escape) and `fuse_killpriv_clears` (clears performed across flagged WRITE / O_TRUNC OPEN / SETATTR and the il-parity ring writes; steady growth on workloads that never touch priv'd files = known-clean-latch regression); the random-small-write program's families (`patch_*` incl. the `patch_ineligible_*` decision ledger and the `patch_edge_rmw_reads`/`write_path_seed_read_bytes` must-stay-0 tripwires, `active_block_ooo_runs`, `write_through_blocks`, `overwrite_seed_*`, `parked_extent_bytes`/`parked_full_buffer_bytes`, `extent_*`, `fold_*`, `staged_rider_*`, `extent_records_*`, `compress_stored_raw`) are listed under **Random-small-write program (Implemented 2026-07)**; the volume-lifecycle program's families (`job_*` incl. `job_remote_*`, `evacuate_*`, `volume_preflight_refusals`, `placement_table_refreshes` / `backend_placement_picks` / `backend_fill_{ratio,spread}`, `fsck_*` incl. the must-stay-0 `fsck_findings` and the repair/quarantine counters, `scrub_*`, `defrag_*` / `frag_d1..d4`, `fuse_reserved_xattr_refusals`) are listed under **Volume lifecycle & online maintenance program (Implemented 2026-07)**; the write-pipeline-depth campaign (2026-07-27, `src/write_pipeline.rs`, `.benchmarks/2026-07-27-write-pipeline-depth.md`) adds `write_pipeline_{inflight_blocks,inflight_bytes,depth_target,admission_waits,fence_drops}` — the internal depth instrument (the rig's iostat `aqu-sz` is the external one; the 2026-07-31 write-wall campaign adds the ALWAYS-ON `write_pipeline_phase_ns` ten-phase residence histograms — admit_wait / detach_lag / lock_wait / crypto / allocate / dma / publish / displaced_free / inval_tail / total, ungated on the stats inode so field mounts can name residence terms without a remount: the fresh-wall kill instrument, `.benchmarks/2026-07-31-write-wall.md`): `depth_target` is the runtime-derived BDP target (NO fixed depth anywhere — user directive; A/B lever `SQUEEZEFS_WRITE_PIPELINE_DEPTH_BLOCKS`, measurement only), `admission_waits` is the honest-writer-backpressure gauge, `fence_drops` is a must-stay-0 tripwire (investigate alongside `writer_guard_fenced`), and in-flight bytes ride the R5 budget as the non-sheddable `write_pipeline_inflight` component (Red clamps the admission target to its floor — converge by completion, never OOM); the write-commit-economy campaign (2026-07-30, `.benchmarks/2026-07-30-write-commit-economy.md` — per-ino publish coalescing + layout delta records: the O(file-size)-per-publish journal term collapsed to O(batch)) adds `layout_publish_{batches,batched_blocks}` (the lever-1 engagement instrument — blocks/batch is the live coalesce factor; ≈ 1 on a streaming shape means the publish conveyor regressed to the serialized per-block posture) and `layout_{delta,full}_commits` / `layout_delta_{bytes,folds}` (lever-2 engagement: streaming publishes onto a persisted inline base must grow `layout_delta_commits`, and `layout_delta_bytes` vs `meta_kv_journal_bytes` is the collapsed-byte instrument; knobs `SQUEEZEFS_PUBLISH_COALESCE_MAX` default 64 / `SQUEEZEFS_LAYOUT_DELTA_MAX_CHAIN` default 64 — `1`/`0` are the A/B levers, never operational escapes; on-disk face: `KV_LAYOUT_DELTAS` = superblock incompat bit 5, stamped+barriered before a volume's first delta record, never on untouched volumes); the rewrite-publish-drain campaign (2026-08-01, `.benchmarks/2026-08-01-rewrite-publish-drain.md` — the 7.7× rewrite publish tax attributed to commit-rate-coupled queueing at a ~92 %-utilized M7 conveyor, NOT rewrite mechanics or the journal device) adds the ALWAYS-ON `publish_phase_ns` decomposition (queue_wait / lock_wait / base_fetch / apply / save_encode / blob_write / meta_commit + the commit interior `commit_{guard,inode_read,slot_probe,tx_wait}` / total) and `meta_txpass_phase_ns` (the M7 pass interior: tx_queue_wait + pass_{admission,leaf_locks,journal_write,total}), the **base-provenance ledger** `publish_base_{dirty_serves,ram_serves,fetches}` (closes EXACTLY against the pass count; steady rewrite must run ram_serves ≈ passes, fetches ≈ first-touch — Lever A: a clean cached entry whose `layout_base_token` matches the ino's CURRENT fencing era serves as the RMW base, foreign eras refetch), the full-save decision ledger `publish_full_save_{indirect,chain_cap,other}` (chain_cap ≈ delta_commits/`SQUEEZEFS_LAYOUT_DELTA_MAX_CHAIN` is the restored re-base cadence), `layout_indirect_map_read{s,_bytes}` + `publish_indirect_blob_bytes` (the big-file publish faces, structurally 0 on inline-map shapes), and the Lever B aggregation pair `publish_commit_group{s,_saves}` (delta-class layout saves drain into ONE multi-ino KvTx per per-volume conveyor window — one journal entry/ring write/fan-out per window; saves/groups is the live aggregation factor; knob `SQUEEZEFS_PUBLISH_COMMIT_GROUP_MAX` — default derives from `SQUEEZEFS_META_COMMIT_BATCH_TXS`, `1` = the pre-campaign per-save commit path, the A/B lever); the probe-up governor campaign (2026-07-29, `.benchmarks/2026-07-29-probe-up-governor.md` — the pure-BDP target is a self-fulfilling equilibrium: the field left +18 % behind at its own converged depth) adds `write_pipeline_depth_target_base` (the un-probed BDP target — current-vs-base is the live probe contribution) and `write_pipeline_depth_probe_{ups,backoffs}` (BBR-flavored probe engagement vs dead-gain retreats: probes launch only on saturated epochs with R5 headroom, adopt only when delivery responds, retreat + duty-cycle cool down when marginal gain dies, and the multiplier bleeds to 1.0 on unsaturated epochs — `ups` stays 0 on latency-sensitive/low-offered-load mounts by design).
Mounted volumes expose process metrics under the virtual **stats** inode (JSON), including layout mix, bg admission, uring queue-full, and lease acquire outcomes. The 2026-08 frontier/rewrite wave adds: the **zcrx lane** family (`zcrx_fill_bytes`, `zcrx_gather_bytes`, `zcrx_lane_poisoned` must-stay-0, area/violation gauges — engagement law `gather ≡ fill`, closure byte-exact; `docs/design-zcrx-read-lane.md`), the **fuse3 transport-geometry/zc** gauges (`transport_max_write`/`transport_max_pages` — the negotiated-geometry law that replaced the hardcoded 256-page plan; `transport_max_readahead` — the kernel's readahead limit echoed VERBATIM at INIT and DELIBERATELY independent of the R2 prefetch window, FUSE-4d; `fuse3_kmbuf_negotiated` 0/1, `fuse3_zc_replies`, and the `commit_flush` fifth phase of `read/write_transport_phase_ns`), the **pre-RC transport/safety tripwires** (all must-stay-0: `transport_cq_overflows` with the `transport_cq_nodrop` capability probe — dropped ring completions, FUSE-3f; `read_dest_overruns` — zero-copy read destinations refused because the serve exceeded the transport's registered window, FUSE-4e; `open_count_stranded` — an inode reaching its FINAL forget with open handles still counted, i.e. a lost RELEASE, POSIX-14 ⊕ FUSE-3k's lookup refcount; `ipc_direct_reap_stalls` — direct-drive reaper enter failures observed with DMA in flight, MEM-7c), and the **rewrite program P0** families (`rewrite_{blocks,user_bytes,device_write_bytes}` — the vehicle-blind `rewrite_amp` SLO attribution, gates ≤1.05 + zero mid-row discards; `rewrite_shadow_{swaps,bytes,fallbacks,fence_drops,open_epochs}` with `fence_drops` must-stay-0 and `open_epochs`→0 at quiesce; the discard-elision debt ledger whose law is `queued + elided ≡ terminal frees`; supersession engagement counters — `docs/design-rewrite-program.md`). Prefer these for live regression signals over ad-hoc logging. Per-volume metadata format + durability fields (`meta_format_version`, `meta_volume_atomicity[_physical]`) and the `meta_kv_*` family are listed under **Metadata format: v3 CoW KV (the only format)**; the metadata-throughput program's families (`writer_guard_*`, `meta_commit_group_*` / `meta_conveyor_*`, `meta_kv_fold_*`, `meta_kv_times_echo_*`, `fuse_op_phase_ns` / `fuse_create_under_lock_ns` / `fuse_op_watchdog_overdue`, `transport_commit_batch*`, `writeback_{superseded_noops,stale_token_retries,orphan_discards}`) are listed under **Metadata-throughput program (Implemented 2026-07)**; the L4 interception program (2026-07-19, `docs/design-preload-interception.md` §8, closing `.benchmarks/2026-07-19-l4-interception-closing.md`) adds the session-host families `ipc_sessions_{active,total,poisoned,reaped}`, `ipc_binds[_dev_override]`, `ipc_bind_refused_{version,nonce,flags,mode,budget,peercred,disabled}` (`disabled` = control-plane-only mounts refusing data HELLOs — the expected default-mount posture, counted apart so the security classes stay clean; Rev 8), `ipc_admission_refusals`, `ipc_arena_bytes`, `ipc_descriptor_rejects` (with `ipc_descriptor_rejects`/`ipc_sessions_poisoned` as must-stay-0 tripwires), the data-plane families `ipc_ops_{read,write}` / `ipc_bytes_{in,out}` (**the charter-rule-4 engagement instrument** — an il benchmark row is INVALID unless their deltas account for the row's ops), `ipc_fast_path_serves` vs `ipc_async_handoffs` + the `ipc_fast_path_{lock,miss}_demotions` split (fast-path health), `ipc_service_threads`, the W1 invalidation pair `ipc_inval_{notifies,suppressed}`, and the il hold-probe engagement pair `ipc_hold_probe_{serves,misses}` (2026-08-03 campaign, `.benchmarks/2026-08-03-il-hold-probe.md`: the read-lane hold is the §5.5.1 sync fast path's fourth leg — staging → hot → hold → NVMe read-cache, the handler ladder's order — probed lock-free on the service thread before any demotion/direct-drive; ledger-visible serves dispatch their R1b ceremony to the fuse3 handler lanes exactly-once; a bracket row is INVALID unless the serves delta accounts for its hold serves; 0 by construction under `SQUEEZEFS_READ_LANE=0` and on `direct_device_true` mounts); the shim-parity campaign (2026-07-28, `.benchmarks/2026-07-28-shim-parity.md`, design Rev 14) adds the placed-sever family `placed_{severs,adoptions,merge_elides,sever_fallbacks,assemblies,assembly_bytes}` — the 1-copy ring write path (whole-block ring chunks sever directly into a shared per-(ino, block) assembly that the first merge ADOPTS as the `ActiveBlockBuf` backing; `merge_elides` counts copies avoided, `sever_fallbacks` counts ineligible shapes riding the pooled path, `placed_assembly_bytes` is the non-sheddable `placed_assemblies` R5 component; the write_matrix **il-vs-kernel parity verdict** is the governing instrument: kernel and IPC at minimum at par, IPC ahead in the majority — any beyond-band il loss exits nonzero); the near-zero-copy campaign (2026-07-31, `.benchmarks/2026-07-31-near-zero-copy.md`) adds `nt_copy_bytes` (NT-store engagement at the two DMA-destined copy sites — a census/lever row is INVALID unless its delta accounts for the row's merge/sever bytes; 0 on `SQUEEZEFS_NT_COPY=0` mounts and sub-256 KiB shapes) and the live arena-THP engagement instrument is the daemon's `smaps_rollup` `ShmemPmdMapped` sampled DURING a row (sessions reap at client exit — `tests/copy_census_rig.sh` is the packaged census instrument); the NUMA-affinity campaign (2026-07-31, `perf/numa-affinity`, `.benchmarks/2026-07-31-numa-affinity.md` — measured field verdict: baseline crossing ≈ 50 % proven on every high-volume pass, stage-1 placement takes il rows to 1.000 local, **wr-il sustained +23.7 % / rd-il +12.7 %**, kern rows par, A0 ≈ B pins attribution) adds the **UPI-crossing estimate instrument** `numa_{local,remote}_bytes` (daemon copy passes: the §5.5.2 ring-write sever + the §5.5.1 arena completion serves) and `fuse3_numa_{local,remote}_bytes` (transport passes: FUSE_WRITE lease delivery ≈ the K1 kernel copy, reply-body serves) — classification is DISTANCE-based (`numa_core::is_local_choice`: local iff the memory node was a minimal-distance choice from the executing node — honest on any topology; unknown nodes never counted) plus `numa_nodes` (the map's node count; 1 = the whole machinery is structurally a no-op). The N-topology-general nearest-resource map lives in `crates/squeezefs-ipc/src/numa_core.rs` (`#[path]`-shared into the root crate and fuse3 per the `thp.rs` precedent, runtime-built from sysfs node distances/cpulists, injectable in tests — contracts in `tests/numa_affinity_tests.rs` cover single-node/2×1:1/NPS-4-node/CXL shapes); placement actions (session-arena `mbind`+populate composed BEFORE the THP collapse, CPU-weighted owner→node partition + locality-first session admission, service-thread + fuse3 queue-worker node pins ∩ process mask, node-targeted handler-lane handoffs `tpc_spawn_on_node`, per-queue payload-arena node binding) are all gated by **`SQUEEZEFS_NUMA=0`** (the A/B lever — the instrument stays alive on both sides) and are structurally inert on single-node machines; the ipc op-economy campaign (2026-07-28, `.benchmarks/2026-07-28-ipc-op-economy.md`) adds the completion-doorbell pair `ipc_cqe_wake_{writes,elided}` (the `transport_wake_*` discipline for the completion direction: wakes are paid only toward parked reapers — `writes/(writes+elided) ≈ 1` under a saturated reaping client means the parked gate stopped eliding; the same campaign made the warm §5.5.1 serve prelude allocation-free, pinned by `tests/ipc_op_economy_tests.rs`); the L3 transport-economy program (2026-07-18) adds `transport_wake_{writes,elided}` — queue-eventfd wake coalescing (lever B): `writes/(writes+elided) ≈ 1` under saturated load means the coalescer stopped eliding (evidence `.benchmarks/2026-07-18-l3-transport-economy.md`); the killpriv-v2 campaign (2026-07-28, `.benchmarks/2026-07-28-fuse-killpriv-v2.md`) adds `fuse_killpriv_negotiated` (0/1 per mount — 1 ⇒ `FUSE_HANDLE_KILLPRIV_V2` accepted at INIT: the kernel's per-write(2) `GETXATTR("security.capability")` killpriv probe is gone and the daemon owns the clearing law — suid always, sgid only if group-exec, caps xattr drop; 0 = pre-5.11 kernel or the testing-only `SQUEEZEFS_FUSE_NO_KILLPRIV=1` escape) and `fuse_killpriv_clears` (clears performed across flagged WRITE / O_TRUNC OPEN / SETATTR and the il-parity ring writes; steady growth on workloads that never touch priv'd files = known-clean-latch regression); the random-small-write program's families (`patch_*` incl. the `patch_ineligible_*` decision ledger and the `patch_edge_rmw_reads`/`write_path_seed_read_bytes` must-stay-0 tripwires, `active_block_ooo_runs`, `write_through_blocks`, `overwrite_seed_*`, `parked_extent_bytes`/`parked_full_buffer_bytes`, `extent_*`, `fold_*`, `staged_rider_*`, `extent_records_*`, `compress_stored_raw`) are listed under **Random-small-write program (Implemented 2026-07)**; the volume-lifecycle program's families (`job_*` incl. `job_remote_*`, `evacuate_*`, `volume_preflight_refusals`, `placement_table_refreshes` / `backend_placement_picks` / `backend_fill_{ratio,spread}`, `fsck_*` incl. the must-stay-0 `fsck_findings` and the repair/quarantine counters, `scrub_*`, `defrag_*` / `frag_d1..d4`, `fuse_reserved_xattr_refusals`) are listed under **Volume lifecycle & online maintenance program (Implemented 2026-07)**; the write-pipeline-depth campaign (2026-07-27, `src/write_pipeline.rs`, `.benchmarks/2026-07-27-write-pipeline-depth.md`) adds `write_pipeline_{inflight_blocks,inflight_bytes,depth_target,admission_waits,fence_drops}` — the internal depth instrument (the rig's iostat `aqu-sz` is the external one; the 2026-07-31 write-wall campaign adds the ALWAYS-ON `write_pipeline_phase_ns` ten-phase residence histograms — admit_wait / detach_lag / lock_wait / crypto / allocate / dma / publish / displaced_free / inval_tail / total, ungated on the stats inode so field mounts can name residence terms without a remount: the fresh-wall kill instrument, `.benchmarks/2026-07-31-write-wall.md`): `depth_target` is the runtime-derived BDP target (NO fixed depth anywhere — user directive; A/B lever `SQUEEZEFS_WRITE_PIPELINE_DEPTH_BLOCKS`, measurement only), `admission_waits` is the honest-writer-backpressure gauge, `fence_drops` is a must-stay-0 tripwire (investigate alongside `writer_guard_fenced`), and in-flight bytes ride the R5 budget as the non-sheddable `write_pipeline_inflight` component (Red clamps the admission target to its floor — converge by completion, never OOM); the write-commit-economy campaign (2026-07-30, `.benchmarks/2026-07-30-write-commit-economy.md` — per-ino publish coalescing + layout delta records: the O(file-size)-per-publish journal term collapsed to O(batch)) adds `layout_publish_{batches,batched_blocks}` (the lever-1 engagement instrument — blocks/batch is the live coalesce factor; ≈ 1 on a streaming shape means the publish conveyor regressed to the serialized per-block posture) and `layout_{delta,full}_commits` / `layout_delta_{bytes,folds}` (lever-2 engagement: streaming publishes onto a persisted inline base must grow `layout_delta_commits`, and `layout_delta_bytes` vs `meta_kv_journal_bytes` is the collapsed-byte instrument; knobs `SQUEEZEFS_PUBLISH_COALESCE_MAX` default 64 / `SQUEEZEFS_LAYOUT_DELTA_MAX_CHAIN` default 64 — `1`/`0` are the A/B levers, never operational escapes; on-disk face: `KV_LAYOUT_DELTAS` = superblock incompat bit 5, stamped+barriered before a volume's first delta record, never on untouched volumes); the rewrite-publish-drain campaign (2026-08-01, `.benchmarks/2026-08-01-rewrite-publish-drain.md` — the 7.7× rewrite publish tax attributed to commit-rate-coupled queueing at a ~92 %-utilized M7 conveyor, NOT rewrite mechanics or the journal device) adds the ALWAYS-ON `publish_phase_ns` decomposition (queue_wait / lock_wait / base_fetch / apply / save_encode / blob_write / meta_commit + the commit interior `commit_{guard,inode_read,slot_probe,tx_wait}` / total) and `meta_txpass_phase_ns` (the M7 pass interior: tx_queue_wait + pass_{admission,leaf_locks,journal_write,total}), the **base-provenance ledger** `publish_base_{dirty_serves,ram_serves,fetches}` (closes EXACTLY against the pass count; steady rewrite must run ram_serves ≈ passes, fetches ≈ first-touch — Lever A: a clean cached entry whose `layout_base_token` matches the ino's CURRENT fencing era serves as the RMW base, foreign eras refetch), the full-save decision ledger `publish_full_save_{indirect,chain_cap,other}` (chain_cap ≈ delta_commits/`SQUEEZEFS_LAYOUT_DELTA_MAX_CHAIN` is the restored re-base cadence), `layout_indirect_map_read{s,_bytes}` + `publish_indirect_blob_bytes` (the big-file publish faces, structurally 0 on inline-map shapes), and the Lever B aggregation pair `publish_commit_group{s,_saves}` (delta-class layout saves drain into ONE multi-ino KvTx per per-volume conveyor window — one journal entry/ring write/fan-out per window; saves/groups is the live aggregation factor; knob `SQUEEZEFS_PUBLISH_COMMIT_GROUP_MAX` — default derives from `SQUEEZEFS_META_COMMIT_BATCH_TXS`, `1` = the pre-campaign per-save commit path, the A/B lever); the probe-up governor campaign (2026-07-29, `.benchmarks/2026-07-29-probe-up-governor.md` — the pure-BDP target is a self-fulfilling equilibrium: the field left +18 % behind at its own converged depth) adds `write_pipeline_depth_target_base` (the un-probed BDP target — current-vs-base is the live probe contribution) and `write_pipeline_depth_probe_{ups,backoffs}` (BBR-flavored probe engagement vs dead-gain retreats: probes launch only on saturated epochs with R5 headroom, adopt only when delivery responds, retreat + duty-cycle cool down when marginal gain dies, and the multiplier bleeds to 1.0 on unsaturated epochs — `ups` stays 0 on latency-sensitive/low-offered-load mounts by design).

**Pre-RC hardening families** (the 2026-08 spec §7/§11/§12 wave — every one verified against `src/fuse_client.rs`'s stats JSON): the **must-stay-0 tripwire set** `data_dma_fence_refusals` (RES-6 — data-plane DMA submissions refused because this mount's D0 writer guard is fenced/fail-stopped; the write-path twin of `block_free_reclaim_fence_halts`, investigate alongside `writer_guard_fenced`), `job_worker_panics` (RES-7 — a job execution UNWOUND; the worker survives and the record flips `Failed` loudly, but the panic is a bug), `detached_task_panics` (RES-8 — a detached data-path task unwound; nothing joins one, so this counter is the only record its work was lost), `invariant_tripwires` (RES-22 — runtime concurrency-outcome violations reported through `crate::note_invariant_tripwire` instead of a `debug_assert!` panic inside a handler task, the class that produced a lost-reply stall — the `transport_lease_overlong` precedent), and `writeback_errors_latched` (POSIX-16 close-time error reporting — the errseq equivalent: growth means close-time data loss IS being reported to applications, which is the point, and something upstream is failing). Plus: `data_volume_write_cache` (DUR-2 — the DATA plane's hardware durability face beside the metadata one, `<backing_dev>=<class>` per distinct data device; `write-back` means acknowledged writes stay volatile until `NvmeBlockDev::flush` completes, the barrier the fsync path issues), `transport_unparked_commits` (the §5.4 park ledger's closure half — `parked − unparked` at quiesce is the WEDGE count; parking itself is the re-arm gate WORKING, so closure is the invariant, not absence — TEST-7 #1's fix), `lease_retry_waits` / `lease_retry_exhaustions` (the POSIX-5 retry ladder: waits lost then won on a later attempt vs. the ladder giving up — growth on the first with 0 on the second is the ladder working), `lseek_holes_reported` (POSIX-2 sparse-export engagement — 0 across a `cp --sparse`/`tar -S` run means no tool ever learned about a hole), and the POSIX-4 pair `meta_parent_scans` / `readdir_parent_memo_hits` (reverse-dentry SCANS — the unindexed O(total dentries) walk, legitimate only on the `open_by_handle_at` reconnect path — vs `..` syntheses served from the parent memo; scans growing per `readdir` means every `ls`/`find`/`du` is paying the scan per directory again). R5 gained the eviction-channel components `read_lru_evict_channel` / `hot_block_evict_channel` (floor 0 — warmth is the cheapest sacrifice — weight 1, and a real shed that clamps SEND-side admission: parked victims belong to the draining worker and are never torn; only caches that ARMED a dehydration receiver park anything, which is why `write_lru` has no channel). **DLM stage S4** (spec §6.9; `src/dlm_slot.rs`, design note `docs/design-dynamic-meta-routing.md` §9) adds the lock-authority trio `dlm_mode` (`solo` = this node is the lock master for every metadata slot — the only shipped mode; lock objects home on the durable slot map via `route_ino_width`, so the lock master and the metadata authority are the same process by construction), `dlm_rpcs` (**must stay 0** in solo mode — by construction, since a foreign home is refused loud at the site the remote acquire will occupy, never granted locally) and `dlm_term` (the process-wide durable writer era every fencing token this mount mints carries; the per-volume face is `writer_guard_term`). **DLM S7** (`src/data_custody.rs`; contracts `tests/dlm_data_fence_tests.rs`, guarantee rows in `docs/operations.md` §The DATA plane) adds the data plane's custody-epoch face: `data_dma_epoch_refusals` (the CLASS SPLIT of the `data_dma_fence_refusals` tripwire — submissions refused because the custody epoch they were authorized under is no longer current, i.e. custody MOVED under an in-flight authorization, as opposed to this mount's own D0 latch firing; ⊆ its parent, must stay 0), `dlm_quarantined_offsets` / `dlm_quarantine_releases` (the dead-epoch **do-not-reallocate** quarantine — admission takes the offset out of the free list, a quarantined offset's `finish_free` DEFERS its publish, and only a drain proof releases it: the landed WERO preempt of the dead epoch's host on a PR substrate, recovery's proof of death otherwise; a gauge that never falls is honestly-unavailable space, never a leak — allocation refuses `StorageFull` rather than hand a possibly-live zombie's offset to a new owner), and `data_plane_fence_mode` (1 = a WERO rtype-3 reservation is held on every data namespace, so a fenced host's DMA is rejected by the DEVICE — the data-plane twin of `writer_guard_mode`; the opt-in is `SQUEEZEFS_MULTI_WRITER=1`, which REFUSES the mount loud on a non-PR substrate or an unstamped format — incompat bit 11, stamped by the default format since the rung-10b flip; `--single-writer`/pre-flip volumes upgrade offline). The authorization point is ONE function (`data_custody::authorize_dma`): every DMA submission passes it, `write_block_authorized` is the form that carries an epoch captured when custody was established (the write-pipeline permit is the first carrier), and the device-side rejection half is the DEFERRED leg specified in `docs/design-nvmeof-target-management.md` §6.8.1. **DLM S5 — the reader families** (`docs/operations.md` §Read-only coherent mounts): the posture `read_only_mount` plus the two DERIVED numbers `reader_revalidate_interval_ms` / `reader_staleness_bound_ms` (= interval + the writer's ≤1 s checkpoint ceiling — published so the guarantee in the docs cannot drift from the one in force; 0 on write mounts), and the activity family `meta_kv_revalidate_{polls,epochs,nodes_dropped,stale_serves,dirty_skips,keys_purged}` + `meta_kv_reader_load_retries` / `meta_kv_node_partition_refusals` — ALL 0 for the life of a write mount, so nonzero values ARE the statement "this mount is a coherent reader". Two are must-stay-0 on every posture (`dirty_skips` = revalidation armed on a mount that writes; `partition_refusals` = an appender reached for a node population it does not own), and two are INVERTED tripwires now that the mount installs the census purge sink: `epochs` advancing while `nodes_dropped` stays flat means the drop pass finds nothing to drop, and `epochs` advancing on a mount with cached blocks while `keys_purged` stays flat means the R-6 purge is not reaching the tiers — each a coherence promise silently not kept, never an unwired one. **DLM stage S8** (spec §6.7 decision 1 + §6.9 S8; `src/meta_ship/`, operator surface `docs/operations.md` §Metadata function shipping) adds the `meta_ship` object — metadata authority is OWNABLE, not lockable, so a verb on a volume another node owns is function-shipped to that owner: `armed` (false on every shipped mount — nothing arms the plane until S9's multi-writer mount), the routing ledger `local_verbs`/`shipped_verbs`/`served_verbs` (a client's shipped count must equal the owner's served count — the engagement law), the pipelining pair `batches`/`batched_verbs` (verbs÷batches is the live coalesce factor; ≈ 1 on a serial stream, which is §6.10 R1's accepted cost made visible rather than hidden), `dedup_hits` (replays answered from the owner's `(client_epoch, request_id)` window instead of double-applying), `retries`, `stale_term_refusals` (issued AS AN OWNER) vs `era_relearns` (observed AS A CLIENT — split so a node that is both cannot double-count one failover), `cross_owner_refusals` (a `rename`/`link` spanning two owners, refused loud with EXDEV naming S3.5 — the cross-volume transaction machinery is NOT built), `not_owner_refusals`, `mint_redirects` (a create redirected to an owned volume — §6.10 R4's one-appender-per-volume constraint engaging), `dlm_rpcs_meta` (the metadata face of the round-trip ledger; `dlm_rpcs` keeps its S4 meaning — LOCK round trips — untouched), the failover pair `dlm_grace_reclaims`/`dlm_grace_conflicts` (a successor's grace window admits reclaim and refuses fresh mutations; conflicts stay 0 on a healthy failover), and the `dlm_token_cache_*` family — S8's discharge of the S4 fencing-read contract, where `dlm_token_cache_misses` is a **must-stay-0 tripwire** (a miss means a fencing read reached a foreign-home object without the metadata RPC that carries its grant) alongside `owner_panics`. `meta_ship_phase_ns` (route/queue_wait/encode/rtt/decode) and `meta_ship_owner_phase_ns` (admit/dispatch/execute/reply_encode/total) are the always-on attribution the deferred `tar -x` A/B needs (rulings D10/D11). **DLM S6 — membership off the journal** (`src/membership.rs` + `src/membership_wire.rs` + `src/membership_sim.rs`; contracts `tests/dlm_membership_tests.rs`; operator surface `docs/operations.md` §Membership plane) adds the `membership_*` family: liveness is a RAM lease renewed over `cluster_wire`, so `membership_renewals` (the heartbeat) grows while `meta_kv_journal_entries` does **not** — that non-growth IS the S6 gate (§6.5 item 3 measured the `client:{uuid}` beat plane serializing 455 journal beats/s against the 1,500/s 15 k clients need) — while `membership_registration_commits` is bounded by mounts and membership CHANGES, so growth proportional to renewals is the regression; `membership_self_fences` is the §6.7 stricter-client-clock instrument (`T_self = T_owner − 2·skew_max − D_purge`, anchored on the member's own send instant: a member fail-stops its own objects BEFORE the owner can re-grant — must stay 0, and an unsafe clock configuration REFUSES the plane rather than clamping); `membership_evictions` mints the S7 dead epoch its offsets are quarantined under; `membership_grace_{refusals,reclaims}` gauge the owner-failover re-assertion window (reclaim admitted, conflicting fresh acquires refused); `membership_census_serves` is the read side that replaced `listxattr(1)` + one `getxattr` per client under `I{1}`; plus the gauges `membership_mode` (`off` — the shipped default until `SQUEEZEFS_MEMBERSHIP_BIND` is set — / `owner` / `member`), `membership_{members,readers,writers}`, `membership_{lease_ttl,self_deadline,grace_remaining}_ms` and `membership_min_acked_free_epoch` (**the exact API §6.8 item 3 calls**: `MemberSession::ack_free_epoch` → `MembershipOwner::{min_acked_free_epoch,members_behind_free_epoch,evict}` — item 3 itself is BUILT on it, `src/free_grace.rs`, described below). §6.2 **item 7**'s claim-set record rides incompat **bit 14** (`KV_CLAIM_SET`, stamped by the default format since the rung-10b flip): un-engaged volumes — and any volume without the bit — read the singleton PROJECTION of `writer_claim`, so single-writer records stay byte-identical, and the record's NVMe registrant keys join S7's shared `WeroHold` rather than forking a second reservation. Readers become visible in `squeezefs clients` for the first time (kinds `member-reader`/`member-writer`) at the cost of zero metadata writes — the S5 gap — and D0's write exclusion rows are untouched: the plane answers *who is here*, never *who may write*. **DLM stage S9 — the multi-writer data plane** (spec §6.9 S9; `src/data_grant.rs` + `src/multi_writer.rs` + `src/meta_ship/publish.rs`; contracts `tests/dlm_multi_writer_tests.rs`; operator surface `docs/operations.md` §Multi-writer data plane) adds the **remote write-custody** ledger as the `dlm_custody` object — `mode` (`off` on every shipped mount / `authority` / `co-writer` / `both`), `dlm_custody_held`, `dlm_custody_{grants,renewals,releases,conflicts}`, `dlm_revokes_{issued,expired}`, `dlm_custody_unknown_leases` (the **pull-based** revocation channel firing: this plane has no push backchannel, so a revoked co-writer learns at its next renewal and the window is bounded by its own `T_self` self-fence and by the device, never by its belief), `dlm_custody_self_fences`, `dlm_custody_{quarantined_offsets,drain_proofs}`, `dlm_custody_grace_conflicts` (**must stay 0** on a healthy failover — the S6/S8 `dlm_grace_conflicts` law for this plane), and the opt-in `dlm_custody_grant_census` (VAL-7a's law: it names peer identities, inos and byte ranges, so it rides `SQUEEZEFS_STATS_KEY_CENSUS`; the count always exports) — plus the epoch pair `dlm_custody_generation` / `dlm_custody_epoch_advances` (S7's epoch gained its per-client low bits: an advance retires every DMA authorization minted under the previous generation and **never poisons**, which is why `data_dma_epoch_refusals` growth is read beside it), the always-on `dlm_custody_phase_ns` (rtt / arbitrate / adopt / renew — the deferred fan-out row's attribution), and the publish ledger `meta_ship_publish.{shipped,local,served,refusals,owner_panics}` where **`refusals` and `owner_panics` must stay 0** (a refusal means the ownership plane was armed without its publish half — a foreign-home publish refuses rather than appending to a peer's journal ring). **The publish plane FRAMES since D-1b (e2e perf audit DLM board #8, 2026-09-02, `.benchmarks/2026-09-02-d1b-publish-plane-batching.md`)**: `PublishRequestFrame` carries N calls (schema 13 — KD-7 same-commit fleets), the client keeps S8's lane shape per authority (a frame is whatever is queued when a frame slot frees — no timer, no added delay on a serial stream) with `SQUEEZEFS_PUBLISH_SHIP_DEPTH` frames in flight on that many sessions (derived `clamp(ceil(cpus/8), 2, 8)` — the owner RPC-lane slope; `1` = the stop-and-wait A/B control), and the owner partitions a frame into D-1's dependency chains so independent inos co-queue into ONE M7 pass while same-ino calls keep submission order; every gate (era, not-owner, witness, RETRIED/REFUSED class, `recomputed`) is PER CALL (`PublishCallOutcome`), and a frame is homogeneous in resend class so a transport resend re-sends the same request ids or nothing. Ledger: `ship_frames` / `ship_framed_calls` (calls ÷ frames = the live coalesce factor, ≈ 1 on a serial stream by design), `ship_depth_waits` (the K+1'th frame parking — saturation at the current depth), `ship_session_dials` (≤ depth per endpoint at steady state; growth = sessions dying between frames), `served_frames` / `served_frame_calls` / `served_chains` (chains ÷ frames ≈ frame width = independent, ≈ 1 = one hot object). In-process (release): 24 concurrent publishes went 24 frames / 24 passes / 122 µs each → 1–5 frames / 3–5 passes / 26 µs each; local fleet A-B-B-A (8 co-writers × 24 streams, tcp devsub): frames/publish 1.00 → 0.61–0.63, owner passes −39 %, journal entries −24 %, per-block publish latency −74 %, aggregate ingest PAR — the row is CPU-bound and the authority's conveyor reaches ρ ≈ 0.97 on the fix (DLM #2's two-stage conveyor is the next wall). **The owner commits a frame as ONE conveyor group since D-1c (§5.3 row 1's residual rung, 2026-09-04, `.benchmarks/2026-09-04-d1c-conveyor-group-per-frame.md` — field A-B-B-A owed)**: since C-2's instant-drain pass a frame's calls reached `commit_tx` at different instants and landed in 3–14 passes per 24 publishes; now the multi-chain dispatch proceeds in ROUNDS — every chain's next call, the round's `SetLayoutAndSize` calls PREPARED concurrently (kvmap probe, custody-scoped compose, over-cap ref chunks) under the round's serve stripes (deduped, ascending) and committed as one `RoutedMetaBackend::set_layout_and_size_group` (one `lock_many` per volume, one `commit_tx_group`), the round's other verbs served beside them, a chain's next call only after its previous finished — so a 24-distinct-ino frame is one round, one group, one pass BY CONSTRUCTION (`a_framed_burst_is_one_owner_conveyor_pass_by_construction` pins it without a held pass). Each grouped call CLAIMS its `(lease_epoch, request_id)` witness synchronously (`sqz_once::OnceCell::{claim_init,complete_init,abandon_init}`); a failed claim takes the serial path (parks on the winner, counts the replay). Lever `SQUEEZEFS_PUBLISH_CONVEYOR_GROUP` (default on; `0` = D-1b's per-chain dispatch, read per served frame); engagement `meta_ship_publish.frame_groups` beside `meta_conveyor_group_{commits,txs}`. Not yet grouped (the next rung): the S8 verb plane's `run_batch`, `MergeLayoutAndSize`/Lever-B, `FreeBlocks`, `RaiseAllocLane`, kvmap puts. Every field is 0/`off` on a single-writer mount BY CONSTRUCTION, and the arm (`SQUEEZEFS_MULTI_WRITER=1`, `SQUEEZEFS_MW_BIND`) refuses on a non-PR substrate, on a format missing any of the six capability bits 7/9/10/11/13/14 (stamped by the default format since the rung-10b flip; `--single-writer`/pre-flip volumes refuse until `volume enable-multi-writer` upgrades them offline), and on a mount whose membership plane is off; S9 takes **no new incompat bit**. **DLM S6 — membership off the journal** (`src/membership.rs` + `src/membership_wire.rs` + `src/membership_sim.rs`; contracts `tests/dlm_membership_tests.rs`; operator surface `docs/operations.md` §Membership plane) adds the `membership_*` family: liveness is a RAM lease renewed over `cluster_wire`, so `membership_renewals` (the heartbeat) grows while `meta_kv_journal_entries` does **not** — that non-growth IS the S6 gate (§6.5 item 3 measured the `client:{uuid}` beat plane serializing 455 journal beats/s against the 1,500/s 15 k clients need) — while `membership_registration_commits` is bounded by mounts and membership CHANGES, so growth proportional to renewals is the regression; `membership_self_fences` is the §6.7 stricter-client-clock instrument (`T_self = T_owner − 2·skew_max − D_purge`, anchored on the member's own send instant: a member fail-stops its own objects BEFORE the owner can re-grant — must stay 0, and an unsafe clock configuration REFUSES the plane rather than clamping); `membership_evictions` mints the S7 dead epoch its offsets are quarantined under; `membership_grace_{refusals,reclaims}` gauge the owner-failover re-assertion window (reclaim admitted, conflicting fresh acquires refused); `membership_census_serves` is the read side that replaced `listxattr(1)` + one `getxattr` per client under `I{1}`; plus the gauges `membership_mode` (`off` — the shipped default until `SQUEEZEFS_MEMBERSHIP_BIND` is set — / `owner` / `member`), `membership_{members,readers,writers}`, `membership_{lease_ttl,self_deadline,grace_remaining}_ms` and `membership_min_acked_free_epoch` (**the exact API §6.8 item 3 calls**: `MemberSession::ack_free_epoch` → `MembershipOwner::{min_acked_free_epoch,members_behind_free_epoch,evict}`). **§6.8 item 3 — the freed-offset grace period** (`src/free_grace.rs`, contracts `tests/reader_free_grace_tests.rs`, operator surface `docs/operations.md` §Freed-offset grace period) is BUILT on that channel and adds the `free_grace_*` family: the posture word `free_grace_mode` (`off` = no plane, the shipped default / `idle` = armed with no members / `armed`), the ledger `free_grace_{deferrals,releases,offsets,bytes}` whose closure law is `deferrals ≡ releases + offsets`, the reallocation bound `free_grace_bound` beside its derived numbers `free_grace_{fence_bound_ms,pressure_bound_ms,ring_cap}` (published so the operator page cannot drift from the machinery — since the pressure valve `fence_bound_ms` is the deadline **in force** and `free_grace_fence_bound_base_ms` the un-tightened derivation, the `write_pipeline_depth_target`/`_base` precedent), the reader-side `free_grace_reader_acks` (**flat while the writer churns IS the failure** — that reader is holding the writer's free list), and the two tripwires `free_grace_forced_releases` / `free_grace_laggard_fences` (**0 on a healthy fleet**: a forced release is the ONE arm that trades a reader's coherence for the writer's progress, and it only ever happens together with that reader's eviction — "fenced, not waited on") plus `free_grace_alloc_stalls` (the ENOSPC-not-corruption ruling's instrument: allocation refused rather than hand out an offset a reader may still resolve — silent on a passthrough volume). **The hold-time campaign (2026-09-06, `.benchmarks/2026-09-06-free-grace-hold-time.md` — finding 15's remaining half: with the supply leak closed the s11 row HOLDS the supply, `bound_age` 8,994 ms at every 1 Hz cadence) adds the decomposition `free_grace_hold_phase_ns` (`defer_checkpointed` / `checkpointed_min_acked` / `min_acked_released` / `total`, exact-sum per placed sample, read off the KV checkpoint mark and `publish_bound`'s own advance — `free_grace_hold_unplaced` counts the rest), `free_grace_hold_ms` (the live residence EWMA — the measured hold the capacity law `churn × hold + live ≤ cap/W` multiplies by; the co-writer publishes the law as `alloc_lane_share_needed_blocks` / `alloc_lane_headroom_pct`), `free_grace_checkpoint_{marks,cycle_ms}`, `free_grace_member_ack_lag_ms` (max/mean/min over members — the min-composition's culprit finder; the per-member census rides `SQUEEZEFS_STATS_KEY_CENSUS`), and two levers landed on in-process evidence (−922 ms of an 8,646 ms coupled hold, closure exact, zero fences): `SQUEEZEFS_FREE_GRACE_ACK_RENEWAL` (a promotion wakes the renewal loop and renews as CARRIAGE — the ack travels now, the beat and the label stay the routine renewal's; `free_grace_ack_renewals`) and `SQUEEZEFS_FREE_GRACE_REFRESH_ON_ACK` (a binding member's advancing ack marks the bound dirty, the next harvest recomputes it rate-limited to `floor ÷ members` / 2× the measured scan; `free_grace_bound_refreshes_on_ack`, `free_grace_bound_scan_ms`). The remaining 6.0 s of the hold is the two DERIVED reader windows (`S + skew`, `S + D_purge` — each term a `P` or the checkpoint ceiling), named as adjudication items in the note's §7; a faster writer checkpoint cadence alone is measured INERT (the reader qualifies on the time bound, not the event). **The ladder re-derivation (user decision 2026-09-06, `.benchmarks/2026-09-06-free-grace-ladder-rederivation.md`, KD-FG-11 amended) landed items 1–3 of that §7:** qualify = the WRITER's checkpoint landing ceiling + skew (`checkpoint_landing_ceiling_ms` = trigger + 2 ticks, 1,100 ms shipped, through the one accessor `MemberSession::checkpoint_ceiling_ms` — the poll interval bounded nothing, the pass IS the poll), the drain's `S` = an epoch-step invalidation of the reader's layout cache (`CachedMetadata::reader_step_gen` vs `ro_coherence::layout_entry_pre_step`, stamped BEFORE the backend read, one relaxed load per serve, refused at the handler resolve / binding recheck / il sync probe / dd prelude / fill tasks), its `D_purge` = an OBSERVED in-flight drain (`ro_coherence::ServeStamp` on every read serve — one `SeqCst` increment + one same-word decrement, per-thread shards, the Dekker pair with the step's bump — `serve_drained_below` at the qualifying pass or on the last straggler's completion wake; `D_purge` survives as the must-stay-0 `free_grace_drain_overdue`); levers `SQUEEZEFS_FREE_GRACE_{QUALIFY_CEILING,DRAIN_EPOCH_STAMP,DRAIN_OBSERVED}` (default on, each `0` = its retired term verbatim), gauges `free_grace_{qualify,drain}_lag_ms`, `free_grace_drain_{observed,overdue,wakes}`, `reader_layout_step_{gen,misses}`, `reader_serves_inflight`, `reader_serve_{step_races,slot_overruns}` (the last must stay 0); fleet-cadence model `bound_age` 7,724 → 2,724 ms, the fleet row owed. **The writer→member checkpoint composite (adjudication item 4, user decision 2026-09-06, `.benchmarks/2026-09-06-free-grace-checkpoint-composite.md`)** lands the cadence lever as the composite it pays as: while the valve is asking (a prod in force) the authority's KV checkpoint ceiling is `max(P/2, 2 × measured cycle)` against the reader's routine poll, the membership grant carries the ceiling in force (`Grant::checkpoint_ceiling_ms` — a promise honoured one routine ceiling past the grant; `CLUSTER_WIRE_SCHEMA` 1 → 2), and the member's prod floor, L2b pass floor and pipeline depth follow it — in-process `bound_age` 7,724 → 6,750 ms at exactly 2× checkpoints/s and 2× lease-lane renewals/s (the accepted cost); gauges `free_grace_checkpoint_ceiling_ms` / `free_grace_checkpoint_elastic_cycles`, lever `SQUEEZEFS_FREE_GRACE_CHECKPOINT_COMPOSITE` (`0` = the shipped cadences exactly), the published staleness bound unchanged. **Composed (the integration, same day): the grant advertises the LANDING ceiling of the decision in force (`checkpoint_landing_ceiling_for_elastic` = `c + 2 × min(tick, c)`; the routine `trigger + 2 × tick`), so `MemberSession::checkpoint_ceiling_ms()` — the qualify term’s and the pass floor’s one input — reads the advertised landing value with the member’s own landing derivation as the fallback; the member-side gauge is `free_grace_advertised_ceiling_ms`, distinct from the writer’s decision-ceiling gauge.** The **pressure-coupled release valve** (rung-20 residual 6, landed 2026-08-21) adds the graded signal `free_grace_pressure_pct` (0 quiet … 100 = the supply is gone at the ring's own measured deferral rate against the smaller of its headroom and the volume's free supply — a rate-derived forecast, never an occupancy) and the two rung gauges `free_grace_prods` (rung a — grants that carried a SHORTER renewal cadence to a member the writer is waiting on, the cadence itself published live as `free_grace_prod_renew_ms`; growth with `free_grace_bound` flat is the stop-and-read signal) and `free_grace_bound_tightenings` (rung b — harvests that evaluated a deadline below the routine bound; its ratio against `forced_releases` is the point of the valve, and both flat while `offsets` climbs means `SQUEEZEFS_FREE_GRACE_VALVE=0`). Rung (c) — the forced release with its eviction — is unchanged and still last, and the tightening's FLOOR is one honest acknowledgement cycle, so no rung can fence a reader that is answering as designed. The reader's ack ladder snapshots a CANDIDATE label (a renewal cadence below `staleness + skew_max` would otherwise re-learn the qualification target out from under every pass and the reader would acknowledge nothing for ever — the "climbs and never drains" signature, reachable without the valve at `SQUEEZEFS_META_FLUSH_INTERVAL_MS=5000`). The gate is one relaxed load when unarmed, sits strictly DOWNSTREAM of the reclaim queue (`finish_free` is its only entry, so pop-ownership/fence-halt/manners are untouched), and composes with S7's dead-epoch quarantine custody-first: an offset released by a drain proof enters grace rather than the free list. Epoch identity is the owner's own monotonic instant echoed as a CAUSAL token — never the per-volume ledger sequence, whose min/max composition starves in an unbalanced set. Readers become visible in `squeezefs clients` for the first time (kinds `member-reader`/`member-writer`) at the cost of zero metadata writes — the S5 gap — and D0's write exclusion rows are untouched: the plane answers *who is here*, never *who may write*. **DLM S9 blocker #3 — the data-plane allocation partition** (`src/data_alloc_lane.rs`; design `docs/design-mw-data-alloc-partition.md`; contracts `tests/mw_data_alloc_lane_tests.rs`; capacity planning `docs/operations.md` §Multi-writer capacity planning) adds the `alloc_lane_*` family: fresh block allocation is a per-writer **residue class** (`b % W`, `crate::lane_core`'s arithmetic reused verbatim from §6.2 item 5's ino lanes) while **frees stay lane-blind** — the owning lane is derivable from the OFFSET, so a writer frees a peer's block with no ownership lookup, no message and no record, and `begin_free`/`finish_free` are untouched. `alloc_lane_writers` is the partition width and **`0` = unpartitioned**, which is every mount today: a SOLO engagement installs nothing at all, so single-writer allocation is not merely equivalent to the shipped path but literally it (pinned, including zero durable commits and zero gauge movement). The rest: `alloc_lane_id`, `alloc_lanes_owned` (own + lanes ADOPTED after their holder was proven dead — the witness is S7's `DeadEpoch`, the same drain proof `release_quarantine` demands, and adoption needs no durable record because the reservation is keyed on the LANE, not the holder), `alloc_lane_reservations` (durable per-lane watermark commits — one per derived grain of **fresh** blocks, `SQUEEZEFS_ALLOC_LANE_RESERVE_BLOCKS`; **reuse pays none**, so the rewrite hot path adds zero), `alloc_lane_adoptions`, `alloc_lane_enospc_refusals` (**must stay 0** — a writer starving while the set has free space; the refusal names the unreachable free blocks and keeps the `StorageFull` class the ENOSPC valve keys on) and `alloc_lane_stranded_bytes` (the **published capacity bound** = `capacity − Σ owned lane shares ≤ cap × (W−1)/W + (W−1)`; the granularity cost when every writer stays inside its `cap/W ± 1` share is only `W − 1` blocks). Durability is one 21-byte versioned+checksummed `alloc_lane:{vol_tag}:{lane}` record on ino 1 (KD-2's plane, VAL-2-allowlist-invisible, `vol_tag` per KD-5) written AHEAD of use, because derived state cannot see a fenced-but-live predecessor's minted-and-unpublished tail; recovery is `max(reservation, derived floor, every foreign-WIDTH record)` rounded into the lane. Contiguity picks keep working WITHIN a lane (one stride coarser — the stated fragmentation cost), fsck C6 now declines to reconcile foreign lanes, and the partition takes **no new incompat bit**: it gates on bit 11, whose meaning already is "the format's recovery paths are expressed for more than one data-plane writer". **The ADMISSION landed with the co-writer lane grant** (`src/alloc_lane_grant.rs`, design §9a, contracts `tests/mw_cowriter_lane_tests.rs`), which is what makes a non-solo `alloc_lane_writers` reachable at all: the authority derives ONE lane per enrolled writer from the durable claim set (itself lane 0, width rounded up to a power of two because `AppendPartition` admits only those — so three writers run at `W = 4` and the unassigned lane joins `alloc_lane_stranded_bytes`), and carries `(writer_lane, writers)` to each co-writer on the S9 **custody lease** — never a knob, since two co-writers choosing lanes is the collision the partition exists to prevent, and never the co-writer's own derivation from the record, since a roster growth would give a later reader a wider width whose classes OVERLAP a live peer's. A member the arm-time snapshot does not name is refused at the custody JOIN (a width change is a new authority ERA), and a renewal that ever answered a different lane makes the mount self-fence. Because the reservation record is a metadata commit a co-writer cannot perform, its raise SHIPS (`PublishCall::RaiseAllocLane`, publish schema 2) and the AUTHORITY commits it — the client names a LANE and the owner derives the record name, checks the lane against the assignment it made under the lease epoch it minted, and holds the monotonicity (the frontier only ever rises) — with two new gauges: `alloc_lane_shipped_reservations` (the co-writer engagement instrument: all of a co-writer's raises travel, so 0 with a nonzero `alloc_lane_id` means a mount is committing its own frontier, which only an authority may do) and `alloc_lane_raise_refusals` (**must stay 0** — a lane/width/lease refusal, and any refusal means the offset was NOT handed out). A co-writer's lane is also OPENED by the authority before its first mint (a raise with `upto == 0` commits nothing and answers the recovery floor), because a co-writer runs no ownership-recovery walk and would otherwise mint from block 0 of a device whose low blocks are live. **A co-writer's PROACTIVE lane refills — the ahead-of-stall watermark tick (`SQUEEZEFS_ALLOC_LANE_HARVEST_AHEAD`) and the lane-push pushed refill (`SQUEEZEFS_FREE_GRACE_LANE_PUSH`, the renewal grant's `lane_supply_blocks` hint → `free_grace_lane_push_wakes` / `alloc_lane_pushed_harvests`) — gate on the authority's ADVERTISED lane supply (`free_grace_lane_supply_hint`, `free_grace::lane_supply_witnessed`), never on the owed ledger alone (the refill-hint gate, 2026-09-07, `.benchmarks/2026-09-07-lane-refill-hint-gate.md`: `alloc_lane_owed_blocks` is the EXPLICIT-ship arm's face and ≈ 90 % of a rewriting co-writer's displaced blocks return through the authority's publish recompute, which notes nothing owed — so the owed gate left both arms dark while the authority advertised hundreds of the lane's blocks and every refill ran from inside an ENOSPC park; `SQUEEZEFS_ALLOC_LANE_REFILL_HINT=0` is the owed-only A/B control, `alloc_lane_hint_refills` the engagement, and the ENOSPC-path harvest is unchanged). **DLM S9 — the CO-WRITER mount posture** (§6.2 item 7's CONSUMER half; `src/cowriter.rs`; contracts `tests/dlm_cowriter_tests.rs`; operator surface `docs/operations.md` §Multi-writer co-writer mounts) adds the posture gauge `mount_posture` (`writer` | `reader` | `co-writer` — the one word that says which of the three shapes a daemon is) and the `cowriter` object: `mw_role`, `custody_endpoint`, `admissions` (1 per admitted mount and then flat), `admission_refusals` (every refusal names its RUNG — the five-rung ladder is declaration → bit 14 durable on EVERY volume → this node named a writer member of the claim set → a live authority lease → a device registrant under the standing WERO hold), `accounting_refusals` (ownership accounting a co-writer may not do — the specific claim, the W1 incarnation retire (a lifetime retire is durable ownership state and the §5.1 fence is process-local, so W1 stays authority-only BY DECISION: a co-writer's small overwrite rides CoW-rewrite + shipped free), the ownership recovery walk, direct device reclaim, and allocator-level frees no product path reaches; **allocation left this list with the lane grant and the router-level terminal free left it with the S9 co-writer FREE path** — a rewrite's displaced free now SHIPS as `PublishCall::FreeBlocks` (publish schema 3) and the AUTHORITY executes the whole ladder (`begin_free` → tier purge → reclaim enqueue → `finish_free`, the §6.8 item-3 grace ring and S7's quarantine composing inside), exactly-once under the `(lease_epoch, request_id)` dedup window (S8's window reused; retries never re-key across a re-join, and a fenced era's in-flight frees refuse by era), with the freed offset re-entering the free supply of the lane `b % W` names; the free-path rows ride the publish ledger — `meta_ship_publish.free_shipped_blocks` (the rewrite-engagement instrument: its delta must account for a co-writer row's displaced blocks) / `free_served_blocks` / `free_replays` (the witness engaging) / `free_stale_refusals` (the era fence) / `free_ship_failures` (**≈ 0**: each is a leak-safe durably-free offset unreturned until the authority's next derivation) / `free_refused_blocks` (the owner's `Refused` verdicts — a genuine double release, the class named on the owner's log line; `served + refused + non-terminal ≡ the peers' shipped`) — so `accounting_refusals` growth on a rewriting co-writer is a BUG again, not the honest append-only gap it used to be; contracts `tests/mw_cowriter_free_tests.rs`. **Finding 15's supply closure (2026-09-06, `.benchmarks/2026-09-06-cowriter-free-refcount-leak.md`, contracts `tests/mw_cowriter_free_leak_tests.rs`):** a recomputed publish's post-commit free set now includes the caller frame's **RAM-only lifetimes** (a block the frame both takes and releases that neither the head nor the composed map names — an epoch-fed overlay dest superseded by a write-through before any save persisted it; the head→composed diff cannot release what no map ever held, so it was freed by NOBODY), an own-mint map blob's lineage closes **exactly once** (`reclaimed_own_mints` memo: the save tail and the insert chokepoint's orphan reclaim were two issuers, and a stale clone re-inserted after a lock-free discard re-armed the free at every later discard — the s11 row's 3,449 refused frees, `publish_blob_orphan_reclaim_dedups`), a live laned co-writer's never-published mint returns to its OWN lane free list (`cowriter.unpublished_recycles`; the quiet abandon stays for poisoned eras and foreign lanes), and the served compose's displaced-blob free runs under the authority-accounting scope (a partial authority's own blob was shipping to the set authority)) and `local_commit_refusals` (**should stay 0** — nonzero means a daemon surface still commits metadata directly instead of shipping it, i.e. S8's un-routed-daemon gap meeting a real workload). The D0 Layer-B2 gate is UNCHANGED: a co-writer enters through `KvMetaBackend::open_co_writer`, reachable only with an admission decision, so `writer_guard_mode` gained the `co-writer` row without weakening one refusal — and `BlockAllocator`'s gate became `plane_gate` with TWO classes (the reader's text and sites untouched) because a device offset's OWNERSHIP is metadata, which is exactly the authority a co-writer lacks while holding data authority under custody. The ALLOCATION arms then split off into `alloc_plane_gate`, whose co-writer class keys on the allocator's engaged LANE: with a granted lane they pass (placing a block in a residue class no peer mints in is a data act), without one they refuse verbatim as before — the relaxation is "an owned lane", never "a co-writer".

**Read-path program families** (`docs/design-read-path.md` §Observability — semantics + regression thresholds there): the serve-latency decomposition campaign (2026-08-01, `.benchmarks/2026-08-01-serve-decomposition.md`) adds the ALWAYS-ON read residence families — `read_serve_phase_ns` (per-op: prelude/meta_resolve/key_resolve/classify_probe/sf_wait/block_fetch/binding_check/slice_out/post_validate/total), `read_fill_phase_ns` (per-fill: dev_queue/dev_service at the NvmeBlockDev funnel, fetch_dma/decode/admission/deposit/fill_total), and the fuse3-side `read_transport_phase_ns` (queue_wait/dispatch_lag/reply_commit/transport_total — `fio clat − transport_total` = the kernel-side residue by subtraction; all three bucket through the shared `#[path]` core `crates/squeezefs-ipc/src/latency_core.rs`, so the tables compose end-to-end) — plus `fuse3_read_inplace_replies` (the P2 in-place READ reply engagement gauge: the campaign found the arm structurally disengaged since `be82794` — the dispatch loop `take()`d the session connection — and rewired it; on an armed session this must account ≈ every READ); `singleflight_waiter_result_serves` (R1a cohort serves); `hot_block_{hits,misses,evictions,probation_drops,dehydrate_skips,current_bytes}` (R4 RAM tier); `read_fill_publishes_skipped` / `read_tier_admissions` / `read_tier_admission_ghost_hits` / `read_tier_admission_mode` (R1b second-touch admission — skipped ≈ streamed cold blocks); `prefetch_{issued,completed,wasted,inflight_bytes,window_hwm,foreground_waits,evicted_unconsumed,active_streams}` (R2 pipeline — `evicted_unconsumed` is the refetch-spiral detector); `ranged_{reads,read_bytes,read_unaligned_bounces,read_rebinds,read_ghost_escalations}` (R3 — `ranged_read_bytes` vs user bytes is the rand-4k amplification bound; `get_obj` counts ranged ops by design; `ghost_escalations` = hybrid-I/O second-touch → whole-block admissions); `read_lane_{fetches,fetch_bytes,holds,serves,serve_bytes,hold_retired,hold_evicted_unconsumed,wasted,depth_target,hold_bytes,inflight_bytes,armed}` (the read-lane campaign, 2026-08-01 — `src/read_lane.rs`, `.benchmarks/2026-08-01-read-lane.md`: the **hold** — a ledger-invisible, purge-integrated (R-6 fifth arm), coverage-retired completed-fill store — is the shipped default win on the fio-gap read plateau (+12.8 % qd8 / +14.5 % qd32 vs the `SQUEEZEFS_READ_LANE=0` A0 control, deep-qd cohort `read_amp` 1.41 → 1.11; `serves` is the cohort-stability instrument, `hold_evicted_unconsumed` the spiral detector, hold budget = the derived consume-behind window on R5 component `read_lane_hold`); the **ahead lane** (ledger-invisible whole-block fetches where R2 declines below its AIMD start window — `resident_share < 2` since 2026-08-05) is **probe-governed** (`read_lane_depth_probe_{ups,backoffs}` — the 2026-08-05 read-throughput campaign's engage-governor: the write-side `ProbeCore` reused verbatim; probes launch on saturated epochs with R5 headroom, adopt only when total fill delivery responds, retreat + cool down on dead gain, bleed to depth 0 on unsaturated epochs; `SQUEEZEFS_READ_LANE_DEPTH` pins for measurement, `0` = ahead off — the hold-only A/B control); both OPEN-LOOP depth derivations (BDP, AIMD window) were FALSIFIED by counted brackets on demand-concurrent venues (ahead-fetches died FIFO-unconsumed racing the demand cohort, −19 %) — the falsified venues are now the governor's duty-cycle-bounded RETREAT arm; the hold-churn campaign (2026-08-05 part 2 — the field's dead-gain acceptance row) adds `read_lane_covered_skips` (the marginal-issue walk: demand-front/store-covered blocks advance the lane cursor as sync probes, never a task or depth slot) and `read_lane_hold_ahead_evictions` (the ahead-class subset of `hold_evicted_unconsumed` — landing-zone pressure: movement within an epoch is zero probe headroom), and fixed the COVERAGE-CREDIT truth (dest-armed fetch-loop serves credited from the post-slice buffer — 0 for every sub-read past a block's first, killing coverage retirement on deep-qd cohort rows; dest arms now credit inline on the full block); classifier gained classified-membership tolerance ±64×len (run-building stays exact — the qd-reorder lane wedge fix); Red ⇒ 0 with `read_lane_inflight` converging by completion); the **READ copy ledger** (read-copy-count campaign, 2026-08-02, `.benchmarks/2026-08-02-read-copy-count.md` — the read twin of the write path's copy census): `read_copy_{dest,bounce}_bytes` / `read_{dest,fill}_dma_bytes` / `ipc_arena_copy_bytes` attribute every daemon CPU pass over read payload bytes at its site — closure (`dest + bounce + dest_dma ≈ kernel user bytes`; `dest + arena + dest_dma ≈ il bytes out`) is the row-validity instrument, `bounce` must stay ≈ 0 on cold il rows (E-IL1 zero-copy slices), `ipc_read_dest_serves ≡ ipc_async_handoffs` on il read rows (E-IL2 arena-dest serves; `SQUEEZEFS_IL_READ_DEST=0` is the A/B lever), and `nt_read_serve_bytes` is the `SQUEEZEFS_NT_READ_SERVE` lever's engagement (dest-arm serve copies ride NT stores — the counted +11 % EXA-cold-read win; floor `SQUEEZEFS_NT_READ_SERVE_MIN`, default 256 KiB, keeps rand-4k/warm-small serves cached); the **A1 warm-serve split** (third-party read-audit adjudication 2026-08-04, D12 board item 5) adds `read_copy_warm_serve_bytes` — the SUBSET of `read_copy_dest_bytes` sourced from warm tier buffers (the hot-block / read-lane-hold / NVMe read-cache serve arms; closure equations unchanged verbatim, cold dest residual = `dest − warm_serve`), and records the A1 decision rule: the tier-buffer handoff for warm serves gets BUILT only if a counted warm row's `warm_serve` share prices it in (never the auditor's +15–25 % blanket); **R-4 read-zc-serve (e2e perf audit read board #4/#5, 2026-09-05, `.benchmarks/2026-09-05-r4-read-zc-serve.md`) split that bucket PER ARM** — `read_copy_{hot,hold,cache}_serve_bytes` partition `warm_serve` exactly (counted at the copy, the handler's dest arms AND the R-2 fast probe's window copy — which had been OUTSIDE the ledger since R-2: a warm armed row could not close) and `read_copy_fill_slice_bytes` names the cold whole-block fill → dest slice-out (the 2.69 row's one daemon pass, ⊆ `dest − warm`) — and adjudicated the lever: on a zc-armed session the kernel reply primitive is `READ_FIXED(fd → slot)`, fd-SOURCED (device fd on the direct leg, the per-ent bounce memfd otherwise) and never VA-sourced, so the warm/cold-slice passes were the daemon's tier → bounce copy plus the kernel's bridge — a DAEMON prerequisite (an fd-addressable tier buffer), not a kernel one. **`SQUEEZEFS_READ_ZC_SERVE`** (default ON since the 2026-09-05 field bracket — daemon CPU/GiB −24 % cold / −25 % warm on the unaligned population, A-B-B-A both orders, closure exact; `0` is the A/B control) builds the whole-block READ fill pool as ONE `MAP_SHARED` memfd slab (`AlignedBufPool::new_memfd_slabbed`, a SEPARATE pool from the write path's `ALIGNED_BUF_POOL`) so the router's hot/hold/fill-slice arms and the fast probe (`PayloadSink::offer_fd_body`) hand the transport the tier buffer's own `(fd, offset)` (`ReplyData::zc_fd_body` / `FastReadProbe::ServedFd` → `CommitMsg::body_fd`) and the queue worker bridges it into the folios with the body `Bytes` as the keepalive until the CQE (the buffer-lease law by construction; a failed fd-source bridge re-stages through the bounce — `PendDone::Restage`, never a lost reply): 2.0 → 1.0 passes/byte warm, 2.69 → 1.69 cold-slice, no kernel change; engagement `read_zc_pool_serve_bytes` (a closure term) + `read_zc_pool_serve_warm_bytes`, `fuse3_zc_fd_body_replies`, tripwire `fuse3_zc_fd_body_fallbacks` ≈ 0; the **READ dest-window lease** (copy-elimination phase 1, 2026-08-06, `.benchmarks/2026-08-06-read-dest-lease.md` — the CPU-wall ruling's first mechanism; kernel adjudication: the fuse-over-uring COMMIT consumes only `commit_id`/`qid` and copies the reply from the REGISTER-time ent VA / kernel-minted kmbuf, so a tier-buffer lease can only RELOCATE the serve copy — the inverse lease ships instead) adds `read_dest_lease_bytes`: cold 4 KiB-aligned sub-block kernel windows on passthrough volumes DMA device bytes STRAIGHT into the reply's registered dest (the ranged primitive, lease-tagged; the serve dest-copy deleted — local A-B-B-A +28.6 % GB/s, daemon CPU/byte −50 %, engagement exact), a SUBSET of `read_dest_dma_bytes` (closure unchanged); dest-leaseable traffic stands the R2/lane speculative fills down (the 2 s yield stamp) and is device-true by design; `SQUEEZEFS_READ_DEST_LEASE=0` is the A/B control (pre-campaign shape byte-identical); contracts `tests/read_dest_lease_tests.rs`; `read_admission_{evicted_unhit,wasted_bytes,governor_denials,governor_clamped}` (scan-resistant admission governor, 2026-07-26 — the beyond-budget random-read collapse: `evicted_unhit` is the `prefetch_evicted_unconsumed` sibling for admissions, `wasted_bytes`÷device-read-bytes ≤ ~`SQUEEZEFS_READ_ADMISSION_FILL_PCT` % (default 5) is the bounded-waste verdict under churn, `governor_denials` must stay ≈ 0 on fitting working sets; evidence `.benchmarks/2026-07-26-read-admission-scan-resistance.md`; `read_admission_stream_transients` — the transient stream window, 2026-07-29: classified-stream ghost-hit re-fills held transient (probation, no publish, ledger-invisible) by the same governor clamp+tokens, refusals deliberately OFF the `governor_denials` instrument, stream-granted victims full-shortfall in `wasted_bytes` but exempt from `evicted_unhit` — sustained growth on beyond-budget stream loops is the designed steady state); `read_odirect_{tier_serves,ghost_admits}` + `read_device_true_reads` + `direct_device_true` (hybrid I/O, 2026-07-15 user directive: O_DIRECT serves/admits like buffered by default — the mount-scoped `-o direct_device_true` / `SQUEEZEFS_DIRECT_DEVICE_TRUE=1` escape restores strict device-true O_DIRECT for `.benchmarks` amplification measurement); `mem_budget_{bytes,pressure_bytes,gauge_sum_bytes,unreclaimable_bytes,level,yellow_events,red_events,hard_backstops,backstop_active,tier_publish_paused,floors_clamped,dehydrate_paused,components{…}}` + `read_tier_publishes_paused` + `parked_gate_{waits,self_flushes,timeouts}` (R5 authority — red_events with no OOM is the designed outcome under pressure; `hard_backstops`/`parked_gate_timeouts` growing on quiet workloads means a convergence regression, §5.7 Red semantics); the rebind-starvation fix (2026-08-04 field capture — the fio read phase racing the write phase's writeback/fold promotion backlog EIO'd hundreds of pure reads through the ladder's "did not settle after 24 binding rebinds" exit) adds `stale_binding_escalations` — read-path rebind-ladder exhaustions that escalated to the SERIALIZED settle arm (one device-direct resolve-then-fetch under the block's `BLOCK_FLUSH_LOCKS` (3) + the ino's `INODE_META_LOCKS` (3.5), the write path's own extended order — an arm legal writer churn cannot beat) instead of surfacing EIO: growth = sustained displacement churn outrunning the latch-free ladder (a latency signal, pair with `stale_binding_rebinds`), and a "did not settle" EIO now means a genuinely broken binding only — a fetch/decode failure on a CURRENT binding (the propagate arm), a write-side (non-escalating) exhaustion, or a mutator outside the stripe/merge disciplines (which also trips `invariant_tripwires` via `read_settle_lost_serialized`). Contracts: `tests/rebind_starvation_tests.rs`. Finding 51 (2026-09-07, `.benchmarks/2026-09-07-read-settle-lost-serialized-authority.md`) adds `served_binding_witnesses` — incarnation words an AUTHORITY published because a SERVED layout publish adopted a FOREIGN-lane block (the serve is its only witness of the co-writer's completed DMA behind a key it can never mint; its own `begin_free` of the offset's previous lifetime had retired the word, so before the witness every authority read/fold of a RECYCLED co-writer block failed validation for ever — the s11-mpiio row's 326 `read_settle_lost_serialized` tripwires and 101 fsync EIOs); 0 by construction on solo and co-writer mounts, ≈ the co-writers' inserted blocks on a rewrite row, and `read_settle_lost_serialized` keeps its must-stay-0 meaning for the remaining class (a retire/claim of a currently-bound key outside the disciplines).

**Executor-primitive faces (e2e audit R-1, 2026-09-02, `.benchmarks/2026-09-02-r1-device-read-executor.md`):** the two `sqz_time` registries — squeezefs-ipc's and the fuse3 fork's `#[path]`-shared copy, each its own global lock + heap + `sqz-timer` thread — export `timer_{arms,tombstones_skipped,heap_entries,live_sleeps}` and `transport_timer_{…}` respectively (arms = every `sleep`/`timeout`/ticked-park registration; a tombstone = a cancelled arm the service thread later pops under the lock; `heap − live` ≈ tombstones queued). On the sqz kernel the kern READ hot path arms the TRANSPORT face at ≈ 0.35–0.45 per op (`InboundQueue::pop`'s ticked recv) and the daemon face at ≈ 0 — `nvme_dev.rs`'s per-`read_block` timeout engages only on the stock-kernel (`SQUEEZEFS_FUSE_ZC=0`), transform, unaligned and device-true postures; measured cost ≤ 3 % daemon CPU / < 0.2 % of clat, adjudicated not-fat (the contention knee sits at ≈ 1.8–2.2 M kernel IOPS). `daemon_cpu_ns_by_class` gained `fuse3-ur` (the FUSE-over-io_uring queue workers — half of a kern rand-4k row's CPU, formerly inside `other`) and `sqz-timer` (both timer threads). Contracts: `tests/audit_instruments_tests.rs`.

**Read board #3 faces (e2e audit R-3, 2026-09-03, `.benchmarks/2026-09-03-r3-fill-issue-economy.md`):** `zc_bridge_phase_ns` — the zc direct leg's four hops around one DMA, exact-sum, always-on, fetch leg only: `msg_hop` (handler send → queue worker take), `sq_wait` (take → the carrying `io_uring_enter`), `device_cq` (enter → CQE popped), `wake_hop` (popped → handler resumed), `total` (≡ Σ hops to the ns; `read_serve_phase_ns.block_fetch − total` = the handler's pre-send probes); op-trace stages `bridge_sent`/`bridge_taken` beside `dev_submit`/`dev_complete` on this leg. The attribution it produced — three cross-thread wakes at 35–47 µs each around a 40 µs device (`msg_hop` 28 / `device_cq` − device ≈ 60 / `wake_hop` 36 on the shipped R-2 shape, per op) — is why the READ dispatch law has a FUSED arm: **THE READ DISPATCH LAW (R-2 ⊕ R-3, `crates/fuse3/src/raw/connection/fast_dispatch.rs`)** — at the delivery CQE a fetched READ is (a) served inline if warm (R-2's try-only probe), else (b) fused onto the queue worker's own lane if the session is zc-armed and the size is at or under the fusion ceiling (`SQUEEZEFS_FUSE_ZC_READ_FUSION`), else (c) handed to the `fuse3-tpc` lane homed on the queue's CPU (R-2); `transport_fast_dispatch_serves + fuse3_zc_read_fusions + transport_fast_dispatch_demotes ≡ READs` is the pinned partition and the reap thread never blocks on any arm. **The fusion lever ships OFF on measurement**: alone against the pre-R-2 dispatch it won the field kern rand-4k row (+15.5 %), but composed behind R-2 it read −6.5 % vs R-2 alone (481 k vs 514 k; p50 +70 µs, p99 −66 %) — both levers converge on the reap thread's per-op serialization (the fused pass's run-queue waits replace the lane hop + the bridge's two worker hops at a net loss) and do not compound; the composed default binary is par with R-2 (507–510 k vs 503–507 k). `1` = the tail-shaped posture. The NvmeBlockDev lane worker now parks with its request-arrival wake IN the ring (an eventfd `READ` SQE the enqueue side writes through the L3 coalescer — a fill's issue never waits for an unrelated fill's completion): `dev_enters` / `dev_fills` (enters ÷ fills ≪ 1 under concurrency), `dev_wake_batches`, `dev_wake_writes` / `dev_wakes_elided`; test seam `nvme_dev::set_test_read_slow` (a linked `TIMEOUT` makes the DEVICE completion late while the worker stays free). The remaining kern term is the fused pass's run-queue wait (`read_transport_phase_ns.queue_wait` + `wake_hop` ≈ 184 of 247 µs daemon-visible); the il reap term (`ipc_direct_phase_ns.device_cq` − device ≈ 42–44 µs, `ipc_direct_inline_reaps` ≈ 8 % of direct ops) is named, not landed.

**Read board R-4 faces (e2e audit R-4, 2026-09-03, `.benchmarks/2026-09-03-r4-reap-thread-economy.md`):** the reap worker's per-op ledger (perf on `f3-ur*` at the field: 17.7 µs/op, 63 % kernel — the COMMIT_AND_FETCH's own work incl. 1.9 µs of `fuse_request_end`/`fuse_uring_req_end` lock contention, the fetch's nvme-tcp submit 2.5, two lane wakes 1.7; 37 % daemon — the pass loop 2.1, the R-2 probe ladder 1.8, instruments 1.0) convicted the daemon's per-op contended words, and the **worker CPU diet** ships: every per-op worker counter is a per-thread `ShardedCounter` (`crates/fuse3/src/raw/read_phase.rs`), the pool pend gauge takes ONE net-delta RMW per pass, the bridge-deadline scan runs on a deadline-derived period (`timeout/1024`), the eventfd drain is one `read(2)`, the pass-bottom completion list is reused — field kern rand-4k +0.7…+2.4 % IOPS (four brackets, both orders; sustained +2.4 %/+0.7 %), CPU/op −3…−4 %, the worker's pass work (`transport_reap_gap_ns.blind`) 6.2 → 4.7 µs. **The spin-before-park governor ships OFF** (`SQUEEZEFS_FUSE_IO_URING_SPIN_US`, default `0`; any value engages it with that cap — the window is 2 × the worker's own park-gap EWMA, only while its queues hold ops in flight, refused past the box's 80 % busy knee; rings carry `IORING_SETUP_TASKRUN_FLAG` so a spin sees deferred task work without entering): at the field it deleted 69 % of the worker's parks (0.76 → 0.24/op) and moved no latency term — `msg_hop` / `device_cq` / `wake_hop` unchanged to the µs — at +5.5 % CPU/op, so the worker's ingress terms are its KERNEL residence per enter and its run-queue standing on a 75–80 %-busy box, not its park/wake. Ledger `transport_spin_{absorbed,expired,ns,refused_busy,window_us}` (`absorbed + expired ≡` spins; 0 on the default). The shared law lives in `crates/squeezefs-ipc/src/spin_governor_core.rs` (`#[path]`-included by the root crate as `spin_governor` and by the fork). **The ledger's KERNEL term shipped as a kernel patch (2026-09-06, `docs/design-kernel-bg-per-queue.md`, `.benchmarks/2026-09-06-kernel-bg-per-queue-ab.md`):** the `fuse_request_end`/`fuse_uring_req_end` contention was `fc->bg_lock`, taken twice per uring completion; sqz series patch **0031** (6.19.14 + 7.1 tracks; **0026** on 7.2) gives every uring queue its own background budget (its share of `max_background` = exactly its ent count on this daemon's INIT reply, so the semantics are unchanged and only the locking moves — §2.5) — the A A B B on squeeze-test (same 1.2.1 `dist` daemon) read the worker **17.4 → 14.3 µs/op (−18 %)**, `native_queued_spin_lock_slowpath` **12.8 % → 0.11 %** of its cycles, kern rand-4k **+9 % IOPS** (508 → 556k sustained), p99 −13…−17 %, seq and the il control par, no WARN/lockdep. Field rows after 2026-09-06 19:48 UTC run 0031; comparisons against earlier field rows must say so.

---

## Branch & Commit Workflow

The integration branch is **`dev`** (not `main`; `main` is reserved for releases). `origin/HEAD` points at `origin/dev`.

- Branch from `dev`: `feat/`, `fix/`, `refactor/`, `perf/`, `docs/` prefixes. Never commit directly to `dev`.
- Tests-first cycle: write failing tests → implement → refine → commit each logical step separately.
- Merge with `--ff-only`; rebase feature branches if `dev` has diverged. Delete branches after merge.
- Conventional commits: `type(scope): description`. Messages explain WHY.
- **Versioning = release train + git commit** (`docs/operations.md` §Versioning & releases; user directive 2026-07-24 superseding the commit-only posture): package versions track the **release train** (currently 1.2.1) and are bumped **as a release act** across the root and the first-party crates (`crates/fuse3`, `crates/squeezefs-ipc`, `crates/squeezefs-preload`) together — never per commit, never by CI. Git commits remain the fine-grained identity (`--version` shows both); release tags (`stable-*`/`lts-*`, created manually as a release act) remain the only release names.

See the full TDD Development Workflow section above for the detailed phased process.

---

## Authoritative Supporting Documents

While this file is the combined one source of truth for agents and contributors:

- `README.md` / `QUICKSTART.md` — user-facing CLI, NVMe-oF, and bare-metal setup.
- `docs/reference-clients-survey.md` — the DAOS-client + JuiceFS reference fast-FUSE client survey (see **Reference fast-FUSE clients** above).
- `docs/design-preload-interception.md` — the L4 LD_PRELOAD POSIX-interception data path (`libsqueezefs-il`): charter labeling discipline, IPC protocol/gates, the survey's P3-A adoption item; since v1.1 also the **libaio interposers** (`io_setup`/`io_submit`/`io_getevents` lane-split — libaio drivers are valid, engagement-verified il instruments; Rev 4 + `.benchmarks/2026-07-19-v1.1-libaio-interposers.md`); since 2026-08-04 (Rev 17) also **direct-link (DT_NEEDED) support** — `-lsqueezefs_il` is a supported linkage (SONAME + `-z nodelete`, linked-mode detection line, gate legs 1e/2b-linked; same KD-7 pairing — operations.md carries the link-order + setuid/env-scrubbing story).
- `docs/design-sdk.md` — the native SDK program (`squeezefs-sdk` + the `libsqueezefs` C ABI): arena-native buffers under the buffer-lease law (deletes the client-side S1 write copy + read consume copy — `.benchmarks/2026-08-02-read-copy-count.md` is the pricing basis), io_uring-shaped batch API, the SKD-4 versioned compat window (SDK sessions only; the shim keeps same-commit KD-7), per-open durability classes composing with design-rewrite-program §9.3. Design phase 2026-08-04; implementation is the follow-on `feat/sdk-…` ladder.
- `docs/PROFILING_AND_GATES.md` (historical) — content now consolidated here.

---

**End of combined AGENTS.md.** Follow these instructions exactly. When working in subdirectories, check for additional project instruction files (AGENTS.md, Claude.md, etc.) but prefer this root document.
