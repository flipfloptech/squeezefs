# Symmetric metadata PR 12b — the N-daemon posture: every non-manager RW mount is a full writer (2026-09-17)

**Branch** `feat/sym-n-daemon-posture` from `dev` @ `8fbf958f` (every rung in, PR 12 LANDED IN PART — its note's §2/§6 is this rung's ledger). **Design** `docs/design-symmetric-metadata.md` §7.3 / §5.1 / §5.3 / §5.4.3 / §5.5 / §5.7 / §5.9 / §11, PR-plan row **12b** (the owner's correction: the MANY-writer posture — every non-manager RW daemon on a volume joins as a full writer; **N is unbounded by design**; N = 2 and 3 in-process are the MINIMUM pins). **Venue**: the dev laptop — every row here is SCOPING evidence (mechanism engaged, tripwires 0); no squeeze-test row exists for this rung (the box is PR 13's).

**Dark**: everything behind incompat bit 17 AND `SQUEEZEFS_SYMMETRIC_META=1`. **`=0` and every bit-17-absent volume are the shipped postures EXACTLY** — the joined door refuses without the knob (pinned), `symmetric_join_target` answers `None` without it (the mount path's open is the shipped one verbatim), and the 39-suite matrix runs its flat leg unchanged (§4).

## 1. What landed

| # | Deliverable | As built | Pin |
|---|---|---|---|
| 1 | **The fifth door** — `KvMetaBackend::open_joined_appender` (`src/meta_backend/kv/backend/joined.rs`) | No flock (the manager's), no DUR-5 repair, no claim; `JoinAppender` FIRST over PR 3's manager wire (idempotent against the directory: a `Live` page of this exact `(node, mount slot)` is a dead predecessor's — its identity presented, the manager answers `already`, the open replays its ring as own residue; a `Recovering` page of this identity is the retryable class), then `open_inner(JoinedAppender)` — the fixed ring + tree 0 replayed as a PROJECTION (no mint, `MintPolicy::Refuse`; `OpenPosture::mints_at_replay()` is `Writer`'s alone), the manager's region stood up as `regions[0]` with `AppenderSet::owns_region(0) == false` (`own_id()` = the joined id; every "region 0 is mine" fold now runs over `own_regions()` — `live`, the ring gauges, `write_appender_pages`, `publish_slot_owners`, `foreign_slot_holder`, `cover_bring_up_residue`, `writer_guard_mode` = `joined-appender`), the own region's page `Live` at `term + 1` under the wire identity with `ckpt_seq` stamped ≥ 1 at the join (one writer per page — the manager's slot-only rewrites yield from the first instant); then `arm_joined_leases`: the projection's lease table, PR 4's rows 5/6 settled against tree 0, the held slots re-adopted on a rejoin, `AcquireSlots(M − rotor)` over the wire installed through `install_wire_grant`, the manager's endpoint bound as appender 0's, the frame fence, the token holder, the gate armed, the carriage plane registered. A THIRD (Nth) daemon joins the same way — nothing in the door names a second writer. | `a_second_daemon_joins_over_the_wire_and_commits_into_its_own_ring` (§4 for its asserts), `three_daemons_hold_disjoint_slots_and_commit_into_three_rings`, `the_joined_door_refuses_without_the_plane_and_never_writes_a_control_entry` |
| 2 | **Its own ring under the commit door** | `ensure_leases_for_tx` refuses a slot another appender holds (`SlotBusy` — the "ship to the holder" class) and first-touches an unleased one over the wire (`joined_acquire_slot` → `AcquireSlot`; `SlotRefused` → the table learns the holder, `Deferred` → `GrantDeferred`); every commit's records land in the own region's ring (`build_queued_tx`'s region routing off tree 0's lessee), the seq-space law's stamps above the departing frontier (`install_lease` raises the receiving ring's offset); the runtime mint refills over the wire on `GrantExhausted` (`slot_or_mint_refilled`); ring growth is DECLINED on a joiner (`joined_ring_grow_declined` — PR 2's drain-then-grow bound holds: it drains at `admissible ÷ cadence`). Every control write refuses: `write_control_entry`, `manager_gate` (`refuse_joined_control` → `joined_control_refusals`, must-stay-0). The manager's ring 0 head does not move for the joiner's records (pinned: `< 24` entries against 24 creates). | the first pin; `a_two_daemon_storm_survives_the_managers_remount_with_zero_violations` (manager + two joiners storm three directories — creates, renames, unlinks, a checkpoint mid-stream on each joiner — all die without a leave; the manager remounts: `self_recoveries == 1` (page 0 alone), both joiner pages listed `Live` and adopted by nobody, both slots still leased to them, the manager's door refuses `SlotBusy`; two `record_death_with_key` + ONE recovery pass replays both rings (72 window entries each): every acked name resolves, every unlinked / renamed-away one is gone, Key / Lease / Extent violation classes unmoved, fsck clean) |
| 3 | **Its own checkpoint task** — `joined_checkpoint_cycle` (dispatched from `checkpoint_cycle` on `is_joined_appender()`) | The own dirty nodes flushed (a leased leaf's SMO in ITS ring under ITS grant — the reactive wire refill on `GrantExhausted { needed }`), the DATA bitmap pages of a held allocation lease (none in this rung's shape), barrier, `note_flush_ceiling`, the region tail from the own slots' floors, `write_appender_pages` over `own_regions()` only (ITS page: `head_hint` = its head, `seq_offset` in force, `ckpt_seq` — PR 4 round 6 Issue 31's words), `checkpoint_seq` / `last_ledger_tail` stores, the grant cadence (`ReturnExtents` of the frees its barrier released; the 50 % refill). Never the ledger, the bitmap, tree 0 or page 0. **The shutdown fixpoint judges a joiner by its OWN ring** (`checkpoint.rs`): ring 0 and the bitmap are the manager's, and the joiner's projection of them — the manager's uncovered window, the replayed deltas' dirty bits — looped the bound and warned of a residue that was never the joiner's (found by the first pin). | the first pin (`page.ckpt_seq ≥ 1`, `page.head_hint == ring.head()`, `page.seq_offset == ring.seq_offset()`, the page names the slot's live root, page 0 untouched: `is_manager`, the manager's mount slot) |
| 4 | **Own-residue replay at reopen; a DIFFERENT identity never adopts it** | A rejoin over an own `Live` page presents the page's identity → `already` → the ring replayed as own residue (`self_recoveries == 1`, `self_recovered`), the term bumped, the held slots re-adopted, every covered AND windowed create present. **The rejoin refills its grant BEFORE the replay** (`open_appender_regions` counts the unrooted slot trees the window names against `unclaimed − in-window claims` and asks over the join's wire — the predecessor died with its grant consumed by the very mints the window records; the first build refused the rejoin for ever at `GrantExhausted`). **A same-node page is own residue on PAGE 0 alone** (`appender::page_is_own` — PR 2's node law narrowed: the flock is the MANAGER's proof; every other page is a joiner's, alive as readily as dead, judged by the exact `(node, mount slot)`; a dead one is the death ledger's) — applied at the region open, the forest replay's `publication_ours`, the unleased-root directory read (PR 10), PR 8's allocation-arm same-node takeover (the holder's page must be page 0), `appender clear`'s own-residue refusal. A joiner named dead by the ledger is recovered by the manager's ONE projection; a THIRD daemon acquires its slot at `g + 1` and reads every acked record; the dead identity's later rejoin gets a FRESH region (§5.8.3). | `a_joiners_rejoin_over_its_live_page_replays_its_window_as_own_residue`; `a_dead_joiners_region_is_recovered_by_the_manager_and_a_third_daemon_takes_its_slot`; the storm pin's remount; PR 2's `a_same_node_live_page_under_another_mount_slot_is_own_residue_under_d0` kept green (page 0) |
| 5 | **Foreign reads under tokens on a WRITER** (PR 9's deviation 4, PR 12's owed divert) | `token_reader_for` takes the WRITER arm (`writer_read_plane_for`): an object in a slot this mount does not lease — the lessee tree 0's table names, or on a joiner the manager for an UNLEASED slot (KD-SYM-2/3) — is read through that holder's token plane, dialed through PR 9's per-holder custody arm (`data_grant::foreign_read_plane` — one JOIN per holder; the recall channel + the mount's purge sink come with it; a lazily dialed plane waits its channel's first round instead of refusing the first read — `TokenReaderPlane::await_channel_fresh`). A leased slot's read pays one gate bit; an unarmed mount one `Option` test; a mount with no custody arm reads locally (the in-process fixtures' default — the mount path always arms). **The transfer's token half**: `TokenHolderPlane::recall_slot` recalls and waits for every outstanding token on a slot's objects — the manager's wire grant runs it before its reply (the slot already foreign, the grantee commits only on the reply), every flush-then-transfer after the door drains (PR 4/5's manager-only assumption: a token the departing holder granted stood after the move — the new lessee's commits recall at ITS plane alone — a reader's cached view of the moved directory would have served stale until its next epoch step). | `a_joiner_reads_foreign_slots_through_tokens_and_serves_the_managers_shipped_step` (the root through the manager's token — a file created AFTER the joiner's open visible at its next resolve; a manager create recalls before it applies (`recalls` +1, `timeouts_live` 0) and the next lookup is exact; an unleased directory read as a token; the first touch's grant recalls the manager's tokens on the moved slot) |
| 6 | **Serving from the joiner's listener** | `DaemonVenue` (the in-process shape of rung 7 on a joiner): the S9 custody owner + the S8 meta service on the JOINER's backend + the token service over its holder planes — never the manager verbs (`arm_authority_planes` leaves `ManagerSetService` off a joiner: a peer dialing them would meet `refuse_joined_control`, not the wire's "no such verb"). The manager creates into a directory the JOINER holds: PR 6's shipped `XvStep` through tree 0's lessee + the bound endpoint, served on the joiner's listener under its lease and ring (`xv_serve_step` — the ONE applier), `steps_shipped` / `steps_served` +1, `invariant_tripwires == 0`. `NotHolder` redirects stay PR 5/9's. The production binding: the new manager verb **`PublishEndpoint { identity, appender_id, endpoint, pr_key }`** (`0xB1`; the joiner's ladder rung 7) — the manager writes the listener into the joiner's claim-set member entry on every volume (the process-less form; `resolve_holder_endpoint` reads it on any mount) and binds it in its own holder table; the joiner binds every Live appender's published endpoint (`bind_live_appender_endpoints`) and appender 0's from its join. | the token/step pin above |
| 7 | **The wire holder's flush-then-transfer on `slot_release_notices`; the offer's acceptance** | `membership` gained the MEMBER side of the slot-lease carriage (`install_slot_carriage_sink` — every adopted grant, routine or carriage, hands its `slot_release_notices` / `offered_slots` to the installed sink; the joined set's sink routes each routing slot to the volume hosting it and SPAWNS the work, never inline in the renewal): `joined_act_on_release_notices` runs the SAME `release_slot_handover` the manager's cadence runs (door closed and drained, ring flushed clear, tokens recalled, page `Releasing`, `ReleaseSlot { root, cursor, seq_floor, tails }` over the wire, page without the slot), `joined_accept_offers` takes an offered slot with the wire first touch. The dominance rule, offer expiry and cooldown are PR 4's, unchanged. | `a_wire_holder_releases_a_slot_on_the_managers_notice_and_accepts_an_offer` (`note_recall` at the manager → `carriage_for` the joiner's identity names the slot → the joined set acts (1, 1) → tree 0 `Unleased` at the lease's `g` with the joiner's root, the recall spent, ring 0's seq space ≥ the departing frontier, the offered slot leased to the joiner, the manager's first touch at `g + 1` reads every acked record — the children in the joiner's rotor slots through the joiner's tokens: `grants_served ≥ 16`) |
| 8 | **The joiner's data allocation** (PR 8's wire halves made production) | `alloc_lease::arm_joined_allocation`: every allocator registered and grant-armed with PR 8's `wire_block_grant_sink` (`BlockGrant` to the manager — volume 0's manager holds every allocation lease in this rung's ONE-shard shape) + `install_wire_free_target`; the router's terminal frees SHIP to the holder on a grant-armed NON-holder allocator (`frees_ship_to_holder` beside the co-writer predicate in `free_block_verdict` / `free_blocks`), `ship_displaced_frees` presenting PR 9's per-holder custody lease when no co-writer client exists (`data_grant::slot_holder_client`). The mount path skips the block-ownership recovery on a joined mount — **a joined open performs ZERO by-block census reads** (the holder's bitmap is the free list; the manager's own open still seeds from the census — the terminal-free ENGINE swap on the manager stays PR 14's, stated in §2). `arm_authority_planes` derives a SOLO lane assignment under the armed plane by law (a PR 12 manager-only assumption: every joiner is a writer member of the roster and the width derived from them would have engaged the S9 partition beside PR 8's grants on one allocator). | the mechanism is PR 8's `a_wire_writer_mints_from_grants…` (disjoint mints over the wire, top-ups, bits SET); the mount-path wiring compiles and is exercised by the fleet legs (§4 — owed) |
| 9 | **The ledger** | §2 — every N-daemon item delivered + pin, or owed with the ONE reason. | — |
| 10 | **Stats on a non-manager** | `mount_posture == writer`; `appender_id ≠ 0` (`AppenderStats::appender_id` = `own_id()`); `slot_leases_held ≥ M`; `manager_lease == peer:<node>`; `writer_guard_mode == joined-appender`; `appenders_live == 1`; the Joined family `joined_appender_id`, `joined_manager_endpoint`, `joined_wire_{verbs,acquires,releases,extent_grants,extent_returns,failures}`, `joined_control_refusals` (must-stay-0), `joined_ring_grow_declined` (`null` per volume on the manager). **Sites that assumed "this mount is the manager", fixed**: `AppenderSet::{live, ring_bytes, ring_segments, ring_grows, stats().appender_id}`, `own_appender_id` / `is_own_region` (crossvol arms), `foreign_slot_holder`, `publish_slot_owners`, `write_appender_pages`, `cover_bring_up_residue` (ring 0 → the own ring), `writer_guard_mode`, the shutdown fixpoint's `uncovered`, `derive_lane_assignment` under the plane, `ManagerSetService` on the listener, `page_is_own` at five sites (§1 row 4). | the first pin's stats block; `assert_must_stay_zero` on every daemon in every contract |
| 11 | **Tests** | `tests/sym_n_daemon_tests.rs` — 13 contracts, TWO and THREE real `KvMetaBackend`s on one file-backed volume in one process (the manager via `open`, the joiners via the joined door over a real `cluster_wire` session to the manager's listener), the fidelity tier's fake PR namespace on the METADATA volume for rung 4; in the matrix (39 suites). The two-process legs: the fidelity `sym-join-ladder` leg grew the N = 3 real-daemon block (§4); the fleet rig grew `--symmetric --writers=K` + the `sym-storm` leg (§4). | §4 |
| 13 | **Rung 4 on a joiner NEVER acquires** (the second commit's three defects) | (a) `sym_join::arm_joined`'s rung 4 ran `arm_data_plane(MultiWriter)` — the MANAGER's acquire, copied from `arm`: under a spec-strict target a second acquire under the manager's standing WERO conflicts, the register ladder's own-stale proof names the LIVE manager's holder key, unregisters it (the set's fence gone) and the joiner takes the hold — the rung-9 finding #1 shape on the appender posture. Now `data_custody::join_wero_as_appender`: CO-LOCATED (the manager's D0 flock on this host, or its claim's boot — one head, one association, the device sees ONE host) ⇒ `adopt_wero_colocated` (KD-SYM-22 — zero device mutation at the join and the leave, the holder cross-checked against enrolled keys); REMOTE ⇒ `join_wero_as_registrant_keyed` under the set's ONE key. (b) The joined door registered NOTHING on the METADATA namespace (PR 3's §5.8.1 law: the manager holds rtype 3, every other appender is a REGISTRANT — a remote joiner's every ring write would be device-rejected): rung 4's metadata half runs at the door BEFORE any wire verb or device write — the volume's `writer_claim` probed, the manager's key DERIVED from it (`WriterClaim::pr_key` = `xxh3(id ‖ NUL ‖ boot)`, the ONE derivation the guard mints by, now shared), the hold adopted (co-located) or registered under (remote — the same key as the data half, so the death ledger's one key fences the member on every namespace class; minted once per set open by `open_routed_meta_set_joined`); a manager holding rtype 1 refuses the join naming `SQUEEZEFS_META_PR_WERO`; a non-PR namespace needs KD-SYM-13's opt-in (announced). The hold is released LAST at the leave (after the final ring write and `LeaveAppender`); every refused join releases it off-runtime. A joiner publishes only a key it REGISTERED itself (`own_registered_key` — never an adopted hold's word) on its membership join and `PublishEndpoint`. (c) PR 10's recovery driver preempted `dead.pr_key` with no own-key guard — under KD-SYM-22 a dead co-located appender's published word can be the manager's OWN key (an adopted hold's, or a peer's `RecordDeath`), and a preempt-and-abort of one's own key takes down one's own fence (the S9 sweep had this law at `multi_writer.rs`'s rung-9 own-key guard; the driver did not): `is_own_registrant_key` skips the preempt, the full tail scan is the fence. Gauge `joined_registrant_posture` (`adopted` / `registrant` / `detection`). | `a_joiners_registrant_rung_adopts_co_located_and_registers_remote_never_acquires`, `a_joiner_refuses_a_manager_holding_rtype_1_on_the_metadata_namespace`, `a_recovery_never_preempts_the_managers_own_key` — each RED with its arm disabled (the door's rung 4 skipped; the guard off), GREEN with it |
| 14 | **N ≥ 3: the third daemon's endpoint** | The slot holder table knows every appender Live at a mount's rung 7 (`bind_live_appender_endpoints`) and, on the manager, every `PublishEndpoint` it served — but joiner 3 is unknown to joiner 2, whose ladder ran first: every foreign act of joiner 2 on joiner 3's slot refused `EAGAIN` for ever. Three laws: (i) `sym_join::bind_holder_endpoint_on_demand` — the table first, else the endpoint resolved and bound ONCE at the first foreign act (`foreign_read_plane`, `acquire_at_slot_holder`'s Unbound arm, `crossvol_tx::step_home_bound` at the SHIP sites, a pre-bind pass in `acquire_guards_leased`); gauge `sym_holder_binds_on_demand`. (ii) The new manager verb **`ResolveEndpoint { appender_id }` → `Endpoint`** (`0xB2`): a JOINED appender asks the MANAGER, whose holder table is exact — a joiner's claim-set projection is its open's (a control record no token carries), so the durable resolver is stale there; the manager and a reader keep the durable resolve. (iii) The writer's divert follows a `NotHolder { holder }` answer: `NotHolderRedirect` is TYPED (a reader keeps PR 5's fail-closed law until its epoch step); `KvMetaBackend::token_serve` — the five divert sites' ONE serve — refreshes the projection and retries ONCE at the holder the manager named, inside the same op. `token_find_dentry` folded into it. | `a_daemon_that_joined_after_anothers_ladder_is_bound_on_demand_at_its_first_foreign_act` (RED at each step: unbound → NotHolder fail-closed → the stale claim set) |
| 15 | **A live joiner across a manager FAILOVER** (PR 10's named row) | `JoinedWire::with_client` — every manager verb runs through ONE retrying door: a TRANSPORT-class failure (`cluster_wire::is_transport_failure` — `Io`, or the wire's own `cluster wire:` refusals; a refusal the manager decoded is returned as is) arms the re-dial and the verb runs once more on the fresh client (every manager verb is idempotent against durable state, §5.3.5); an absorbed failure counts on `joined_wire_redials`, never `failures`. The re-dial resolves the manager's CURRENT endpoint off durable state — the projection refreshed, then page 0's identity → its claim-set entry (`resolve_holder_endpoint(vol, 0)`); the holder table's word for appender 0 follows. **`refresh_control_projection` is keyed on the ledger SEQ, never the root pointer**: a log-structured node's root does not move on an append, and the manager appends its lease records and claim-set entries INTO the images the projection holds — the first build compared roots and re-read stale leaves for as long as no SMO moved them (the `Unleased { g: 1 }` the N ≥ 3 pin met after a refresh; the failover pin's stale endpoint). The refresh discards the projection's images WHOLE — tree 0 (`NodeCache::discard_tree_nodes`) and the manager's NATIVE slot tree (ino 1: the claim set, the rendezvous record), the open's window fold included: never this mount's records, never flushed by it — and re-reads under the roots the new record names (the reader's bounded-staleness law for a projection). A joined mount skips the `client:` heartbeat write (ino 1 is the manager's slot — the door refused it per beat); its liveness is the membership lease. | `a_live_joiner_follows_a_manager_failover_to_the_successors_listener` (the manager leaves, a successor publishes at a new address, the joiner's next verb fails once at the dead endpoint, re-dials, the first-touch acquire lands at the successor: `redials` 1, the table's word moved, every acked record readable) |
| 16 | **Found on the REAL target — N = 3 daemons, each its own process** (the fidelity tier's `sym_n_daemon_leg` + the `sym-crash` fleet leg; nothing in-process could see them) | (a) **PR 8 / S9 — a joiner's terminal frees were REFUSED and ABANDONED**: the shipped-free executor rode `arm_authority_planes`'s LANE branch and the armed plane runs no lane partition, so a joined appender's every displaced-block free (`FreeBlocks` to the allocation-lease holder — `free_target_for`'s route) met status 82 and was abandoned after three attempts: the bit stayed SET in the holding's bitmap with no reference (one leaked block per displaced block — PR 8's C6 LEAK half). The executor installs under the plane, and on a grant-armed allocator its untracked arm reads the bitmap bit (the holder's free list, PR 8) so a duplicate free of a joiner-minted block is the double-release lineage. (b) **PR 12 rung 7 — the lane map named NOBODY**: `arm_authority_planes` derived a 1-way partition from the claim set under the plane, and the custody owner refused every joiner's JOIN against it (every joiner op `EAGAIN`); no lane map is installed on a symmetric writer. (c) **PR 12 rung 3 — the manager's claim-set entry enrolled key 0** (rung 4's hold did not exist yet), so a joiner's rung-4 adoption found no enrolled key; `publish_owner_endpoint` refreshes the entry's key from the live hold beside the endpoint. (d) **PR 12b — the joiner's durable writer era read 0** at the custody owner's arm; it is its page's `term`, adopted at the join. (e) **S8 — a fencing read of a lease-foreign slot's object** (a joiner reading the manager's directory) with no live custody tripped `meta_ship::foreign_fencing_token` on all three daemons: under the plane the object's home is the SLOT lease, not the S8 owner map, and a read there needs no grant — served the owner era's base (`dlm_slot_lease_foreign_reads`). (f) **S6 — the membership reclaim SPUN at 1 ms against the dead manager** for the member's whole `T_self` (≈ 900 refusals a second per joiner) and never reached the successor whose record stood in the rendezvous (only the PARKED arm read `successor_endpoint()`): ONE law before and after `T_self` — the venue is the successor's when observed, the beat the cadence law over the window left (`min(renew_interval, remaining / 3)`) cut short by the observation, a joiner's observation refreshing its projection first; `membership_reclaim_refusals` is the face. (g) **VL2 — the job fabric / job wire started on a joiner** and died at `job:enroll` (ino 1 is the manager's slot); a joined mount runs no fabric. (h) **the job wire RE-MINTED `job:enroll` at every coordinator start** — every session a live member (a joiner, an S9 co-writer, an S6 member reader) dialed the successor with was `mac invalid` for the member's life: the `sym-crash` leg's joiners PARKED at `T_self` against a successor whose grace window would have admitted their reclaim. The record is the SET's root of trust (ruling D2) — minted once, reused by every later coordinator (a SHIPPED-behaviour change on every layout, stated). (i) the corpse sweep walked every slot (a joiner sweeps the slots it may mutate); (j) PR 9 — a holder that died and rejoined at a new listener is re-resolved once when moved (`rebind_holder_endpoint_if_moved`) — and, from the fleet's second round, **the per-holder TOKEN PLANE re-points too**: a manager failover keeps appender 0's identity and publishes a NEW listener, the plane dialed at the old one never freshened and refused every ROOT read of every joiner `EIO` for the member's life (ino 1 is the manager's slot — every path walk on a joiner starts there); `foreign_read_plane` re-resolves once when the channel never freshens, stops the stale plane dead (its tokens dropped, PR 5's law) and dials the successor's. (k) **S6 / §6.8 item 3 — a JOINED writer never acknowledged a freed-offset label**: the owner's bound is the MIN over EVERY member, a member that acked nothing holds it at 0, and a joiner runs no S5 pass (it appends to every volume — the reader ladder is `revalidating_volumes`'s, empty on it), so beside ONE S5 reader the manager's every deferred free sat in the grace ring for the joiners' lives (`free_grace_releases 0`, `free_grace_member_ack_lag_ms.max` 80 s, `data_alloc_bitmap_population` at the volume — ENOSPC in the fleet's fifth round). A joiner's qualification is STRUCTURAL — its every foreign binding is token-governed (the freeing publish recalled the token, the ack's drain + R-6 purge preceded the free) and its own frees are its own — so it acknowledges the label its grant carried at once, riding the next renewal (`ack_learned_label_on_a_joined_appender`); the fleet leg pins `membership_min_acked_free_epoch > 0` within two beats of every successor's arm. (l) **KV — the node cache's `retired` veto is a ONE-writer law** (the `sym-storm` leg's 84 absent files, four of a dead joiner's slot trees; the in-process pin found the same class as a FAILED recovery — "node 0x160000 is a retired extent"): an extent this mount retired stays dead in its cache until THIS mount publishes a node there — but under the plane a retired extent returns to the heap and is GRANTED to a joiner, whose node there the manager must read back at the joiner's death and at every transfer. The carve un-retires what it hands out (`NodeCache::unretire_extent`), a joiner's `ReturnExtents` does the same for its projection; a stale pointer into the extent's previous life is still caught by the §4.2 `node_seq` check. (m) **PR 10 — the recovery's and the region open's root choice by NODE SEQ** ("the newer of the page root and the grant-time root"): one seq space in PR 10's fixture, two on real daemons — KD-SYM-3 is the law and is now the code: a leased slot's root is its lessee's PAGE entry at the lease's GENERATION (`se.g ≥ lease.g`), tree 0's record the grant-time root, a page entry below `g` a stale attestation (`leased_root_from_directory(…, g, …)`, the recovery's step 4, the own-page pass, the `Recovering`-page install); the RAM tree is installed whenever it stands elsewhere (never ahead of the page by construction). The diagnostic lines the recovery now logs when a page entry is absent or stale fired nowhere on the re-run. (n) **a live joiner reading a DEAD joiner's recovered slots** — its lease projection named the dead holder for as long as no wire re-dial refreshed it, every read of the recovered directory `EAGAIN` for the member's life (1,752 of 1,752 acked names absent at the survivor): `token_serve` re-projects tree 0 once when a foreign read cannot reach its holder and reads again through the holder the projection names now (the manager for an unleased slot). The harness: the leg's directory names matched its own `^f` count; it judged the directory before the joiner PROCESSES had exited, so the guard leg's `pgrep | head -1` killed a lingering joiner instead of its own daemon (every `pgrep` in the tier names the exact mount point now; the leg waits for both joiners to exit, bounded past PR 9's dead-grant recall law); N full writers on ONE box each register 32 × 32 × 1 MiB of kmbuf rings and the fifth daemon met the registration's ENOMEM on a page-cache-churned box with 84 GB available (the rig compacts memory before each joined writer's mount — a single-box act). | the leg's free rows (`free_shipped_blocks ≥ 1`, `free_ship_failures 0`, `free_served_blocks ≥ 1`, `free_refused_blocks 0`); `a_joiners_durable_writer_term_is_its_page_term_and_rises_at_a_rejoin`; `the_managers_published_claim_set_entry_names_its_live_data_registrant_key`; `a_custody_owner_with_no_lane_map_admits_every_joiner_solo`; `membership_liveness_tests::{a_refused_reclaim_beats_at_the_cadence_law_never_a_spin, a_refused_reclaim_lands_at_the_successor_the_observation_names}` (RED on the base: 769 refusals in 1,300 ms; the successor never reached); `job_wire_tests::a_coordinators_restart_keeps_the_sets_enroll_secret` (RED on the base) |
| 12 | **Docs** | `docs/operations.md` (the joined-appender posture, the Joined stats row), `AGENTS.md` (the PR-12b paragraph; PR 12's "LANDED IN PART" resolved), design row 12 → LANDED + row 12b's as-built and deviations, this note. | — |

## 2. The N-daemon ledger — every "OWED — PR 12b" row of PR 12's §2

| Owed by | Item | Verdict | Pin / reason |
|---|---|---|---|
| PR 4 | the two-armed-volume + wire-joiner pin (a wire joiner whose checkpoint writes its page) | **DELIVERED** | the first pin (the joiner's own checkpoint writes its page with the Issue-31 words; the manager's rewrite yields from `ckpt_seq ≥ 1`) |
| PR 4 | the wire holder's flush-then-transfer on `slot_release_notices` | **DELIVERED** | `a_wire_holder_releases_a_slot_on_the_managers_notice_and_accepts_an_offer` |
| PR 4/5 | tokens on a slot's objects at its TRANSFER (found here) | **DELIVERED** — `TokenHolderPlane::recall_slot` at the wire grant and every flush-then-transfer | the token pin's first-touch arm (`recalls` moved at the grant) |
| PR 5 | the departure sink's cross-shard face | **OWED — PR 13/14 (measurement)**: one shard exists in this rung's shape (every joiner is a member of volume 0's manager's shard); the owner-role fusion (V shards over V managers, a home ≠ volume 0) is the second-shard shape below | — |
| PR 6/10 | a dead INITIATOR's intent adopted by a foreign recoverer | **COVERED by construction, pin OWED — PR 13 (the two-process venue)**: a joiner's intents land in ITS rotor slot's ring; its death is recovered by `recover_region` + `roll_forward_open_intents` (PR 10's arm, unchanged), and the storm pin's recovery replays a joiner's ring holding renames (intents) — the dead-INITIATOR-mid-plan shape needs a kill inside a cross-owner op, which the in-process fixture cannot time (`TEST_XV_SEAM_AFTER_STEPS` is process-global and would halt the manager's own ops) | — |
| PR 7 | the served `MarkShared` through the routed door for a mid-cutover slot | **OWED — PR 13 (measurement)**: no cutover runs in this rung's fixtures; the served arm's guard is PR 12's | — |
| PR 7 | un-share when the population returns to 1 | **OWED — PR 14 (the share-in-flight witness is protocol work, designed with the deletion of the S5 index seam)** | — |
| PR 7/8 | "a forest ARMED mount's open performs zero by-block census reads" | **DELIVERED on the JOINER** (the mount path skips the recovery walk; the holder's bitmap is its free list); **OWED on the MANAGER — PR 14 (the RAM refcount map's replacement as the terminal-free engine is the deletion rung's)** | `main.rs` (the joined arm) |
| PR 8 | the `shared_ref:` migration at a home change; homes ≠ volume 0; two shards | **OWED — PR 13 (measurement: the second shard's home is chosen at a join by load, and no venue here has two homes)**: every joiner homes on volume 0 (`home_volume: 0` on its page) | — |
| PR 8 | the wire block-grant top-up carried on the renewal grant | **STAYS A PULL** (PR 12's arithmetic) — the joiner's window asks at 50 % over the wire | — |
| PR 9 | the writer's read divert to its per-holder token planes | **DELIVERED** | the token pin |
| PR 9 | the per-object epoch capture; the owner's grace at the arm | **OWED — PR 13 (measurement: the write pipeline's one-word carrier is priced against a real fleet's custody churn)** | — |
| PR 7b | the remote holder's `IsEmpty { scope: 0 }` under a striped rmdir | **OWED — PR 13 (the two-process venue; in-process the rmdir's initiator IS the holder's process)** | — |
| PR 10 | the `Recovering` page's recoverer identity (a rejoin over a live recoverer's page) | **DELIVERED in part**: the joined door refuses a `Recovering` page of its identity (the retryable class) — the rejoin never opens over a live recovery; the identity WORD on the page stays PR 13's | the refusal pin's arm |
| PR 10 | the cross-shard rejoin retirement (Issue 25) | **OWED — PR 13**: one shard here; the rejoin's `JoinAppender` at volume 0's manager retires nothing yet (`retire_death_record` is the manager's — stated) | — |
| PR 2 | drain-then-grow | **STAYS THE STATED BOUND** — a joiner's ring never grows (`joined_ring_grow_declined`); it drains at `admissible ÷ cadence` | — |
| — (found here) | PR 4's wire release left the manager's RAM tree of the slot STALE (the lessee appends INTO the node images; the root pointer never moves) | **DELIVERED** — `adopt_transferred_slot_tree`, ONE cross-daemon barrier for every transfer (both directions) | the first pin's post-leave manager read (RED before) |
| — (found here) | PR 3's `ExtentGrant` idempotency answered a wire appender's page grant word verbatim while the joiner had claimed it | **DELIVERED** — the joiner writes its page with the remainder before every ask and after every fresh carve (`wire_extent_refill`) | the first pin's 24 creates (5 reactive refills) |
| — (found here) | a rejoin whose predecessor died with its grant consumed refused for ever at `GrantExhausted` | **DELIVERED** — the region open pre-sizes the grant over the join's wire | the rejoin pin |
| — (found here) | PR 2's "a same-node page is own residue" under N daemons on one host | **DELIVERED** — `page_is_own` (page 0's node law; exact identity elsewhere) | the storm pin's remount |
| PR 10 | the busy appender across a manager FAILOVER (the N-daemon shape PR 10 named) | **DELIVERED** — `JoinedWire::with_client`'s re-dial + the seq-keyed projection refresh (§1 row 15) | `a_live_joiner_follows_a_manager_failover_to_the_successors_listener`; the fleet's `sym-crash` per-round joiner asserts (§4) |
| PR 6 | the endpoint binding + the wire initiator for a holder that joined LATER | **DELIVERED** — on-demand binding at every foreign act + `ResolveEndpoint` (§1 row 14) | the N ≥ 3 pin |
| PR 3 / 12 | rung 4 on a non-manager (the registrant, both namespace classes) | **DELIVERED** — adopt co-located / register remote, never acquire (§1 row 13) | the three rung-4 pins |
| PR 10 | the recovery's own-key guard (a co-located dead appender's word) | **DELIVERED** — `is_own_registrant_key` | `a_recovery_never_preempts_the_managers_own_key` |
| PR 8 | PR 8's laws on a REAL second daemon — the terminal free lands on the holder's ladder | **DELIVERED** — the executor under the plane; the bitmap-aware untracked arm (§1 row 16a) | the fidelity leg's four free rows |
| S6 | a member's reclaim across a manager failover (the successor's venue, the beat) | **DELIVERED** (§1 row 16f/16h) | the two liveness pins + the enroll-secret pin; the fleet's `sym-crash` per-round joiner asserts |
| — (found here) | a joiner's durable `client:` heartbeat (the offline `appender clear`'s freshness check on a manager-less set) | **OWED — PR 13**: its liveness is the membership lease; while the manager lives its flock protects every joiner's page; a manager-less set with a parked joiner is the window (stated) | — |

## 3. Manager-only assumptions found by the two-/three-backend fixture, by PR

| PR | Site | Assumption | Fix |
|---|---|---|---|
| 2 | `AppenderIdentity::owned_by_node` at the region open, the forest replay, the unleased-root read | every same-node `Live` page is a dead predecessor's (the flock's proof) | `appender::page_is_own` — page 0 alone |
| 2 | `cover_bring_up_residue` | the residue to cover is ring 0's | the joiner's own ring |
| 2 | the shutdown fixpoint | ring 0 and the bitmap are this mount's | a joiner is judged by its own ring |
| 3 | `manager_extent_grant` / `ExtentGrant`'s idempotency | a wire appender's page grant word is its live remainder | the joiner keeps the page's word current around every ask |
| 3 | `write_wire_joiner_page_grant` | the manager writes a wire joiner's grant word | yields to a checkpoint-owned page (`ckpt_seq ≥ 1`) |
| 4 | `manager_release_slot` (wire branch) | the released tree's RAM images are current here | `adopt_transferred_slot_tree` (PR 10's barrier, generalized) |
| 4 | `install_lease(fresh)` on the grantee | the tree opened once is current | the same barrier at `install_wire_grant` |
| 4/5 | the wire grant, `transfer_slot_locked` | tokens on a moved slot's objects die with the move | `TokenHolderPlane::recall_slot` |
| 8 | `arm_symmetric_allocation`'s `Busy` arm | a same-node holder at another mount slot is dead | the holder's page must be PAGE 0 |
| 10 | `appender clear` | a same-node page is own residue | `page_is_own` |
| 12 | `arm_authority_planes` | the lane width derives from the claim set on every writer | SOLO under the armed plane |
| 12 | `arm_authority_planes` | every armed writer serves the manager verbs | never a joiner |
| 12b (own) | the recovered-ring replay's mints | the region's grant covers the window's unrooted trees | the pre-replay wire refill |
| 12b (own) | `arm_joined`'s rung 4 | a joiner arms the data plane as the manager does (`arm_data_plane` = an acquire) | `join_wero_as_appender`: adopt co-located, register remote |
| 3 | the joined door | the metadata namespace needs no registrant (every appender is the manager) | rung 4's metadata half at the door, before any device write |
| 10 | `recover_region` step 1 | a dead appender's key is never this process's | the own-key guard (the S9 sweep's law) |
| 12 | `bind_live_appender_endpoints` | every holder was Live when this mount's ladder ran | the on-demand binding + `ResolveEndpoint` |
| 5 | the reader's `NotHolder` law on a WRITER | a writer's divert never lags the lease (it does — by a checkpoint cadence, on a joiner) | `token_serve`'s typed redirect retry |
| 8 / S9 | `arm_authority_planes`'s free-executor install | a shipped free's shipper is a LANE holder | installed under the plane (solo lane) |
| S9 | `execute_shipped_frees`' untracked arm | the local free list sees every earlier release | the holding's bitmap bit on a grant-armed allocator |
| 12 | `arm_authority_planes` (rung 7) | a symmetric writer's era derives a lane map | none under the plane |
| 12 | `publish_owner_endpoint` (rung 3) | the entry's registrant key is known before rung 4 | refreshed from the live hold |
| 12b (own) | the joined region's open | the custody owner's era comes from the D0 claim | the page's `term`, adopted |
| S8 | `get_fencing_token_ino` | a foreign-home object's read carries an S8 grant | a lease-foreign slot serves the era base |
| S6 | `spawn_member_renewal` / `member_renewal_tick` | the owner's venue never moves before `T_self`; a failed renewal's next beat is `renew_at` | the successor's venue + the cadence law over the window left |
| S6 / §6.8 item 3 | the member's freed-offset acknowledgement | every WRITER member runs the S5 ladder (a co-writer's `revalidating_volumes`) | a joined appender acks the learned label at its renewal (token-governed bindings) |
| 9 / 12b | `foreign_read_plane` | a dialed holder plane's endpoint is for the mount's life | re-pointed once when the channel never freshens and the holder moved |
| KV (PR 1) | `NodeCache::retired` | an extent this mount retired hosts nothing until this mount publishes there | un-retired at the carve that grants it out and at the return |
| 10 | `recover_region` step 4; `leased_root_from_directory`; the own-page pass | page vs record roots compare by node seq (one handle) | by the lease's generation (KD-SYM-3) |
| 12b (own) | `token_serve` | the lease projection names a live holder | re-projected once when the holder is unreachable |
| VL2 | the job fabric / wire start | every write mount is a coordinator | never a joined mount |
| VL2 | `JobWireHost::start` | the coordinator is the only long-lived party | the enroll secret minted once, reused |
| — | `sweep_unlinked_corpses` | every slot is this mount's to mutate | the slots `inode_plane_owns_slot` admits |
| 12b (own) | `refresh_control_projection` | a projection is fresh when its root pointer matches the ledger's | keyed on the ledger seq; the images discarded whole |
| 12b (own) | `JoinedWire` | the manager's endpoint never moves | the re-dial off durable state |

## 4. Verification (dev box, `--all-features` unless stated)

| Row | Result |
|---|---|
| `tests/sym_n_daemon_tests.rs` (17 contracts) | green (stamped fixtures; the refusal pin flat + stamped); RED-first as stated in §1 — the three rung-4 pins, the N ≥ 3 pin, the failover pin, the durable-era / live-key / no-lane-map pins, the retired-extent pin (`a_dead_joiners_root_swapped_into_an_extent_the_manager_once_retired_is_recovered_whole` — the recovery FAILED "node 0x160000 is a retired extent" before the fix) |
| `tests/membership_liveness_tests.rs` (+2) / `tests/job_wire_tests.rs` (+1) | green; RED on the base (769 reclaim refusals in 1,300 ms; the successor never reached; the enroll secret re-minted) |
| `tests/run_sym_forest_suites.sh` — 39 suites flat THEN stamped, the per-suite watchdog | **PASS both legs** on the final tree (78 suite runs green, 0 failed, no NOTE / HUNG; the flat leg's `sym_crash_matrix_tests` was RED once on the way — a premise read of a dead lessee's slot through the divert, the read moved ahead of the custody arm) |
| `sym_n_daemon_tests` ×10 stamped from zero (final tree) | **10/10** (17 contracts each, ≈ 21 s a run) |
| `cargo clippy --all-targets --all-features -- -D warnings` + the shipped config | clean at every commit |
| `cargo fmt --check` | clean |
| `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps` | clean — RED twice on the way (a private intra-doc link each time), fixed before the commit both times: the permanent row earned its keep |
| `task check:fuzz` (the fuzz workspace type-checks; `manager_call_frame` carries `LeaveAppender` / `PublishEndpoint` / `ResolveEndpoint`) + `decoder_property_tests` (61) | clean / green |
| the live-FUSE trio + `posix_semantics_tests` under `SQUEEZEFS_TEST_REQUIRE_MOUNT=1` | green both legs, 0 skips (posix_mount_semantics 3/3, inline_raise 7/7, corpse_sweep 4/4, posix_semantics 13/13) |
| the fidelity tier's `sym-join-ladder` at N = 3 real daemons (`quick`, root, kernel nvmet — `sym_n_daemon_leg`) | **PASS=118 FAIL=0** on the final binary (2m31s), after six rounds: 108/5 at round 5 with every residual attributed (§1 row 16) |
| `mw_fleet.sh create N=2 --symmetric --writers=3 --lease-ttl-ms=15000` (`SQZ_MWFLEET_OSS_GB=16`, the tcp devsub) + `run_mw_matrix.sh sym-crash --rounds=5` | **GREEN 5/5** — per round: the successor's own residue, `manager_lease held`, tripwires 0, every joined writer follows the failover (`joined_wire_redials` 1 → 5 monotone, a fresh directory + write lands, the successor reads it), `membership_min_acked_free_epoch > 0` within two beats, fsck clean |
| `run_mw_matrix.sh sym-storm --rounds=5` | round 1 **GREEN** on three fresh fleets (8,754 / 9,649 acked files from 3 joiners + the manager under the per-file-fsync oracle, all present at the manager and at a survivor; the victim recovered by the manager's ledger in 14–18 s (the 15 s lease TTL + the recovery), remounted into a FRESH region, reading every name; fsck clean; the must-stay-0 set flat on every daemon); round 2 was RED twice on the same four names (the manager's first four lookups in a live joiner's new directory — the pooled token sessions the holder had closed idle = the pool depth), fixed at `a48d4838`; the final ×5 run on `a48d4838`: rounds 1–2 GREEN (3,446 / 5,779 acked), round 3 the oracle GREEN (4,081) but the online fsck found 442 C10 dangling dentries in one `rm -rf`'d directory of an earlier round — OPEN (the summary's item 1) |
| zero residue | every `mw_fleet.sh teardown` "complete — zero residue"; the fidelity tier's teardown leg PASS |

## 5. The D1 arithmetic (§5.10) at N = 2 / 3 — read off the in-process pins

| Op | RPCs at the joiner | Locks | Source |
|---|---|---|---|
| create / rename / unlink in an OWN slot | 0 | the door token + 4a guards, all local | `three_daemons…`: `dlm_rpcs` unmoved over 24 creates on three daemons |
| the first touch of an unleased slot | 1 (`AcquireSlot`) + the manager's `recall_slot` wait | — | the first pin |
| the join | 1 `JoinAppender` + 1 `AcquireSlots` (64 rotor slots in ONE verb) + 1 initial `ExtentGrant` (the manager mints it inside the join) | — | the first pin (`wire_acquires ≥ 1`, `verbs`) |
| 24 creates from a fresh grant of 8 extents (64 KiB nodes) | 5 reactive `ExtentGrant`s of 4 + 1 cadence refill of 8 | — | the first pin's log |
| a foreign directory's first read | 1 custody JOIN + 1 token grant, then 0 until recalled | — | the token pin (`grants_served` moved once per object) |
| a create INTO a foreign directory | 1 `XvStep` (+ the initiator's local records) | the travelling guard | the token pin (`steps_shipped` +1) |
| a release on notice | 1 `ReleaseSlot` after the local flush cycles | the handover mutex | the notice pin |
| the clean leave (65 slots) | 65 `ReleaseSlot` + 1 `LeaveAppender` | — | the first pin (`leaves == 1` at the manager) |

The wall of a release on notice and of the join are the dev laptop's — SCOPING only; PR 13's box rows measure them.

## 6. What this rung does NOT contain — the stop-and-report

- **The counted two-process rows on the BOX** (PR 13's): the two-process legs ran on the dev box (§4) as scoping — mechanism engaged, tripwires 0, walls stated as the laptop's (a joiner's region recovered 14–18 s after its kill at a 15 s TTL; a manager failover ridden by a joiner within one re-dial).
- **Observed, not fixed (PR 9's law, stated)**: a writer's custody client to a DEAD holder retries at 25 ms for `T_self` (≈ 38 WARN lines/s for ≈ 43 s) before its scoped fence — the S9 cadence reused verbatim; a re-resolve of the holder inside the renewal loop is a PR 13 refinement.
- **Observed, by design (PR 5's law, stated)**: a dead reader's tokens hold every writer's commits on the objects it held for one lease TTL (the `sym-storm` run whose kill landed at the phase's start read 15 s stalls on every daemon's first ops: `dlm_token_lease_swept_grants` 1, no timeout tripwire) — a 15 s TTL fleet pays it once per death.
- **A joiner's durable `client:` heartbeat** (§2) — PR 13.
- **A second SHARD** (a home ≠ volume 0): every joiner homes on volume 0; the owner-role fusion and the `shared_ref:` migration ride it (§2).
- The MANAGER's zero-by-block-census open (the terminal-free engine swap — PR 14).

## 7. Deviations from the design, stated

1. The joiner's endpoint travels at rung 7 as its own verb (`PublishEndpoint`), not as "the joiner's endpoint word at `JoinAppender`": the listener does not exist at the open (rung 5 runs inside it, rung 7 after).
2. The manager's `ExtentGrant` idempotency is kept (the page's grant word is the witness); the JOINER keeps the word current — written before every ask and after every carve, no barrier (a crash between the record and the page write leaves C13's claimed-unreachable class, never an empty grant).
3. A joiner's frees SHIP to the holder through PR 9's per-holder custody lease (the co-writer's set-authority client does not exist on a joiner) — the verb and the owner half are S9's, verbatim.
4. The token divert on a writer reads LOCALLY when no custody arm exists (the in-process fixtures' default; the mount path always arms) — the honest posture for a backend with no wire, stated in the code.
5. The slot-lease carriage's member side spawns its work off the renewal (a release cycles the checkpoint) — the design's "the member runs flush-then-transfer" as a spawned act.
6. Rung 4's metadata half lives at the joined DOOR (before `JoinAppender`), not in `sym_join::arm_joined` — the joiner's first device write (a rejoin's grant-word page write inside the open) precedes the ladder's rung 7; the data half stays in the ladder. One key per set open serves both halves (`JoinedAppenderAdmission::registrant_key`).
7. A joiner's projection refresh re-reads the manager's NATIVE slot tree too (not tree 0 alone) — the claim set and the rendezvous record live on ino 1, and a manager failover moves them.
8. The `NotHolder` answer stays fail-closed for a READER (PR 5's law) and is a typed one-shot redirect for a WRITER (`NotHolderRedirect`); the manager's word is fresher than a joiner's ledger-cadence projection.

## 8. Box footprint

None — no squeeze-test row (PR 13's).

## 9. Review round 1 → round 2 (2026-09-18): thirteen issues, five bugs, all fixed red-first

The reviewer's round 1 read **OPEN 13 / BUGS 5 — NOT LANDABLE**. Every issue is fixed on the tree; the review file (`/tmp/grok-justin/grok-exec-review-dadee1dd-pr-12b.md`) carries the per-issue responses. The bugs:

| # | Class | Fix | Red pin |
|---|---|---|---|
| 1 | fsck inode plane (product defect) | the DENTRY pass records NO verdict over a volume with a slot leased to a lessee the installed S6 owner lists LIVE (`fsck_inode_plane_foreign_dentry_scoped`); a lessee not known live stays PR 10's frozen-tree class (judged, window scoped). Fleet legs: `.stats` before the verdict, fsck bounded (`HUNG` red), the "deleted stays deleted" arm | `sym_n_daemon_tests::the_managers_census_takes_no_verdict_over_a_live_joiners_stale_projected_dentries` — red at `inode_plane_volumes_covered == 0` (the plane judged 901 stale refs vs 838 live off a projection) |
| 2 | joiner data plane vs failover | `alloc_lease::HolderVenue` re-resolves the allocation holder off `alloc_lease:` → page identity → published endpoint after a transport failure, re-homes the free target; **second face found by the pin**: the successor's re-hold cleared a LIVE joiner's window remainder and re-carved it (200 mints → 144 distinct) — deferred while another appender page is Live (`data_alloc_bitmap_leaks_deferred`) | the failover pin's data half (red before the sink fix: every post-failover ask failed; red before the deferral: 144 ≠ 200); `sym-crash`'s post-failover write is 5 MiB striped + a rewrite, grant at the successor, free served there |
| 12 | own-page root choice (regression) | generation orders the page against tree 0 only (an `Unleased` slot's entry is never current); the seq guard against the mount's own replayed tree restored; the `Recovering` arm reads seq | stamped `corpse_sweep_tests` ×10 green, the reviewer's 1,746-entry window shape among the runs (round 1 was red on it) |
| 11 | door/cadence legal schedule → fail-stop (PR 4's) | the holder op noted INSIDE the door; `LeaseGate::verdict` admits a content apply under `Releasing` while a door token is out (`slot_door_draining_admits`) | `sym_slot_transfer_tests::a_token_holding_commit_lands_under_the_release_that_drains_it` via `TEST_CONVEYOR_HOLD_POST_DOOR` — red at the exact reviewer-quoted refusal ×10 with (b) disabled; the core's unit pin |
| 13 | mixed sync/async scc lock in the KV loader (SHIPPED bug, every layout) | one acquisition style per node-cache table; C1 fans out in lane-sized waves; the fleet collect loop has a shard-lease progress deadline and relocals a wedged worker's shard | `tests/kv_loader_lock_style_tests.rs` (static rail red on HEAD's loader + a flat 720-load stress) + the stamped stress; the harness bounds the fleet fsck |

Suggestions 3–10: the enroll secret's three outcomes + `squeezefs job rotate-enroll` + the 1.3.0 note; the dead-initiator-mid-plan pin on the two-backend venue (`TEST_XV_SEAM_INITIATOR`); `appender clear` refusing inside `T_park_max`; the `unretire_extent` argument; identity verbs bound to the session peer (`RpcRequest.peer`, `screen_identity_peer`, fuzz + proptest); a joiner's intent adoption scoped to its leased slots; the channel-fresh Notify park, poison-tolerant locks, the divert's own error, `joined_meta_hold_standing`; the operations bullet with the two product gaps and their bounds; the forced shrink through the mount's own region + the joiner's census refresh (pinned at 1,024 writers: rotor 64 → 32, `appenders_known` 2 → 3).

Stated for PR 13: a CROSS-HOST manager failover's allocation-lease re-hold (a dead FOREIGN holder needs the death ledger); the JoinAppender identity binding under an ad-hoc peer (PR 3's contracts' shape); the census through the holder's tokens (the instrument that restores the dentry verdict with joiners live).

### 9a. Round 2's fleet leg: two NEW findings (OPEN — round 3's first act)

`sym-storm` round 1 passed its acked-writes oracle (6,049 fsynced files from 3 joiners + the manager, all present) and then the round-2 harness arms found what round 1's fsck hang had hidden: **(F1)** the joiner killed in the round was evicted at `T_owner`, recovered and its region released by the manager (PR 10's path), and REMOUNTED 20 s later as a FRESH join at the same appender id — 17 s after which its maintenance pass refused `CoW violation: rewrite of node 0x1200000 must target a fresh extent` on every tick and the volume FAIL-STOPPED: the fresh joiner's extent grant held an extent with a LIVE node of a slot tree it re-acquired, so the recovery (or the recovered region's release) had returned a reachable node's extent to the heap — a P0 class (a live image re-granted). **(F2)** a surviving joiner's `lookup` of a removed directory whose child's holder was that dead-then-rejoined joiner parked for ever (24 min at station `[entry]`) behind a `NotHolder` redirect to the dead endpoint — the divert has no bound. Logs: `/tmp/grok-justin/r2-root/storm-m{0,60,62}.log`. The harness bounds the deletion arm's `stat` at 60 s now (a parked lookup dies red naming its daemon). Both are round 3's: an in-process two-backend pin for F1 (kill → eviction → recovery → release → fresh rejoin → the rejoined joiner's SMO) and the fix at the census; a derived bound on the divert's dead-holder path for F2.

### 9b. Round 3 — F1 and F2 fixed red-first

**F1, attributed to the line** (`storm-m0.log`): step 8 logged `appender 1's 2 orphan image extent(s) returned (C13 for a dead appender)` on the one volume that fail-stopped (the sibling returned none, never failed). The extents were LIVE leaves of a slot the joiner had released over the wire before dying: `manager_release_slot` ran `grant_record_minus_images` over the manager's STALE RAM tree of the slot BEFORE `adopt_transferred_slot_tree` (the lessee's current images stayed CLAIMED in its record), and `dead_appender_orphans` judged "reached by no root" over the slots the dead appender STILL leased — the released slot's live images went back to the heap, were re-granted (lowest-free-first) to the rejoined writer, whose first SMO on the re-acquired tree wrote into its own source extent. Fixed: the adoption precedes the walk; the census reads EVERY slot tree's root (an extent any root reaches is never returned); the recovery's cache barrier runs for a wire lessee whatever the roots read (a lessee appends bsets under an unchanged root pointer — the pin's `keep` slot exposed 40 invisible records). Pin `a_dead_joiners_recovery_never_returns_an_extent_a_released_trees_root_still_reaches` (red before: "live image extent 78 … RETURNED").

**F2**: the manager answers `HolderDead { holder }` for a lessee the S6 owner lists dead (`slot_resolve_dead_redirects`), the reader maps it to EAGAIN with no dial, and a joiner's failed per-holder read asks the manager's word once (never a second dial at a dead address); after the recovery the read is exact at the manager and a removed name is ENOENT. Pin `a_peers_lookup_through_a_dead_holder_is_refused_inside_the_bound_and_exact_after_recovery` (≈ 4 s to the refusal — one bounded dial; red before: `NotHolder` handed out, `slot_resolve_dead_redirects == 0`). The fleet's exact 24-minute park had Issue 14's fail-stopped rejoin as its precondition; the `sym-storm` rerun with the bounded `stat` arm is the acceptance instrument.

### 9c. Round 3's first fleet re-run: two MORE product defects on the death path (F3/F4), fixed red-first

The `sym-storm` re-run on `659fb2b5` (F1/F2 fixed) went red at round 1 — 25 acked files (every daemon's first op parked ≈ 17 s; no gauge of the stall on record — the harness now snapshots every daemon's `.stats` mid-storm) — and a single-round diagnostic re-run (8,791 files, oracle GREEN) died AFTER its fsck with `rc 1` and no line: **`m62.log`** — `ReturnExtents failed on the wire (the coordinator closed the session) — re-dialing the manager and retrying once` at 13:04:10 (the wire's 60 s idle close after the storm), then `conveyor pass parked 5000 ms waiting for journal-ring admission — the drain is not advancing` ×3 and `3 consecutive journal write failures — volume marked FAILED` at 13:04:25, under the harness's `rm -rf`, on a healthy device. **F3 (Issue 16)**: `JoinedWire::client_at`'s re-dial runs `refresh_control_projection`, which takes the volume's SMO mutex — the mutex the checkpoint task holds for its whole cycle, and the cadence's `ReturnExtents` / refills run INSIDE the cycle: the task deadlocked on itself, the ring never drained, D1.b fail-stopped the volume. Fixed by threading the held guard as a witness (`SmoHeld`, `with_client_under_smo`, `refresh_control_projection_at`) — the cycle's verbs re-dial on the guard they hold and follow a failover from inside the cycle; the commit path's refill keeps the last-known endpoint (a refresh under the mint guard would invert the census's SMO → mint order). **F4 (Issue 17), found by F3's pin**: a wire grant moved the slot tree's live images into the joiner's durable `extent_grant` record while the joiner's RAM grant never claimed them (`transfer_in` ran for an in-process region only), so the joiner's own retirement of an inherited image was dropped at `free_pending`'s claimed guard — an orphan only C13 or the leave reached; `install_wire_grant` now claims the adopted tree's images. Pin `a_joiners_checkpoint_cycle_meeting_a_dead_manager_completes_and_follows_the_successor` (red-first at both: `grant_pending` flat after the compaction; a 60 s bound around `checkpoint_now` fired on the deadlock), ×10 stamped. **Harness (Issue 18)**: the `sym-crash` post-failover grant arm read a covering pre-failover window as a lost grant (`block_grants=0 (was 0)` in round 2) — judged on the joiner's ASK now (`block_grant_topups` / `block_grant_window_remaining` exported); every `set -e` exit of the matrix runner names its line.

### 9d. The second and third fleet re-runs: F5 / F7 / F8 fixed red-first, F9 attributed and OPEN

The re-run on `d92065ed` (F3/F4 in) found three more, each named to its line: **F7 (Issue 19)** — the storm victim's remount FAILED (`appender 1's page is Recovered … the manager's reply and the directory disagree`): its region recovered and RELEASED, the same identity rejoined 16 s later reusing the Free page at term 2, and the manager's next ledger poll — the `dead_member:` record standing, the member not yet re-listed live (rung 3 is after the open) — recovered the live rejoiner's fresh region (PR 10 round 3's Issue-25 obligation for PR 12, met in-shard); the served `JoinAppender` retires the identity's record at the ledger volume BEFORE its page goes Live (`ManagerService` carries ordinal 0; `recover_region`'s post-page record re-read closes the race by ordering). **F5 (Issue 20)** — `sym-crash` round 2: `free_shipped_blocks` flat 60 s after the rewrite — the second failover moved the holder while the joiner's grant WINDOW covered its writes, so the grant sink (round 2's only re-resolving arm) never ran and the free target kept the dead address (`custody JOIN … Connection refused … declined until the next renewal beat`); the free path re-resolves the venue at its own transport failure (`alloc_lease::refresh_free_target`, the venue registered at the joined allocation arm) and follows once with a client JOINed at the successor. **F8 (Issue 21)** — `token reply: undecodable frame body (179 B): invalid value: integer 105` → a user op's EIO: the token service's membership screen answers `STATUS_REFUSED` with a reason STRING; the client now surfaces the text in the retryable class. Pins: `a_rejoined_identitys_fresh_region_is_never_recovered_under_its_predecessors_death_record`, `a_joiners_free_target_follows_a_failover_the_grant_window_covered`, the ghost contract's `probe()` arm — each red-first, ×10 stamped. Harness: the "deleted stays deleted" arm's bare `timeout … stat` exited the leg silently under `set -e` on its EXPECTED ENOENT (every re-run that reached it); fixed, `-E` carries the ERR trap into the leg functions.

On `96a9a42c` (all of the above): **`sym-crash` ×3 from zero GREEN** (3/3 rounds; every joiner follows both failovers on the KV, grant and free planes; fsck clean each round). **`sym-storm`**: round 1 GREEN in the counted run (2,740 acked files) and in the diagnostic 3-round run (6,136); round 2 of the diagnostic run RED on **F9 (Issue 23, OPEN)** — 7,713 of 8,575 acked files "absent" through the REMOUNTED joiner, every one a `Metadata volume 1 is disabled` refusal: the harness's `rm -rf` of the dead incarnation's round-1 tree THROUGH the rejoined daemon first-touched 64 `Unleased` slots in 3 s (`slot_leases_held` 64 → 128), past the page's `SLOT_PAGE_BUDGET` = 108; a wire lessee has no tree-0 publication for the 20 overflow roots (the manager's overflow law is in-process), an unpublished root is a ring-tail FLOOR, the round-2 storm filled the 512 KiB ring (`meta_kv_journal_full_stalls` 0 → 303, `appender_pressure_cycles` 344, `joined_ring_grow_declined` 284, 21 checkpoints/s with the tail pinned), the pass parked at admission and D1.b fail-stopped the volume at 15 s; the cadence's LRU release past the budget releases IDLE slots only and could not drain past the sibling floors regardless. The fix is the wire form of the overflow law (a `PublishRoots` verb the joiner's cycle ships for the slots its page cannot name) — round 4's first act. The earlier storm stalls (25 / 303 acked files in a round, the first ops parked ≈ 17 s at every daemon) were the `recall_and_wait` of a served cross-owner step waiting 18.75 s (`meta_txpass_phase_ns.pass_token_recall` 446 ms mean, one `dlm_token_recall_rtt_ns.ack` sample > 16 s with `send`/`drain` ≤ 256 µs, `xv_cross_owner_phase_ns.ship_rtt` > 16 s at the initiator) — the acks arrived within microseconds and the pass observed them 18.75 s later; not attributed to a line in this round (the 5-s watchdog run that would name the parked station did not reproduce it), stated as such.

### 9e. Round 4: Issues 22 and 23 closed red-first, and the storm leg's two remaining defects found and fixed on the way

**Issue 23 / F9 — the WIRE form of the page-budget overflow law** (`4832be3a` test, `c6b87d06` fix). `ManagerCall::PublishRoots { appender_id, roots: [WireSlotRoot { slot, g, root, cursor, slot_tree_extents }] }` (`0xB3`, appended in PR 12b's block; `ManagerReply::RootsPublished { published, already }`): the joiner's checkpoint cycle (`joined_publish_overflow_roots`) ships, for every slot its page cannot hold — the manager's own overflow selection, `region_page_overflow` ∩ `roots_to_publish` — the roots in frames of 256 through the round-3 `SmoHeld` door, and lifts the floors (`note_published`) ONLY at the manager's durable ack; the manager (`manager_publish_roots_wire`) screens every word against the caller's leased set at exactly its `g`, the bounded-execution screen `screen_publish_root_words` (a release's screen minus the seq-floor clauses; fuzzed + the proptest mirror), the node witness for a moved root, and idempotency against tree 0's current words, then rewrites every `Leased { …, root, cursor, slot_tree_extents }` under ONE `write_control_entry`. Gauges `slot_roots_shipped` / `slot_roots_published_for_lessees`. Found by the pin and fixed beside it: the manager kept a STALE cached image of an extent it had granted away (a lessee's retired root extent, returned, re-granted and rewritten under another slot) — the `ExtentGrant` to a wire lessee drops the manager's cached images inside the granted extents, and `install_recovered_root` re-reads a cached image older than the pointer. The bound: PR 4's cadence LRU release past the budget WAS wired on a wire lessee (`release_for_cadence` → the wire `ReleaseSlot`); what starved it on the fleet was F9's floors and `T_idle` — pinned green-first. Pins `a_wire_lessee_past_its_page_budget_keeps_committing_and_its_overflow_roots_ride_tree_zero` (RED before at a 35 KiB pinned tail after two cycles) and `a_wire_lessee_sheds_its_idle_overflow_to_the_page_budget_within_one_beat`, ×10 stamped.

**Issue 22 — the custody renewal's pace** (`af990589` test in `membership_liveness_tests`, `2aed3894` fix). A SHIPPED-behaviour change on the S9 co-writer loop: a failed renewal waits `clamp(remaining-to-T_self / 3, 100 ms, one cadence)` instead of 25 ms; the §6.7 fence is byte-identical (`T_self` from the LAST successful renewal). FLAT pin `a_custody_renewal_at_a_dead_authority_paces_its_dials_and_fences_at_t_self` (the REAL loop, a raw acceptor at the dead authority's address counting every dial): **66 dials inside the 1.3 s `T_self` window before → 14 (6 paced retries) after**, the fence at 1.36 s on both; ×10 flat; the 1.3.0 bullet. Under the armed plane the per-holder client re-resolves its holder off durable state at every paced retry (`SlotHolderCache::appender_at_endpoint` → `sym_join::resolve_holder_endpoint`) and a MOVED holder fences the SCOPED client at once — the owner's word that the lease died with its listener (`dlm_custody_holder_moves`); the unarmed mount installs no resolver. Gauges `dlm_custody_renew_retries` / `dlm_custody_holder_moves`.

**The first fleet run on the fixed tree (`2aed3894`) — `sym-crash` ×3 GREEN, `sym-storm` round 1 RED in a NEW shape**: 36 "absent" over 78 fsynced files, every one at ONE survivor (m61) for its OWN round directory and a live peer's, the manager reading all 78 — and 78 files in a round is itself the round-3 unattributed stall (`unique=124 … 16,898 ms … still unreplied`). The oracle's `LOST (absent)` hid the errno (the harness now carries the stat's own error into the finding); the manager logged no non-ENOENT error → a stale NEGATIVE at m61. A diagnostic run (`SQUEEZEFS_TIMEOUT=5` + debug on the token planes, `/tmp/grok-justin/r4-diag/`) reproduced it at m62 through the "deleted stays deleted" arm — `Dentry storm-w62-r1 not found in parent 1` at the daemon that CREATED it, its root-dentry token (Grant 19) granted at 18:19:44 and never recalled again (`dlm_token_recalls` 8 = acks 8, all to m61/m60) — and named the mechanism: **F10 (`50188ec9` test, `296f4a42` fix)** — in `serve_grant`, `grant_register` found m62 ALREADY registered on ino 1 because the recall of its previous token (Grant 4/5, recalled by the manager's own `mkdir`) was delivered but its ack not yet processed; the grant was served on that registration (post-commit records, correctly parked), then the ack's `retire_recall` removed it — the token stood at the reader untracked and no later pass on ino 1 recalled it. The same hole for a FRESH registration parked behind a pass that saw and recalled it. The grant now waits a pending recall of `(client, object)` out (`await_recall_retired`), re-arms its registration before every read (the gate's register-then-check order) and repeats the read while a pass straddles it; `dlm_token_regrant_under_recall_waits`. Pin `sym_coherence_tests::a_grant_under_a_pending_recall_registers_afresh_and_stays_recallable` (the recall's ack held behind a `ServeStamp`, a fresh read inside the window): RED on the original at `holders(1) == 0` with the reader holding the token. **F11 (the 18.75 s storm-start stall, same fix)**: the pass's completion predicate in `recall_and_wait` was `holders(o) == 0` — a first-touch grant registered AFTER the union went in flight (never recalled, parked on the gate until the settle) counted, so the pass waited for a holder that could only leave by the grant's park bound: the recall deadline, 15 s (the manager's `pass_token_recall > 16 s` for one pass, `dlm_guard_wait` 4–17 s on 8 samples, every joiner's `lookup(1)` parked at the gate and their `mkdir -p` behind it, the 4a guards held across it; four daemons' `mkdir -p` under one root at once). The pass waits for `holders_under_recall(o) == 0` — the recalls it ISSUED. The first pin ran 46 s on the original (the park bound + the wire's 10 s call timeout) and 2.2 s fixed. The recall lane gained its permanent debug tape (frame issue / deferral / hand-over / stale ack).

**The second fleet run (`296f4a42`) — `sym-storm` rounds 1–2 GREEN with fsck (6,335 / 5,307 acked files), round 3's oracle GREEN (8,655) and its fsck RED; `sym-crash` ×3 GREEN.** The manager's online fsck VERIFIED `C1Torn` on `vol1/slot312` — a slot a LIVE joiner leases: the manager holds that tree as a projection, the joiner moves its root and rewrites its images under grants the manager handed out, and the manager's raw C1 walk from ITS root word read a routing loop (`root-seq` restarts to the traversal budget) over a healthy tree. Round 1 had scoped the DENTRY pass out of a live lessee's trees by the S6 owner's word; the raw C1 units were left in. **F12 (`48b4ca3e` test, `9b817dd8` fix)**: `c1_units` skips the slot trees whose lessee the installed owner lists live (`SlotCoverage::foreign_live_slots`), a lessee not known live stays PR 10's frozen-tree class; `fsck_c1_foreign_live_slots_scoped`. Pin `the_managers_c1_walk_skips_the_slot_trees_a_live_joiner_leases` (the F9 shape with the joiner live and the FULL engine at the manager): RED on the original with the fleet's exact signature on every slot the joiner leases. The harness kept no transcript of a red fsck (`out="$(…)"` under `set -e`) — fixed (`9e888280`).

**The third fleet run — from zero on the final tree (`9e888280`, the count restarted after F12): `sym-storm` GREEN 3/3** (4,438 / 8,650 / 7,236 acked files; every writer's ledger present with content at the manager and a survivor; the victim's remount a FRESH region; fsck clean after EACH round with the inode plane scoped and the raw C1 walk skipping the live lessees' trees; "deleted stays deleted" through every mount; the symmetric must-stay-0 set flat on every daemon) **and `sym-crash` GREEN 3/3** (every joiner follows both failovers on the KV, grant and free planes; `min_acked_free_epoch` advancing; fsck clean). Logs: `/tmp/grok-justin/r4-root3/`. The earlier rounds' rows stand as the finding tapes: `/tmp/grok-justin/r4-root/` (the stale negative — F10), `/tmp/grok-justin/r4-diag/` (the mechanism tape: `pass_token_recall > 16 s`, the parked lookups, m62's Grant 19), `/tmp/grok-justin/r4-root2/` (the false C1 — F12).
