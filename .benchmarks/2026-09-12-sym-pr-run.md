# Symmetric shared-disk metadata — the PR run (PR 1 → PR 16 → release 1.3)

**Owner directive (2026-09-12, verbatim):** "we are skipping the hero run and
focusing on this PR RUN, start PR1 and go on to PR16 without stopping, run full
release gate at the end with this tagged as 1.3".

Design: [`docs/design-symmetric-metadata.md`](../docs/design-symmetric-metadata.md)
(Rev 7, approved 0-open after 7 review rounds; owner rulings R-SYM-1..8 folded).
Run state: `/tmp/grok-justin/grok-exec-plan-dadee1dd.json` (plan `dadee1dd`).
Base: `dev` @ `89f34ed7`.

## Landing law for this run

Adapted from the execute-plan skill to this repo's laws (no PR stack):

1. Each PR is implemented by ONE subagent in an isolated worktree off the
   current `dev` tip (or its dependency's landed tip): red-first tests first,
   then the feature, then docs + the evidence note; the implementer runs its
   own suites + `cargo fmt` + clippy in BOTH configs. It never runs
   `task check`, never pushes, never touches `dev`.
2. A mandatory reviewer subagent reviews every PR in the implementer's
   worktree; a fix loop runs until 0 open issues (wontfix needs a technical
   justification; a stalemate escalates to the owner).
3. The orchestrator fetches the worktree's commits, fast-forwards them onto
   `dev`, and pushes. A batch `task check` from the pinned gate worktree
   (`~/Source/squeezefs-gate`, `/tmp/five/gate/gate.sh`) runs every 1–2
   landed PRs with the laptop otherwise IDLE (no implementer runs beside a
   gate). A red gate is fixed on `dev` before the next PR branches.
4. The squeeze-test solo re-gate (A-B-B-A, 1.2.4 dist vs the PR's rocky8
   build) runs at the format-changing PRs 1/2 and at 13/14.
5. PR 15 (the cloud row) needs the owner's expressed approval at launch —
   asked up front 2026-09-12, declined-to-answer; it is the ONE planned stop.
6. Release 1.3.0 at the end: the full release chain from zero (task check,
   fstests `-g auto`, pjdfstests, LTP, require-mount, zc-capability, fuzz),
   the release act (version bumps across the four crates, RELEASE_NOTES,
   tag `stable-2026.MM.N`), `task dist:all`, the rocky8 pair to the box.

## Linearized order (levels)

| Level | PRs |
|---|---|
| 0 | PR 1 (forest), PR 16 (retire SPDK) |
| 1 | PR 2 (appender region) |
| 2 | PR 3 (manager lease, WERO, REGCTL) |
| 3 | PR 4 (slot leases), PR 11 (convert / format --symmetric) |
| 4 | PR 5 (read tokens), PR 6 (cross-owner tx), PR 7 (pack + shared refs), PR 8 (allocation lease + sharded membership) |
| 5 | PR 7b (dir striping), PR 9 (custody), PR 10 (recovery) |
| 6 | PR 12 (every mount a writer) |
| 7 | PR 13 (acceptance on squeeze-test + SIM-1 + fidelity) |
| 8 | PR 15 (cloud row — approval) |
| 9 | PR 14 (default flip) |

## Log

| When (local) | Event |
|---|---|
| 2026-09-12 23:45 | Run opened. pr-1 implementer `01a098c7` (feat/sym-forest) and pr-16 implementer `01a098c8` (feat/retire-spdk-target) launched in isolated worktrees off `89f34ed7`. |
| 2026-09-13 01:00 | pr-16 implemented (`d758eb8a`, 4 commits, −10,427/+1,920); reviewer `01a0990b`: 11 issues (0 bugs); fix round → `ee2c1974` (11/11 fixed); re-review `01a09931` launched. |
| 2026-09-13 01:40 | pr-1 implemented (`ef05b8a7`, 4 commits, +3,882/−698; 22 new contracts, 23 regression suites green); reviewer `01a09932` launched. |
| 2026-09-13 01:55 | **pr-16 LANDED** on dev `b2673eab` (7 commits, 3 review rounds, 13 issues fixed, 0 bugs). Landing evidence (design §7 3b): nvmet-only fidelity `quick` PASS 18/0 in 1m07s; `full` PASS 90/0 in 2m14s (11 legs incl. guard-nvmet-x10, teardown zero residue) — logs `/tmp/sym-run/pr16-fidelity-{quick,full}.log`. Venue note: root's PATH lacks `jq` on NixOS — run the tier with `sudo env PATH=<nix jq>:$PATH`. Batch gate owed (with pr-1). |
| 2026-09-13 02:05 | pr-1 review round 1 (`01a09932`): NOT mergeable — 16 issues (7 bugs: forest keys panic the write-side audit on block-ref/block-map records; refs probe only the native slot; KEY_SPACE_MAX interior pointers route to a phantom slot; deferred root publish loses the floor; flat hot path gained an Arc clone/drop; RO reader mis-roots guests; the stamped-suite parametrization never run). Fix round `01a0995d` launched. |
| 2026-09-13 03:20 | pr-1 fix round 1 done (`b349d809`, 11 commits; all 16 marked fixed; `tests/run_sym_forest_suites.sh` 15 suites × flat/stamped; fuzz targets + proptest mirror; `FOREST_SLOT_MAX` codec hole found by the round-trip law). Re-review `01a099c6` launched. |
| 2026-09-13 04:45 | pr-1 review round 2 (`01a099c6`): 14/16 verified-fixed; **OPEN 6, BUGS 3** — stamped `kv_leaf_merge_tests` nondeterministic (4 red of 7 full-binary runs, two convergence/throttle expectations; attribution owed); non-writer opens (`-o ro`, co-writer) MINT at replay ⇒ device writes from a reader (PR-substrate co-writer mount refused when the authority has a fresh slot in its window); replay mint on `claim_user` ⇒ a `heap_full` volume that crashed with an unpublished mint is unmountable; plus Issue 13 (literal still there), `fsck_repair_tests` forest branch + gate-list widening, two nits. Fix round 2 `01a099eb` launched (stamped leaf-merge ×10 from zero required). |
| 2026-09-13 08:00 | pr-1 fix round 2 done (`5c843311`, 25 commits total, 0 wontfix). Issue 17 attributed: line 184 = PRODUCT (§4.7 P1 — `admit_heap_batch` promised a member's leaves one at a time and never returned the earlier promise when a later leaf was refused; layout-independent, red 12/12 on the shipped flat layout; fix `2ccb8f72`), line 1294 = HARNESS premise (the D4 arm's order). Stamped `kv_leaf_merge_tests` ×10 green from zero on the final binary (40–45 s each), flat ×3. Issue 18 = SKIP law (`OpenPosture` → `MintPolicy::{User,Recovery,Refuse}`; `meta_kv_forest_reader_window_skips`); Issue 19 = replay mint on `claim_internal`; Issue 20 surfaced a second product defect — `SlotTrees::range` failed a whole page at a refused key ⇒ truncated census ⇒ fsck repair FREED LIVE BLOCKS; now skip-not-fail walks + a raw per-slot-tree C1 walk (`C1RawKey`) repaired in place. Gate list widened to 20 suites, 20/20 both ways. Re-review round 3 `01a09a9a` launched. |
| 2026-09-13 09:20 | pr-1 review round 3 (`01a09a9a`): 13/17–21 all verified-fixed; OPEN 4, BUGS 1 (low) — the skip-not-fail law leaked into `refs_window` (a forest under-counts a block's population on a malformed ref key where flat refuses loud); 23 non-writer partial window unstated; 24 N re-mints need N reserve extents; 25 `C1RawKey` repair deletes unknown kinds. Fix round 3 `01a09abe` launched. |
| 2026-09-13 11:00 | pr-1 fix round 3 done (`c28a495b`, 39 commits total, 0 wontfix, 16 files +1,101/−114): refs scan refuses (flat law); Issue 23 became a PRODUCT rule — a non-writer's directory pager lists a child iff it holds the child's slot tree (`readdir_page_held`, gauge `meta_kv_forest_reader_unpublished_children`); §7.1 freezes the kind set (a new kind = a new incompat bit); the raw C1 repair deletes only a NAMEABLE malformed known-kind key, unknown kinds report-only; replay counts its re-mints and refuses ENOSPC-class by count. The classifier's proptest law found two more codec holes (known kind in the by-block position; decoder accepting a slot above the namespace the encoder refuses) — both closed+pinned. 20/20 both ways. Final verification pass `01a09b11` launched. |
| 2026-09-13 12:05 | pr-1 review round 4 (`01a09b11`): 22–25 verified-fixed incl. the beyond-the-ask pager, codec and ENOSPC-count changes; **one new bug (Issue 26)** — on the forest `kv_scale_tests::rightmost_separator_pointer_record_replays_clean` idles for exactly the parked 60 s cadence with every thread asleep (7/8 in-sequence stamped runs, 1/5 isolated, one run > 500 s; flat never in 9) — a wait only the cadence releases (lost ring-full→checkpoint wake or a forest-only floor holding the tail). NOT LANDABLE until attributed. Fix round 4 `01a09b4a` launched (attribute first, product fix, ×10 within 2× of flat). |
| 2026-09-13 13:20 | pr-1 fix round 4 done (`d47f54dc`, 44 commits). Issue 26 attributed to the LINE — verdict (c), a SHIPPED layout-blind lost wake: `shutdown()` = set flag + `Notify::notify_waiters` (wakes only registered waiters, stores no permit); a checkpoint task INSIDE its maintenance pass missed it and parked a fresh `notified()` with the flag unread until the cadence fired (gdb: the awaiter is `re.shutdown().await`; `journal_full_stalls` = 0; zero CPU). The forest exposes it because its post-replay fold is one big mixed-leaf split still running at shutdown. Fix: the signal is a permit (`notify_one`) + the task re-arms on the flag; the pending-times drain task had the identical shape and is fixed too. Pin RED on `c28a495b` on BOTH layouts (task parked 59 s), green ≈ 15 ms. Acceptance ×10: pair 20.1–21.3 s stamped vs 17.3–18.4 flat (1.13–1.23×; pre-fix 79–80 s = 4.6×), isolated ≤ 1.2×; the > 500 s run not reproduced (0/31). Gate now TIMED (per-suite wall per leg, stamped/flat ratio NOTE), 21 suites, 21/21 both ways, max ratio 1.38×. Verification round 5 `01a09b8a` launched. |
| 2026-09-13 14:10 | pr-1 review round 5 (`01a09b8a`): Issue 26 verified-fixed (the reviewer re-ran the pin RED on `c28a495b` in a throwaway worktree, both layouts); LANDABLE with one nit (Issue 27: the drain task had the permit but not the pre-park flag re-check; the pin ordered on a 300 ms sleep) → fixed `13c5f1f3` (belt added; the pin orders on the `shutdown_signalled` trace edge). OPEN 0 / BUGS 0. |
| 2026-09-13 14:40 | **pr-1 LANDED** on dev `73d3e74e` (45 commits rebased clean onto `3228fcb8`; `cargo check --all-targets --all-features` + knob-registry/docs-parity/forest/SPDK-retirement suites green on the rebased tip). Totals: 6 review rounds, 27 issues (10 bugs), 0 wontfix. **Three SHIPPED, layout-blind defects found by chasing forest-only symptoms and fixed red-first on both layouts**: (1) `admit_heap_locked` stranded a refused multi-leaf member's earlier promises for the life of the mount (§4.7 P1); (2) `SlotTrees::range`/the census failing a whole page at one refused key ⇒ fsck repair FREED LIVE BLOCKS (forest arm); (3) `shutdown()`'s `notify_waiters` to the checkpoint and pending-times drain tasks stored no permit — a task mid-pass slept to the cadence deadline (≤ 1 s per unmount shipped; 60 s under the parked test cadence). Owed: batch `task check` (with PR 2), squeeze-test A-B-B-A solo re-gate (design gate 1), the one-walk fsck census, the orphaned-extent hygiene sweep, the nightly fuzz campaign for `slot_tree_record`/`slot_state_record`, the one unexplained > 500 s pre-fix run (note §7 recipe). |
| 2026-09-13 14:45 | pr-2 implementer launched off dev `73d3e74e` (feat/sym-appender-region). |

